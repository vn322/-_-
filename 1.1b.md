 Мы построим логическую цепочку: **Предел → Непрерывность → Свойства непрерывных функций → Производная**.



---

# 1. Предел функции (Limit of a Function)

### Интуитивное понимание
Представь, что ты приближаешься к какой-то точке $x_0$ на оси $X$. Тебе интересно, к какому значению $A$ при этом приближается функция $f(x)$. Важно: нас не интересует, что происходит *в самой точке* $x_0$ (функция там может быть даже не определена), нас интересует поведение *вокруг* неё.

### Строгое определение (по Коши)
Пусть функция $f(x)$ определена в некоторой окрестности точки $x_0$ (может быть, кроме самой точки $x_0$).
Число $A$ называется **пределом функции** $f(x)$ при $x \to x_0$, если:

$$
\forall \varepsilon > 0 \quad \exists \delta > 0 \quad : \quad \forall x \in D(f), \quad 0 < |x - x_0| < \delta \implies |f(x) - A| < \varepsilon
$$

**Обозначение:** $\lim_{x \to x_0} f(x) = A$.

**Расшифровка:**
*   $\forall \varepsilon > 0$: Какой бы малый допуск по вертикали ($\varepsilon$) ты ни задал...
*   $\exists \delta > 0$: ...найдется такой малый допуск по горизонтали ($\delta$)...
*   $0 < |x - x_0| < \delta$: ...что для всех $x$, попавших в $\delta$-окрестность $x_0$ (но не равных $x_0$)...
*   $|f(x) - A| < \varepsilon$: ...значение функции попадет в $\varepsilon$-окрестность числа $A$.

### Наглядный пример
Рассмотрим функцию $f(x) = 2x + 1$. Найдем предел при $x \to 3$.
Интуитивно: $2 \cdot 3 + 1 = 7$.
Проверим строго: хотим показать, что $\lim_{x \to 3} (2x + 1) = 7$.
Нужно оценить $|f(x) - 7| = |(2x + 1) - 7| = |2x - 6| = 2|x - 3|$.
Мы хотим, чтобы $2|x - 3| < \varepsilon$. Это равносильно $|x - 3| < \frac{\varepsilon}{2}$.
Значит, если мы выберем $\delta = \frac{\varepsilon}{2}$, определение выполнится.

### Типовая задача
**Задача:** Вычислить предел $\lim_{x \to 1} \frac{x^2 - 1}{x - 1}$.

**Решение:**
1.  **Попытка подстановки:** Если подставить $x = 1$, получим $\frac{0}{0}$ — неопределенность. Это значит, что в точке $x=1$ функция не определена, но предел может существовать.
2.  **Преобразование:** Разложим числитель на множители по формуле разности квадратов: $x^2 - 1 = (x - 1)(x + 1)$.

    $$\frac{x^2 - 1}{x - 1} = \frac{(x - 1)(x + 1)}{x - 1}$$
    
4.  **Сокращение:** Так как в пределе $x \to 1$, но $x \neq 1$, мы можем сократить на $(x - 1)$:

    $$\lim_{x \to 1} (x + 1) $$
    
6.  **Ответ:** Теперь подставляем $x = 1$: $1 + 1 = 2$.
   
    **Ответ:** 2.

---

# 2. Непрерывность функции (Continuity)

### Интуитивное понимание
Функция непрерывна, если её график можно нарисовать, не отрывая карандаша от бумаги. Нет разрывов, скачков или дырок.

### Строгое определение
Функция $f(x)$ называется **непрерывной в точке** $x_0$, если выполняются три условия:
1.  Функция определена в точке $x_0$ (существует $f(x_0)$).
2.  Существует предел $\lim_{x \to x_0} f(x)$.
3.  Предел равен значению функции:
   
    $$\lim_{x \to x_0} f(x) = f(x_0)$$
    

Функция называется непрерывной на множестве, если она непрерывна в каждой точке этого множества.

### Наглядный пример
Функция $f(x) = x^2$ непрерывна всюду.
Функция $f(x) = \frac{1}{x}$ непрерывна везде, *кроме* точки $x = 0$ (там она не определена, разрыв).

### Типовая задача
**Задача:** Исследовать на непрерывность функцию в точке $x = 0$:

$$
f(x) = \begin{cases}
x + 2, & \text{если } x < 0 \\
x^2 + 2, & \text{если } x \ge 0
\end{cases}
$$

**Решение:**
1.  **Значение в точке:** $f(0) = 0^2 + 2 = 2$.
2.  **Предел слева ($x \to -0$):** Используем первую формулу. $\lim_{x \to -0} (x + 2) = 0 + 2 = 2$.
3.  **Предел справа ($x \to +0$):** Используем вторую формулу. $\lim_{x \to +0} (x^2 + 2) = 0 + 2 = 2$.
4.  **Сравнение:** Предел слева = Предел справа = Значению функции ($2 = 2 = 2$).
**Вывод:** Функция непрерывна в точке $x = 0$.

*(Если бы пределы слева и справа не совпадали, это был бы разрыв 1-го рода. Если хотя бы один предел бесконечен — разрыв 2-го рода).*

---

# 3. Свойства непрерывной функции на отрезке

Здесь мы переходим от точек к интервалам. Пусть функция $f(x)$ непрерывна на отрезке $[a, b]$.

### Основные теоремы (без доказательств, но с сутью)

1.  **Теорема Вейерштрасса (1): Ограниченность.**
    Непрерывная на отрезке функция ограничена на этом отрезке.
    *Суть:* График не улетает в бесконечность внутри отрезка.

2.  **Теорема Вейерштрасса (2): О достижении экстремумов.**
    Непрерывная на отрезке функция достигает на нем своего наибольшего ($M$) и наименьшего ($m$) значения.
    *Суть:* Существуют точки $x_{max}$ и $x_{min}$ на отрезке, где функция равна максимуму и минимуму.

3.  **Теорема Больцано-Коши (О промежуточном значении).**
    Если $f(a) \neq f(b)$, то для любого числа $C$, находящегося между $f(a)$ и $f(b)$, существует точка $c \in (a, b)$ такая, что $f(c) = C$.
    *Следствие (О корнях):* Если $f(a)$ и $f(b)$ имеют разные знаки (один плюс, другой минус), то внутри отрезка есть хотя бы один корень ($f(c) = 0$).

### Типовая задача
**Задача:** Доказать, что уравнение $x^3 + x - 1 = 0$ имеет хотя бы один корень на отрезке $[0, 1]$.

**Решение:**
1.  Рассмотрим функцию $f(x) = x^3 + x - 1$. Это многочлен, значит, он непрерывен везде, в том числе на $[0, 1]$.
2.  Вычислим значения на концах отрезка:
    *   $f(0) = 0^3 + 0 - 1 = -1$ (отрицательно).
    *   $f(1) = 1^3 + 1 - 1 = 1$ (положительно).
3.  Так как $f(0) < 0$ и $f(1) > 0$, функция меняет знак.
4.  По теореме Больцано-Коши (следствие о корнях), существует точка $c \in (0, 1)$, такая что $f(c) = 0$.
**Вывод:** Корень существует.

---

# 4. Понятие производной (Derivative)

### Интуитивное понимание
Производная показывает **скорость изменения** функции.
*   **Геометрически:** Это угловой коэффициент касательной к графику функции в данной точке (тангенс угла наклона).
*   **Физически:** Если $S(t)$ — путь, то $S'(t)$ — мгновенная скорость.

### Строгое определение
Пусть функция $f(x)$ определена в окрестности точки $x_0$.
**Производной** функции $f(x)$ в точке $x_0$ называется предел отношения приращения функции к приращению аргумента, когда приращение аргумента стремится к нулю:

$$
f'(x_0) = \lim_{\Delta x \to 0} \frac{f(x_0 + \Delta x) - f(x_0)}{\Delta x}
$$

Или через $h$:

$$
f'(x_0) = \lim_{h \to 0} \frac{f(x_0 + h) - f(x_0)}{h}
$$

Если этот предел существует и конечен, функция называется **дифференцируемой** в точке $x_0$.
*Важно:* Если функция дифференцируема в точке, она обязательно непрерывна в ней. Обратное неверно (пример: $y=|x|$ непрерывна в 0, но производной нет, "уголок").

### Наглядный пример
Найдем производную функции $f(x) = x^2$ в произвольной точке $x$ по определению.
1.  Приращение функции: $f(x + \Delta x) - f(x) = (x + \Delta x)^2 - x^2 = x^2 + 2x\Delta x + (\Delta x)^2 - x^2 = 2x\Delta x + (\Delta x)^2$.
2.  Отношение: $\frac{2x\Delta x + (\Delta x)^2}{\Delta x} = 2x + \Delta x$.
3.  Предел при $\Delta x \to 0$: $\lim_{\Delta x \to 0} (2x + \Delta x) = 2x$.
**Итог:** $(x^2)' = 2x$.

### Типовая задача
**Задача:** Найти уравнение касательной к графику функции $f(x) = x^2 - 3x + 5$ в точке с абсциссой $x_0 = 2$.

**Решение:**
Уравнение касательной имеет вид: $y = f(x_0) + f'(x_0)(x - x_0)$.

1.  **Найдем значение функции:**
2.  
    $f(2) = 2^2 - 3 \cdot 2 + 5 = 4 - 6 + 5 = 3$.
    
    Точка касания: $(2; 3)$.

4.  **Найдем производную функции** (используем правила дифференцирования, так как они выведены из определения):
   
    $f'(x) = (x^2)' - (3x)' + (5)' = 2x - 3$.

6.  **Найдем значение производной в точке:**

$f'(2) = 2 \cdot 2 - 3 = 1$.

    Это угловой коэффициент касательной ($k = 1$).

8.  **Составим уравнение:**

    $y = 3 + 1 \cdot (x - 2)$

    $y = 3 + x - 2$

    $y = x + 1$

**Ответ:** $y = x + 1$.

---

### Резюме модуля
1.  **Предел** — это поведение функции *около* точки.
2.  **Непрерывность** — это когда предел равен значению функции (нет разрывов).
3.  **Свойства на отрезке** — гарантируют существование максимумов, минимумов и корней для непрерывных функций.
4.  **Производная** — это предел отношения приращений, показывающий скорость роста и наклон касательной.

