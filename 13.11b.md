
Тема **Оценок параметров и метода максимального правдоподобия** открывает раздел **Математической статистики**. Здесь мы переходим от теории вероятностей (где закон распределения известен) к статистике (где закон неизвестен и должен быть оценен по выборке).

---

# 1. Интуитивное понимание

### Задача статистики
В теории вероятностей мы знаем распределение (например, $X \sim N(\mu, \sigma^2)$) и вычисляем вероятности.
В математической статистике у нас есть **выборка данных**, и мы должны оценить неизвестные параметры распределения ($\mu$, $\sigma^2$).

**Оценка** — это правило (формула), которое по выборке вычисляет приближенное значение параметра.

### Требования к хорошим оценкам
Не всякая формула подходит. Хорошая оценка должна быть:
1.  **Несмещенной:** В среднем не завышать и не занижать параметр.
2.  **Состоятельной:** С ростом выборки стремиться к истинному значению.
3.  **Эффективной:** Иметь минимально возможный разброс среди всех несмещенных оценок.

---

# 2. Классификация оценок

### Точечные и интервальные оценки

| Тип | Описание | Пример |
|:---:|:---:|:---:|
| **Точечная** | Одно число, приближающее параметр | $\bar{x} = 5.2$ |
| **Интервальная** | Интервал, накрывающий параметр с заданной вероятностью | $\mu \in [4.8, 5.6]$ с доверием 95% |

### Свойства точечных оценок

Пусть $\hat{\theta}_n$ — оценка параметра $\theta$ по выборке объема $n$.

#### 1. Несмещенность
Оценка называется **несмещенной**, если её математическое ожидание равно истинному параметру:
$$
E[\hat{\theta}_n] = \theta
$$
Если $E[\hat{\theta}_n] \neq \theta$, оценка **смещенная**. Смещение: $b(\hat{\theta}) = E[\hat{\theta}] - \theta$.

#### 2. Состоятельность
Оценка называется **состоятельной**, если она сходится по вероятности к истинному параметру при $n \to \infty$:
$$
\hat{\theta}_n \xrightarrow{P} \theta \quad \text{при } n \to \infty
$$
Или: $\lim_{n \to \infty} P(|\hat{\theta}_n - \theta| < \varepsilon) = 1$.

#### 3. Эффективность
Оценка называется **эффективной**, если она имеет минимальную дисперсию среди всех несмещенных оценок:
$$
D[\hat{\theta}_{\text{eff}}] \le D[\tilde{\theta}] \quad \text{для любой несмещенной } \tilde{\theta}
$$

#### 4. Достаточность
Оценка называется **достаточной**, если она содержит всю информацию о параметре, имеющуюся в выборке.

---

# 3. Функция правдоподобия

### Интуитивное понимание
**Функция правдоподобия** отвечает на вопрос: «Насколько вероятно получить нашу выборку при заданных параметрах?»
Мы ищем такие значения параметров, при которых наблюдаемые данные были бы **наиболее вероятны**.

### Строгое определение
Пусть $X_1, X_2, \dots, X_n$ — выборка из распределения с плотностью (или вероятностью) $f(x; \theta)$, где $\theta$ — неизвестный параметр.

**Функция правдоподобия** — это функция параметра $\theta$:
$$
L(\theta; x_1, \dots, x_n) = \prod_{i=1}^{n} f(x_i; \theta)
$$

Для независимых наблюдений — это произведение плотностей (вероятностей) каждого наблюдения.

### Логарифмическая функция правдоподобия
Так как произведение неудобно дифференцировать, используют логарифм:
$$
\ell(\theta) = \ln L(\theta) = \sum_{i=1}^{n} \ln f(x_i; \theta)
$$
Максимум $L(\theta)$ и $\ell(\theta)$ достигается при одном и том же $\theta$.

---

# 4. Метод максимального правдоподобия (ММП)

### Алгоритм нахождения оценок

1.  **Записать функцию правдоподобия:**
    $$
    L(\theta) = \prod_{i=1}^{n} f(x_i; \theta)
    $$
2.  **Прологарифмировать:**
    $$
    \ell(\theta) = \ln L(\theta)
    $$
3.  **Продифференцировать по $\theta$:**
    $$
    \frac{d\ell(\theta)}{d\theta} = 0
    $$
4.  **Решить уравнение** (уравнение правдоподобия) относительно $\theta$.
5.  **Получить оценку:** $\hat{\theta}_{\text{MLE}}$.

### Свойства оценок ММП

1.  **Состоятельность:** При $n \to \infty$ оценка сходится к истинному значению.
2.  **Асимптотическая нормальность:** При больших $n$ распределение оценки близко к нормальному.
3.  **Асимптотическая эффективность:** При больших $n$ дисперсия оценки минимальна (достигает нижней границы Крамера-Рао).
4.  **Инвариантность:** Если $\hat{\theta}$ — ММП-оценка для $\theta$, то $g(\hat{\theta})$ — ММП-оценка для $g(\theta)$.

---

# 5. Типовые задачи и решения

### Задача 1. Оценка для нормального распределения (ММП)
**Задание:** Найти оценки максимального правдоподобия для параметров $\mu$ и $\sigma^2$ нормального распределения $N(\mu, \sigma^2)$ по выборке $x_1, \dots, x_n$.

**Решение:**
1.  **Плотность:**
    $$
    f(x; \mu, \sigma^2) = \frac{1}{\sqrt{2\pi\sigma^2}} e^{-\frac{(x-\mu)^2}{2\sigma^2}}
    $$
2.  **Функция правдоподобия:**
    $$
    L(\mu, \sigma^2) = \prod_{i=1}^{n} \frac{1}{\sqrt{2\pi\sigma^2}} e^{-\frac{(x_i-\mu)^2}{2\sigma^2}} = (2\pi\sigma^2)^{-n/2} e^{-\frac{1}{2\sigma^2}\sum(x_i-\mu)^2}
    $$
3.  **Логарифмическая функция:**
    $$
    \ell(\mu, \sigma^2) = -\frac{n}{2}\ln(2\pi) - \frac{n}{2}\ln(\sigma^2) - \frac{1}{2\sigma^2}\sum_{i=1}^{n}(x_i-\mu)^2
    $$
4.  **Оценка для $\mu$:**
    $$
    \frac{\partial \ell}{\partial \mu} = \frac{1}{\sigma^2}\sum_{i=1}^{n}(x_i-\mu) = 0
    $$
    $$
    \sum x_i - n\mu = 0 \quad \Rightarrow \quad \hat{\mu} = \frac{1}{n}\sum_{i=1}^{n} x_i = \bar{x}
    $$
5.  **Оценка для $\sigma^2$:**
    $$
    \frac{\partial \ell}{\partial \sigma^2} = -\frac{n}{2\sigma^2} + \frac{1}{2(\sigma^2)^2}\sum(x_i-\mu)^2 = 0
    $$
    $$
    \hat{\sigma}^2 = \frac{1}{n}\sum_{i=1}^{n}(x_i - \bar{x})^2
    $$

**Ответ:** $\hat{\mu} = \bar{x}$, $\hat{\sigma}^2 = \frac{1}{n}\sum(x_i - \bar{x})^2$.

*(Примечание: Оценка дисперсии смещенная! Несмещенная оценка имеет делитель $n-1$)*.

---

### Задача 2. Оценка для экспоненциального распределения
**Задание:** Найти ММП-оценку для параметра $\lambda$ экспоненциального распределения $f(x) = \lambda e^{-\lambda x}$, $x \ge 0$.

**Решение:**
1.  **Функция правдоподобия:**
    $$
    L(\lambda) = \prod_{i=1}^{n} \lambda e^{-\lambda x_i} = \lambda^n e^{-\lambda \sum x_i}
    $$
2.  **Логарифм:**
    $$
    \ell(\lambda) = n\ln\lambda - \lambda \sum_{i=1}^{n} x_i
    $$
3.  **Производная:**
    $$
    \frac{d\ell}{d\lambda} = \frac{n}{\lambda} - \sum_{i=1}^{n} x_i = 0
    $$
4.  **Оценка:**
    $$
    \hat{\lambda} = \frac{n}{\sum x_i} = \frac{1}{\bar{x}}
    $$

**Ответ:** $\hat{\lambda} = \frac{1}{\bar{x}}$.

---

### Задача 3. Проверка несмещенности
**Задание:** Является ли оценка $\hat{\sigma}^2 = \frac{1}{n}\sum(x_i - \bar{x})^2$ несмещенной?

**Решение:**
1.  **Математическое ожидание:**
    $$
    E[\hat{\sigma}^2] = E\left[\frac{1}{n}\sum(x_i - \bar{x})^2\right]
    $$
2.  **Известный факт:**
    $$
    E\left[\sum(x_i - \bar{x})^2\right] = (n-1)\sigma^2
    $$
3.  **Вычисление:**
    $$
    E[\hat{\sigma}^2] = \frac{n-1}{n}\sigma^2 \neq \sigma^2
    $$
4.  **Вывод:** Оценка **смещенная**. Смещение: $-\frac{\sigma^2}{n}$.
5.  **Исправление:** Несмещенная оценка:
    $$
    S^2 = \frac{1}{n-1}\sum(x_i - \bar{x})^2
    $$

**Ответ:** Нет, оценка смещенная. Несмещенная имеет делитель $n-1$.

---

### Задача 4. Оценка для равномерного распределения
**Задание:** Найти ММП-оценку для параметра $\theta$ равномерного распределения $U[0, \theta]$.

**Решение:**
1.  **Плотность:**
    $$
    f(x; \theta) = \begin{cases} \frac{1}{\theta}, & 0 \le x \le \theta \\ 0, & \text{иначе} \end{cases}
    $$
2.  **Функция правдоподобия:**
    $$
    L(\theta) = \prod_{i=1}^{n} \frac{1}{\theta} = \frac{1}{\theta^n}, \quad \text{если } \max(x_i) \le \theta
    $$
3.  **Максимизация:**
    Функция $\frac{1}{\theta^n}$ убывает по $\theta$. Максимум достигается при **минимальном** допустимом $\theta$.
    Условие: $\theta \ge \max(x_i)$.
4.  **Оценка:**
    $$
    \hat{\theta} = \max(x_1, \dots, x_n) = X_{(n)}
    $$

**Ответ:** $\hat{\theta} = \max(x_i)$ (порядковая статистика).

---

### Задача 5. Сравнение эффективности оценок
**Задание:** Для $N(\mu, 1)$ сравнить эффективность оценок $\hat{\mu}_1 = \bar{X}$ и $\hat{\mu}_2 = X_1$ (первое наблюдение).

**Решение:**
1.  **Дисперсия $\hat{\mu}_1$:**
    $$
    D[\bar{X}] = \frac{\sigma^2}{n} = \frac{1}{n}
    $$
2.  **Дисперсия $\hat{\mu}_2$:**
    $$
    D[X_1] = \sigma^2 = 1
    $$
3.  **Сравнение:**
    При $n > 1$: $D[\bar{X}] < D[X_1]$.
4.  **Вывод:** $\bar{X}$ эффективнее, так как использует всю выборку. При $n \to \infty$ дисперсия $\bar{X} \to 0$, а $X_1$ остается 1.

**Ответ:** $\bar{X}$ эффективнее (дисперсия в $n$ раз меньше).

---

# 6. Резюме модуля

| Свойство | Формула / Критерий |
|:---:|:---:|
| **Несмещенность** | $E[\hat{\theta}] = \theta$ |
| **Состоятельность** | $\hat{\theta}_n \xrightarrow{P} \theta$ |
| **Эффективность** | Минимальная дисперсия среди несмещенных |
| **Функция правдоподобия** | $L(\theta) = \prod f(x_i; \theta)$ |
| **ММП-оценка** | $\hat{\theta} = \arg\max L(\theta)$ |

**Сравнение методов оценивания:**

| Метод | Плюсы | Минусы |
|:---:|:---:|:---:|
| **Метод моментов** | Простота вычислений | Может быть неэффективным |
| **ММП** | Асимптотическая эффективность, инвариантность | Может быть смещенным, сложнее вычислять |

**Ключевые выводы:**
1.  **Несмещенность** важна для малых выборок, **состоятельность** — для больших.
2.  **ММП-оценки** — золотой стандарт статистики (состоятельны, асимптотически эффективны).
3.  **Оценка дисперсии** по ММП смещенная (делитель $n$), несмещенная имеет делитель $n-1$.
4.  **Эффективность** означает минимальный разброс оценок вокруг истинного параметра.
5.  **Достаточные статистики** содержат всю информацию о параметре (например, $\bar{X}$ для нормального $\mu$).

