Вот тот же исправленный и структурированный конспект, адаптированный для файла `.md`. Все формулы обёрнуты в `\(...\)` для инлайн-режима и `\[...\]` для отображения в отдельной строке, что гарантирует корректное отображение в любом Markdown-редакторе с поддержкой LaTeX (например, Typora, Jupyter, GitLab, GitHub с плагинами).

```markdown
# Конспект к экзамену: Математический анализ, Алгебра, Диффуры, ТВиМС, Численные методы

## Раздел 1. Математический анализ

### 1. Предел, непрерывность, производная
- **Предел:**  
  \[
  \lim_{x \to a} f(x) = L \;\Leftrightarrow\; \forall \varepsilon>0\; \exists \delta>0:\; 0<|x-a|<\delta \Rightarrow |f(x)-L|<\varepsilon
  \]
- **Непрерывность:** \(\lim_{x \to a} f(x) = f(a)\).
- **Свойства на \([a,b]\):** достижимость min/max (Вейерштрасс), прохождение всех промежуточных значений (Больцано-Коши), равномерная непрерывность (Кантор).
- **Производная:**  
  \[
  f'(x_0) = \lim_{h \to 0} \frac{f(x_0+h)-f(x_0)}{h}
  \]

### 2. Функции многих переменных
- **Полный дифференциал:**  
  \[
  df = \frac{\partial f}{\partial x}dx + \frac{\partial f}{\partial y}dy
  \]
- **Геометрический смысл:** касательная плоскость к \(z=f(x,y)\) в точке \((x_0,y_0)\):  
  \[
  z - z_0 = f_x(x_0,y_0)(x-x_0) + f_y(x_0,y_0)(y-y_0)
  \]
- **Достаточное условие дифференцируемости:** частные производные \(f_x, f_y\) непрерывны в окрестности.
- **Градиент:**  
  \[
  \nabla f = \left( \frac{\partial f}{\partial x}, \frac{\partial f}{\partial y} \right)
  \]

### 3. Интеграл Римана. Формула Ньютона-Лейбница
- **Первообразная:** \(F'(x)=f(x)\).
- **Неопределённый интеграл:** \(\int f(x)dx = F(x)+C\).
- **Определённый интеграл (Риман):**  
  \[
  \int_a^b f(x)dx = \lim_{\max \Delta x_i \to 0} \sum_{i=1}^n f(\xi_i)\Delta x_i
  \]
- **Формула Ньютона-Лейбница:**  
  \[
  \int_a^b f(x)dx = F(b)-F(a)
  \]

### 4. Числовые ряды
- **Сходимость:** \(\sum_{n=1}^\infty a_n = \lim_{N\to\infty} S_N\) (конечный предел).
- **Критерий Коши:**  
  \[
  \forall \varepsilon>0\;\exists N: \forall n>N,\forall p\in\mathbb{N}\; |a_{n+1}+\dots+a_{n+p}|<\varepsilon
  \]
- **Признаки сходимости:** Даламбера (\(\lim \frac{a_{n+1}}{a_n}<1\)), Коши (\(\lim \sqrt[n]{|a_n|}<1\)), интегральный (сходимость \(\int f\) и \(\sum f(n)\) эквивалентна).

### 5. Абсолютная и условная сходимость
- **Абсолютная:** \(\sum |a_n|\) сходится \(\Rightarrow\) \(\sum a_n\) сходится.
- **Условная:** \(\sum a_n\) сходится, но \(\sum |a_n|\) расходится (признак Лейбница: \(a_n=(-1)^n b_n,\; b_n\downarrow 0\)).
- **Свойства:** абсолютно сходящийся ряд можно переставлять; условно сходящийся — можно получить любую сумму (теорема Римана).
- **Умножение рядов (Коши):**  
  \[
  c_n = \sum_{k=0}^n a_k b_{n-k},\quad \sum c_n = \left(\sum a_n\right)\left(\sum b_n\right) \text{ при абс. сход.}
  \]

### 6. Функциональные ряды
- **Равномерная сходимость:**  
  \[
  \sup_{x\in X} \left|\sum_{k=n+1}^\infty f_k(x)\right| \to 0 \quad (n\to\infty)
  \]
- **Признак Вейерштрасса:** \(|f_n(x)|\le M_n\) и \(\sum M_n\) сходится \(\Rightarrow\) \(\sum f_n(x)\) сходится равномерно.
- **Свойства:** непрерывность суммы, почленное интегрирование и дифференцирование (при равномерной сходимости производных).

### 7. Степенные ряды. Ряд Тейлора
- **Степенной ряд:** \(\sum_{n=0}^\infty c_n (x-a)^n\).
- **Радиус сходимости (Коши-Адамар):**  
  \[
  R = \frac{1}{\limsup_{n\to\infty} \sqrt[n]{|c_n|}}
  \]
- **Свойства:** внутри \((a-R,a+R)\) — равномерная сходимость, почленное интегрирование и дифференцирование.
- **Ряд Тейлора:**  
  \[
  f(x)=\sum_{n=0}^\infty \frac{f^{(n)}(a)}{n!}(x-a)^n
  \]

### 8. Несобственные интегралы
- **1-й род:** \(\int_a^\infty f(x)dx = \lim_{b\to\infty} \int_a^b f(x)dx\).
- **2-й род (особенность в \(b\)):** \(\int_a^b f(x)dx = \lim_{\varepsilon\to0^+} \int_a^{b-\varepsilon} f(x)dx\).
- **Сходимость:** абсолютная (\(\int |f|\) сходится) и условная.

### 9. Теоремы Ферма, Ролля, Лагранжа, Коши
- **Ферма:** \(x_0\) — локальный экстремум, \(f\) дифференцируема \(\Rightarrow\) \(f'(x_0)=0\).
- **Ролль:** \(f(a)=f(b)\), непрерывна на \([a,b]\), дифференцируема на \((a,b)\) \(\Rightarrow\) \(\exists c\in(a,b): f'(c)=0\).
- **Лагранж:** \(\exists c\in(a,b): \frac{f(b)-f(a)}{b-a} = f'(c)\).
- **Коши:** \(\frac{f(b)-f(a)}{g(b)-g(a)} = \frac{f'(\xi)}{g'(\xi)}\), если \(g'(x)\neq0\).

---

## Раздел 2. Алгебра и геометрия

### 1. Прямые и плоскости в пространстве
- **Плоскость:** общее уравнение \(Ax+By+Cz+D=0\), нормаль \(\mathbf{n}=(A,B,C)\).
- **Прямая:** каноническое \(\frac{x-x_0}{m}=\frac{y-y_0}{n}=\frac{z-z_0}{p}\), направляющий вектор \(\mathbf{s}=(m,n,p)\).
- **Расстояние от точки до плоскости:**  
  \[
  d = \frac{|Ax_0+By_0+Cz_0+D|}{\sqrt{A^2+B^2+C^2}}
  \]

### 2. Кривые второго порядка
- **Эллипс:** \(\frac{x^2}{a^2}+\frac{y^2}{b^2}=1\).
- **Гипербола:** \(\frac{x^2}{a^2}-\frac{y^2}{b^2}=1\).
- **Парабола:** \(y^2=2px\).
- **Приведение к каноническому виду:** поворот (уничтожение \(xy\)) и перенос (выделение полных квадратов).

### 3. Системы линейных алгебраических уравнений (СЛАУ)
- **Методы:** Гаусса, Крамера (\(x_i = \det A_i / \det A\)), матричный (\(X=A^{-1}B\)).
- **Кронекер-Капелли:** система совместна \(\iff \mathrm{rank}(A)=\mathrm{rank}(A|B)\).
- **Однородная система \(AX=0\):** ФСР состоит из \(n-\mathrm{rank}(A)\) линейно независимых решений. Общее решение — их линейная комбинация.
- **Неоднородная система:** \(X = X_{\text{част}} + X_{\text{общ. однор.}}\).

### 4. Собственные векторы и собственные значения
- **Определение:** \(A\mathbf{v} = \lambda \mathbf{v},\ \mathbf{v}\neq 0\).
- **Характеристический многочлен:** \(\det(A - \lambda I)=0\).
- \(\lambda\) — корни, \(\mathbf{v}\in \ker(A-\lambda I)\).

### 5. Линейные операторы
- **Определение:** \(A(\alpha x+\beta y)=\alpha Ax+\beta Ay\).
- **Матрица оператора:** \([A(e_1),\dots,A(e_n)]\) в базисе \(\{e_i\}\).
- **Действия:** \((A+B)x=Ax+Bx\), \((cA)x=c(Ax)\), \((AB)x=A(Bx)\).

---

## Раздел 3. Дифференциальные уравнения

### 1. ДУ первого порядка. Теорема Коши
- **Вид:** \(y' = f(x,y)\).
- **Теорема Коши (существование и единственность):** если \(f\) и \(\frac{\partial f}{\partial y}\) непрерывны в области, то через \((x_0,y_0)\) проходит единственное решение.

### 2. ЛДУ второго порядка
- **Однородное:** \(y'' + p(x)y' + q(x)y = 0\).
- **Линейная зависимость функций:** \(\exists c_1,c_2\) не все 0: \(c_1y_1+c_2y_2=0\).
- **Фундаментальная система решений (ФСР):** два ЛНЗ решения.
- **Определитель Вронского:**  
  \[
  W(y_1,y_2)=\det\begin{pmatrix}y_1&y_2\\ y_1'&y_2'\end{pmatrix} \neq 0 \text{ для ФСР}
  \]
- **Общее решение:** \(y = C_1 y_1 + C_2 y_2\).
- **Неоднородное:** \(y_{\text{общ}} = y_{\text{одн}} + y_{\text{част}}\).

### 3. Устойчивость по Ляпунову. Теоремы Ляпунова и Четаева
- **Устойчивость:** \(\forall\varepsilon>0\;\exists\delta>0: \|x(0)\|<\delta \Rightarrow \|x(t)\|<\varepsilon\ \forall t\ge0\).
- **Асимптотическая устойчивость:** устойчивость + \(\lim_{t\to\infty}\|x(t)\|=0\).
- **Теорема Ляпунова:** существует \(V(x)>0,\ \dot V\le 0\) \(\Rightarrow\) устойчивость; \(\dot V<0\) \(\Rightarrow\) асимптотическая устойчивость.
- **Теорема Четаева:** существует \(V(x)>0,\ \dot V>0\) в любой близости к 0 \(\Rightarrow\) неустойчивость.

### 4. ДУЧП первого порядка
- **Квазилинейное:** \(a(x,y,u)u_x + b(x,y,u)u_y = c(x,y,u)\).
- **Метод характеристик:**  
  \[
  \frac{dx}{a} = \frac{dy}{b} = \frac{du}{c}
  \]
- **Структура общего решения:** \(\Phi(\phi_1,\phi_2)=0\), где \(\phi_1,\phi_2\) — первые интегралы.

---

## Раздел 4. Дискретная математика и теория вероятностей

### 1. Комбинаторика
- **Перестановки:** \(P_n = n!\).
- **Размещения:** \(A_n^k = \frac{n!}{(n-k)!}\).
- **Сочетания:** \(C_n^k = \binom{n}{k} = \frac{n!}{k!(n-k)!}\).
- **Формула включений-исключений:**  
  \[
  \left|\bigcup_{i=1}^n A_i\right| = \sum_{i=1}^n |A_i| - \sum_{1\le i<j\le n} |A_i\cap A_j| + \sum_{1\le i<j<k\le n} |A_i\cap A_j\cap A_k| - \cdots + (-1)^{n-1}\left|\bigcap_{i=1}^n A_i\right|
  \]
- **Бином Ньютона:** \((a+b)^n = \sum_{k=0}^n \binom{n}{k} a^{n-k} b^k\).
- **Свойства биномиальных коэффициентов:** \(\binom{n}{k}=\binom{n}{n-k}\), \(\binom{n}{k}=\binom{n-1}{k-1}+\binom{n-1}{k}\).

### 2. Булевы функции
- **СДНФ (совершенная дизъюнктивная нормальная форма):**  
  \[
  f(x_1,\dots,x_n) = \bigvee_{(a_1,\dots,a_n): f(a)=1} \left( x_1^{a_1} \wedge \cdots \wedge x_n^{a_n} \right), \quad x^a = \begin{cases}x & a=1\\ \neg x & a=0\end{cases}
  \]
- **СКНФ (совершенная конъюнктивная нормальная форма):**  
  \[
  f(x_1,\dots,x_n) = \bigwedge_{(a_1,\dots,a_n): f(a)=0} \left( x_1^{a_1} \vee \cdots \vee x_n^{a_n} \right), \quad x^a = \begin{cases}\neg x & a=1\\ x & a=0\end{cases}
  \]
- **Принцип двойственности:** замена \(\wedge\leftrightarrow\vee,\ 0\leftrightarrow1\) даёт двойственную функцию.
- **Алгоритм Куайна-Мак-Класки:** построение таблицы простых импликант и поиск минимального покрытия.

### 3. Производящие функции, рекуррентные соотношения
- **Производящая функция:** \(G(x) = \sum_{n\ge0} a_n x^n\).
- **Линейное рекуррентное соотношение:** \(a_n = c_1 a_{n-1} + \dots + c_k a_{n-k}\).
- **Характеристическое уравнение:** \(\lambda^k - c_1\lambda^{k-1} - \dots - c_k = 0\). Решение: \(a_n = \sum P_i(n) \lambda_i^n\).

### 4. Аксиоматика вероятности
- **\(\sigma\)-алгебра \(\mathcal{F}\):** замкнута относительно счётных объединений, пересечений и дополнений.
- **Аксиомы Колмогорова:** \(P(\Omega)=1,\ P(A)\ge0,\ P(\bigcup A_i)=\sum P(A_i)\) для несовместных.
- **Классическая вероятность:** \(P(A)=\frac{|A|}{|\Omega|}\).
- **Геометрическая вероятность:** \(P(A)=\frac{\text{mes}(A)}{\text{mes}(\Omega)}\).

### 5. Схема Бернулли. Предельные теоремы
- **Формула Бернулли:** \(P_n(k) = C_n^k p^k q^{n-k}\).
- **Локальная Муавр-Лаплас:**  
  \[
  P_n(k) \sim \frac{1}{\sqrt{npq}} \varphi\left(\frac{k-np}{\sqrt{npq}}\right), \quad \varphi(x)=\frac{e^{-x^2/2}}{\sqrt{2\pi}}
  \]
- **Интегральная Муавр-Лаплас:**  
  \[
  P\left(a \le \frac{k-np}{\sqrt{npq}} \le b\right) \to \Phi(b)-\Phi(a), \quad \Phi(x)=\int_{-\infty}^x \varphi(t)dt
  \]
- **Теорема Пуассона:** при \(n\to\infty,\ p\to0,\ np\to\lambda\):  
  \[
  P_n(k) \to e^{-\lambda}\frac{\lambda^k}{k!}
  \]

### 6. Условная вероятность, независимость, формулы
- **Условная вероятность:** \(P(A|B)=\frac{P(A\cap B)}{P(B)}\).
- **Формула умножения:** \(P(A\cap B)=P(A)P(B|A)\).
- **Независимость в совокупности:** для любых поднаборов \(P(\bigcap A_{i_k})=\prod P(A_{i_k})\).
- **Формула полной вероятности:** \(P(A)=\sum_{i} P(B_i)P(A|B_i)\).
- **Формула Байеса:**  
  \[
  P(B_k|A)=\frac{P(B_k)P(A|B_k)}{\sum_i P(B_i)P(A|B_i)}
  \]

### 7. Дискретные случайные величины
- **Функция распределения:** \(F(x)=P(X\le x)=\sum_{x_i\le x} p_i\).
- **Биномиальное:** \(p_k=C_n^k p^k q^{n-k}\), \(E=np,\ D=npq\).
- **Пуассона:** \(p_k=e^{-\lambda}\lambda^k/k!\), \(E=\lambda,\ D=\lambda\).
- **Геометрическое:** \(p_k=(1-p)^{k-1}p\), \(E=1/p\).

### 8. Непрерывные случайные величины
- **Плотность:** \(f(x)=F'(x)\), \(F(x)=\int_{-\infty}^x f(t)dt\).
- **Равномерное \([a,b]\):** \(f(x)=1/(b-a)\), \(E=(a+b)/2\).
- **Экспоненциальное \(\lambda\):** \(f(x)=\lambda e^{-\lambda x}\ (x\ge0)\), \(E=1/\lambda\).
- **Нормальное \(N(\mu,\sigma^2)\):**  
  \[
  f(x)=\frac{1}{\sigma\sqrt{2\pi}}e^{-(x-\mu)^2/(2\sigma^2)},\quad E=\mu,\ D=\sigma^2
  \]
- **Гамма-распределение:** \(f(x)=\frac{\beta^\alpha}{\Gamma(\alpha)}x^{\alpha-1}e^{-\beta x},\ x>0\).

### 9. Многомерные случайные величины
- **Совместная функция распределения:** \(F(x,y)=P(X\le x, Y\le y)\).
- **Совместная плотность:** \(f(x,y)=\frac{\partial^2 F}{\partial x\partial y}\). Маргинальные: \(f_X(x)=\int f(x,y)dy\).

### 10. Математическое ожидание, дисперсия, ковариация
- **Матожидание:** \(E[X]=\int x f(x)dx\) (непр.) / \(\sum x_i p_i\) (дискр.). Свойства: \(E[aX+b]=aE[X]+b\).
- **Дисперсия:** \(D[X]=E[(X-E[X])^2]=E[X^2]-E^2[X]\). Свойства: \(D[aX+b]=a^2D[X]\).
- **Ковариация:** \(\mathrm{Cov}(X,Y)=E[XY]-E[X]E[Y]\).
- **Коэффициент корреляции:** \(\rho_{XY}=\frac{\mathrm{Cov}(X,Y)}{\sqrt{D[X]D[Y]}}\in[-1,1]\).

### 11. Характеристические функции
- **Определение:** \(\phi_X(t)=E[e^{itX}]\).
- **Свойства:** \(\phi(0)=1\), \(|\phi(t)|\le1\), \(\phi_{aX+b}(t)=e^{ibt}\phi_X(at)\).
- **Для нормального \(N(\mu,\sigma^2)\):** \(\phi(t)=e^{i\mu t - \sigma^2 t^2/2}\).
- **Для Пуассона(\(\lambda\)):** \(\phi(t)=e^{\lambda(e^{it}-1)}\).
- **Для биномиального \(B(n,p)\):** \(\phi(t)=(pe^{it}+q)^n\).

### 12. Неравенство Чебышёва, закон больших чисел, ЦПТ
- **Неравенство Чебышёва:** \(P(|X-E[X]|\ge\varepsilon) \le \frac{D[X]}{\varepsilon^2}\).
- **Закон больших чисел (Хинчин):** \(\frac{1}{n}\sum_{i=1}^n X_i \xrightarrow{p} \mu\) для i.i.d.
- **Центральная предельная теорема (Линдеберг-Леви):**  
  \[
  \frac{\sum_{i=1}^n X_i - n\mu}{\sigma\sqrt{n}} \xrightarrow{d} N(0,1)
  \]

### 13. Математическая статистика: оценки и гипотезы
- **Несмещённость:** \(E[\hat{\theta}]=\theta\).
- **Состоятельность:** \(\hat{\theta}\xrightarrow{p}\theta\).
- **Эффективность:** минимальная дисперсия среди несмещённых оценок.
- **Функция правдоподобия:** \(L(\theta)=\prod_{i=1}^n f(x_i;\theta)\).
- **Оценка максимального правдоподобия (ОМП):** \(\hat{\theta}_{\text{МП}} = \arg\max L(\theta)\).
- **Ошибки:** 1-го рода (отвергнуть \(H_0\) при \(H_0\) верна, \(\alpha\)), 2-го рода (принять \(H_0\) при \(H_1\) верна, \(\beta\)). **Мощность:** \(1-\beta\).
- **Критерий \(\chi^2\) Пирсона:**  
  \[
  \sum_{i=1}^k \frac{(O_i-E_i)^2}{E_i} \sim \chi^2_{k-1-s}
  \]
  (\(s\) — число оценённых параметров).

### 14. Элементы математической статистики (выборки, гистограммы)
- **Выборка:** \(X_1,\dots,X_n\).
- **Вариационный ряд:** упорядоченная выборка.
- **Эмпирическая функция распределения:** \(F_n(x) = \frac{\#\{X_i \le x\}}{n}\).
- **Гистограмма и полигон частот:** оценки плотности распределения.
- **Выборочные моменты:** \(\bar{X} = \frac{1}{n}\sum X_i\), \(s^2 = \frac{1}{n}\sum (X_i-\bar{X})^2\).

---

## Раздел 5. Численные методы, функциональный анализ, оптимизация

### 1. Ортогонализация Шмидта, неравенство Бесселя, равенство Парсеваля
- **Процесс Грама-Шмидта:**  
  \[
  u_1 = v_1,\quad u_k = v_k - \sum_{i=1}^{k-1} \frac{\langle v_k, u_i \rangle}{\langle u_i, u_i \rangle} u_i
  \]
- **Неравенство Бесселя:** \(\sum_{k=1}^\infty |\langle f, e_k\rangle|^2 \le \|f\|^2\).
- **Равенство Парсеваля:** \(\sum_{k=1}^\infty |\langle f, e_k\rangle|^2 = \|f\|^2\) (для полной ортонормированной системы).

### 2. Интерполяционные формулы
- **Интерполяционный многочлен Лагранжа:**  
  \[
  L_n(x) = \sum_{i=0}^n y_i \prod_{j\neq i} \frac{x-x_j}{x_i-x_j}
  \]
- **Интерполяционная формула Ньютона (вперёд, равные узлы):**  
  \[
  P_n(x)=y_0 + q\Delta y_0 + \frac{q(q-1)}{2!}\Delta^2 y_0 + \dots,\quad q=\frac{x-x_0}{h}
  \]

### 3. Численное решение задачи Коши для ОДУ
- **Задача:** \(y'=f(x,y),\ y(x_0)=y_0\).
- **Метод Эйлера (явный):** \(y_{n+1}=y_n + h f(x_n,y_n)\).
- **Метод 2-го порядка (усреднённый):**  
  \[
  y_{n+1}=y_n + h f\!\left(x_n+\frac{h}{2},\ y_n+\frac{h}{2}f(x_n,y_n)\right)
  \]
- **Метод Рунге-Кутта 4-го порядка:**  
  \[
  \begin{cases}
  k_1 = f(x_n,y_n),\\
  k_2 = f(x_n+\frac{h}{2},y_n+\frac{h}{2}k_1),\\
  k_3 = f(x_n+\frac{h}{2},y_n+\frac{h}{2}k_2),\\
  k_4 = f(x_n+h,y_n+hk_3),\\
  y_{n+1}=y_n + \frac{h}{6}(k_1+2k_2+2k_3+k_4)
  \end{cases}
  \]

### 4. Треугольное разложение матриц. Метод Гаусса
- \(A = LU\) (\(L\) — нижняя треугольная с единицами на диагонали, \(U\) — верхняя треугольная).
- **Метод Гаусса:**  
  - Прямой ход: построение \(L\) и \(U\).  
  - Обратный ход: \(Ly=b \Rightarrow y\), \(Ux=y\).

### 5. Нормы векторов и матриц
- **Векторные нормы:**  
  \(\|x\|_1=\sum|x_i|,\quad \|x\|_2=\sqrt{\sum x_i^2},\quad \|x\|_\infty=\max|x_i|\).
- **Подчинённые матричные нормы:** \(\|A\|_p = \max_{\|x\|_p=1}\|Ax\|_p\).  
  \(\|A\|_1 = \max_j \sum_i |a_{ij}|\) (максимум по столбцам),  
  \(\|A\|_\infty = \max_i \sum_j |a_{ij}|\) (максимум по строкам),  
  \(\|A\|_2 = \sqrt{\rho(A^T A)}\) (спектральная норма).

### 6. Итерационные методы решения СЛАУ
- \(x^{(k+1)} = B x^{(k)} + c\). Сходимость \(\iff \rho(B) < 1\).
- **Метод Якоби:**  
  \[
  x_i^{(k+1)} = \frac{1}{a_{ii}}\left(b_i - \sum_{j\neq i} a_{ij}x_j^{(k)}\right)
  \]

### 7. Нелинейные уравнения. Метод Ньютона. Сжимающие отображения
- **Метод Ньютона (одномерный, 2-й порядок):**  
  \[
  x_{n+1}=x_n - \frac{f(x_n)}{f'(x_n)}
  \]
- **Многомерный метод Ньютона:**  
  \[
  x_{n+1}=x_n - J_f^{-1}(x_n) f(x_n),\quad J_f\text{ — матрица Якоби}
  \]
- **Принцип сжимающих отображений:**  
  \(\|F(x)-F(y)\| \le q\|x-y\|,\ q<1\) \(\Rightarrow\) единственная неподвижная точка; итерации \(x_{n+1}=F(x_n)\) сходятся.

### 8. Поиск минимума: градиентный спуск
- **Стационарная точка:** \(\nabla f(x)=0\).
- **Градиентный спуск:** \(x_{k+1}=x_k - \alpha_k \nabla f(x_k)\).
- **Наискорейший спуск:** \(\alpha_k = \arg\min_{\alpha>0} f(x_k - \alpha \nabla f(x_k))\).

### 9. Функциональный анализ: Банах-Штейнгауз, об обратном операторе
- **Теорема Банаха-Штейнгауза (принцип равномерной ограниченности):**  
  если \(\sup_n \|A_n x\| < \infty\) для каждого \(x\in X\), то \(\sup_n \|A_n\| < \infty\).
- **Теорема Банаха об обратном операторе:**  
  если \(A: X\to Y\) — линейный биективный непрерывный оператор (\(X,Y\) — банаховы), то \(A^{-1}\) непрерывен.

### 10. Уравнения математической физики
- **Метод Фурье (разделение переменных):**  
  \(u(x,t)=X(x)T(t)\) \(\Rightarrow\) задача Штурма-Лиувилля \(\Rightarrow\) разложение по собственным функциям.
- **Метод Галёркина:**  
  \(u_n(x,t)=\sum_{k=1}^n c_k(t)\phi_k(x)\), проекция невязки на \(\phi_j\) даёт систему ОДУ для \(c_k(t)\).
- **Сильно непрерывная полугруппа:** \(T(t): X\to X\), \(T(0)=I\), \(T(t+s)=T(t)T(s)\), \(\lim_{t\to0}\|T(t)x-x\|=0\). Решение \(u_t = Au,\ u(0)=u_0\): \(u(t)=T(t)u_0\).

### 11. Вариационное исчисление (Эйлер-Лагранж, сопряжённые точки)
- **Функционал:** \(J[y]=\int_{x_1}^{x_2} L(x,y,y')dx\).
- **Уравнение Эйлера-Лагранжа (необходимое условие экстремума):**  
  \[
  \frac{d}{dx}\left( \frac{\partial L}{\partial y'} \right) - \frac{\partial L}{\partial y} = 0
  \]
- **Сопряжённые точки:** точки \(x=\tilde x > x_1\), в которых нетривиальное решение уравнения Якоби обращается в ноль. Обеспечивают достаточные условия экстремума.

### 12. Комплексный анализ
- **Условия Коши-Римана:** \(u_x = v_y,\ u_y = -v_x\).
- **Теорема Коши:** \(\oint_\gamma f(z)dz = 0\) для аналитической \(f\) в односвязной области.
- **Интегральная формула Коши:**  
  \[
  f(a)=\frac{1}{2\pi i}\oint_\gamma \frac{f(z)}{z-a}dz
  \]
- **Ряд Лорана:**  
  \[
  f(z)=\sum_{n=-\infty}^\infty c_n (z-z_0)^n,\quad c_n=\frac{1}{2\pi i}\oint_\gamma \frac{f(z)}{(z-z_0)^{n+1}}dz
  \]
- **Особые точки:** устранимая (\(c_n=0\) для \(n<0\)), полюс (конечное число \(n<0\)), существенно особая (бесконечное число \(n<0\)).
- **Вычет:** \(\mathrm{Res}(f,z_0)=c_{-1}=\frac{1}{2\pi i}\oint_\gamma f(z)dz\).
- **Основная теорема о вычетах:** \(\oint_\gamma f(z)dz = 2\pi i \sum \mathrm{Res}(f,z_k)\).
```

### Инструкция по сохранению:
1. Скопируйте текст выше.
2. Вставьте в любой текстовый редактор (Блокнот, VS Code, Notepad++).
3. Сохраните файл с именем, например, `exam_cheat_sheet.md`.
4. Для просмотра формул откройте в Markdown-редакторе с поддержкой LaTeX (Typora, Jupyter, Overleaf, или в VS Code с расширением `Markdown+Math`).

Если нужен плоский текст без формул (для печати без рендеринга), дайте знать — я заменю LaTeX на юникод или словесные описания.
