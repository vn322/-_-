
Тема **Норм векторов и матриц** — это фундамент численного анализа и линейной алгебры. Нормы позволяют измерять «размер» векторов и матриц, оценивать погрешности вычислений и анализировать устойчивость алгоритмов.

---

# 1. Интуитивное понимание

### Что такое норма?
**Норма** — это обобщение понятия «длины» или «величины» на векторы и матрицы. Она отвечает на вопрос: «Насколько велик этот объект?»

**Аналогии:**
*   **Вектор:** Длина стрелки в пространстве.
*   **Матрица:** Максимальное «растяжение», которое матрица может применить к вектору.
*   **Погрешность:** Норма разности между точным и приближенным решением показывает ошибку.

### Зачем это нужно?
1.  **Оценка погрешности:** $\|x_{\text{точное}} - x_{\text{прибл}}\|$
2.  **Устойчивость алгоритмов:** Число обусловленности матрицы $\|A\| \cdot \|A^{-1}\|$
3.  **Сходимость итераций:** $\|x_{k+1} - x_k\| \to 0$
4.  **Оптимизация:** Минимизация нормы невязки в методе наименьших квадратов.

---

# 2. Строгие определения

## Норма вектора

**Нормой** вектора $x \in \mathbb{R}^n$ называется функция $\|x\|: \mathbb{R}^n \to \mathbb{R}$, удовлетворяющая трем аксиомам:

| Аксиома | Формулировка |
|:---:|:---:|
| **Неотрицательность** | $\|x\| \ge 0$, причем $\|x\| = 0 \Leftrightarrow x = 0$ |
| **Однородность** | $\|\alpha x\| = |\alpha| \cdot \|x\|$ для любого $\alpha \in \mathbb{R}$ |
| **Неравенство треугольника** | $\|x + y\| \le \|x\| + \|y\|$ |

## Норма матрицы

**Нормой** матрицы $A \in \mathbb{R}^{m \times n}$ называется функция $\|A\|: \mathbb{R}^{m \times n} \to \mathbb{R}$, удовлетворяющая аксиомам нормы, плюс:

| Аксиома | Формулировка |
|:---:|:---:|
| **Согласованность** | $\|Ax\| \le \|A\| \cdot \|x\|$ |
| **Субмультипликативность** | $\|AB\| \le \|A\| \cdot \|B\|$ (для квадратных матриц) |

---

# 3. Основные нормы векторов

## Норма $L_1$ (Манхэттенская)
Сумма модулей компонент:

$$
\|x\|_1 = \sum_{i=1}^{n} |x_i|
$$

**Геометрический смысл:** Расстояние в городе с прямоугольной сеткой улиц.

## Норма $L_2$ (Евклидова)
Корень из суммы квадратов:

$$
\|x\|_2 = \sqrt{\sum_{i=1}^{n} x_i^2} = \sqrt{x^T x}
$$

**Геометрический смысл:** Обычная длина вектора в пространстве.

## Норма $L_\infty$ (Максимальная)
Максимум модулей компонент:

$$
\|x\|_\infty = \max_{1 \le i \le n} |x_i|
$$

**Геометрический смысл:** Размер наибольшей компоненты.

## Норма $L_p$ (Общая)

$$
\|x\|_p = \left( \sum_{i=1}^{n} |x_i|^p \right)^{1/p}, \quad p \ge 1
$$

---

# 4. Основные нормы матриц

## Индуцированные (естественные) нормы
Определяются через действие матрицы на вектор:

$$
\|A\| = \max_{x \neq 0} \frac{\|Ax\|}{\|x\|} = \max_{\|x\|=1} \|Ax\|
$$

### Индуцированная $L_1$-норма (по столбцам)
Максимальная сумма модулей элементов **столбца**:

$$
\|A\|_1 = \max_{1 \le j \le n} \sum_{i=1}^{m} |a_{ij}|
$$

### Индуцированная $L_\infty$-норма (по строкам)
Максимальная сумма модулей элементов **строки**:

$$
\|A\|_\infty = \max_{1 \le i \le m} \sum_{j=1}^{n} |a_{ij}|
$$

### Индуцированная $L_2$-норма (Спектральная)
Максимальное сингулярное число матрицы:

$$
\|A\|_2 = \sigma_{\max}(A) = \sqrt{\lambda_{\max}(A^T A)}
$$

## Норма Фробениуса
Аналог евклидовой нормы для матриц:

$$
\|A\|_F = \sqrt{\sum_{i=1}^{m} \sum_{j=1}^{n} |a_{ij}|^2} = \sqrt{\text{tr}(A^T A)}
$$

**Важно:** Норма Фробениуса **не является** индуцированной, но согласована с векторной $L_2$-нормой.

---

# 5. Свойства норм

### Общие свойства

| Свойство | Формула |
|:---:|:---:|
| **Неотрицательность** | $\|x\| \ge 0$ |
| **Нуль-вектор** | $\|x\| = 0 \Leftrightarrow x = 0$ |
| **Однородность** | $\|\alpha x\| = |\alpha| \|x\|$ |
| **Неравенство треугольника** | $\|x + y\| \le \|x\| + \|y\|$ |
| **Обратное неравенство** | $\|x - y\| \ge |\|x\| - \|y\||$ |

### Свойства матричных норм

| Свойство | Формула |
|:---:|:---:|
| **Согласованность** | $\|Ax\| \le \|A\| \cdot \|x\|$ |
| **Субмультипликативность** | $\|AB\| \le \|A\| \cdot \|B\|$ |
| **Единичная матрица** | $\|I\| = 1$ (для индуцированных норм) |
| **Спектральный радиус** | $\rho(A) \le \|A\|$ |

### Эквивалентность норм
В конечномерном пространстве все нормы эквивалентны. Для $\mathbb{R}^n$:

$$
\|x\|_\infty \le \|x\|_2 \le \sqrt{n} \|x\|_\infty
$$

$$
\|x\|_2 \le \|x\|_1 \le \sqrt{n} \|x\|_2
$$

$$
\|x\|_\infty \le \|x\|_1 \le n \|x\|_\infty
$$

---

# 6. Типовые задачи и решения

### Задача 1. Вычисление норм вектора
**Задание:** Найти $\|x\|_1$, $\|x\|_2$, $\|x\|_\infty$ для вектора $x = (3, -4, 0)^T$.

**Решение:**
1.  **$L_1$-норма:**

$$
    \|x\|_1 = |3| + |-4| + |0| = 3 + 4 + 0 = 7
    $$
    
2.  **$L_2$-норма:**

$$
    \|x\|_2 = \sqrt{3^2 + (-4)^2 + 0^2} = \sqrt{9 + 16} = \sqrt{25} = 5
    $$
    
3.  **$L_\infty$-норма:**

$$
    \|x\|_\infty = \max(|3|, |-4|, |0|) = 4
    $$

**Ответ:** $\|x\|_1 = 7$, $\|x\|_2 = 5$, $\|x\|_\infty = 4$.

---

### Задача 2. Вычисление норм матрицы
**Задание:** Найти $\|A\|_1$, $\|A\|_\infty$, $\|A\|_F$ для матрицы:

$$
A = \begin{pmatrix}
1 & -2 \\
3 & 4
\end{pmatrix}
$$

**Решение:**
1.  **$L_1$-норма (по столбцам):**
    *   Столбец 1: $|1| + |3| = 4$
    *   Столбец 2: $|-2| + |4| = 6$
    *   $\|A\|_1 = \max(4, 6) = 6$
2.  **$L_\infty$-норма (по строкам):**
    *   Строка 1: $|1| + |-2| = 3$
    *   Строка 2: $|3| + |4| = 7$
    *   $\|A\|_\infty = \max(3, 7) = 7$
3.  **Норма Фробениуса:**

$$
    \|A\|_F = \sqrt{1^2 + (-2)^2 + 3^2 + 4^2} = \sqrt{1 + 4 + 9 + 16} = \sqrt{30} \approx 5.477
    $$

**Ответ:** $\|A\|_1 = 6$, $\|A\|_\infty = 7$, $\|A\|_F = \sqrt{30}$.

---

### Задача 3. Проверка неравенства треугольника
**Задание:** Проверить неравенство треугольника для векторов $x = (1, 2)^T$, $y = (3, -1)^T$ в норме $L_1$.

**Решение:**
1.  **Нормы векторов:**

$$
    \|x\|_1 = |1| + |2| = 3, \quad \|y\|_1 = |3| + |-1| = 4
    $$
    
2.  **Сумма векторов:**
$$
    x + y = (4, 1)^T, \quad \|x + y\|_1 = |4| + |1| = 5
    $$
    
4.  **Проверка:**

$$
    \|x + y\|_1 = 5 \le \|x\|_1 + \|y\|_1 = 3 + 4 = 7
    $$
    
5.  **Вывод:** Неравенство выполняется.

**Ответ:** $5 \le 7$, неравенство выполняется.

---

### Задача 4. Оценка погрешности через норму
**Задание:** Точное решение $x = (1, 1, 1)^T$, приближенное $\tilde{x} = (1.1, 0.9, 1.05)^T$. Найти абсолютную и относительную погрешность в норме $L_\infty$.

**Решение:**
1.  **Вектор погрешности:**

$$
    e = x - \tilde{x} = (-0.1, 0.1, -0.05)^T
    $$
    
2.  **Абсолютная погрешность:**

$$
    \|e\|_\infty = \max(0.1, 0.1, 0.05) = 0.1
    $$
3.  **Относительная погрешность:**

$$    
    \frac{\|e\|_\infty}{\|x\|_\infty} = \frac{0.1}{1} = 0.1 = 10\%
    $$

**Ответ:** Абсолютная погрешность 0.1, относительная 10%.

---

### Задача 5. Спектральная норма матрицы
**Задание:** Найти $\|A\|_2$ для матрицы $A = \begin{pmatrix} 2 & 0 \\ 0 & 3 \end{pmatrix}$.

**Решение:**
1.  **Для диагональной матрицы:** Спектральная норма равна максимальному модулю собственного числа.
2.  **Собственные числа:** $\lambda_1 = 2$, $\lambda_2 = 3$.
3.  **Норма:**

$$
    \|A\|_2 = \max(|2|, |3|) = 3
    $$
    
4.  **Проверка через $A^T A$:**

$$
    A^T A = \begin{pmatrix} 4 & 0 \\ 0 & 9 \end{pmatrix}, \quad \lambda_{\max} = 9
    $$

$$
    \|A\|_2 = \sqrt{9} = 3
    $$

**Ответ:** $\|A\|_2 = 3$.

---

### Задача 6. Число обусловленности
**Задание:** Оценить число обусловленности матрицы $A = \begin{pmatrix} 1 & 0 \\ 0 & 0.01 \end{pmatrix}$ в норме $L_\infty$.

**Решение:**
1.  **Норма матрицы:**

$$
    \|A\|_\infty = \max(1, 0.01) = 1
    $$
    
2.  **Обратная матрица:**

$$
    A^{-1} = \begin{pmatrix} 1 & 0 \\ 0 & 100 \end{pmatrix}
    $$
    
3.  **Норма обратной:**

$$
    \|A^{-1}\|_\infty = \max(1, 100) = 100
    $$
    
4.  **Число обусловленности:**

$$
    \text{cond}(A) = \|A\|_\infty \cdot \|A^{-1}\|_\infty = 1 \cdot 100 = 100
    $$
    
5.  **Вывод:** Матрица плохо обусловлена (погрешность может усилиться в 100 раз).

**Ответ:** $\text{cond}(A) = 100$.

---

# 7. Таблица основных норм

| Норма | Вектор | Матрица | Вычисление |
|:---:|:---:|:---:|:---:|
| **$L_1$** | $\sum |x_i|$ | $\max_j \sum_i |a_{ij}|$ | Сумма / макс. столбец |
| **$L_2$** | $\sqrt{\sum x_i^2}$ | $\sqrt{\lambda_{\max}(A^T A)}$ | Евклидова / сингулярное |
| **$L_\infty$** | $\max |x_i|$ | $\max_i \sum_j |a_{ij}|$ | Максимум / макс. строка |
| **Фробениуса** | — | $\sqrt{\sum |a_{ij}|^2}$ | Корень из суммы квадратов |

---

# 8. Резюме модуля

| Понятие | Формула / Свойство |
|:---:|:---:|
| **Аксиомы нормы** | Неотрицательность, однородность, неравенство треугольника |
| **Согласованность** | $\|Ax\| \le \|A\| \cdot \|x\|$ |
| **Эквивалентность** | Все нормы в $\mathbb{R}^n$ эквивалентны |
| **Число обусловленности** | $\text{cond}(A) = \|A\| \cdot \|A^{-1}\|$ |
| **Спектральный радиус** | $\rho(A) \le \|A\|$ |

**Ключевые выводы:**
1.  **Норма** — это мера «размера» вектора или матрицы.
2.  **Разные нормы** подходят для разных задач ($L_1$ для разреженных данных, $L_2$ для геометрии, $L_\infty$ для оценок погрешности).
3.  **Матричные нормы** согласованы с векторными: $\|Ax\| \le \|A\| \cdot \|x\|$.
4.  **Число обусловленности** показывает устойчивость решения СЛАУ к погрешностям.
5.  **Эквивалентность норм** гарантирует, что сходимость в одной норме означает сходимость в любой другой (в конечномерном пространстве).

