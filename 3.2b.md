
Тема **Линейных дифференциальных уравнений второго порядка** — это вершина теории обыкновенных дифференциальных уравнений (ОДУ). Они описывают колебания (пружины, маятники, электрические контуры) и являются фундаментом для технической физики.

---

# 1. Интуитивное понимание

### Физический смысл
Представьте груз на пружине.
*   $y(t)$ — положение груза.
*   $y'(t)$ — скорость.
*   $y''(t)$ — ускорение (сила инерции).
Уравнение второго порядка связывает ускорение, скорость и положение.
Почему **второго** порядка? Потому что закон Ньютона $F = ma$ связывает силу с ускорением (второй производной координаты).
Почему **линейного**? Потому что силы часто пропорциональны смещению (закон Гука) или скорости (сила сопротивления), без квадратов и синусов от самой неизвестной функции.

### Общее количество решений
Уравнение первого порядка имеет одну константу $C$ в общем решении.
Уравнение **второго** порядка требует **двух** начальных условий (положение и скорость в начальный момент), поэтому его общее решение содержит **две** независимые константы $C_1$ и $C_2$.

---

# 2. Строгие определения

### Линейное уравнение второго порядка
Уравнение вида:
$$
y'' + p(x)y' + q(x)y = f(x)
$$
где $p(x), q(x), f(x)$ — непрерывные функции на некотором интервале.
*   Если $f(x) \equiv 0$, уравнение называется **однородным**:
    $$
    y'' + p(x)y' + q(x)y = 0
    $$
*   Если $f(x) \neq 0$, уравнение называется **неоднородным**.

### Линейная зависимость и независимость функций
Две функции $y_1(x)$ и $y_2(x)$ называются **линейно зависимыми** на интервале, если одна из них пропорциональна другой:
$$
y_1(x) = C \cdot y_2(x)
$$
Если такого числа $C$ не существует, функции **линейно независимы**.

**Определитель Вронского (Вронскиан):**
Для проверки независимости двух решений используют определитель:
$$
W(y_1, y_2)(x) = \begin{vmatrix} y_1 & y_2 \\ y_1' & y_2' \end{vmatrix} = y_1 y_2' - y_2 y_1'
$$
*   Если $W(x) \neq 0$ хотя бы в одной точке интервала, функции линейно независимы.
*   Если $W(x) \equiv 0$, функции линейно зависимы.

---

# 3. Структура решений однородного уравнения

### Фундаментальная система решений (ФСР)
ФСР однородного уравнения второго порядка — это пара любых **линейно независимых** решений $y_1(x)$ и $y_2(x)$.

### Общее решение однородного уравнения
Если $\{y_1, y_2\}$ — ФСР, то общее решение имеет вид:
$$
y_{\text{одн}} = C_1 y_1(x) + C_2 y_2(x)
$$
где $C_1, C_2$ — произвольные постоянные.

**Принцип суперпозиции:** Любая линейная комбинация решений однородного уравнения также является решением.

---

# 4. Структура решений неоднородного уравнения

### Теорема о структуре общего решения
Общее решение неоднородного уравнения равно сумме:
1.  **Общего решения соответствующего однородного уравнения** ($y_{\text{одн}}$).
2.  **Частного решения неоднородного уравнения** ($y_{\text{част}}$).

$$
y_{\text{общ}} = y_{\text{одн}} + y_{\text{част}} = C_1 y_1 + C_2 y_2 + y_{\text{част}}
$$

---

# 5. Уравнения с постоянными коэффициентами (Самый важный частный случай)

Если $p(x) = a$ и $q(x) = b$ (константы), уравнение выглядит так:
$$
y'' + ay' + by = 0
$$
Решение ищется в виде $y = e^{kx}$. Подстановка приводит к **характеристическому уравнению**:
$$
k^2 + ak + b = 0
$$
Вид ФСР зависит от корней $k_1, k_2$:

1.  **Два различных действительных корня** ($D > 0$):
    $$ y_1 = e^{k_1 x}, \quad y_2 = e^{k_2 x} $$
2.  **Один кратный корень** ($D = 0$, $k_1 = k_2 = k$):
    $$ y_1 = e^{kx}, \quad y_2 = x \cdot e^{kx} $$
3.  **Комплексные корни** ($D < 0$, $k_{1,2} = \alpha \pm i\beta$):
    $$ y_1 = e^{\alpha x} \cos(\beta x), \quad y_2 = e^{\alpha x} \sin(\beta x) $$

---

# 6. Типовые задачи и решения

### Задача 1. Проверка линейной независимости (Вронскиан)
**Задание:** Являются ли функции $y_1 = e^x$ и $y_2 = e^{2x}$ линейно независимыми?

**Решение:**
1.  Найдем производные:
    $$ y_1' = e^x, \quad y_2' = 2e^{2x} $$
2.  Составим определитель Вронского:
    $$
    W = \begin{vmatrix} e^x & e^{2x} \\ e^x & 2e^{2x} \end{vmatrix} = e^x \cdot 2e^{2x} - e^{2x} \cdot e^x
    $$
3.  Вычислим:
    $$
    W = 2e^{3x} - e^{3x} = e^{3x}
    $$
4.  Так как экспонента $e^{3x}$ никогда не равна нулю, $W \neq 0$.
5.  **Вывод:** Функции линейно независимы и могут образовать ФСР.

**Ответ:** Да, линейно независимы.

---

### Задача 2. Нахождение общего решения (Постоянные коэффициенты)
**Задание:** Найти общее решение уравнения $y'' - 5y' + 6y = 0$.

**Решение:**
1.  Составим характеристическое уравнение (заменяем $y'' \to k^2$, $y' \to k$, $y \to 1$):
    $$
    k^2 - 5k + 6 = 0
    $$
2.  Найдем корни (через дискриминант или теорему Виета):
    $$
    D = 25 - 24 = 1
    $$
    $$
    k_1 = \frac{5 - 1}{2} = 2, \quad k_2 = \frac{5 + 1}{2} = 3
    $$
3.  Корни действительные и различные. ФСР: $y_1 = e^{2x}$, $y_2 = e^{3x}$.
4.  Запишем общее решение:
    $$
    y = C_1 e^{2x} + C_2 e^{3x}
    $$

**Ответ:** $y = C_1 e^{2x} + C_2 e^{3x}$.

---

### Задача 3. Случай кратных корней
**Задание:** Найти общее решение уравнения $y'' - 4y' + 4y = 0$.

**Решение:**
1.  Характеристическое уравнение:
    $$
    k^2 - 4k + 4 = 0 \quad \Rightarrow \quad (k - 2)^2 = 0
    $$
2.  Корень один (кратности 2): $k = 2$.
3.  По теории, в этом случае ФСР имеет вид:
    $$
    y_1 = e^{2x}, \quad y_2 = x \cdot e^{2x}
    $$
4.  Общее решение:
    $$
    y = C_1 e^{2x} + C_2 x e^{2x} = e^{2x}(C_1 + C_2 x)
    $$

**Ответ:** $y = e^{2x}(C_1 + C_2 x)$.

---

### Задача 4. Структура решения неоднородного уравнения
**Задание:** Известно, что общее решение однородного уравнения $y'' + y = 0$ есть $y_{\text{одн}} = C_1 \cos x + C_2 \sin x$. Найти общее решение неоднородного уравнения $y'' + y = x$, если известно частное решение $y_{\text{част}} = x$.

**Решение:**
1.  Проверяем частное решение (для надежности):
    $$ y_{\text{част}} = x \implies y' = 1, \implies y'' = 0 $$
    Подставляем в уравнение: $0 + x = x$. Верно.
2.  Используем теорему о структуре:
    $$
    y_{\text{общ}} = y_{\text{одн}} + y_{\text{част}}
    $$
3.  Подставляем известные части:
    $$
    y = C_1 \cos x + C_2 \sin x + x
    $$

**Ответ:** $y = C_1 \cos x + C_2 \sin x + x$.

---

### Задача 5. Задача Коши для уравнения 2-го порядка
**Задание:** Найти частное решение уравнения $y'' - 3y' + 2y = 0$, удовлетворяющее условиям $y(0) = 1$, $y'(0) = 0$.

**Решение:**
1.  **Общее решение:**
    Характеристическое уравнение: $k^2 - 3k + 2 = 0$.
    Корни: $k_1 = 1, k_2 = 2$.
    $$ y = C_1 e^x + C_2 e^{2x} $$
2.  **Найдем производную общего решения:**
    $$ y' = C_1 e^x + 2C_2 e^{2x} $$
3.  **Подставим начальные условия:**
    *   $y(0) = 1 \implies C_1 e^0 + C_2 e^0 = 1 \implies C_1 + C_2 = 1$
    *   $y'(0) = 0 \implies C_1 e^0 + 2C_2 e^0 = 0 \implies C_1 + 2C_2 = 0$
4.  **Решим систему уравнений:**
    $$
    \begin{cases}
    C_1 + C_2 = 1 \\
    C_1 + 2C_2 = 0
    \end{cases}
    $$
    Вычтем первое из второго: $C_2 = -1$.
    Тогда $C_1 = 1 - C_2 = 1 - (-1) = 2$.
5.  **Запишем ответ:**
    $$
    y = 2e^x - e^{2x}
    $$

**Ответ:** $y = 2e^x - e^{2x}$.

---

# Резюме модуля

1.  **Линейное ОДУ 2-го порядка:** $y'' + p(x)y' + q(x)y = f(x)$.
2.  **Однородное ($f=0$):** Общее решение — линейная комбинация двух линейно независимых решений (ФСР): $y = C_1 y_1 + C_2 y_2$.
3.  **Неоднородное ($f \neq 0$):** Общее решение = Общее однородное + Частное неоднородное.
4.  **Определитель Вронского:** Инструмент проверки независимости решений ($W \neq 0$).
5.  **Постоянные коэффициенты:** Решаются через характеристическое уравнение $k^2 + ak + b = 0$. Вид корней определяет вид функций в ФСР (экспоненты, многочлены, синусы/косинусы).

