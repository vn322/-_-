
Тема **Интерполяции** — одна из ключевых в вычислительной математике. Она позволяет восстановить функцию по набору известных точек, что критически важно для работы с табличными данными, численного интегрирования и решения дифференциальных уравнений.

---

# 1. Интуитивное понимание

### Что такое интерполяция?
Представьте, что у вас есть таблица значений функции (например, измерения температуры каждый час), но вам нужно узнать значение в момент времени, которого нет в таблице (например, через 30 минут).
**Интерполяция** — это поиск функции $P(x)$, которая проходит **точно через все заданные узлы** $(x_i, y_i)$.

### Постановка задачи
Даны $n+1$ точек (узлов интерполяции):

$$
(x_0, y_0), (x_1, y_1), \dots, (x_n, y_n)
$$

где все $x_i$ различны.
Требуется найти многочлен $P_n(x)$ степени не выше $n$, такой что:

$$
P_n(x_i) = y_i \quad \text{для всех } i = 0, 1, \dots, n
$$

**Теорема:** Такой многочлен существует и **единственен**. Разница между формулами Лагранжа и Ньютона лишь в способе записи этого многочлена.

---

# 2. Интерполяционная формула Лагранжа

### Интуитивное понимание
Идея Лагранжа изящна: мы строим сумму из $n+1$ слагаемых. Каждое слагаемое $L_i(x)$ устроено так, что:

В точке $x_i$ оно равно $y_i$.

Во всех остальных узлах $x_j$ ($j \neq i$) оно равно **0**.

В сумме это дает точное совпадение во всех узлах.

### Строгая формула

$$
P_n(x) = \sum_{i=0}^{n} y_i \cdot l_i(x)
$$

где $l_i(x)$ — **фундаментальные многочлены Лагранжа**:

$$
l_i(x) = \prod_{\substack{j=0 \\ j \neq i}}^{n} \frac{x - x_j}{x_i - x_j}
$$

**Расшифровка произведения:**
Для $l_i(x)$ в числителе перемножаются все $(x - x_j)$, кроме $(x - x_i)$. В знаменателе — те же разности, но для фиксированного узла $x_i$.

### Плюсы и минусы

**+** Удобна для теоретических выкладок.

**+** Легко программируется (два цикла).

**–** При добавлении нового узла нужно пересчитывать **все** слагаемые заново.

---

# 3. Интерполяционная формула Ньютона

### Интуитивное понимание
Ньютон записывает многочлен в другом виде, используя **конечные разности**. Это позволяет строить интерполяцию «наращиванием»: если добавился новый узел, нужно просто дописать одно новое слагаемое, не трогая старые.

### Конечные разности

**Разность 1-го порядка:** $f[x_i, x_{i+1}] = \frac{f(x_{i+1}) - f(x_i)}{x_{i+1} - x_i}$

**Разность 2-го порядка:** $f[x_i, x_{i+1}, x_{i+2}] = \frac{f[x_{i+1}, x_{i+2}] - f[x_i, x_{i+1}]}{x_{i+2} - x_i}$

**Разность $k$-го порядка:** Рекуррентно через разности $(k-1)$-го порядка.

### Формула Ньютона (через разделенные разности)

$$
P_n(x) = f[x_0] + f[x_0, x_1](x - x_0) + f[x_0, x_1, x_2](x - x_0)(x - x_1) + \dots
$$

$$
P_n(x) = \sum_{k=0}^{n} f[x_0, \dots, x_k] \cdot \prod_{j=0}^{k-1} (x - x_j)
$$

### Частный случай: Равноотстоящие узлы
Если узлы расположены с шагом $h$ ($x_i = x_0 + ih$), удобно использовать **конечные разности** $\Delta^k y_0$:

$$
P_n(x) = y_0 + q \Delta y_0 + \frac{q(q-1)}{2!} \Delta^2 y_0 + \dots + \frac{q(q-1)\dots(q-n+1)}{n!} \Delta^n y_0
$$

где $q = \frac{x - x_0}{h}$.

### Плюсы и минусы

**+** Вычислительно эффективнее при добавлении узлов.

**+** Удобна для ручного счета при равноотстоящих узлах.

**–** Требует построения таблицы разностей.

---

# 4. Оценка погрешности интерполяции

### Формула остаточного члена
Если интерполируемая функция $f(x)$ имеет непрерывную производную $(n+1)$-го порядка, то погрешность в точке $x$ оценивается как:

$$
R_n(x) = f(x) - P_n(x) = \frac{f^{(n+1)}(\xi)}{(n+1)!} \cdot \omega(x)
$$

где:

$\xi$ — некоторая точка из интервала, содержащего все узлы и точку $x$.

$\omega(x) = (x - x_0)(x - x_1)\dots(x - x_n)$.

### Выводы из формулы
1.  Погрешность растет при удалении от узлов интерполяции (из-за множителя $\omega(x)$).
2.  Погрешность зависит от гладкости функции (чем больше производная, тем хуже).
3.  **Феномен Рунге:** При большом числе узлов ($n > 7$) на равномерной сетке погрешность на краях может резко возрастать. В таких случаях используют сплайны или узлы Чебышева.

---

# 5. Типовые задачи и решения

### Задача 1. Интерполяция по Лагранжу (3 точки)
**Задание:** Построить интерполяционный многочлен для точек: $(0, 1), (1, 3), (2, 2)$.

**Решение:**
1.  Узлы: $x_0=0, y_0=1$; $x_1=1, y_1=3$; $x_2=2, y_2=2$.
2.  Строим фундаментальные многочлены:

$l_0(x) = \frac{(x-1)(x-2)}{(0-1)(0-2)} = \frac{x^2 - 3x + 2}{2}$

$l_1(x) = \frac{(x-0)(x-2)}{(1-0)(1-2)} = \frac{x^2 - 2x}{-1} = -x^2 + 2x$

$l_2(x) = \frac{(x-0)(x-1)}{(2-0)(2-1)} = \frac{x^2 - x}{2}$

3.  Суммируем с весами $y_i$:

$$
    P_2(x) = 1 \cdot l_0(x) + 3 \cdot l_1(x) + 2 \cdot l_2(x)
    $$

$$
    P_2(x) = 0.5(x^2 - 3x + 2) + 3(-x^2 + 2x) + 0.5(x^2 - x)
    $$
    
4.  Приводим подобные:
    *   $x^2: 0.5 - 3 + 0.5 = -2$
    *   $x: -1.5 + 6 - 0.5 = 4$
    *   Свободный: $1$
    
    $$
    P_2(x) = -2x^2 + 4x + 1
    $$
    
6.  **Проверка:** $P(0)=1, P(1)=3, P(2)=-8+8+1=1 \neq 2$.

(Ошибка в расчете коэффициента при $l_2$: $2 \cdot 0.5 = 1$. Пересчитаем свободный член: $1 \cdot 1 + 3 \cdot 0 + 2 \cdot 0 = 1$. Проверка $P(2)$: $-2(4) + 4(2) + 1 = 1$. Ошибка в условии или расчете. Давайте проверим $l_0(2)=0, l_1(2)=0, l_2(2)=1$. Значит $P(2)=y_2=2$. В упрощении ошибка)
    
    **Исправление упрощения:**
$$
    P_2(x) = \frac{1}{2}(x^2 - 3x + 2) + 3(-x^2 + 2x) + \frac{2}{2}(x^2 - x)
    $$
 
$$
    = 0.5x^2 - 1.5x + 1 - 3x^2 + 6x + x^2 - x
    $$

$$
    = (0.5 - 3 + 1)x^2 + (-1.5 + 6 - 1)x + 1 = -1.5x^2 + 3.5x + 1
    $$

Проверка $P(2) = -1.5(4) + 3.5(2) + 1 = -6 + 7 + 1 = 2$. Верно.

**Ответ:** $P_2(x) = -1.5x^2 + 3.5x + 1$.

---

### Задача 2. Интерполяция по Ньютону (Таблица разностей)
**Задание:** Построить многочлен Ньютона для точек: $(1, 1), (2, 4), (3, 9)$.

**Решение:**
1.  **Составим таблицу конечных разностей:**

| $x_i$ | $y_i$ | $\Delta y$ | $\Delta^2 y$ |
|:---:|:---:|:---:|:---:|
| 1 | 1 | | |
| | | $4-1=3$ | |
| 2 | 4 | | $5-3=2$ |
| | | $9-4=5$ | |
| 3 | 9 | | |

2.  **Берем значения с верхней диагонали:**
    *   $y_0 = 1$
    *   $\Delta y_0 = 3$
    *   $\Delta^2 y_0 = 2$
3.  **Шаг $h = 1$.** Вводим $q = \frac{x - x_0}{h} = x - 1$.
4.  **Формула:**

$$
    P_2(x) = 1 + 3q + \frac{2 \cdot q(q-1)}{2} = 1 + 3q + q^2 - q = q^2 + 2q + 1
    $$
    
5.  **Возвращаемся к $x$:**

$$
    P_2(x) = (x-1)^2 + 2(x-1) + 1 = x^2 - 2x + 1 + 2x - 2 + 1 = x^2
    $$
    
6.  **Проверка:** $1^2=1, 2^2=4, 3^2=9$. Верно.

**Ответ:** $P_2(x) = x^2$.

---

### Задача 3. Оценка погрешности
**Задание:** Оценить погрешность интерполяции функции $f(x) = \sin x$ на отрезке $[0, \pi/2]$ с узлами $0, \pi/4, \pi/2$ в точке $x = \pi/6$.

**Решение:**
1.  **Формула остатка для $n=2$:**

$$
    R_2(x) = \frac{f'''(\xi)}{3!} (x - x_0)(x - x_1)(x - x_2)
    $$
    
2.  **Производная:** $f(x) = \sin x \implies f'''(x) = -\cos x$.
Максимум модуля: $|f'''(\xi)| = |-\cos \xi| \le 1$.
3.  **Произведение разностей в точке $x = \pi/6$:**
    *   $x_0 = 0, x_1 = \pi/4, x_2 = \pi/2$.
    *   $(x - 0) = \pi/6 \approx 0.524$
    *   $(x - \pi/4) = \pi/6 - \pi/4 = -\pi/12 \approx -0.262$
    *   $(x - \pi/2) = \pi/6 - \pi/2 = -\pi/3 \approx -1.047$
    *   Произведение: $0.524 \cdot (-0.262) \cdot (-1.047) \approx 0.144$
4.  **Оценка:**

$$
    |R_2| \le \frac{1}{6} \cdot 0.144 \approx 0.024
    $$

**Ответ:** Погрешность не превышает 0.024.

---

### Задача 4. Выбор формулы
**Задание:** Есть таблица из 10 точек. Нужно вычислить значение в новой точке, а затем добавить еще одну точку в таблицу и посчитать снова. Какую формулу выгоднее использовать?

**Решение:**
1.  **Лагранж:** При добавлении точки все 10 слагаемых меняются, нужно пересчитывать сумму полностью. Сложность $O(n^2)$ для каждого нового расчета.
2.  **Ньютон:** Нужно вычислить только одно новое слагаемое (с разностью 10-го порядка), предыдущие 9 слагаемых остаются неизменными.
3.  **Вывод:** Формула Ньютона вычислительно эффективнее для динамически пополняемых таблиц.

**Ответ:** Формулу Ньютона.

---

### Задача 5. Восстановление функции
**Задание:** Известно, что интерполяционный многочлен для неизвестной функции $f(x)$ по узлам $0, 1, 2$ имеет вид $P(x) = x^2 + 1$. Чему равно $f(1)$?

**Решение:**
1.  По свойству интерполяции, многочлен совпадает с функцией в узлах.
2.  Узел $x = 1$ входит в набор.
3.  Значит, $f(1) = P(1)$.
4.  $P(1) = 1^2 + 1 = 2$.

**Ответ:** $f(1) = 2$.

---

# Резюме модуля

| Характеристика | Формула Лагранжа | Формула Ньютона |
|:---:|:---:|:---:|
| **Вид записи** | Сумма произведений | Вложенные произведения |
| **Добавление узла** | Пересчет всего | Добавление слагаемого |
| **Вычисления** | Много делений | Таблица разностей |
| **Применение** | Теория, фиксированные узлы | Практика, таблицы |

1.  **Суть:** Оба многочлена тождественно равны, это просто разные формы записи одного и того же полинома.
2.  **Лагранж:** Симметричная формула, удобная, когда все узлы известны заранее.
3.  **Ньютон:** Рекурсивная структура, удобная для последовательного уточнения.
4.  **Погрешность:** Зависит от шага сетки и гладкости функции. При $n > 7$ осторожнее с равномерными узлами (феномен Рунге).


3.  **Решение нелинейных уравнений** (Метод Ньютона, простой итерации, отделение корней).

Жду твоего выбора!
