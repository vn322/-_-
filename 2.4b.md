

Тема **собственных векторов и собственных значений** — одна из самых важных в линейной алгебре. Она лежит в основе анализа динамических систем, квантовой механики, машинного обучения (PCA) и многих других областей.

---

# 1. Интуитивное понимание

### Геометрический смысл
Представьте, что матрица $A$ — это оператор, который преобразует пространство (растягивает, сжимает, поворачивает векторы).
Большинство векторов при таком преобразлении меняют свое направление.
Но существуют особенные векторы, которые **не меняют направления** (или меняют на противоположное), а лишь растягиваются или сжимаются в некоторое количество раз.

*   Эти векторы называются **собственными векторами**.
*   Коэффициент растяжения/сжатия называется **собственным значением**.

**Пример:** Если вы растягиваете резиновый лист вдоль одной оси, то векторы, лежащие на этой оси, только удлиняются, но не поворачиваются. Они и будут собственными векторами этого преобразования.

---

# 2. Строгие определения

### Собственный вектор и собственное значение
Пусть $A$ — квадратная матрица размера $n \times n$.
Не нулевой вектор $\mathbf{x}$ называется **собственным вектором** матрицы $A$, если существует такое число $\lambda$, что:

$$
A \mathbf{x} = \lambda \mathbf{x}
$$

Число $\lambda$ называется **собственным значением** матрицы $A$, соответствующим этому вектору.

**Важно:** Собственный вектор не может быть нулевым ($\mathbf{x} \neq \mathbf{0}$), иначе равенство выполняется тривиально для любого $\lambda$.

### Характеристический многочлен
Чтобы найти собственные значения, перепишем уравнение:

$$
A \mathbf{x} - \lambda \mathbf{x} = \mathbf{0} \quad \Rightarrow \quad (A - \lambda I) \mathbf{x} = \mathbf{0}
$$

где $I$ — единичная матрица.
Это однородная система линейных уравнений. Чтобы у неё существовало ненулевое решение $\mathbf{x}$, определитель матрицы системы должен быть равен нулю.

**Характеристическим уравнением** называется:

$$
\det(A - \lambda I) = 0
$$

Многочлен $P(\lambda) = \det(A - \lambda I)$ называется **характеристическим многочленом** матрицы $A$. Его корни и есть собственные значения.

---

# 3. Алгоритм нахождения

1.  **Составить матрицу** $(A - \lambda I)$.
2.  **Вычислить определитель** $\det(A - \lambda I)$ и приравнять его к нулю.
3.  **Найти корни** полученного уравнения — это собственные значения $\lambda_1, \lambda_2, \dots$.
4.  **Для каждого $\lambda_i$** решить систему $(A - \lambda_i I)\mathbf{x} = \mathbf{0}$, чтобы найти соответствующие собственные векторы.

---

# 4. Свойства собственных значений

Пусть $\lambda_1, \dots, \lambda_n$ — собственные значения матрицы $A$.

**След матрицы:** Сумма собственных значений равна следу матрицы (сумме элементов главной диагонали):

$$
\sum_{i=1}^{n} \lambda_i = \text{tr}(A) = \sum_{i=1}^{n} a_{ii}
$$

**Определитель:** Произведение собственных значений равно определителю матрицы:

$$
\prod_{i=1}^{n} \lambda_i = \det(A)
$$

**Симметричные матрицы:** Если матрица $A$ симметрична ($A = A^T$), то все её собственные значения — действительные числа.

---

# 5. Типовые задачи и решения

### Задача 1. Нахождение собственных значений и векторов
**Задание:** Найти собственные значения и собственные векторы матрицы:

$$
A = \begin{pmatrix}
3 & 1 \\
1 & 3
\end{pmatrix}
$$

**Решение:**
**Составим характеристическое уравнение:**

$$
\det(A - \lambda I) = \det \begin{pmatrix}
3 - \lambda & 1 \\
1 & 3 - \lambda
\end{pmatrix} = 0
$$

2.  **Вычислим определитель:**

$$
(3 - \lambda)(3 - \lambda) - 1 \cdot 1 = 0
$$

$$
(3 - \lambda)^2 - 1 = 0
$$

$$
\lambda^2 - 6\lambda + 9 - 1 = 0 \quad \Rightarrow \quad \lambda^2 - 6\lambda + 8 = 0
$$

**Найдем корни (собственные значения):**
По теореме Виета или через дискриминант:

$$
\lambda_1 = 2, \quad \lambda_2 = 4
$$

*(Проверка: Сумма $2+4=6=\text{tr}(A)$, произведение $2 \cdot 4=8=\det(A)$. Верно).*

**Найдем собственные векторы:**
**Для $\lambda_1 = 2$:** Решаем систему $(A - 2I)\mathbf{x} = \mathbf{0}$.

$$
\begin{pmatrix}
3-2 & 1 \\
1 & 3-2
\end{pmatrix} \begin{pmatrix} x_1 \\ x_2 \end{pmatrix} = \begin{pmatrix} 0 \\ 0 \end{pmatrix}
\quad \Rightarrow \quad
\begin{pmatrix}
1 & 1 \\
1 & 1
\end{pmatrix} \begin{pmatrix} x_1 \\ x_2 \end{pmatrix} = \begin{pmatrix} 0 \\ 0 \end{pmatrix}
$$

Уравнение: $x_1 + x_2 = 0 \Rightarrow x_1 = -x_2$.
Пусть $x_2 = 1$, тогда $x_1 = -1$.
Вектор: $\mathbf{x}_1 = \begin{pmatrix} -1 \\ 1 \end{pmatrix}$.

**Для $\lambda_2 = 4$:** Решаем систему $(A - 4I)\mathbf{x} = \mathbf{0}$.

$$
\begin{pmatrix}
3-4 & 1 \\
1 & 3-4
\end{pmatrix} \begin{pmatrix} x_1 \\ x_2 \end{pmatrix} = \begin{pmatrix} 0 \\ 0 \end{pmatrix}
\quad \Rightarrow \quad
\begin{pmatrix}
-1 & 1 \\
1 & -1
\end{pmatrix} \begin{pmatrix} x_1 \\ x_2 \end{pmatrix} = \begin{pmatrix} 0 \\ 0 \end{pmatrix}
$$

Уравнение: $-x_1 + x_2 = 0 \Rightarrow x_1 = x_2$.
Пусть $x_2 = 1$, тогда $x_1 = 1$.
Вектор: $\mathbf{x}_2 = \begin{pmatrix} 1 \\ 1 \end{pmatrix}$.

**Ответ:**
$\lambda_1 = 2, \quad \mathbf{x}_1 = C_1 \begin{pmatrix} -1 \\ 1 \end{pmatrix}$
$\lambda_2 = 4, \quad \mathbf{x}_2 = C_2 \begin{pmatrix} 1 \\ 1 \end{pmatrix}$

---

### Задача 2. Треугольная матрица
**Задание:** Найти собственные значения матрицы:

$$
A = \begin{pmatrix}
2 & 5 & 1 \\
0 & 3 & 9 \\
0 & 0 & -1
\end{pmatrix}
$$

**Решение:**
1.  Это верхняя треугольная матрица.
2.  Определитель треугольной матрицы равен произведению элементов главной диагонали.
3.  Характеристический многочлен:

$$
\det(A - \lambda I) = (2 - \lambda)(3 - \lambda)(-1 - \lambda) = 0
$$

4.  Корни уравнения видны сразу — это элементы диагонали.

**Ответ:** $\lambda_1 = 2, \quad \lambda_2 = 3, \quad \lambda_3 = -1$.
*(Общее правило: собственные значения треугольной матрицы равны её диагональным элементам).*

---

### Задача 3. Кратные собственные значения
**Задание:** Найти собственные векторы матрицы:

$$
A = \begin{pmatrix}
2 & 1 \\
0 & 2
\end{pmatrix}
$$

**Решение:**
**Собственные значения:** Матрица треугольная, значит $\lambda_1 = 2, \lambda_2 = 2$ (собственное значение кратности 2).
**Собственные векторы:** Решаем $(A - 2I)\mathbf{x} = \mathbf{0}$.
$$
\begin{pmatrix}
2-2 & 1 \\
0 & 2-2
\end{pmatrix} \begin{pmatrix} x_1 \\ x_2 \end{pmatrix} = \begin{pmatrix} 0 \\ 0 \end{pmatrix}
\quad \Rightarrow \quad
\begin{pmatrix}
0 & 1 \\
0 & 0
\end{pmatrix} \begin{pmatrix} x_1 \\ x_2 \end{pmatrix} = \begin{pmatrix} 0 \\ 0 \end{pmatrix}
$$

Получаем уравнение: $0 \cdot x_1 + 1 \cdot x_2 = 0 \Rightarrow x_2 = 0$.

Переменная $x_1$ может быть любой.

Вектор имеет вид $\begin{pmatrix} C \\ 0 \end{pmatrix}$.

Базис собственного пространства: $\mathbf{x} = \begin{pmatrix} 1 \\ 0 \end{pmatrix}$.

**Вывод:** Несмотря на то, что собственное значение имеет кратность 2, существует только **один** линейно независимый собственный вектор. Такая матрица не приводится к диагональному виду (она дефектна).

**Ответ:** $\lambda = 2$ (кратность 2), собственный вектор $\mathbf{x} = C \begin{pmatrix} 1 \\ 0 \end{pmatrix}$.

---

# Резюме модуля

1.  **Собственное уравнение:** $A \mathbf{x} = \lambda \mathbf{x}$. Вектор $\mathbf{x}$ не меняет направления при преобразовании $A$.
2.  **Поиск $\lambda$:** Решаем характеристическое уравнение $\det(A - \lambda I) = 0$.
3.  **Поиск $\mathbf{x}$:** Для каждого $\lambda$ решаем однородную систему $(A - \lambda I)\mathbf{x} = \mathbf{0}$.
4.  **Свойства:**
    *   Сумма $\lambda$ = След матрицы.
    *   Произведение $\lambda$ = Определитель матрицы.
    *   Для треугольных матриц $\lambda$ равны элементам диагонали.
5.  **Геометрия:** Собственные векторы задают главные оси растяжения пространства оператором $A$.


