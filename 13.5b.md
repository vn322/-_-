
Тема **Математического ожидания** — одна из центральных в теории вероятностей. Это главная числовая характеристика, описывающая «центр» распределения случайной величины. Она позволяет заменить сложную случайную величину одним числом, которое представляет её среднее значение.

---

# 1. Интуитивное понимание

### Что такое математическое ожидание?
Представьте, что вы много раз повторяете случайный эксперимент и записываете результаты. Если вы возьмете среднее арифметическое всех полученных чисел, то при большом числе испытаний оно будет стремиться к **математическому ожиданию**.

*   **Пример 1:** Среднее число очков на игральной кости равно 3.5. Вы никогда не выбросите 3.5 за один бросок, но в среднем за много бросков результат будет близок к этому числу.
*   **Пример 2:** Средняя продолжительность жизни, средний доход, средняя температура за год.

### Физический смысл
Если представить распределение вероятностей как распределение массы вдоль оси (для дискретной величины — грузы в точках $x_i$ с массой $p_i$), то математическое ожидание — это **центр масс** (центр тяжести) этой системы.

---

# 2. Строгие определения

### Для дискретной случайной величины
Пусть дискретная случайная величина $X$ принимает значения $x_1, x_2, \dots$ с вероятностями $p_1, p_2, \dots$.
**Математическим ожиданием** называется сумма произведений всех возможных значений на их вероятности:

$$
E[X] = \sum_{i} x_i \cdot p_i
$$

**Условие существования:** Ряд должен сходиться **абсолютно**, то есть $\sum |x_i| p_i < \infty$.

### Для непрерывной случайной величины
Пусть непрерывная случайная величина $X$ имеет плотность распределения $f(x)$.
**Математическим ожиданием** называется несобственный интеграл:

$$
E[X] = \int_{-\infty}^{+\infty} x \cdot f(x) \, dx
$$

**Условие существования:** Интеграл должен сходиться **абсолютно**, то есть $\int_{-\infty}^{+\infty} |x| f(x) \, dx < \infty$.

### Общее определение (через функцию распределения)
Для любой случайной величины (через интеграл Стильтьеса):
$$
E[X] = \int_{-\infty}^{+\infty} x \, dF(x)
$$

---

# 3. Свойства математического ожидания

Пусть $X, Y$ — случайные величины, $C$ — постоянная (число).

1.  **Математическое ожидание постоянной:**
    $$
    E[C] = C
    $$
    *(Если величина всегда равна C, её среднее равно C)*.

2.  **Вынос постоянного множителя:**
    $$
    E[C \cdot X] = C \cdot E[X]
    $$

3.  **Линейность (Аддитивность):**
    $$
    E[X + Y] = E[X] + E[Y]
    $$
    *(Это свойство верно всегда, даже если величины зависимы!)*.
    Обобщение: $E[\sum C_i X_i] = \sum C_i E[X_i]$.

4.  **Независимость:**
    Если $X$ и $Y$ независимы, то:
    $$
    E[X \cdot Y] = E[X] \cdot E[Y]
    $$
    *(Обратное неверно: из равенства ожиданий не следует независимость)*.

5.  **Монотонность:**
    Если $X \ge Y$ (почти наверняка), то:
    $$
    E[X] \ge E[Y]
    $$
    Следствие: $|E[X]| \le E[|X|]$.

6.  **Математическое ожидание функции:**
    Если $Y = g(X)$, то не обязательно искать плотность $Y$.
    *   Для дискретной: $E[g(X)] = \sum g(x_i) p_i$
    *   Для непрерывной: $E[g(X)] = \int_{-\infty}^{+\infty} g(x) f(x) \, dx$

---

# 4. Типовые задачи и решения

### Задача 1. Вычисление для дискретной величины (Кость)
**Задание:** Найти математическое ожидание числа очков, выпавших на игральной кости.

**Решение:**
1.  **Значения:** $X \in \{1, 2, 3, 4, 5, 6\}$.
2.  **Вероятности:** Для правильной кости $p_i = \frac{1}{6}$ для всех $i$.
3.  **Формула:**
    $$
    E[X] = \sum_{i=1}^{6} x_i \cdot p_i = 1 \cdot \frac{1}{6} + 2 \cdot \frac{1}{6} + \dots + 6 \cdot \frac{1}{6}
    $$
4.  **Вычисление:**
    $$
    E[X] = \frac{1}{6} (1 + 2 + 3 + 4 + 5 + 6) = \frac{21}{6} = 3.5
    $$

**Ответ:** 3.5.

---

### Задача 2. Вычисление для непрерывной величины (Равномерное распределение)
**Задание:** Случайная величина $X$ распределена равномерно на отрезке $[a, b]$. Найти $E[X]$.

**Решение:**
1.  **Плотность:** $f(x) = \frac{1}{b - a}$ при $x \in [a, b]$, иначе 0.
2.  **Интеграл:**
    $$
    E[X] = \int_{a}^{b} x \cdot \frac{1}{b - a} \, dx
    $$
3.  **Вычисление:**
    $$
    E[X] = \frac{1}{b - a} \int_{a}^{b} x \, dx = \frac{1}{b - a} \cdot \left[ \frac{x^2}{2} \right]_{a}^{b}
    $$
    $$
    = \frac{1}{b - a} \cdot \frac{b^2 - a^2}{2} = \frac{1}{b - a} \cdot \frac{(b - a)(b + a)}{2} = \frac{a + b}{2}
    $$
4.  **Смысл:** Среднее значение находится ровно посередине отрезка.

**Ответ:** $\frac{a + b}{2}$.

---

### Задача 3. Использование свойства линейности
**Задание:** Известно, что $E[X] = 2$ и $E[Y] = 5$. Найти $E[3X - 2Y + 4]$.

**Решение:**
1.  Используем свойства линейности и выноса константы:
    $$
    E[3X - 2Y + 4] = E[3X] - E[2Y] + E[4]
    $$
2.  Выносим множители:
    $$
    = 3E[X] - 2E[Y] + 4
    $$
3.  Подставляем значения:
    $$
    = 3 \cdot 2 - 2 \cdot 5 + 4 = 6 - 10 + 4 = 0
    $$
    *(Заметьте: нам не нужно знать, зависимы ли X и Y)*.

**Ответ:** 0.

---

### Задача 4. Математическое ожидание функции случайной величины
**Задание:** Случайная величина $X$ имеет плотность $f(x) = 2x$ на $[0, 1]$. Найти $E[X^2]$.

**Решение:**
1.  **Формула для функции:** $E[g(X)] = \int g(x) f(x) \, dx$. Здесь $g(x) = x^2$.
2.  **Интеграл:**
    $$
    E[X^2] = \int_{0}^{1} x^2 \cdot (2x) \, dx = \int_{0}^{1} 2x^3 \, dx
    $$
3.  **Вычисление:**
    $$
    = 2 \cdot \left[ \frac{x^4}{4} \right]_{0}^{1} = 2 \cdot \frac{1}{4} = 0.5
    $$
    *(Важно: $E[X^2] \neq (E[X])^2$)*.

**Ответ:** 0.5.

---

### Задача 5. Математическое ожидание для стандартных распределений
**Задание:** Найти математическое ожидание для случайной величины, имеющей распределение Пуассона с параметром $\lambda = 3$.

**Решение:**
1.  **Формула Пуассона:** $P(X = k) = \frac{\lambda^k e^{-\lambda}}{k!}$.
2.  **Определение:**
    $$
    E[X] = \sum_{k=0}^{\infty} k \cdot \frac{\lambda^k e^{-\lambda}}{k!}
    $$
3.  **Преобразование:** Первый член (при $k=0$) равен 0. Начинаем сумму с $k=1$:
    $$
    E[X] = \sum_{k=1}^{\infty} k \cdot \frac{\lambda^k e^{-\lambda}}{k \cdot (k-1)!} = \sum_{k=1}^{\infty} \frac{\lambda^k e^{-\lambda}}{(k-1)!}
    $$
4.  **Вынос $\lambda$:**
    $$
    E[X] = \lambda e^{-\lambda} \sum_{k=1}^{\infty} \frac{\lambda^{k-1}}{(k-1)!}
    $$
    Замена индекса $m = k - 1$:
    $$
    E[X] = \lambda e^{-\lambda} \sum_{m=0}^{\infty} \frac{\lambda^m}{m!}
    $$
5.  **Сумма ряда:** $\sum \frac{\lambda^m}{m!} = e^{\lambda}$.
    $$
    E[X] = \lambda e^{-\lambda} \cdot e^{\lambda} = \lambda
    $$
6.  **Подстановка:** $\lambda = 3$.

**Ответ:** 3.

---

# 5. Таблица математических ожиданий основных распределений

| Распределение | Параметры | Формула $E[X]$ |
|:---:|:---:|:---:|
| **Дискретное равномерное** | $N$ значений | $\frac{N + 1}{2}$ |
| **Биномиальное** $B(n, p)$ | $n, p$ | $np$ |
| **Пуассона** $\text{Pois}(\lambda)$ | $\lambda$ | $\lambda$ |
| **Геометрическое** $\text{Geom}(p)$ | $p$ | $\frac{1}{p}$ |
| **Непрерывное равномерное** $U(a, b)$ | $a, b$ | $\frac{a + b}{2}$ |
| **Экспоненциальное** $\text{Exp}(\lambda)$ | $\lambda$ | $\frac{1}{\lambda}$ |
| **Нормальное** $N(\mu, \sigma^2)$ | $\mu, \sigma$ | $\mu$ |

---

# Резюме модуля

| Понятие | Формула / Свойство |
|:---:|:---:|
| **Определение (дискретное)** | $E[X] = \sum x_i p_i$ |
| **Определение (непрерывное)** | $E[X] = \int x f(x) dx$ |
| **Линейность** | $E[X + Y] = E[X] + E[Y]$ |
| **Независимость** | $E[XY] = E[X]E[Y]$ (если независимы) |
| **Физический смысл** | Центр масс распределения |

**Ключевые выводы:**
1.  **Математическое ожидание** — это средневзвешенное значение случайной величины.
2.  **Свойство линейности** работает всегда, даже для зависимых величин (это мощный инструмент упрощения).
3.  **Произведение ожиданий** равно ожиданию произведения только для независимых величин.
4.  **Для стандартных распределений** ожидание выражается через параметры (например, для нормального это $\mu$, для Пуассона — $\lambda$).
5.  **Существование:** Математическое ожидание существует не всегда (требуется абсолютная сходимость ряда или интеграла).

