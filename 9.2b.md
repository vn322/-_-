
Тема **Интегральной теоремы Коши и формулы Коши** — это сердце комплексного анализа. Эти результаты не имеют аналогов в вещественном анализе и открывают удивительные возможности для вычисления интегралов и изучения свойств функций.

---

# 1. Интуитивное понимание

### Интеграл по кривой в комплексной плоскости
В вещественном анализе мы интегрируем по отрезку прямой. В комплексном анализе мы интегрируем по **кривым** (контурам) на комплексной плоскости.

**Вопрос:** Зависит ли значение интеграла от пути интегрирования?
*   В общем случае — **да**.
*   Но для **аналитических функций** в односвязной области — **нет**!

### Теорема Коши (Интуитивно)
Если функция аналитична (гладкая, без особенностей) внутри замкнутого контура и на нем, то интеграл по этому контуру равен **нулю**.

**Аналогия с физикой:**
Представьте поле сил. Если поле потенциально (не имеет вихрей), то работа по замкнутому пути равна нулю. Аналитическая функция ведет себя как потенциальное поле.

### Формула Коши (Интуитивно)
Значение аналитической функции **внутри** контура полностью определяется её значениями **на границе** контура.
Это удивительное свойство: зная функцию только на окружности, мы можем восстановить её во всех внутренних точках!

---

# 2. Строгие определения

### Интеграл по кривой
Пусть $f(z)$ — непрерывная функция на кривой $\gamma$, заданной параметрически $z(t)$, $a \le t \le b$.
**Криволинейный интеграл** определяется как:

$$
\int_{\gamma} f(z) \, dz = \int_{a}^{b} f(z(t)) \cdot z'(t) \, dt
$$

### Интегральная теорема Коши
Пусть:
1.  Область $D$ — **односвязная** (без «дырок»).
2.  Функция $f(z)$ **аналитична** в области $D$ и непрерывна на её замыкании.
3.  $\gamma$ — любой **замкнутый кусочно-гладкий контур**, лежащий в $D$.

Тогда:

$$
\oint_{\gamma} f(z) \, dz = 0
$$

### Важное замечание об односвязности
*   **Односвязная область:** Любой замкнутый контур можно стянуть в точку, не выходя из области (нет дырок).
*   **Многосвязная область:** Есть «дырки» (особые точки функции). Теорема Коши в чистом виде не применима.

### Интегральная формула Коши
Пусть:
1.  $f(z)$ аналитична в односвязной области $D$.
2.  $\gamma$ — замкнутый контур в $D$.
3.  Точка $a$ лежит **внутри** $\gamma$.

Тогда значение функции в точке $a$ выражается через интеграл:

$$
f(a) = \frac{1}{2\pi i} \oint_{\gamma} \frac{f(z)}{z - a} \, dz
$$

Или в эквивалентной форме:

$$
\oint_{\gamma} \frac{f(z)}{z - a} \, dz = 2\pi i \cdot f(a)
$$

### Формула для производных (Обобщение)
Для производных любого порядка $n$:

$$
f^{(n)}(a) = \frac{n!}{2\pi i} \oint_{\gamma} \frac{f(z)}{(z - a)^{n+1}} \, dz
$$

**Важный вывод:** Если функция аналитична, она имеет производные **всех порядков**! (В вещественном анализе это не так).

---

# 3. Геометрический и физический смысл

### Независимость от пути
Для аналитической функции интеграл зависит только от начальной и конечной точки, а не от формы пути:

$$
\int_{z_1}^{z_2} f(z) \, dz = F(z_2) - F(z_1)
$$

где $F'(z) = f(z)$ — первообразная.

### Среднее значение
Формулу Коши можно переписать для окружности радиуса $R$ с центром в $a$:

$$
f(a) = \frac{1}{2\pi} \int_{0}^{2\pi} f(a + Re^{i\theta}) \, d\theta
$$

**Смысл:** Значение функции в центре окружности равно **среднему арифметическому** её значений на окружности.

### Принцип максимума модуля
Из формулы среднего следует: модуль аналитической функции не может достигать локального максимума внутри области (только на границе).

---

# 4. Типовые задачи и решения

### Задача 1. Применение теоремы Коши
**Задание:** Вычислить интеграл $\oint_{|z|=2} \frac{z^2 + 1}{z - 3} \, dz$.

**Решение:**
1.  **Подынтегральная функция:** $f(z) = \frac{z^2 + 1}{z - 3}$.
2.  **Особая точка:** Знаменатель равен нулю при $z = 3$.
3.  **Контур:** Окружность $|z| = 2$ (радиус 2, центр в 0).
4.  **Расположение особенности:** Точка $z = 3$ лежит **вне** контура ($|3| = 3 > 2$).
5.  **Вывод:** Функция аналитична везде внутри и на контуре. По теореме Коши:

$$
    \oint_{|z|=2} \frac{z^2 + 1}{z - 3} \, dz = 0
    $$

**Ответ:** 0.

---

### Задача 2. Применение формулы Коши
**Задание:** Вычислить интеграл $\oint_{|z|=2} \frac{e^z}{z - 1} \, dz$.

**Решение:**
1.  **Вид интеграла:** $\oint_{\gamma} \frac{f(z)}{z - a} \, dz$, где $f(z) = e^z$, $a = 1$.
2.  **Проверка аналитичности:** $f(z) = e^z$ аналитична всюду.
3.  **Расположение точки:** $a = 1$ лежит **внутри** контура $|z| = 2$ ($|1| = 1 < 2$).
4.  **Применяем формулу Коши:**

$$
    \oint_{|z|=2} \frac{e^z}{z - 1} \, dz = 2\pi i \cdot f(1) = 2\pi i \cdot e^1
    $$

**Ответ:** $2\pi i e$.

---

### Задача 3. Формула Коши для производных
**Задание:** Вычислить интеграл $\oint_{|z|=3} \frac{\sin z}{(z - \pi)^2} \, dz$.

**Решение:**
1.  **Вид интеграла:** $\oint_{\gamma} \frac{f(z)}{(z - a)^{n+1}} \, dz$, где $f(z) = \sin z$, $a = \pi$, $n + 1 = 2 \Rightarrow n = 1$.
2.  **Проверка аналитичности:** $\sin z$ аналитична всюду.
3.  **Расположение точки:** $a = \pi \approx 3.14$. Контур $|z| = 3$.
    *   **Внимание:** $|\pi| > 3$, точка лежит **вне** контура!
4.  **Вывод:** Подынтегральная функция аналитична внутри контура (особенность снаружи). По теореме Коши:

$$
    \oint_{|z|=3} \frac{\sin z}{(z - \pi)^2} \, dz = 0
    $$

(Если бы контур был $|z| = 4$, тогда точка $\pi$ была бы внутри и ответ был бы $2\pi i \cdot f'(\pi) = 2\pi i \cdot \cos \pi = -2\pi i$).

**Ответ:** 0.

---

### Задача 4. Контур с несколькими особенностями
**Задание:** Вычислить $\oint_{|z|=3} \frac{1}{z^2 - 1} \, dz$.

**Решение:**
1.  **Особые точки:** $z^2 - 1 = 0 \Rightarrow z = \pm 1$.
2.  **Расположение:** Обе точки $z = 1$ и $z = -1$ лежат **внутри** контура $|z| = 3$.
3.  **Разложение на простейшие дроби:**

$$
    \frac{1}{z^2 - 1} = \frac{1}{(z-1)(z+1)} = \frac{1}{2} \left( \frac{1}{z - 1} - \frac{1}{z + 1} \right)
    $$
    
4.  **Интеграл:**

$$
    \oint_{|z|=3} \frac{1}{z^2 - 1} \, dz = \frac{1}{2} \oint_{|z|=3} \frac{dz}{z - 1} - \frac{1}{2} \oint_{|z|=3} \frac{dz}{z + 1}
    $$
    
5.  **Применяем формулу Коши** (для $f(z) = 1$):
    *   $\oint \frac{dz}{z - 1} = 2\pi i \cdot 1 = 2\pi i$
    *   $\oint \frac{dz}{z + 1} = 2\pi i \cdot 1 = 2\pi i$
6.  **Вычисление:**
 
 $$
    \frac{1}{2} (2\pi i) - \frac{1}{2} (2\pi i) = 0
    $$

**Ответ:** 0.

---

### Задача 5. Восстановление функции по интегралу
**Задание:** Известно, что для аналитической функции $f(z)$ выполняется:

$$
\oint_{|z|=2} \frac{f(z)}{z - i} \, dz = 4\pi i
$$

Найти $f(i)$.

**Решение:**
1.  **Формула Коши:**
$$
    \oint_{|z|=2} \frac{f(z)}{z - i} \, dz = 2\pi i \cdot f(i)
    $$
    
3.  **Приравниваем:**

$$
    2\pi i \cdot f(i) = 4\pi i
    $$
    
4.  **Находим значение:**

$$
    f(i) = \frac{4\pi i}{2\pi i} = 2
    $$

**Ответ:** $f(i) = 2$.

---

# Резюме модуля

| Теорема / Формула | Условия | Результат |
|:---:|:---:|:---:|
| **Теорема Коши** | $f(z)$ аналитична внутри $\gamma$ | $\oint_{\gamma} f(z) \, dz = 0$ |
| **Формула Коши** | $a$ внутри $\gamma$, $f(z)$ аналитична | $\oint_{\gamma} \frac{f(z)}{z-a} \, dz = 2\pi i f(a)$ |
| **Для производных** | $n$-я производная | $\oint_{\gamma} \frac{f(z)}{(z-a)^{n+1}} \, dz = \frac{2\pi i}{n!} f^{(n)}(a)$ |

**Ключевые выводы:**
1.  **Теорема Коши** позволяет сразу сказать, что интеграл равен нулю, если особенностей внутри нет.
2.  **Формула Коши** связывает значения функции внутри контура с интегралом по границе.
3.  **Аналитические функции** бесконечно дифференцируемы (в отличие от вещественных).
4.  **Важно проверять:** Лежит ли особая точка внутри или вне контура!

