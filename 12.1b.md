
Тема **Необходимых условий экстремума и уравнений Эйлера-Лагранжа** — это фундамент вариационного исчисления. Здесь мы учимся искать не просто числа, а **функции**, которые доставляют минимум или максимум некоторому функционалу (например, пути наименьшего действия в физике).

---

# 1. Интуитивное понимание

### От функций к функционалам
В обычном анализе мы ищем точку $x_0$, где функция $f(x)$ достигает экстремума. Условие: $f'(x_0) = 0$.

В вариационном исчислении мы ищем **функцию** $y(x)$, которая минимизирует **функционал** $J[y]$.
*   **Функция:** Число $\to$ Число ($f: \mathbb{R} \to \mathbb{R}$)
*   **Функционал:** Функция $\to$ Число ($J: C^1 \to \mathbb{R}$)

**Пример:** Длина кривой между двумя точками — это функционал. Он ставит в соответствие каждой кривой (функции) число (её длину).

### Вариация
Аналог приращения функции в вариационном исчислении называется **вариацией** $\delta J$.
*   В анализе: $df = f'(x) dx$
*   В вариации: $\delta J = \text{линейная часть изменения функционала}$

**Необходимое условие:** Для экстремума функционала его вариация должна равняться нулю: $\delta J = 0$. Это приводит к **уравнению Эйлера-Лагранжа**.

---

# 2. Строгие определения

### Функционал
Пусть $\mathcal{F}$ — пространство функций (например, $C^1[a, b]$).
**Функционалом** называется отображение $J: \mathcal{F} \to \mathbb{R}$.

Классический вид функционала в вариационном исчислении:

$$
J[y] = \int_{a}^{b} F(x, y(x), y'(x)) \, dx
$$

где $F$ — заданная функция трех переменных (подынтегральная функция).

### Вариация функционала
Пусть $y(x)$ — функция, $\delta y(x) = \varepsilon h(x)$ — её вариация (малое возмущение), где $h(a) = h(b) = 0$ (граничные условия фиксированы).

**Вариацией функционала** называется линейная часть его приращения:

$$
\Delta J = J[y + \delta y] - J[y] = \delta J + o(\|\delta y\|)
$$

### Экстремум функционала
Функция $y_0(x)$ доставляет **слабый локальный минимум** функционалу $J$, если существует $\varepsilon > 0$ такое, что для всех $y$ из $\varepsilon$-окрестности $y_0$:

$$
J[y] \ge J[y_0]
$$

---

# 3. Уравнение Эйлера-Лагранжа

### Вывод (схема)
1.  Рассмотрим приращение функционала на вариации $y + \varepsilon h$:

$$
    J[y + \varepsilon h] = \int_{a}^{b} F(x, y + \varepsilon h, y' + \varepsilon h') \, dx
    $$
    
2.  Разложим $F$ по Тейлору до первого порядка по $\varepsilon$:

$$
    \Delta J \approx \varepsilon \int_{a}^{b} \left( \frac{\partial F}{\partial y} h + \frac{\partial F}{\partial y'} h' \right) dx
    $$
    
3.  Проинтегрируем второе слагаемое по частям:

$$
    \int_{a}^{b} \frac{\partial F}{\partial y'} h' \, dx = \left[ \frac{\partial F}{\partial y'} h \right]_{a}^{b} - \int_{a}^{b} \frac{d}{dx} \left( \frac{\partial F}{\partial y'} \right) h \, dx
    $$

Граничный член равен 0, так как $h(a) = h(b) = 0$.

4.  Получаем вариацию:

$$
    \delta J = \varepsilon \int_{a}^{b} \left( \frac{\partial F}{\partial y} - \frac{d}{dx} \frac{\partial F}{\partial y'} \right) h(x) \, dx
    $$
    
5.  Для $\delta J = 0$ при любой $h(x)$ необходимо (лемма Дюбуа-Реймона):

$$
    \frac{\partial F}{\partial y} - \frac{d}{dx} \frac{\partial F}{\partial y'} = 0
    $$

### Формулировка теоремы
Если функция $y(x) \in C^2[a, b]$ доставляет экстремум функционалу

$$
J[y] = \int_{a}^{b} F(x, y, y') \, dx
$$

при фиксированных граничных условиях $y(a) = A$, $y(b) = B$, то она удовлетворяет **уравнению Эйлера-Лагранжа**:

$$
\frac{\partial F}{\partial y} - \frac{d}{dx} \left( \frac{\partial F}{\partial y'} \right) = 0
$$

Это обыкновенное дифференциальное уравнение второго порядка.

---

# 4. Частные случаи уравнения Эйлера-Лагранжа

### 1. $F$ не зависит от $y$ ($F = F(x, y')$ )
Тогда $\frac{\partial F}{\partial y} = 0$, и уравнение принимает вид:

$$
\frac{d}{dx} \left( \frac{\partial F}{\partial y'} \right) = 0 \quad \Rightarrow \quad \frac{\partial F}{\partial y'} = C
$$

(Первый интеграл)


### 2. $F$ не зависит от $x$ ($F = F(y, y')$ )
Тогда существует первый интеграл (закон сохранения энергии):

$$
F - y' \frac{\partial F}{\partial y'} = C
$$

### 3. $F$ не зависит от $y'$ ($F = F(x, y)$ )
Уравнение вырождается в алгебраическое:

$$
\frac{\partial F}{\partial y} = 0
$$

(Решение обычно не удовлетворяет граничным условиям — нет экстремали в классе гладких функций)


---

# 5. Типовые задачи и решения

### Задача 1. Задача о кратчайшем пути (Геодезическая)
**Задание:** Найти кривую наименьшей длины, соединяющую точки $(0, 0)$ и $(1, 1)$.

**Решение:**
1.  **Функционал длины:**

$$
    J[y] = \int_{0}^{1} \sqrt{1 + (y')^2} \, dx
    $$

Здесь $F(y') = \sqrt{1 + (y')^2}$.

2.  **Уравнение Эйлера-Лагранжа:**
    Так как $F$ не зависит от $y$ и $x$, используем первый интеграл:

$$
    \frac{\partial F}{\partial y'} = \frac{y'}{\sqrt{1 + (y')^2}} = C
    $$
    
3.  **Решение:**
    Отсюда $y' = \text{const} = k$.
    Интегрируем: $y(x) = kx + b$.
4.  **Граничные условия:**
    *   $y(0) = 0 \Rightarrow b = 0$
    *   $y(1) = 1 \Rightarrow k = 1$
5.  **Ответ:** $y(x) = x$ (прямая линия).

**Ответ:** $y(x) = x$.

---

### Задача 2. Задача о брахистохроне
**Задание:** Найти форму кривой, по которой материальная точка скатится из точки $A$ в точку $B$ под действием силы тяжести за наименьшее время.

**Решение:**
1.  **Функционал времени:**
 
 $$
    T[y] = \int_{0}^{x_1} \frac{\sqrt{1 + (y')^2}}{\sqrt{2gy}} \, dx
    $$

Здесь $F(y, y') = \frac{\sqrt{1 + (y')^2}}{\sqrt{y}}$ (константы опускаем).

2.  **Уравнение Эйлера-Лагранжа:**
    Так как $F$ не зависит от $x$, используем первый интеграл:

$$
    F - y' \frac{\partial F}{\partial y'} = C
    $$
    
3.  **Вычисления:**

$$
    \frac{\sqrt{1 + (y')^2}}{\sqrt{y}} - y' \cdot \frac{y'}{\sqrt{y}\sqrt{1 + (y')^2}} = C
    $$

Упрощаем:

$$
    \frac{1}{\sqrt{y}\sqrt{1 + (y')^2}} = C \quad \Rightarrow \quad y(1 + (y')^2) = \frac{1}{C^2} = 2R
    $$
    
4.  **Решение ДУ:**
    Отсюда $y' = \sqrt{\frac{2R - y}{y}}$. Решение в параметрической форме — **циклоида**:

$$
    \begin{cases}
    x = R(t - \sin t) \\
    y = R(1 - \cos t)
    \end{cases}
    $$

**Ответ:** Циклоида.

---

### Задача 3. Функционал с зависимостью от $x$
**Задание:** Найти экстремали функционала $J[y] = \int_{0}^{1} (y')^2 \, dx$ с условиями $y(0) = 0$, $y(1) = 1$.

**Решение:**
1.  **Подынтегральная функция:** $F = (y')^2$.
2.  **Частные производные:**

$$
    \frac{\partial F}{\partial y} = 0, \quad \frac{\partial F}{\partial y'} = 2y'
    $$
    
3.  **Уравнение Эйлера-Лагранжа:**

$$
    0 - \frac{d}{dx}(2y') = 0 \quad \Rightarrow \quad y'' = 0
    $$
    
4.  **Общее решение:** $y(x) = C_1 x + C_2$.
5.  **Граничные условия:**
    *   $y(0) = 0 \Rightarrow C_2 = 0$
    *   $y(1) = 1 \Rightarrow C_1 = 1$
6.  **Ответ:** $y(x) = x$.

**Ответ:** $y(x) = x$.

---

### Задача 4. Условный экстремум (Множители Лагранжа)
**Задание:** Минимизировать $J[y] = \int_{0}^{1} (y')^2 \, dx$ при условии $\int_{0}^{1} y^2 \, dx = 1$ и $y(0)=y(1)=0$.

**Решение:**
1.  **Вспомогательный функционал:** Вводим множитель Лагранжа $\lambda$:
 
 $$
    J^*[y] = \int_{0}^{1} \left( (y')^2 - \lambda y^2 \right) dx
    $$
    
2.  **Функция $F^*$:** $F^* = (y')^2 - \lambda y^2$.
3.  **Уравнение Эйлера-Лагранжа:**

$$
    \frac{\partial F^*}{\partial y} - \frac{d}{dx} \frac{\partial F^*}{\partial y'} = -2\lambda y - \frac{d}{dx}(2y') = 0
    $$

$$ \frac{\partial F^{\prime}}{\partial y} - \frac{d}{dx} \frac{\partial F^{\prime}}{\partial y'} = -2\lambda y - \frac{d}{dx}(2y') = 0 $$    

$$
    y'' + \lambda y = 0
    $$
    
4.  **Решение:** Это задача Штурма-Лиувилля.
    *   При $\lambda > 0$: $y = C \sin(\sqrt{\lambda} x)$.
    *   Из $y(1) = 0$: $\sin(\sqrt{\lambda}) = 0 \Rightarrow \sqrt{\lambda} = \pi n$.
    *   Наименьшее собственное значение: $\lambda = \pi^2$, $y = C \sin(\pi x)$.
5.  **Нормировка:** Из условия $\int y^2 = 1$ находим $C = \sqrt{2}$.

**Ответ:** $y(x) = \sqrt{2} \sin(\pi x)$, $\lambda = \pi^2$.

---

### Задача 5. Проверка достаточных условий (Лежандра)
**Задание:** Является ли найденная экстремаль в Задаче 3 минимумом?

**Решение:**
1.  **Условие Лежандра:** Для минимума необходимо $\frac{\partial^2 F}{\partial (y')^2} \ge 0$.
2.  **Вычисление:**
 
 $$
    F = (y')^2 \quad \Rightarrow \quad \frac{\partial^2 F}{\partial (y')^2} = 2 > 0
    $$
    
3.  **Условие Якоби:** Для функционала с $F_{y'y'} > 0$ и линейным уравнением Эйлера экстремаль доставляет сильный минимум, если нет сопряженных точек. Для уравнения $y''=0$ сопряженных точек нет.
4.  **Вывод:** Экстремаль $y=x$ доставляет **минимум** функционалу.

**Ответ:** Да, это минимум.

---

# Резюме модуля

| Понятие | Формула / Условие |
|:---:|:---:|
| **Функционал** | $J[y] = \int F(x, y, y') dx$ |
| **Вариация** | $\delta J = 0$ (необходимое условие) |
| **Уравнение Эйлера-Лагранжа** | $\frac{\partial F}{\partial y} - \frac{d}{dx} \frac{\partial F}{\partial y'} = 0$ |
| **Первый интеграл ($F_x = 0$)** | $F - y' F_{y'} = C$ |
| **Условный экстремум** | Вспомогательный функционал с множителем $\lambda$ |

**Ключевые выводы:**
1.  **Уравнение Эйлера-Лагранжа** сводит вариационную задачу к решению ОДУ второго порядка.
2.  **Первые интегралы** упрощают решение, если $F$ не зависит от $x$ или $y$.
3.  **Множители Лагранжа** позволяют решать изопериметрические задачи (с ограничениями-интегралами).
4.  **Достаточные условия** (Лежандра, Якоби) проверяют, является ли экстремаль минимумом или максимумом.

