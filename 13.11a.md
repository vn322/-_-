Мы переходим к важнейшему разделу математической статистики — **теории оценивания**. На практике мы никогда не знаем истинных параметров распределения (например, среднего генеральной совокупности) и вынуждены оценивать их по выборке. Здесь возникают вопросы: как строить оценки, какие из них лучше и как проверить их качество.

---

### 1. Классификация оценок

#### 1.1 Точечные и интервальные оценки

*   **Точечная оценка** — одно число, вычисленное по выборке, которое приближает истинное значение параметра. Пример: выборочное среднее \(\bar{x}\) как оценка математического ожидания \(\mu\).
*   **Интервальная оценка** — интервал, который с заданной вероятностью накрывает истинное значение параметра. Пример: доверительный интервал для среднего.

Мы сосредоточимся на точечных оценках.

#### 1.2 Свойства точечных оценок

Пусть \(\theta\) — истинное значение параметра, а \(\hat{\theta} = \hat{\theta}(X_1, \dots, X_n)\) — его оценка по выборке.

1.  **Несмещённость:** Оценка называется **несмещённой**, если её математическое ожидание равно истинному значению параметра:
    \[
    \mathbb{E}[\hat{\theta}] = \theta
    \]
    Если смещение есть, то \(\text{Bias}(\hat{\theta}) = \mathbb{E}[\hat{\theta}] - \theta\).

    *Пример:* Выборочное среднее \(\bar{X}\) — несмещённая оценка \(\mu\). Выборочная дисперсия \(S^2 = \frac{1}{n-1}\sum(X_i - \bar{X})^2\) — несмещённая оценка \(\sigma^2\); а \(\frac{1}{n}\sum(X_i - \bar{X})^2\) — смещённая.

2.  **Состоятельность:** Оценка называется **состоятельной**, если она сходится по вероятности к истинному значению параметра при \(n \to \infty\):
    \[
    \hat{\theta}_n \stackrel{P}{\to} \theta
    \]
    То есть с ростом объёма выборки точность оценки неограниченно увеличивается.

3.  **Эффективность:** Сравнение несмещённых оценок по дисперсии. Чем меньше дисперсия, тем оценка точнее.

4.  **Достаточность:** Оценка называется **достаточной**, если она содержит всю информацию о параметре, которую можно извлечь из выборки. Формально: условное распределение выборки при фиксированной оценке не зависит от параметра.

---

### 2. Эффективность оценок

#### 2.1 Сравнение оценок

Для несмещённых оценок естественным критерием качества является дисперсия: чем она меньше, тем оценка лучше (точнее).

#### 2.2 Неравенство Рао-Крамера (нижняя граница дисперсии)

Существует теоретический предел точности любой несмещённой оценки — **неравенство Рао-Крамера**.

Пусть \(f(x; \theta)\) — плотность (или вероятность) распределения, удовлетворяющая некоторым условиям регулярности. Тогда для любой несмещённой оценки \(\hat{\theta}\) выполняется:
\[
\boxed{\mathbb{D}[\hat{\theta}] \ge \frac{1}{n I(\theta)}}
\]
где \(I(\theta)\) — **информация Фишера**, содержащаяся в одном наблюдении:
\[
I(\theta) = \mathbb{E}\left[\left(\frac{\partial}{\partial\theta} \ln f(X; \theta)\right)^2\right] = -\mathbb{E}\left[\frac{\partial^2}{\partial\theta^2} \ln f(X; \theta)\right]
\]

#### 2.3 Определение эффективности

*   Несмещённая оценка, достигающая нижней границы Рао-Крамера (т.е. с минимально возможной дисперсией), называется **эффективной**.
*   **Асимптотически эффективная** оценка — та, у которой дисперсия стремится к границе Рао-Крамера при \(n \to \infty\).

---

### 3. Функция правдоподобия

#### 3.1 Определение

Пусть у нас есть выборка \(X_1, \dots, X_n\) из распределения с плотностью \(f(x; \theta)\) (для дискретных — вероятность \(P(X = x; \theta)\)). **Функцией правдоподобия** называется совместная плотность (вероятность) выборки, рассматриваемая как функция от параметра \(\theta\) при фиксированных наблюдениях:
\[
L(\theta; X_1, \dots, X_n) = \prod_{i=1}^n f(X_i; \theta)
\]

**Логарифмическая функция правдоподобия** (логарифмировать удобно, так как это превращает произведение в сумму):
\[
\ell(\theta) = \ln L(\theta) = \sum_{i=1}^n \ln f(X_i; \theta)
\]

#### 3.2 Интуиция

Функция правдоподобия отвечает на вопрос: "Насколько правдоподобно (вероятно) наблюдать именно эти данные, если истинный параметр равен \(\theta\)?" Мы ищем такое \(\theta\), которое делает наши наблюдения наиболее вероятными.

---

### 4. Метод максимального правдоподобия (ММП)

#### 4.1 Определение

**Оценкой максимального правдоподобия (ОМП)** называется значение параметра \(\hat{\theta}_{\text{МП}}\), при котором функция правдоподобия достигает максимума:
\[
\boxed{\hat{\theta}_{\text{МП}} = \arg\max_{\theta} L(\theta)}
\]
Часто вместо максимизации \(L(\theta)\) максимизируют \(\ell(\theta)\), что эквивалентно (логарифм — монотонная функция).

#### 4.2 Нахождение ОМП

Обычно решают **уравнение правдоподобия**:
\[
\frac{\partial \ell(\theta)}{\partial \theta} = 0
\]
(а также проверяют, что найденная точка — максимум, а не минимум).

#### 4.3 Свойства ОМП (при достаточно общих условиях регулярности)

1.  **Состоятельность:** \(\hat{\theta}_{\text{МП}} \stackrel{P}{\to} \theta\).
2.  **Асимптотическая нормальность:** 
    \[
    \sqrt{n}(\hat{\theta}_{\text{МП}} - \theta) \stackrel{d}{\to} N\left(0, \frac{1}{I(\theta)}\right)
    \]
3.  **Асимптотическая эффективность:** Дисперсия асимптотически достигает границы Рао-Крамера.
4.  **Инвариантность:** Если \(\hat{\theta}\) — ОМП для \(\theta\), то для любой функции \(g\) оценка \(g(\hat{\theta})\) является ОМП для \(g(\theta)\).

---

### 5. Примеры

#### Пример 1: Оценка параметра \(p\) в схеме Бернулли

Пусть \(X_1, \dots, X_n \sim \text{Bernoulli}(p)\). Тогда:
\[
L(p) = \prod_{i=1}^n p^{X_i} (1-p)^{1-X_i} = p^{\sum X_i} (1-p)^{n - \sum X_i}
\]
\[
\ell(p) = \left(\sum X_i\right) \ln p + \left(n - \sum X_i\right) \ln(1-p)
\]
Производная:
\[
\frac{\partial \ell}{\partial p} = \frac{\sum X_i}{p} - \frac{n - \sum X_i}{1-p} = 0 \Rightarrow \sum X_i (1-p) = (n - \sum X_i) p \Rightarrow \sum X_i = np
\]
Отсюда:
\[
\hat{p}_{\text{МП}} = \frac{1}{n} \sum_{i=1}^n X_i = \bar{X}
\]
Это хорошо известная оценка — частота успехов.

#### Пример 2: Оценка параметров нормального распределения

Пусть \(X_1, \dots, X_n \sim N(\mu, \sigma^2)\).
\[
L(\mu, \sigma^2) = \prod_{i=1}^n \frac{1}{\sqrt{2\pi\sigma^2}} \exp\left(-\frac{(X_i - \mu)^2}{2\sigma^2}\right) = (2\pi\sigma^2)^{-n/2} \exp\left(-\frac{1}{2\sigma^2} \sum (X_i - \mu)^2\right)
\]
\[
\ell(\mu, \sigma^2) = -\frac{n}{2}\ln(2\pi) - \frac{n}{2}\ln(\sigma^2) - \frac{1}{2\sigma^2} \sum (X_i - \mu)^2
\]

Производные:
*   По \(\mu\): \(\frac{\partial \ell}{\partial \mu} = \frac{1}{\sigma^2} \sum (X_i - \mu) = 0 \Rightarrow \sum X_i - n\mu = 0 \Rightarrow \hat{\mu}_{\text{МП}} = \bar{X}\).
*   По \(\sigma^2\): \(\frac{\partial \ell}{\partial \sigma^2} = -\frac{n}{2\sigma^2} + \frac{1}{2\sigma^4} \sum (X_i - \mu)^2 = 0\).
    При подстановке \(\hat{\mu} = \bar{X}\) получаем:
    \[
    \hat{\sigma}^2_{\text{МП}} = \frac{1}{n} \sum (X_i - \bar{X})^2
    \]
    (обратите внимание: это **смещённая** оценка дисперсии; несмещённая получается делением на \(n-1\), но ОМП даёт именно эту).

#### Резюме

| Понятие | Определение / Формула |
|:---|:---|
| **Несмещённость** | \(\mathbb{E}[\hat{\theta}] = \theta\) |
| **Состоятельность** | \(\hat{\theta}_n \stackrel{P}{\to} \theta\) |
| **Эффективность** | Достижение нижней границы Рао-Крамера |
| **Функция правдоподобия** | \(L(\theta) = \prod f(X_i; \theta)\) |
| **Логарифмическое правдоподобие** | \(\ell(\theta) = \ln L(\theta)\) |
| **ОМП** | \(\hat{\theta}_{\text{МП}} = \arg\max L(\theta)\) |
