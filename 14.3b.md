
Тема **Итерационных методов решения СЛАУ** — это важный раздел численной линейной алгебры. В отличие от прямых методов (Гаусса, LU-разложения), итерационные методы находят решение постепенно, уточняя его на каждом шаге. Они особенно эффективны для больших разреженных систем.

---

# 1. Интуитивное понимание

### Прямые vs Итерационные методы

| Характеристика | Прямые методы | Итерационные методы |
|:---:|:---:|:---:|
| **Пример** | Гаусс, LU-разложение | Якоби, Гаусса-Зейделя |
| **Точность** | Точное решение (с учетом округлений) | Приближенное решение |
| **Сложность** | $O(n^3)$ | $O(n^2)$ на итерацию |
| **Память** | Требуют хранения всей матрицы | Работают с ненулевыми элементами |
| **Применение** | Малые и средние системы | Большие разреженные системы |

### Основная идея
Вместо того чтобы решать систему напрямую, мы:
1.  Берем **начальное приближение** $x^{(0)}$.
2.  Строим **итерационный процесс**: $x^{(k+1)} = f(x^{(k)})$.
3.  Останавливаемся, когда **погрешность** становится достаточно малой.

**Аналогия:**
*   **Прямой метод:** Точный расчет маршрута по карте.
*   **Итерационный метод:** Движение к цели с постоянной коррекцией направления («тепло-холодно»).

---

# 2. Строгие определения

### Общая схема итерационных методов

Исходная система $Ax = b$ преобразуется к эквивалентному виду:

$$
x = Bx + c
$$

где $B$ — **матрица итераций**, $c$ — вектор.

**Итерационный процесс:**

$$
x^{(k+1)} = B x^{(k)} + c, \quad k = 0, 1, 2, \dots
$$

### Сходимость метода

**Теорема о сходимости:**
Итерационный процесс сходится к единственному решению при любом начальном приближении тогда и только тогда, когда **спектральный радиус** матрицы итераций меньше единицы:

$$
\rho(B) < 1
$$

где $\rho(B) = \max_i |\lambda_i(B)|$ — максимальный модуль собственного числа.

**Достаточное условие:**
Если $\|B\| < 1$ в какой-либо норме, то метод сходится.

### Оценка погрешности

**Априорная оценка** (до вычислений):

$$
\|x^{(k)} - x^*\| \le \frac{\|B\|^k}{1 - \|B\|} \|x^{(1)} - x^{(0)}\|
$$

**Апостериорная оценка** (после вычислений):

$$
\|x^{(k)} - x^*\| \le \frac{\|B\|}{1 - \|B\|} \|x^{(k)} - x^{(k-1)}\|
$$

---

# 3. Основные итерационные методы

## Метод простой итерации

### Алгоритм
1.  Преобразуем систему: $x = (I - A)x + b$ (или другое преобразование).
2.  Итерация: $x^{(k+1)} = (I - A)x^{(k)} + b$.

### Сходимость
Требуется $\|I - A\| < 1$.

---

## Метод Якоби

### Идея
Выражаем каждую переменную из соответствующего уравнения и подставляем значения с **предыдущей** итерации.

### Алгоритм
Для системы $Ax = b$:

$$
x_i^{(k+1)} = \frac{1}{a_{ii}} \left( b_i - \sum_{j \neq i} a_{ij} x_j^{(k)} \right), \quad i = 1, \dots, n
$$

### Матричная форма
Разложим $A = D + L + U$, где:
*   $D$ — диагональная часть
*   $L$ — нижняя треугольная
*   $U$ — верхняя треугольная

Тогда:

$$
x^{(k+1)} = D^{-1}(b - (L + U)x^{(k)})
$$

Матрица итераций: $B = -D^{-1}(L + U)$.

### Условие сходимости
**Достаточное условие:** Матрица $A$ имеет **строгое диагональное преобладание**:

$$
|a_{ii}| > \sum_{j \neq i} |a_{ij}| \quad \text{для всех } i
$$

---

## Метод Гаусса-Зейделя

### Идея
То же, что и Якоби, но используем **уже вычисленные** значения текущей итерации.

### Алгоритм

$$
x_i^{(k+1)} = \frac{1}{a_{ii}} \left( b_i - \sum_{j < i} a_{ij} x_j^{(k+1)} - \sum_{j > i} a_{ij} x_j^{(k)} \right)
$$

### Матричная форма

$$
x^{(k+1)} = (D + L)^{-1}(b - U x^{(k)})
$$

Матрица итераций: $B = -(D + L)^{-1}U$.

### Сходимость
*   Сходится быстрее метода Якоби (обычно в 2 раза).
*   Сходится, если $A$ симметрична и положительно определена.
*   Сходится при строгом диагональном преобладании.

---

## Метод верхней релаксации (SOR)

### Идея
Ускорение метода Гаусса-Зейделя с помощью параметра релаксации $\omega$.

### Алгоритм

$$
x_i^{(k+1)} = (1 - \omega)x_i^{(k)} + \frac{\omega}{a_{ii}} \left( b_i - \sum_{j < i} a_{ij} x_j^{(k+1)} - \sum_{j > i} a_{ij} x_j^{(k)} \right)
$$

### Выбор параметра $\omega$
*   $0 < \omega < 1$ — нижняя релаксация (затухание)
*   $1 < \omega < 2$ — верхняя релаксация (ускорение)
*   $\omega = 1$ — метод Гаусса-Зейделя
*   **Оптимальное $\omega$:** Зависит от спектра матрицы, обычно подбирается экспериментально.

---

# 4. Типовые задачи и решения

### Задача 1. Метод Якоби (одна итерация)
**Задание:** Выполнить одну итерацию метода Якоби для системы:

$$
\begin{cases}
4x_1 + x_2 = 5 \\
x_1 + 4x_2 = 6
\end{cases}
$$

с начальным приближением $x^{(0)} = (0, 0)^T$.

**Решение:**
1.  **Выразим переменные:**

$$
    x_1 = \frac{5 - x_2}{4}, \quad x_2 = \frac{6 - x_1}{4}
    $$
    
2.  **Итерация $k = 0$:**

$$
    x_1^{(1)} = \frac{5 - 0}{4} = 1.25
    $$

$$
    x_2^{(1)} = \frac{6 - 0}{4} = 1.5
    $$
    
3.  **Ответ:** $x^{(1)} = (1.25, 1.5)^T$.

*(Точное решение: $x_1 = 1, x_2 = 1.25$)*

**Ответ:** $x^{(1)} = (1.25, 1.5)^T$.

---

### Задача 2. Метод Гаусса-Зейделя (одна итерация)
**Задание:** Выполнить одну итерацию метода Гаусса-Зейделя для той же системы.

**Решение:**
1.  **Выразим переменные:**

$$
    x_1 = \frac{5 - x_2}{4}, \quad x_2 = \frac{6 - x_1}{4}
    $$
    
2.  **Итерация $k = 0$:**
    *   Сначала $x_1^{(1)}$ (используем $x_2^{(0)} = 0$):

$$
        x_1^{(1)} = \frac{5 - 0}{4} = 1.25
        $$
        
    *   Затем $x_2^{(1)}$ (используем **новое** $x_1^{(1)} = 1.25$):

$$
        x_2^{(1)} = \frac{6 - 1.25}{4} = \frac{4.75}{4} = 1.1875
        $$
        
3.  **Ответ:** $x^{(1)} = (1.25, 1.1875)^T$.

(Ближе к точному решению, чем метод Якоби!)

**Ответ:** $x^{(1)} = (1.25, 1.1875)^T$.

---

### Задача 3. Проверка сходимости
**Задание:** Будет ли сходиться метод Якоби для матрицы:

$$
A = \begin{pmatrix}
5 & 1 & 1 \\
1 & 4 & 1 \\
1 & 1 & 3
\end{pmatrix}
$$

**Решение:**
1.  **Проверка диагонального преобладания:**
    *   Строка 1: $|5| > |1| + |1| = 2$ ✓
    *   Строка 2: $|4| > |1| + |1| = 2$ ✓
    *   Строка 3: $|3| > |1| + |1| = 2$ ✓
2.  **Вывод:** Матрица имеет строгое диагональное преобладание → метод Якоби **сходится**.

**Ответ:** Да, метод сходится.

---

### Задача 4. Оценка числа итераций
**Задание:** Для метода с $\|B\| = 0.5$ оценить число итераций для достижения точности $\varepsilon = 10^{-4}$, если $\|x^{(1)} - x^{(0)}\| = 1$.

**Решение:**
1.  **Априорная оценка:**

$$
    \frac{\|B\|^k}{1 - \|B\|} \|x^{(1)} - x^{(0)}\| \le \varepsilon
    $$
    
2.  **Подстановка:**

$$
    \frac{0.5^k}{0.5} \cdot 1 \le 10^{-4} \quad \Rightarrow \quad 0.5^{k-1} \le 10^{-4}
    $$
    
3.  **Логарифмирование:**

$$
    (k-1) \ln(0.5) \le \ln(10^{-4}) \quad \Rightarrow \quad k-1 \ge \frac{-4 \ln(10)}{\ln(0.5)} \approx 13.29
    $$
    
4.  **Ответ:** $k \ge 15$ итераций.

**Ответ:** Не менее 15 итераций.

---

### Задача 5. Сравнение методов
**Задание:** Сравнить скорость сходимости методов Якоби и Гаусса-Зейделя для матрицы $A = \begin{pmatrix} 2 & 1 \\ 1 & 2 \end{pmatrix}$.

**Решение:**
1.  **Матрица итераций Якоби:**

$$
    B_J = -D^{-1}(L+U) = -\begin{pmatrix} 1/2 & 0 \\ 0 & 1/2 \end{pmatrix} \begin{pmatrix} 0 & 1 \\ 1 & 0 \end{pmatrix} = \begin{pmatrix} 0 & -0.5 \\ -0.5 & 0 \end{pmatrix}
    $$

Собственные числа: $\lambda = \pm 0.5$, $\rho(B_J) = 0.5$.

2.  **Матрица итераций Гаусса-Зейделя:**

$$
    B_{GZ} = -(D+L)^{-1}U = -\begin{pmatrix} 2 & 0 \\ 1 & 2 \end{pmatrix}^{-1} \begin{pmatrix} 0 & 1 \\ 0 & 0 \end{pmatrix} = \begin{pmatrix} 0 & -0.5 \\ 0 & 0.25 \end{pmatrix}
    $$

Собственные числа: $\lambda = 0, 0.25$, $\rho(B_{GZ}) = 0.25$.

3.  **Сравнение:**

$\rho(B_{GZ}) = \rho(B_J)^2 = 0.25$. Метод Гаусса-Зейделя сходится в 2 раза быстрее.

**Ответ:** Гаусс-Зейдель сходится быстрее ($\rho = 0.25$ против $0.5$).

---

### Задача 6. Критерий остановки
**Задание:** Предложите критерий остановки для итерационного процесса.

**Решение:**
Основные критерии:
1.  **По невязке:**

$$
    \|r^{(k)}\| = \|b - Ax^{(k)}\| < \varepsilon
    $$
    
2.  **По изменению решения:**

$$
    \|x^{(k)} - x^{(k-1)}\| < \varepsilon
    $$
    
3.  **По относительной погрешности:**

$$
    \frac{\|x^{(k)} - x^{(k-1)}\|}{\|x^{(k)}\|} < \varepsilon
    $$
    
4.  **Максимальное число итераций:** $k < k_{\max}$ (защита от зацикливания).

**Ответ:** Комбинация критериев по невязке и изменению решения.

---

# 5. Таблица сравнения методов

| Метод | Матрица итераций | Сходимость | Скорость |
|:---:|:---:|:---:|:---:|
| **Якоби** | $-D^{-1}(L+U)$ | При диагональном преобладании | Медленнее |
| **Гаусса-Зейделя** | $-(D+L)^{-1}U$ | При диагональном преобладании или SPD | Быстрее (в 2 раза) |
| **SOR** | Зависит от $\omega$ | При $0 < \omega < 2$ и SPD | Может быть очень быстрой |
| **Простой итерации** | $I - A$ | При $\|I - A\| < 1$ | Зависит от преобразования |

**SPD** — симметричная положительно определенная матрица.

---

# 6. Резюме модуля

| Понятие | Формула / Условие |
|:---:|:---:|
| **Итерационная схема** | $x^{(k+1)} = Bx^{(k)} + c$ |
| **Условие сходимости** | $\rho(B) < 1$ |
| **Диагональное преобладание** | $|a_{ii}| > \sum_{j \neq i} |a_{ij}|$ |
| **Метод Якоби** | Все $x_j$ с предыдущей итерации |
| **Метод Гаусса-Зейделя** | Новые $x_j$ используются сразу |
| **Критерий остановки** | $\|x^{(k)} - x^{(k-1)}\| < \varepsilon$ |

**Ключевые выводы:**
1.  **Итерационные методы** эффективны для больших разреженных систем.
2.  **Сходимость** гарантирована при $\rho(B) < 1$ или диагональном преобладании.
3.  **Гаусс-Зейдель** обычно сходится быстрее Якоби (использует новые значения).
4.  **Параметр релаксации** в SOR может значительно ускорить сходимость.
5.  **Критерий остановки** должен балансировать между точностью и вычислительными затратами.

