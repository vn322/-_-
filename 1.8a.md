Мы завершаем наше путешествие по основам математического анализа. Интеграл Римана был определен для ограниченной функции на конечном отрезке. Но на практике часто встречаются задачи, где промежуток интегрирования бесконечен (например, работа по перемещению тела в космос) или функция неограничена вблизи точки (например, $\(1/\sqrt{x}\)$ вблизи нуля). Для таких случаев и вводят **несобственные интегралы**.

---

### 1. Определение несобственного интеграла

Несобственный интеграл — это интеграл с "бесконечностью" в пределах или с "разрывом" подынтегральной функции. Он определяется через предельный переход.

#### 1.1 Интегралы с бесконечными пределами (I род)

Пусть функция $\(f(x)\)$ непрерывна на промежутке $\([a, +\infty)\)$.

**Определение.** **Несобственным интегралом I рода** от функции $\(f(x)\)$ по промежутку $\([a, +\infty)\)$ называется предел:

$$\[
\int\limits_a^{+\infty} f(x) \, dx = \lim_{b \to +\infty} \int\limits_a^{b} f(x) \, dx
\]$$

Если этот предел существует и **конечен**, то интеграл называется **сходящимся**. Если предел не существует или бесконечен — **расходящимся**.

Аналогично определяются:
*   $\(\int_{-\infty}^{b} f(x) \, dx = \lim_{a \to -\infty} \int_{a}^{b} f(x) \, dx\)$
*   $\(\int_{-\infty}^{+\infty} f(x) \, dx = \int_{-\infty}^{c} f(x) \, dx + \int_{c}^{+\infty} f(x) \, dx\)$ (интеграл сходится, если сходятся оба слагаемых).

#### 1.2 Интегралы от неограниченных функций (II род)

Пусть функция $\(f(x)\)$ непрерывна на промежутке $\([a, b)\)$, но **неограничена** вблизи точки $\(b\)$ (например, $\(f(x) \to \infty\) при \(x \to b-\))$. Точка $\(b\)$ называется **особой точкой**.

**Определение.** **Несобственным интегралом II рода** от функции $\(f(x)\)$ по промежутку $\([a, b)\)$ называется предел:

$$\[
\int\limits_a^{b} f(x) \, dx = \lim_{\varepsilon \to 0+} \int\limits_a^{b-\varepsilon} f(x) \, dx
\]$$

Если предел существует и конечен — интеграл сходится, иначе — расходится.

Аналогично, если особая точка находится внутри промежутка $\([a, c]\)$, разбивают на два интеграла.

---

### 2. Критерий Коши сходимости несобственного интеграла

Как и для рядов, для несобственных интегралов существует универсальный критерий сходимости.

#### 2.1 Для интегралов I рода (бесконечный предел)

**Критерий Коши.** Интеграл $\(\int_a^{+\infty} f(x) \, dx\)$ сходится тогда и только тогда, когда для любого $\(\varepsilon > 0\)$ существует такое $\(B > a\)$, что для всех $\(b_2 > b_1 > B\)$ выполняется:

$$\[
\left| \int_{b_1}^{b_2} f(x) \, dx \right| < \varepsilon
\]$$

*Смысл:* "хвост" интеграла (от достаточно большого $\(b_1\) до \(b_2\)$ ) можно сделать сколь угодно малым.

#### 2.2 Для интегралов II рода (неограниченная функция)

Для интеграла $\(\int_a^b f(x) \, dx\)$ с особой точкой \(b\) критерий формулируется аналогично: для любого $\(\varepsilon > 0\)$ существует $\(\delta > 0\)$ такое, что для любых отрезков $\([\eta_1, \eta_2]\)$, лежащих в $\((b-\delta, b)\)$, выполняется $\(|\int_{\eta_1}^{\eta_2} f(x) dx| < \varepsilon\)$.

---

### 3. Эталонные интегралы (для сравнения)

Как и в случае рядов, основной метод исследования сходимости несобственных интегралов — сравнение с "эталонными" интегралами, поведение которых известно.

#### 3.1 Эталон I рода: $\(\int_a^{+\infty} \frac{dx}{x^p}\) (\(a > 0\))$

Это аналог обобщенного гармонического ряда.

$$\[
\int_a^{+\infty} \frac{dx}{x^p} = 
\begin{cases}
\frac{a^{1-p}}{p-1}, & p > 1 \quad (\text{сходится}) \\
+\infty, & p \le 1 \quad (\text{расходится})
\end{cases}
\]$$

При $\(p = 1\): \(\int_a^{+\infty} \frac{dx}{x} = \lim_{b\to\infty} (\ln b - \ln a) = +\infty\)$ (расходится).

#### 3.2 Эталон II рода: $\(\int_a^{b} \frac{dx}{(b-x)^p}\)$ (особая точка $\(x=b\$ )

$$\[
\int_a^{b} \frac{dx}{(b-x)^p} = 
\begin{cases}
\frac{(b-a)^{1-p}}{1-p}, & p < 1 \quad (\text{сходится}) \\
+\infty, & p \ge 1 \quad (\text{расходится})
\end{cases}
\]$$

При $\(p = 1\): \(\int_a^{b} \frac{dx}{b-x} = \lim_{\varepsilon\to0} (-\ln \varepsilon + \ln(b-a)) = +\infty\)$ (расходится).

**Обратите внимание на инверсию:** Для бесконечного предела сходимость при $\(p>1\)$, а для неограниченной функции в конечной точке — при $\(p<1\)$. Это важно не перепутать.

---

### 4. Признаки сходимости для знакопостоянных функций

Если подынтегральная функция не меняет знак (например, $\(f(x) \ge 0\)$ ), работают признаки сравнения, полностью аналогичные признакам для числовых рядов.

#### 4.1 Признак сравнения (в обычной форме)

Пусть $\(0 \le f(x) \le g(x)\) на \([a, +\infty)\)$.
*   Если $\(\int g\)$ сходится, то $\(\int f\)$ сходится.
*   Если $\(\int f\)$ расходится, то $\(\int g\)$ расходится.

#### 4.2 Признак сравнения в предельной форме (самый удобный)

Пусть $\(f(x) \ge 0, g(x) > 0\) на \([a, +\infty)\)$ и существует предел:

$$\[
\lim_{x \to +\infty} \frac{f(x)}{g(x)} = K, \quad 0 < K < \infty
\]$$

Тогда интегралы $\(\int_a^{+\infty} f(x) dx\) и \(\int_a^{+\infty} g(x) dx\)$ ведут себя одинаково (оба сходятся или оба расходятся).

**Как применять:** Нужно подобрать функцию $\(g(x)\)$ вида $\(\frac{1}{x^p}\)$, которая на бесконечности ведет себя так же, как $\(f(x)\)$ (имеет тот же порядок роста/убывания). Затем сравнить с эталоном.

**Пример:** Исследовать $\(\int_1^{+\infty} \frac{dx}{x^2 + 3x + 1}\)$.
При $\(x \to \infty\), \(\frac{1}{x^2 + 3x + 1} \sim \frac{1}{x^2}\)$. Берем $\(g(x) = 1/x^2\) (\(p=2 > 1\)$, эталон сходится).

$$\[
\lim_{x\to\infty} \frac{1/(x^2+3x+1)}{1/x^2} = \lim \frac{x^2}{x^2+3x+1} = 1
\]$$

Предел конечен и не равен нулю. Значит, исходный интеграл сходится.

---

### 5. Абсолютная и условная сходимость (для знакопеременных функций)

Если функция $\(f(x)\)$ меняет знак на промежутке интегрирования, вводятся те же понятия, что и для рядов.

**Определение.**
*   Несобственный интеграл $\(\int f(x) dx\)$ называется **абсолютно сходящимся**, если сходится интеграл от модуля $\(\int |f(x)| dx\)$.
*   Он называется **условно сходящимся**, если сам интеграл $\(\int f(x) dx\)$ сходится, а интеграл от модуля $\(\int |f(x)| dx\)$ расходится.

**Теорема.** Если интеграл сходится абсолютно, то он сходится и просто (как и в случае рядов).

Для исследования условной сходимости часто используют **признак Дирихле-Абеля** (аналог признака Лейбница для рядов, но в интегральной форме), который рассматривает интегралы вида $\(\int g(x) h(x) dx\), где \(g(x)\)$ — осциллирующая функция (типа $\(\sin x\)), а \(h(x)\)$ монотонно стремится к нулю.

**Классический пример условной сходимости:**

$$\[
\int_1^{+\infty} \frac{\sin x}{x} \, dx
\]$$

Этот интеграл сходится, но не абсолютно (интеграл от модуля $\(|\sin x/x|\)$ расходится, ведет себя как гармонический ряд).

#### Резюме

| Тип | Условие | Пример сходимости | Пример расходимости |
| :--- | :--- | :--- | :--- |
| **I род (беск. предел)** | $\(\int_a^{\infty} \frac{dx}{x^p}\)$ | $\(p > 1\)$ | $\(p \le 1\)$ |
| **II род (неогр. ф-я)** | $\(\int_a^b \frac{dx}{(b-x)^p}\)$ | $\(p < 1\)$ | $\(p \ge 1\)$ |
| **Знакопеременные** | $\(\int \frac{\sin x}{x^p} dx\) при \(x\to\infty\)$ | $\(p > 0\)$ (Дирихле) | $\(p \le 0\)$ |
| **Абсолютная сходимость** | Сходится $\(\int|f|\)$ | $\(\int\frac{\sin x}{x^2}dx\)$ | $\(\int\frac{\sin x}{x}dx\)$ |
