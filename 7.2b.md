


Тема **Теорем Лагранжа и Коши** продолжает фундаментальный ряд теорем дифференциального исчисления. Если теорема Ролля требовала равенства значений на концах отрезка, то теорема Лагранжа снимает это ограничение, а теорема Коши обобщает результат на две функции сразу.

---

# 1. Теорема Лагранжа (Теорема о среднем значении)

### Интуитивное понимание
**Физический смысл:**
Представьте, что вы проехали 100 км за 1 час. Ваша средняя скорость была 100 км/ч.
Теорема Лагранжа утверждает, что в какой-то момент времени во время этой поездки ваш спидометр показывал **ровно** 100 км/ч. Вы не могли все время ехать медленнее или быстрее среднего, если движение было плавным.

**Геометрический смысл:**
Проведем секущую через точки графика $(a, f(a))$ и $(b, f(b))$.
Теорема гарантирует, что найдется точка $c$ внутри интервала, где касательная к графику будет **параллельна этой секущей**.

### Строгое определение
Пусть функция $f(x)$ удовлетворяет двум условиям:
1.  **Непрерывна** на отрезке $[a, b]$.
2.  **Дифференцируема** на интервале $(a, b)$.

Тогда существует хотя бы одна точка $c \in (a, b)$, такая что:

$$
f(b) - f(a) = f'(c) \cdot (b - a)
$$

Или в другом виде:

$$
f'(c) = \frac{f(b) - f(a)}{b - a}
$$

### Связь с теоремой Ролля
Теорема Ролля — это частный случай теоремы Лагранжа.
Если $f(a) = f(b)$, то правая часть равенства равна нулю:

$$
f'(c) = \frac{0}{b - a} = 0
$$

Что полностью соответствует формулировке теоремы Ролля.

---

# 2. Теорема Коши (Обобщенная теорема о среднем значении)

### Интуитивное понимание
Теорема Лагранжа связывает приращение функции с приращением аргумента.
Теорема Коши связывает приращения **двух функций** друг с другом.
Представьте две машины, едущие по разным трассам, но за одно и то же время. Отношение пройденных путей равно отношению их скоростей в какой-то момент времени.

### Строгое определение
Пусть функции $f(x)$ и $g(x)$ удовлетворяют условиям:
1.  Непрерывны на отрезке $[a, b]$.
2.  Дифференцируемы на интервале $(a, b)$.
3.  Производная $g'(x) \neq 0$ для всех $x \in (a, b)$ (чтобы не делить на ноль).

Тогда существует точка $c \in (a, b)$, такая что:

$$
\frac{f(b) - f(a)}{g(b) - g(a)} = \frac{f'(c)}{g'(c)}
$$

### Связь с теоремой Лагранжа
Если выбрать $g(x) = x$, то $g'(x) = 1$, $g(b) - g(a) = b - a$.
Подставляя в формулу Коши, получаем формулу Лагранжа:

$$
\frac{f(b) - f(a)}{b - a} = \frac{f'(c)}{1}
$$

Значит, теорема Лагранжа — это частный случай теоремы Коши.

---

# 3. Типовые задачи и решения

### Задача 1. Проверка теоремы Лагранжа
**Задание:** Проверить выполнение теоремы Лагранжа для функции $f(x) = x^2 - 3x + 2$ на отрезке $[1, 4]$ и найти точку $c$.

**Решение:**
1.  **Проверка условий:**
    Функция — многочлен, значит непрерывна на $[1, 4]$ и дифференцируема на $(1, 4)$.
2.  **Вычисление частей формулы:**
    *   $f(1) = 1 - 3 + 2 = 0$
    *   $f(4) = 16 - 12 + 2 = 6$
    *   Приращение функции: $f(4) - f(1) = 6 - 0 = 6$
    *   Длина отрезка: $b - a = 4 - 1 = 3$
    *   Среднее значение производной: $\frac{6}{3} = 2$
3.  **Нахождение производной:**

$$
    f'(x) = 2x - 3
    $$
    
4.  **Поиск точки $c$:**
    Решаем уравнение $f'(c) = 2$:

$$
    2c - 3 = 2 \quad \Rightarrow \quad 2c = 5 \quad \Rightarrow \quad c = 2.5
    $$
    
5.  **Проверка принадлежности:**
    Точка $c = 2.5$ лежит внутри интервала $(1, 4)$.

**Ответ:** Теорема выполняется, $c = 2.5$.

---

### Задача 2. Оценка приращения (Неравенство)
**Задание:** Используя теорему Лагранжа, доказать неравенство $|\sin b - \sin a| \le |b - a|$.

**Решение:**
1.  Рассмотрим функцию $f(x) = \sin x$ на отрезке $[a, b]$ (пусть $a < b$).
2.  Функция непрерывна и дифференцируема везде, $f'(x) = \cos x$.
3.  По теореме Лагранжа существует $c \in (a, b)$:

$$
    \sin b - \sin a = \cos c \cdot (b - a)
    $$
    
4.  Возьмем модуль от обеих частей:

$$
    |\sin b - \sin a| = |\cos c| \cdot |b - a|
    $$
    
5.  Так как $|\cos c| \le 1$ для любого $c$, то:

$$
    |\sin b - \sin a| \le 1 \cdot |b - a|
    $$

**Ответ:** Неравенство доказано.
*(Это означает, что синус меняется не быстрее, чем аргумент).*

---

### Задача 3. Проверка теоремы Коши
**Задание:** Проверить теорему Коши для функций $f(x) = x^2$ и $g(x) = x^3$ на отрезке $[1, 2]$.

**Решение:**
1.  **Проверка условий:**
    Обе функции — многочлены, непрерывны и дифференцируемы.
    $g'(x) = 3x^2$. На интервале $(1, 2)$ производная не равна нулю ($3x^2 > 3$).
2.  **Вычисление левой части формулы:**
    *   $f(2) - f(1) = 4 - 1 = 3$
    *   $g(2) - g(1) = 8 - 1 = 7$
    *   Отношение: $\frac{3}{7}$
3.  **Вычисление правой части (производные):**
    *   $f'(x) = 2x$
    *   $g'(x) = 3x^2$
    *   Отношение производных в точке $c$: $\frac{2c}{3c^2} = \frac{2}{3c}$
4.  **Поиск точки $c$:**
    Приравниваем левую и правую части:

$$
    \frac{3}{7} = \frac{2}{3c}
    $$

$$
    9c = 14 \quad \Rightarrow \quad c = \frac{14}{9} \approx 1.55
    $$
    
5.  **Проверка принадлежности:**

$1 < 1.55 < 2$. Точка внутри интервала.

**Ответ:** Теорема выполняется, $c = 14/9$.

---

### Задача 4. Применение для доказательства равенства
**Задание:** Доказать, что если $f'(x) = 0$ на интервале $(a, b)$, то $f(x) = C$ (const) на этом интервале.

**Решение:**
1.  Возьмем любые две точки $x_1, x_2 \in (a, b)$, где $x_1 < x_2$.
2.  Рассмотрим функцию на отрезке $[x_1, x_2]$. Условия теоремы Лагранжа выполнены.
3.  Существует $c \in (x_1, x_2)$ такая что:

$$
    f(x_2) - f(x_1) = f'(c) \cdot (x_2 - x_1)
    $$
    
4.  По условию $f'(x) = 0$ везде, значит $f'(c) = 0$.
5.  Следовательно:

$$
    f(x_2) - f(x_1) = 0 \cdot (x_2 - x_1) = 0 \quad \Rightarrow \quad f(x_2) = f(x_1)
    $$
    
6.  Так как значения в любых двух точках равны, функция постоянна.

**Ответ:** Функция постоянна на интервале.

---

### Задача 5. Геометрическая задача
**Задание:** На графике функции $f(x) = \sqrt{x}$ найти точку, в которой касательная параллельна хорде, соединяющей точки с абсциссами $x=1$ и $x=4$.

**Решение:**
1.  Это прямая задача на теорему Лагранжа.
2.  Угловой коэффициент хорды (секущей):

$$
    k_{\text{хорды}} = \frac{f(4) - f(1)}{4 - 1} = \frac{2 - 1}{3} = \frac{1}{3}
    $$
    
3.  Угловой коэффициент касательной равен производной:

$$
    f'(x) = (\sqrt{x})' = \frac{1}{2\sqrt{x}}
    $$
    
4.  Приравниваем производную к коэффициенту хорды:

$$
    \frac{1}{2\sqrt{c}} = \frac{1}{3}
    $$
    
5.  Решаем уравнение:

$$
    2\sqrt{c} = 3 \quad \Rightarrow \quad \sqrt{c} = 1.5 \quad \Rightarrow \quad c = 2.25
    $$
    
6.  Точка $c = 2.25$ лежит внутри $(1, 4)$.

**Ответ:** Точка касания имеет абсциссу $x = 2.25$.

---

# Резюме модуля

| Теорема | Формула | Условия | Геометрический смысл |
|:---:|:---:|:---:|:---:|
| **Лагранжа** | $f(b)-f(a) = f'(c)(b-a)$ | Непрерывность на $[a,b]$, дифференцируемость на $(a,b)$ | Касательная параллельна секущей |
| **Коши** | $\frac{f(b)-f(a)}{g(b)-g(a)} = \frac{f'(c)}{g'(c)}$ | То же + $g'(x) \neq 0$ | Отношение приращений функций равно отношению производных |

**Ключевые выводы:**
1.  **Теорема Лагранжа** позволяет оценить приращение функции через производную: $\Delta y \approx f'(c) \Delta x$.
2.  **Теорема Коши** является основой для вывода **правила Лопиталя** (раскрытие неопределенностей вида $0/0$).
3.  Обе теоремы являются **теоремами существования**. Они говорят, что точка $c$ *существует*, но не говорят, как её точно найти (хотя в задачах мы её ищем).

