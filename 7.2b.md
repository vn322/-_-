
Тема **Теорем Ферма и Ролля** — это фундамент дифференциального исчисления. Эти теоремы связывают поведение функции (значения, максимумы, минимумы) с её производной. Они являются ключом к доказательству более сложных теорем (Лагранжа, Коши) и правил (Лопиталя).

---

# 1. Теорема Ферма

### Интуитивное понимание
Представьте график функции как холмистую местность.
Если вы находитесь в самой высокой точке холма (локальный максимум) или в самой низкой точке ямы (локальный минимум), и поверхность гладкая (нет острых пиков), то касательная к земле в этой точке будет **горизонтальной**.
Горизонтальная касательная означает, что её угловой коэффициент (производная) равен нулю.

### Строгое определение
**Стационарная точка:** Точка $x_0$, в которой производная функции равна нулю: $f'(x_0) = 0$.

**Теорема Ферма:**
Пусть функция $f(x)$ определена в некоторой окрестности точки $x_0$ и принимает в этой точке **локальный экстремум** (максимум или минимум).
Если в точке $x_0$ существует конечная производная $f'(x_0)$, то она равна нулю:
$$
f'(x_0) = 0
$$

### Геометрический смысл
В точке локального экстремума касательная к графику функции параллельна оси абсцисс ($Ox$).

### Важное замечание
Обратное утверждение **неверно**. Если $f'(x_0) = 0$, это не гарантирует, что в точке $x_0$ есть экстремум.
*   **Пример:** $f(x) = x^3$. Производная $f'(x) = 3x^2$. В точке $x = 0$ производная равна 0, но экстремума нет (это точка перегиба, функция просто продолжает расти).
*   Такие точки называются **стационарными**, но они могут быть точками экстремума, а могут и не быть.

---

# 2. Теорема Ролля

### Интуитивное понимание
Представьте, что вы начали путешествие в точке $A$ и закончили в точке $B$, причем высота над уровнем моря в точке $A$ и в точке $B$ одинакова.
Если вы двигались плавно (без телепортации и острых изломов пути), то в какой-то момент между $A$ и $B$ вы должны были либо остановиться по вертикали (достичь пика или впадины), либо двигаться горизонтально.
То есть, найдется точка, где мгновенная скорость изменения высоты (производная) равна нулю.

### Строгое определение
Пусть функция $f(x)$ удовлетворяет трем условиям:
1.  **Непрерывна** на отрезке $[a, b]$.
2.  **Дифференцируема** на интервале $(a, b)$.
3.  Принимает **равные значения** на концах отрезка: $f(a) = f(b)$.

Тогда существует хотя бы одна точка $c \in (a, b)$, такая что:
$$
f'(c) = 0
$$

### Геометрический смысл
Если гладкая кривая начинается и заканчивается на одной высоте, то на ней найдется точка, где касательная горизонтальна.

### Связь с теоремой Ферма
Доказательство теоремы Ролля опирается на теорему Ферма.
*   Если функция постоянна ($f(x) = const$), то производная всюду 0.
*   Если функция не постоянна, то по теореме Вейерштрасса она достигает наибольшего или наименьшего значения внутри интервала (так как на концах значения равны). В этой точке внутреннего экстремума по теореме Ферма производная равна 0.

---

# 3. Типовые задачи и решения

### Задача 1. Применение теоремы Ферма
**Задание:** Найти стационарные точки функции $f(x) = x^3 - 3x + 2$ и выяснить, являются ли они точками экстремума.

**Решение:**
1.  **Найдем производную:**
    $$
    f'(x) = 3x^2 - 3
    $$
2.  **Приравняем к нулю (условие Ферма):**
    $$
    3x^2 - 3 = 0 \quad \Rightarrow \quad x^2 = 1 \quad \Rightarrow \quad x = \pm 1
    $$
    Стационарные точки: $x_1 = 1$, $x_2 = -1$.
3.  **Проверка на экстремум (через знак производной):**
    *   При $x < -1$ (например, -2): $f'(-2) = 3(4) - 3 = 9 > 0$ (функция растет).
    *   При $-1 < x < 1$ (например, 0): $f'(0) = -3 < 0$ (функция убывает).
    *   При $x > 1$ (например, 2): $f'(2) = 9 > 0$ (функция растет).
4.  **Вывод:**
    *   В точке $x = -1$ производная меняет знак с $+$ на $-$ $\Rightarrow$ **Локальный максимум**.
    *   В точке $x = 1$ производная меняет знак с $-$ на $+$ $\Rightarrow$ **Локальный минимум**.

**Ответ:** Стационарные точки $x = \pm 1$ являются точками экстремума.

---

### Задача 2. Проверка условий теоремы Ролля
**Задание:** Проверить справедливость теоремы Ролля для функции $f(x) = x^2 - 4x + 3$ на отрезке $[1, 3]$.

**Решение:**
1.  **Проверка условий:**
    *   Функция — многочлен, значит непрерывна на $[1, 3]$ и дифференцируема на $(1, 3)$.
    *   Проверим значения на концах:
        $$
        f(1) = 1 - 4 + 3 = 0
        $$
        $$
        f(3) = 9 - 12 + 3 = 0
        $$
        Условие $f(1) = f(3)$ выполняется.
2.  **Поиск точки $c$:**
    Найдем производную: $f'(x) = 2x - 4$.
    Приравняем к нулю: $2x - 4 = 0 \Rightarrow x = 2$.
3.  **Проверка принадлежности интервалу:**
    Точка $c = 2$ лежит внутри интервала $(1, 3)$.
4.  **Вывод:** Теорема Ролля справедлива, точка $c = 2$ существует.

**Ответ:** Теорема справедлива, $c = 2$.

---

### Задача 3. Нарушение условий (Контрпример)
**Задание:** Почему теорема Ролля не применима к функции $f(x) = |x|$ на отрезке $[-1, 1]$, хотя $f(-1) = f(1)$?

**Решение:**
1.  **Проверка условий:**
    *   Непрерывность: Функция $|x|$ непрерывна на $[-1, 1]$. (Условие выполнено).
    *   Равенство на концах: $f(-1) = 1$, $f(1) = 1$. (Условие выполнено).
    *   Дифференцируемость: В точке $x = 0$ функция имеет излом («уголок»).
        $$
        f'_-(0) = -1, \quad f'_+(0) = 1
        $$
        Производная в точке $x = 0$ не существует.
2.  **Нарушение:** Условие дифференцируемости на всем интервале $(-1, 1)$ нарушено.
3.  **Следствие:** Действительно, $f'(x) = 1$ при $x > 0$ и $f'(x) = -1$ при $x < 0$. Нет точки, где $f'(c) = 0$.

**Ответ:** Теорема не применима, так как функция не дифференцируема в точке $x = 0$.

---

### Задача 4. Использование теоремы Ролля для оценки корней
**Задание:** Доказать, что уравнение $x^3 + x - 1 = 0$ имеет не более одного действительного корня.

**Решение:**
1.  **Существование:** $f(x) = x^3 + x - 1$. $f(0) = -1$, $f(1) = 1$. По теореме Больцано-Коши корень есть (так как функция непрерывна и меняет знак).
2.  **Единственность (от противного):**
    Предположим, что есть два различных корня $x_1$ и $x_2$ ($x_1 < x_2$).
    Тогда $f(x_1) = 0$ и $f(x_2) = 0$.
3.  **Применение Ролля:**
    Функция непрерывна и дифференцируема everywhere. По теореме Ролля на отрезке $[x_1, x_2]$ должна существовать точка $c$, где $f'(c) = 0$.
4.  **Проверка производной:**
    $$
    f'(x) = 3x^2 + 1
    $$
    Так как $x^2 \ge 0$, то $f'(x) \ge 1 > 0$ для всех $x$.
    Производная никогда не равна нулю.
5.  **Противоречие:** Теорема Ролля гарантирует существование точки с нулевой производной, но мы показали, что таких точек нет.
6.  **Вывод:** Предположение о двух корнях неверно.

**Ответ:** Уравнение имеет ровно один действительный корень.

---

### Задача 5. Геометрическая интерпретация
**Задание:** Функция $f(x)$ дифференцируема на $\mathbb{R}$ и $f'(x) \neq 0$ для всех $x$. Может ли быть так, что $f(0) = f(1)$?

**Решение:**
1.  Предположим, что $f(0) = f(1)$.
2.  Тогда выполнены все условия теоремы Ролля на отрезке $[0, 1]$ (непрерывность и дифференцируемость есть, значения на концах равны).
3.  По теореме Ролля должна существовать точка $c \in (0, 1)$, где $f'(c) = 0$.
4.  Но по условию задачи $f'(x) \neq 0$ везде.
5.  Получили противоречие.

**Ответ:** Нет, не может. Если производная не обращается в ноль, функция не может принимать одинаковые значения в двух разных точках (она строго монотонна).

---

# Резюме модуля

| Теорема | Условия | Заключение | Геометрический смысл |
|:---:|:---:|:---:|:---:|
| **Ферма** | Локальный экстремум в $x_0$, существует $f'(x_0)$ | $f'(x_0) = 0$ | Касательная горизонтальна в вершине |
| **Ролль** | Непрерывность на $[a,b]$, дифференцируемость на $(a,b)$, $f(a)=f(b)$ | $\exists c \in (a,b): f'(c) = 0$ | Между двумя точками одной высоты есть горизонтальная касательная |

**Ключевые выводы:**
1.  **Теорема Ферма** дает необходимый признак экстремума: если есть экстремум и производная существует, то она равна 0.
2.  **Теорема Ролля** гарантирует существование стационарной точки внутри интервала, если функция начинается и заканчивается на одной высоте.
3.  **Нарушение условий** (разрыв, излом, разные значения на концах) может привести к тому, что вывод теоремы не выполнится.



