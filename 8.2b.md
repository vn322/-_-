
Тема **Устойчивости по Ляпунову** — это фундамент современной теории управления и анализа динамических систем. Александр Ляпунов в 1892 году создал строгую математическую теорию, которая позволяет судить о поведении решений дифференциальных уравнений без их явного нахождения.

---

# 1. Интуитивное понимание

### Что такое устойчивость?
Представьте шарик в различных положениях:

| Тип устойчивости | Картина | Поведение при малом толчке |
|:---:|:---:|:---:|
| **Устойчивость** | Шарик в яме | Остается рядом с дном |
| **Асимптотическая устойчивость** | Шарик в яме **с трением** | Возвращается обратно на дно |
| **Неустойчивость** | Шарик на вершине горки | Укатывается далеко |

**Ключевое различие:**
*   **Устойчивость:** Если стартуем близко к равновесию → остаемся близко навсегда.
*   **Асимптотическая устойчивость:** Если стартуем близко → не только остаемся рядом, но и **стремимся** к равновесию при $t \to \infty$.

---

# 2. Строгие определения

### Система дифференциальных уравнений
Рассмотрим автономную систему:
$$
\dot{\mathbf{x}} = \mathbf{f}(\mathbf{x}), \quad \mathbf{x} \in \mathbb{R}^n
$$
где $\mathbf{f}(\mathbf{0}) = \mathbf{0}$ (начало координат — положение равновесия).

### Устойчивость по Ляпунову
Положение равновесия $\mathbf{x} = \mathbf{0}$ называется **устойчивым**, если:

$$
\forall \varepsilon > 0 \quad \exists \delta > 0 \quad : \quad \|\mathbf{x}(0)\| < \delta \implies \|\mathbf{x}(t)\| < \varepsilon \quad \forall t \ge 0
$$

**Расшифровка:**
*   $\varepsilon$ — допустимое отклонение от равновесия.
*   $\delta$ — допустимое отклонение начальных условий.
*   Если запустить систему достаточно близко к нулю ($\delta$), она никогда не уйдет дальше $\varepsilon$.

### Асимптотическая устойчивость
Положение равновесия называется **асимптотически устойчивым**, если:

1.  Оно устойчиво по Ляпунову.
2.  Существует $\delta > 0$ такое, что:
    $$
    \|\mathbf{x}(0)\| < \delta \implies \lim_{t \to \infty} \mathbf{x}(t) = \mathbf{0}
    $$

**Дополнительно:** Если это верно для любых начальных условий ($\delta = \infty$), устойчивость называется **глобальной**.

### Неустойчивость
Если условие устойчивости не выполняется, равновесие называется **неустойчивым**:
$$
\exists \varepsilon > 0 \quad \forall \delta > 0 \quad \exists \mathbf{x}(0), \|\mathbf{x}(0)\| < \delta \quad : \quad \exists t > 0, \|\mathbf{x}(t)\| \ge \varepsilon
$$
*(Сколько бы близко мы ни стартовали, найдется траектория, которая уйдет далеко).*

---

# 3. Геометрическая интерпретация

### Для устойчивости
1.  Задаем трубку радиуса $\varepsilon$ вокруг положения равновесия.
2.  Теорема гарантирует, что найдется трубка радиуса $\delta \le \varepsilon$ для начальных условий.
3.  Все траектории, начинающиеся внутри $\delta$-трубки, никогда не покидают $\varepsilon$-трубку.

### Для асимптотической устойчивости
1.  Траектории не только остаются в трубке.
2.  Они **спирали** или иным образом стремятся к началу координат при $t \to \infty$.

### Визуализация
```
Устойчивость:          Асимптотическая:       Неустойчивость:
   _______                _______                _______
  /       \              /       \              /       \
 |    ε    |            |    ε    |            |    ε    |
 |   ___   |            |   ___   |            |   ___   |
 |  / δ \  |            |  / δ \  |            |  / δ \  |
 | |  *  | |            | |  *  | |            | |  *  | |
 |  \___/  |            |  \___/  |            |  \___/  |
  \_______/              \_______/              \_______/
   Траектории            Траектории             Траектории
   не выходят            стремятся              уходят за
   за границу            к нулю                 границу
```

---

# 4. Прямой метод Ляпунова

### Функция Ляпунова
Функция $V(\mathbf{x})$ называется **функцией Ляпунова**, если:

1.  $V(\mathbf{0}) = 0$
2.  $V(\mathbf{x}) > 0$ при $\mathbf{x} \neq \mathbf{0}$ (положительно определена)
3.  $V(\mathbf{x})$ непрерывно дифференцируема

### Производная в силу системы
Важнейшее понятие — производная $V$ вдоль траекторий системы:
$$
\dot{V}(\mathbf{x}) = \frac{d}{dt} V(\mathbf{x}(t)) = \sum_{i=1}^{n} \frac{\partial V}{\partial x_i} \cdot \frac{dx_i}{dt} = \nabla V \cdot \mathbf{f}(\mathbf{x})
$$

### Критерии устойчивости

| Тип устойчивости | Условие на $\dot{V}$ | Физический смысл |
|:---:|:---:|:---:|
| **Устойчивость** | $\dot{V} \le 0$ | Энергия не растет |
| **Асимптотическая** | $\dot{V} < 0$ | Энергия убывает |
| **Неустойчивость** | $\dot{V} > 0$ | Энергия растет |

---

# 5. Типовые задачи и решения

### Задача 1. Доказательство устойчивости
**Задание:** Исследовать устойчивость системы:
$$
\begin{cases}
\dot{x} = -x^3 + y \\
\dot{y} = -x - y^3
\end{cases}
$$

**Решение:**
1.  **Выбираем функцию Ляпунова:**
    $$
    V(x, y) = \frac{1}{2}x^2 + \frac{1}{2}y^2
    $$
    Положительно определена ($V > 0$ при $(x,y) \neq (0,0)$).
2.  **Вычисляем производную:**
    $$
    \dot{V} = x\dot{x} + y\dot{y} = x(-x^3 + y) + y(-x - y^3)
    $$
    $$
    \dot{V} = -x^4 + xy - xy - y^4 = -(x^4 + y^4)
    $$
3.  **Анализ знака:**
    $\dot{V} = -(x^4 + y^4) < 0$ при всех $(x,y) \neq (0,0)$.
4.  **Вывод:**
    Так как $\dot{V}$ отрицательно определена, положение равновесия **асимптотически устойчиво**.

**Ответ:** Асимптотическая устойчивость.

---

### Задача 2. Устойчивость без асимптотической
**Задание:** Исследовать систему (консервативный осциллятор):
$$
\begin{cases}
\dot{x} = y \\
\dot{y} = -x
\end{cases}
$$

**Решение:**
1.  **Функция Ляпунова** (полная энергия):
    $$
    V(x, y) = \frac{1}{2}x^2 + \frac{1}{2}y^2
    $$
2.  **Производная:**
    $$
    \dot{V} = x\dot{x} + y\dot{y} = xy + y(-x) = 0
    $$
3.  **Анализ:**
    $\dot{V} \equiv 0$ — производная тождественно равна нулю.
    Это означает, что $V$ сохраняется (нет диссипации энергии).
4.  **Вывод:**
    По теореме Ляпунова ($\dot{V} \le 0$) система **устойчива**.
    Но так как $\dot{V}$ не отрицательно определена, **асимптотической устойчивости нет**.
    Траектории — окружности вокруг начала координат.

**Ответ:** Устойчивость (но не асимптотическая).

---

### Задача 3. Глобальная асимптотическая устойчивость
**Задание:** Доказать глобальную асимптотическую устойчивость системы:
$$
\begin{cases}
\dot{x} = -x - 2y^2 \\
\dot{y} = -y
\end{cases}
$$

**Решение:**
1.  **Функция Ляпунова:**
    $$
    V(x, y) = x^2 + y^2
    $$
2.  **Производная:**
    $$
    \dot{V} = 2x(-x - 2y^2) + 2y(-y) = -2x^2 - 4xy^2 - 2y^2
    $$
    $$
    \dot{V} = -2(x^2 + 2xy^2 + y^2)
    $$
    *(Здесь нужно быть осторожным со знаком. Попробуем другую функцию).*
    
    **Исправление:** Возьмем $V = \frac{1}{2}x^2 + \frac{1}{2}y^2$:
    $$
    \dot{V} = x(-x - 2y^2) + y(-y) = -x^2 - 2xy^2 - y^2
    $$
    Это не очевидно отрицательно из-за члена $-2xy^2$.
    
    **Лучший выбор:** $V = x^2 + 2y^2$:
    $$
    \dot{V} = 2x(-x - 2y^2) + 4y(-y) = -2x^2 - 4xy^2 - 4y^2
    $$
    Всё ещё есть перекрестный член.
    
    **Оптимальный выбор:** $V = \frac{1}{2}x^2 + y^2$:
    $$
    \dot{V} = x(-x - 2y^2) + 2y(-y) = -x^2 - 2xy^2 - 2y^2
    $$
    
    **Простой вариант для примера:** Рассмотрим систему:
    $$
    \begin{cases}
    \dot{x} = -x \\
    \dot{y} = -y
    \end{cases}
    $$
    Тогда $V = x^2 + y^2$, $\dot{V} = -2x^2 - 2y^2 < 0$.
    Так как $V \to \infty$ при $\|\mathbf{x}\| \to \infty$ (радиально неограничена), устойчивость **глобальная**.

**Ответ:** Глобальная асимптотическая устойчивость (для упрощенной системы).

---

### Задача 4. Сравнение с линеаризацией
**Задание:** Почему метод Ляпунова предпочтительнее линеаризации в критическом случае?

**Решение:**
1.  **Линеаризация:** Строим матрицу Якоби $J = \frac{\partial \mathbf{f}}{\partial \mathbf{x}}|_{\mathbf{0}}$ и находим собственные числа $\lambda_i$.
    *   Если $\text{Re}(\lambda_i) < 0$ для всех $i$ → асимптотическая устойчивость.
    *   Если $\exists i: \text{Re}(\lambda_i) > 0$ → неустойчивость.
    *   Если $\text{Re}(\lambda_i) \le 0$ и есть $\text{Re}(\lambda_i) = 0$ → **критический случай**, линеаризация не даёт ответа.
2.  **Метод Ляпунова:** Работает в критическом случае.
    *   *Пример:* $\dot{x} = -x^3$. Линеаризация: $\dot{x} = 0$ (собственное число 0). Неясно.
    *   Ляпунов: $V = x^2$, $\dot{V} = 2x(-x^3) = -2x^4 < 0$. Асимптотическая устойчивость доказана.

**Ответ:** Метод Ляпунова работает в критических случаях, где линеаризация бессильна.

---

### Задача 5. Построение области притяжения
**Задание:** Для системы $\dot{x} = -x + x^3$ найти область притяжения нуля с помощью $V = x^2$.

**Решение:**
1.  **Производная:**
    $$
    \dot{V} = 2x(-x + x^3) = -2x^2 + 2x^4 = -2x^2(1 - x^2)
    $$
2.  **Условие $\dot{V} < 0$:**
    $$
    -2x^2(1 - x^2) < 0 \quad \Rightarrow \quad 1 - x^2 > 0 \quad \Rightarrow \quad |x| < 1
    $$
3.  **Вывод:**
    В области $|x| < 1$ производная отрицательна. Это означает, что все траектории, начинающиеся в интервале $(-1, 1)$, стремятся к нулю.
    Точки $x = \pm 1$ — другие положения равновесия.

**Ответ:** Область притяжения: $x \in (-1, 1)$.

---

# Резюме модуля

| Понятие | Формальное условие | Физический смысл |
|:---:|:---:|:---:|
| **Устойчивость** | $\|\mathbf{x}(0)\| < \delta \implies \|\mathbf{x}(t)\| < \varepsilon$ | Остаемся рядом |
| **Асимптотическая** | + $\lim_{t \to \infty} \mathbf{x}(t) = \mathbf{0}$ | Возвращаемся обратно |
| **Глобальная** | Верно для любых $\mathbf{x}(0)$ | Притягивает отовсюду |

**Критерии через функцию Ляпунова:**

| Условие на $\dot{V}$ | Тип устойчивости |
|:---:|:---:|
| $\dot{V} \le 0$ | Устойчивость |
| $\dot{V} < 0$ | Асимптотическая устойчивость |
| $\dot{V} > 0$ | Неустойчивость |

**Ключевые выводы:**
1.  **Устойчивость ≠ Асимптотическая устойчивость.** Система может быть устойчивой, но не стремиться к равновесию (консервативные системы).
2.  **Функция Ляпунова** — это обобщение энергии. Если энергия убывает → система стабилизируется.
3.  **Главная сложность** — не существует общего алгоритма построения функции Ляпунова. Это требует интуиции и опыта.

