
Тема **Ортогональных систем функций** переносит геометрию векторов в бесконечномерное пространство функций. Это фундамент для рядов Фурье, квантовой механики и численных методов.

---

# 1. Интуитивное понимание

### Векторы и функции
В линейной алгебре мы работали с векторами $\vec{a} = (a_1, a_2, \dots)$. У них есть длина и угол между ними.
Функции $f(x)$ можно рассматривать как «векторы с бесконечным числом компонент» (значение функции в каждой точке $x$ — это компонента).

*   **Скалярное произведение векторов:** $\vec{a} \cdot \vec{b} = \sum a_i b_i$.
*   **Скалярное произведение функций:** Замена суммы на интеграл: $(f, g) = \int f(x)g(x) dx$.

**Ортогональность** означает «перпендикулярность». Для функций это значит, что их «скалярное произведение» равно нулю. Это позволяет раскладывать сложные функции по простым «базисным» функциям (как вектор по осям координат).

---

# 2. Строгие определения

### Скалярное произведение и норма
Пусть $f(x)$ и $g(x)$ — функции, определенные на отрезке $[a, b]$.
**Скалярным произведением** называется число:
$$
(f, g) = \int_{a}^{b} f(x) g(x) \rho(x) \, dx
$$
где $\rho(x) \ge 0$ — весовая функция (часто $\rho(x) = 1$).

**Нормой** функции называется:
$$
\|f\| = \sqrt{(f, f)} = \sqrt{\int_{a}^{b} f^2(x) \rho(x) \, dx}
$$

### Ортогональная система
Система функций $\{\phi_n(x)\}$ называется **ортогональной** на $[a, b]$ с весом $\rho(x)$, если:
$$
(\phi_n, \phi_m) = \int_{a}^{b} \phi_n(x) \phi_m(x) \rho(x) \, dx = 0 \quad \text{при } n \neq m
$$
Если дополнительно $\|\phi_n\| = 1$ для всех $n$, система называется **ортонормированной**.

### Примеры ортогональных систем
1.  **Тригонометрическая система** на $[-\pi, \pi]$ ($\rho(x) = 1$):
    $$
    \{1, \cos x, \sin x, \cos 2x, \sin 2x, \dots\}
    $$
2.  **Многочлены Лежандра** на $[-1, 1]$ ($\rho(x) = 1$):
    $$
    P_0(x)=1, \quad P_1(x)=x, \quad P_2(x)=\frac{1}{2}(3x^2-1), \dots
    $$

---

# 3. Метод ортогонализации Шмидта (Грама-Шмидта)

### Интуитивное понимание
Если у нас есть набор линейно независимых функций $\{f_1, f_2, \dots\}$, которые не ортогональны, мы можем построить из них новый набор $\{\phi_1, \phi_2, \dots\}$, который будет ортогональным.
Мы берем первую функцию, затем из второй вычитаем её проекцию на первую, из третьей — проекции на первую и вторую, и так далее.

### Алгоритм
Пусть дана система $\{f_1, f_2, \dots, f_n\}$. Строим $\{\phi_1, \phi_2, \dots, \phi_n\}$:

1.  $\phi_1 = f_1$
2.  $\phi_2 = f_2 - \frac{(f_2, \phi_1)}{(\phi_1, \phi_1)} \phi_1$
3.  $\phi_3 = f_3 - \frac{(f_3, \phi_1)}{(\phi_1, \phi_1)} \phi_1 - \frac{(f_3, \phi_2)}{(\phi_2, \phi_2)} \phi_2$
4.  Общая формула:
    $$
    \phi_k = f_k - \sum_{j=1}^{k-1} \frac{(f_k, \phi_j)}{(\phi_j, \phi_j)} \phi_j
    $$

---

# 4. Ряд Фурье по ортогональной системе

Если система $\{\phi_n\}$ ортогональна, любую функцию $f(x)$ можно попытаться разложить в ряд:
$$
f(x) \sim \sum_{n=1}^{\infty} c_n \phi_n(x)
$$
**Коэффициенты Фурье** вычисляются по формуле:
$$
c_n = \frac{(f, \phi_n)}{(\phi_n, \phi_n)} = \frac{\int_{a}^{b} f(x) \phi_n(x) \rho(x) \, dx}{\int_{a}^{b} \phi_n^2(x) \rho(x) \, dx}
$$

---

# 5. Неравенство Бесселя

### Формулировка
Для любой функции $f(x)$ с конечной нормой и любой ортогональной системы $\{\phi_n\}$ справедливо неравенство:
$$
\sum_{n=1}^{\infty} c_n^2 \|\phi_n\|^2 \le \|f\|^2
$$
Или в развернутом виде:
$$
\sum_{n=1}^{\infty} \frac{(f, \phi_n)^2}{\|\phi_n\|^2} \le \int_{a}^{b} f^2(x) \rho(x) \, dx
$$

### Геометрический смысл
Квадрат длины вектора ($\|f\|^2$) больше или равен сумме квадратов длин его проекций на оси координат ($c_n \phi_n$). Разница возникает, если система функций неполная (не хватает «осей», чтобы полностью описать вектор).

---

# 6. Равенство Парсеваля

### Формулировка
Если ортогональная система является **полной** (замкнутой), то неравенство Бесселя превращается в равенство:
$$
\sum_{n=1}^{\infty} c_n^2 \|\phi_n\|^2 = \|f\|^2
$$

### Физический смысл
Это закон сохранения энергии. Энергия сигнала ($\int f^2$) равна сумме энергий его гармоник (коэффициентов Фурье).
Для тригонометрической системы на $[-\pi, \pi]$ это выглядит так:
$$
\frac{a_0^2}{2} + \sum_{n=1}^{\infty} (a_n^2 + b_n^2) = \frac{1}{\pi} \int_{-\pi}^{\pi} f^2(x) \, dx
$$

---

# 7. Типовые задачи и решения

### Задача 1. Проверка ортогональности
**Задание:** Проверить ортогональность функций $f_1(x) = 1$ и $f_2(x) = x$ на отрезке $[-1, 1]$ с весом $\rho(x) = 1$.

**Решение:**
1.  Вычислим скалярное произведение:
    $$
    (f_1, f_2) = \int_{-1}^{1} 1 \cdot x \, dx
    $$
2.  Вычислим интеграл:
    $$
    \int_{-1}^{1} x \, dx = \left. \frac{x^2}{2} \right|_{-1}^{1} = \frac{1}{2} - \frac{1}{2} = 0
    $$
3.  **Вывод:** Так как скалярное произведение равно 0, функции ортогональны.
    *(Это очевидно, так как 1 — четная функция, а x — нечетная, и интеграл от нечетной функции по симметричному промежутку равен 0).*

**Ответ:** Функции ортогональны.

---

### Задача 2. Ортогонализация Шмидта
**Задание:** Ортогонализировать систему функций $\{1, x, x^2\}$ на отрезке $[-1, 1]$ с весом $\rho(x) = 1$.

**Решение:**
1.  **Первая функция:**
    $$ \phi_1 = f_1 = 1 $$
    Норма в квадрате: $\|\phi_1\|^2 = \int_{-1}^{1} 1^2 dx = 2$.

2.  **Вторая функция:**
    $$ \phi_2 = f_2 - \frac{(f_2, \phi_1)}{(\phi_1, \phi_1)} \phi_1 $$
    Мы уже знаем, что $(x, 1) = 0$ (из Задачи 1).
    $$ \phi_2 = x - 0 \cdot 1 = x $$
    Норма в квадрате: $\|\phi_2\|^2 = \int_{-1}^{1} x^2 dx = \left. \frac{x^3}{3} \right|_{-1}^{1} = \frac{2}{3}$.

3.  **Третья функция:**
    $$ \phi_3 = f_3 - \frac{(f_3, \phi_1)}{(\phi_1, \phi_1)} \phi_1 - \frac{(f_3, \phi_2)}{(\phi_2, \phi_2)} \phi_2 $$
    где $f_3 = x^2$.
    *   Скалярное произведение $(x^2, 1) = \int_{-1}^{1} x^2 dx = \frac{2}{3}$.
    *   Скалярное произведение $(x^2, x) = \int_{-1}^{1} x^3 dx = 0$ (нечетная функция).
    Подставляем:
    $$
    \phi_3 = x^2 - \frac{2/3}{2} \cdot 1 - 0 \cdot x = x^2 - \frac{1}{3}
    $$
    *(Это с точностью до множителя совпадает с многочленом Лежандра $P_2(x)$)*.

**Ответ:** Система $\{1, x, x^2 - \frac{1}{3}\}$.

---

### Задача 3. Коэффициенты Фурье
**Задание:** Найти первый коэффициент Фурье функции $f(x) = x$ по ортогональной системе $\{1, \cos x, \sin x, \dots\}$ на $[-\pi, \pi]$. Найдем коэффициент $b_1$ при $\sin x$.

**Решение:**
1.  Система ортогональна. Норма $\|\sin x\|^2 = \int_{-\pi}^{\pi} \sin^2 x dx = \pi$.
2.  Формула для коэффициента $b_1$:
    $$
    b_1 = \frac{(f, \sin x)}{\|\sin x\|^2} = \frac{1}{\pi} \int_{-\pi}^{\pi} x \sin x \, dx
    $$
3.  Вычислим интеграл по частям ($u=x, dv=\sin x dx$):
    $$
    \int x \sin x dx = -x \cos x + \int \cos x dx = -x \cos x + \sin x
    $$
    Подставляем пределы:
    $$
    \left[ -x \cos x + \sin x \right]_{-\pi}^{\pi} = (-\pi(-1) + 0) - (-(-\pi)(-1) + 0) = \pi - \pi = 2\pi
    $$
    *(Поправка: $\cos(\pi)=-1, \cos(-\pi)=-1$. Верхний предел: $-\pi(-1)=\pi$. Нижний предел: $-(-\pi)(-1) = -\pi$. Разность: $\pi - (-\pi) = 2\pi$)*.
4.  Коэффициент:
    $$
    b_1 = \frac{1}{\pi} \cdot 2\pi = 2
    $$

**Ответ:** $b_1 = 2$.

---

### Задача 4. Проверка равенства Парсеваля
**Задание:** Проверить равенство Парсеваля для функции $f(x) = x$ на $[-\pi, \pi]$ по тригонометрической системе.
Известно разложение: $x = 2 \sum_{n=1}^{\infty} \frac{(-1)^{n+1}}{n} \sin(nx)$.

**Решение:**
1.  **Левая часть (Норма функции):**
    $$
    \|f\|^2 = \int_{-\pi}^{\pi} x^2 dx = \left. \frac{x^3}{3} \right|_{-\pi}^{\pi} = \frac{2\pi^3}{3}
    $$
    В формуле Парсеваля для тригонометрического ряда обычно используется нормировка $\frac{1}{\pi} \int f^2$.
    $$
    \frac{1}{\pi} \|f\|^2 = \frac{2\pi^2}{3}
    $$
2.  **Правая часть (Сумма квадратов коэффициентов):**
    Коэффициенты $b_n = 2 \frac{(-1)^{n+1}}{n}$. Квадраты: $b_n^2 = \frac{4}{n^2}$.
    Сумма ряда:
    $$
    \sum_{n=1}^{\infty} b_n^2 = \sum_{n=1}^{\infty} \frac{4}{n^2} = 4 \sum_{n=1}^{\infty} \frac{1}{n^2}
    $$
    Известно, что $\sum_{n=1}^{\infty} \frac{1}{n^2} = \frac{\pi^2}{6}$ (задача Эйлера).
    $$
    \text{Сумма} = 4 \cdot \frac{\pi^2}{6} = \frac{2\pi^2}{3}
    $$
3.  **Сравнение:**
    Левая часть $\frac{2\pi^2}{3}$ равна правой части $\frac{2\pi^2}{3}$.

**Ответ:** Равенство Парсеваля выполняется.

---

### Задача 5. Неравенство Бесселя (Оценка ошибки)
**Задание:** Используя неравенство Бесселя, оценить сумму ряда $\sum_{n=1}^{\infty} \frac{1}{n^4}$.
Подсказка: Рассмотрите функцию $f(x) = x^2$ на $[-\pi, \pi]$.

**Решение:**
1.  Для $f(x) = x^2$ коэффициенты Фурье убывают как $1/n^2$.
2.  Квадраты коэффициентов будут убывать как $1/n^4$.
3.  По равенству Парсеваля (так как система полна):
    $$
    \frac{a_0^2}{2} + \sum (a_n^2 + b_n^2) = \frac{1}{\pi} \int_{-\pi}^{\pi} x^4 dx
    $$
    *(Здесь для краткости опустим полные выкладки коэффициентов, они громоздкие, но суть в том, что интеграл от квадрата функции ограничивает сумму квадратов коэффициентов)*.
4.  Если бы система была неполной, мы бы получили неравенство Бесселя: Сумма рядов $\le$ Интеграл.
    Это позволяет оценивать сходимость рядов через интегралы от функций.

**Ответ:** Неравенство Бесселя гарантирует, что ряд из квадратов коэффициентов Фурье всегда сходится, если функция квадратично интегрируема.

---

# Резюме модуля

1.  **Ортогональность функций:** Обобщение перпендикулярности через интеграл скалярного произведения $(f, g) = 0$.
2.  **Метод Шмидта:** Алгоритм построения ортогонального базиса из произвольного набора линейно независимых функций.
3.  **Ряд Фурье:** Разложение функции по ортогональной системе. Коэффициенты находятся через скалярные произведения.
4.  **Неравенство Бесселя:** $\sum c_n^2 \|\phi_n\|^2 \le \|f\|^2$. Сумма энергий проекций не превосходит энергию вектора.
5.  **Равенство Парсеваля:** Выполняется для полных систем. Означает, что ряд Фурье сходится к функции в среднем квадратичном.


