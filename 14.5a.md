**Принцип сжимающего отображения** (теорема Банаха о неподвижной точке) и **метод Ньютона** — это два фундаментальных инструмента вычислительной математики. Первый даёт общие условия существования и единственности решения, а также гарантирует сходимость простейших итерационных методов. Второй — самый известный метод для быстрого нахождения корней, использующий производные.

---

## 1. Принцип сжимающего отображения

### 1.1 Определения

Пусть \((X, \rho)\) — полное метрическое пространство (например, \(\mathbb{R}^n\) с евклидовой метрикой).

**Определение.** Отображение \(\Phi: X \to X\) называется **сжимающим**, если существует число \(q \in [0, 1)\) такое, что для любых \(x, y \in X\):
\[
\rho(\Phi(x), \Phi(y)) \le q \cdot \rho(x, y)
\]

Число \(q\) называется коэффициентом сжатия.

### 1.2 Теорема Банаха о неподвижной точке

**Теорема (Принцип сжимающего отображения).** Если \(\Phi\) — сжимающее отображение на полном метрическом пространстве \(X\), то:
1.  Существует **единственная** неподвижная точка \(x^*\) такая, что \(\Phi(x^*) = x^*\).
2.  Для любого начального приближения \(x^{(0)} \in X\) итерационный процесс
    \[
    x^{(k+1)} = \Phi(x^{(k)})
    \]
    сходится к \(x^*\).
3.  Справедлива оценка скорости сходимости:
    \[
    \rho(x^{(k)}, x^*) \le \frac{q^k}{1-q} \rho(x^{(1)}, x^{(0)})
    \]

### 1.3 Применение к решению уравнений

Для решения уравнения \(F(x) = 0\) его нужно преобразовать к эквивалентному виду \(x = \Phi(x)\). Например:
*   \(x = x - \tau F(x)\) (метод простой итерации с параметром \(\tau\))
*   \(x = x - A^{-1}F(x)\) (если есть подходящая матрица \(A\))

**Условие сходимости:** В окрестности корня должно выполняться \(\|\Phi'(x^*)\| < 1\).

### 1.4 Пример

Решим уравнение \(x = \cos x\). Возьмём \(\Phi(x) = \cos x\).
*   \(|\Phi'(x)| = |\sin x| \le 1\), но в точке \(x^* \approx 0.74\) имеем \(|\sin x^*| \approx 0.67 < 1\).
*   Итерации \(x_{n+1} = \cos x_n\) сходятся к \(x^* \approx 0.739085\).

---

## 2. Метод Ньютона

### 2.1 Одномерный случай

Для уравнения \(f(x) = 0\) метод Ньютона (метод касательных) задаётся формулой:
\[
x_{n+1} = x_n - \frac{f(x_n)}{f'(x_n)}
\]

**Геометрический смысл:** Проводим касательную к графику \(f\) в точке \((x_n, f(x_n))\) и берём её пересечение с осью \(x\).

### 2.2 Многомерный случай

Для системы \(F(x) = 0\), где \(F: \mathbb{R}^n \to \mathbb{R}^n\), метод Ньютона:
\[
x^{(k+1)} = x^{(k)} - [J(x^{(k)})]^{-1} F(x^{(k)})
\]
где \(J(x)\) — матрица Якоби.

На практике решают линейную систему относительно поправки \(\Delta x^{(k)}\):
\[
J(x^{(k)}) \Delta x^{(k)} = -F(x^{(k)})
\]
\[
x^{(k+1)} = x^{(k)} + \Delta x^{(k)}
\]

### 2.3 Сходимость метода Ньютона

**Теорема (о локальной сходимости).** Пусть \(F(x^*) = 0\), матрица Якоби \(J(x^*)\) невырождена, и \(J(x)\) непрерывна по Липшицу в окрестности \(x^*\). Тогда существует окрестность корня, такая что при любом начальном приближении из этой окрестности метод Ньютона сходится, причём:
*   **Квадратичная сходимость:** \(\|x^{(k+1)} - x^*\| \le C \|x^{(k)} - x^*\|^2\).

### 2.4 Связь с принципом сжимающего отображения

Метод Ньютона можно рассматривать как метод простой итерации с отображением:
\[
\Phi(x) = x - J(x)^{-1} F(x)
\]

В окрестности корня \(\Phi'(x^*) = 0\), поэтому отображение является сжимающим с коэффициентом, стремящимся к нулю при приближении к корню. Это объясняет быструю сходимость.

### 2.5 Пример

Решим \(f(x) = x^2 - 2 = 0\) (корень \(\sqrt{2} \approx 1.41421356\)):
\[
x_{n+1} = x_n - \frac{x_n^2 - 2}{2x_n} = \frac{x_n}{2} + \frac{1}{x_n}
\]

| \(n\) | \(x_n\) | Ошибка |
|:---:|:---:|:---:|
| 0 | 1.5 | \(8.6 \times 10^{-2}\) |
| 1 | 1.4166667 | \(2.5 \times 10^{-3}\) |
| 2 | 1.4142157 | \(2.1 \times 10^{-6}\) |
| 3 | 1.4142136 | \(< 10^{-12}\) |

Видно, как быстро растёт число верных знаков.

---

## 3. Сравнение методов

| Характеристика | Принцип сжимающего отображения | Метод Ньютона |
|:---|:---|:---|
| **Тип** | Общая теория | Конкретный алгоритм |
| **Порядок сходимости** | Линейный (если \(\Phi'(x^*) \neq 0\)) | Квадратичный (вблизи корня) |
| **Требования** | Сжимаемость отображения | Вычисление производных (Якобиана) |
| **Область сходимости** | Глобальная (если отображение сжимающее на всём пространстве) | Локальная |
| **Сложность итерации** | Одно вычисление \(\Phi\) | Решение линейной системы + вычисление Якобиана |

---

## 4. Глобализация метода Ньютона

Метод Ньютона сходится только вблизи корня. Для расширения области сходимости используют:

1.  **Метод Ньютона с демпфированием:**
    \[
    x^{(k+1)} = x^{(k)} - \lambda_k [J(x^{(k)})]^{-1} F(x^{(k)})
    \]
    где \(\lambda_k\) выбирается так, чтобы уменьшалась норма невязки или некоторая целевая функция.

2.  **Метод продолжения по параметру:** Решают задачу \(H(x, t) = 0\), где \(H(x, 0) = 0\) легко решается, а \(H(x, 1) = F(x)\).

3.  **Комбинация с методами оптимизации:** Используют методы спуска вдали от корня и переключаются на метод Ньютона вблизи.

#### Резюме

| Метод | Формула | Сходимость |
|:---|:---|:---|
| **Простая итерация** | \(x_{n+1} = \Phi(x_n)\) | Линейная, если \(\|\Phi'\| < 1\) |
| **Ньютона (1D)** | \(x_{n+1} = x_n - f(x_n)/f'(x_n)\) | Квадратичная |
| **Ньютона (nD)** | \(x^{(k+1)} = x^{(k)} - J^{-1}F\) | Квадратичная |
