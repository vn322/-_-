Мы переходим к важнейшему разделу вычислительной математики — **интерполяции**. Задача интерполяции возникает, когда функция известна только в отдельных точках (узлах), а нужно восстановить её значения в промежуточных точках. Методы Ньютона и Лагранжа — два классических подхода к построению интерполяционного многочлена.

---

### 1. Постановка задачи интерполяции

Пусть известны значения некоторой функции $\(f(x)\)$ в точках $\(x_0, x_1, \dots, x_n\)$:

$$\[
y_i = f(x_i), \quad i = 0, 1, \dots, n
\]$$

Точки $\(x_i\)$ называются **узлами интерполяции**.

Требуется построить многочлен $\(P_n(x)\)$ степени не выше $\(n\)$, такой что:

$$\[
P_n(x_i) = y_i \quad \text{для всех } i = 0, 1, \dots, n
\]$$

То есть многочлен совпадает с функцией в узлах.

**Существование и единственность:** Такой многочлен существует и единствен (при условии $\(x_i \neq x_j\) для \(i \neq j\)$ ). Методы Лагранжа и Ньютона дают разные формы записи одного и того же многочлена.

---

### 2. Интерполяционный многочлен Лагранжа

#### 2.1 Идея метода

Идея Лагранжа — построить многочлен как линейную комбинацию значений функции в узлах с весовыми коэффициентами, которые равны 1 в "своём" узле и 0 во всех остальных.

#### 2.2 Формула

$$\[
\boxed{L_n(x) = \sum_{i=0}^n y_i \cdot \ell_i(x)}
\]$$

где $\(\ell_i(x)\)$ — **фундаментальные многочлены Лагранжа**:

$$\[
\ell_i(x) = \prod_{\substack{j=0 \\ j \neq i}}^n \frac{x - x_j}{x_i - x_j} = \frac{(x - x_0)(x - x_1)\cdots (x - x_{i-1})(x - x_{i+1})\cdots (x - x_n)}{(x_i - x_0)(x_i - x_1)\cdots (x_i - x_{i-1})(x_i - x_{i+1})\cdots (x_i - x_n)}
\]$$

**Свойства $\(\ell_i(x)\)$:**

$\(\ell_i(x_i) = 1\)$

$\(\ell_i(x_j) = 0\) при \(j \neq i\)$

$Степень \(\ell_i(x)\) равна \(n\)$.

#### 2.3 Пример

Для $\(n=1\)$ (две точки $\(x_0, x_1\))$:

$$\[
L_1(x) = y_0 \frac{x - x_1}{x_0 - x_1} + y_1 \frac{x - x_0}{x_1 - x_0}
\]$$

Это просто уравнение прямой, проходящей через две точки.

**Достоинства:** Формула простая и наглядная, легко программируется.
**Недостатки:** При добавлении нового узла все вычисления нужно проводить заново.

---

### 3. Интерполяционный многочлен Ньютона

Метод Ньютона использует **разделённые разности** и удобен тем, что при добавлении новых узлов не требует пересчёта уже найденных коэффициентов.

#### 3.1 Разделённые разности

**Определение.** Разделённые разности нулевого порядка: $\(f[x_i] = y_i\)$.

Разделённые разности первого порядка:

$$\[
f[x_i, x_{i+1}] = \frac{f[x_{i+1}] - f[x_i]}{x_{i+1} - x_i}
\]$$

Разделённые разности второго порядка:

$$\[
f[x_i, x_{i+1}, x_{i+2}] = \frac{f[x_{i+1}, x_{i+2}] - f[x_i, x_{i+1}]}{x_{i+2} - x_i}
\]$$

И так далее. Рекуррентная формула:

$$\[
f[x_i, \dots, x_{i+k}] = \frac{f[x_{i+1}, \dots, x_{i+k}] - f[x_i, \dots, x_{i+k-1}]}{x_{i+k} - x_i}
\]$$

#### 3.2 Интерполяционная формула Ньютона для неравноотстоящих узлов

Многочлен Ньютона строится как:

$$\[
\boxed{N_n(x) = f[x_0] + f[x_0, x_1](x - x_0) + f[x_0, x_1, x_2](x - x_0)(x - x_1) + \dots + f[x_0, \dots, x_n](x - x_0)\cdots(x - x_{n-1})}
\]$$

#### 3.3 Формулы Ньютона для равноотстоящих узлов

Если узлы равноотстоящие: $\(x_i = x_0 + i h\), \(h\)$ — шаг, то разделённые разности связаны с конечными разностями $\(\Delta^k y_i\)$.

*   **Первая интерполяционная формула Ньютона (для интерполяции в начале таблицы):**

$$\[
    P_n(x_0 + t h) = y_0 + t \Delta y_0 + \frac{t(t-1)}{2!} \Delta^2 y_0 + \dots + \frac{t(t-1)\cdots(t-n+1)}{n!} \Delta^n y_0
    \]$$
    
где $\(t = \frac{x - x_0}{h}\)$.

*   **Вторая интерполяционная формула Ньютона (для интерполяции в конце таблицы):**

$$\[
    P_n(x_n + t h) = y_n + t \Delta y_{n-1} + \frac{t(t+1)}{2!} \Delta^2 y_{n-2} + \dots + \frac{t(t+1)\cdots(t+n-1)}{n!} \Delta^n y_0
    \]$$

где $\(t = \frac{x - x_n}{h}\)$.

---

### 4. Сравнение методов Лагранжа и Ньютона

| Характеристика | Лагранж | Ньютон |
|:---|:---|:---|
| **Форма записи** | Сумма произведений | Сумма с нарастающими множителями |
| **Вычислительная сложность при добавлении узла** | Полный пересчёт | Только добавление нового слагаемого |
| **Наглядность** | Высокая | Требуется понимание разделённых разностей |
| **Применение** | Теоретические выкладки, небольшие задачи | Практические расчёты, адаптивные алгоритмы |

---

### 5. Погрешность интерполяции

Если $\(f(x)\)$ достаточно гладкая (имеет $\(n+1\)$ производную), то погрешность интерполяции оценивается формулой:

$$\[
|f(x) - P_n(x)| \le \frac{M_{n+1}}{(n+1)!} \cdot |\omega_{n+1}(x)|
\]$$

где:

$\(M_{n+1} = \max_{\xi \in [a,b]} |f^{(n+1)}(\xi)|\)$

$\(\omega_{n+1}(x) = (x - x_0)(x - x_1)\cdots(x - x_n)\)$

Эта оценка показывает, что погрешность зависит как от гладкости функции, так и от расположения узлов.

#### Резюме

| Понятие | Формула |
|:---|:---|
| **Многочлен Лагранжа** | $\(L_n(x) = \sum_{i=0}^n y_i \prod_{j \neq i} \frac{x - x_j}{x_i - x_j}\)$ |
| **Разделённая разность** | $\(f[x_i, \dots, x_{i+k}] = \frac{f[x_{i+1}, \dots, x_{i+k}] - f[x_i, \dots, x_{i+k-1}]}{x_{i+k} - x_i}\)$ |
| **Многочлен Ньютона** | $\(N_n(x) = \sum_{k=0}^n f[x_0, \dots, x_k] \prod_{j=0}^{k-1} (x - x_j)\)$ |
| **Первая формула Ньютона (равные узлы)** | $\(P_n(x_0 + t h) = \sum_{k=0}^n \binom{t}{k} \Delta^k y_0\)$ |
| **Погрешность интерполяции** | $\(R_n(x) = \frac{f^{(n+1)}(\xi)}{(n+1)!} \omega_{n+1}(x)\)$ |
