
Тема **Треугольного разложения матриц и метода Гаусса** — это фундамент численной линейной алгебры. Эти методы позволяют эффективно решать системы линейных уравнений, что критически важно для инженерных расчетов, компьютерной графики и машинного обучения.

---

# 1. Интуитивное понимание

### Зачем нужно треугольное разложение?
Решать систему с **треугольной матрицей** очень просто (методом обратной подстановки). Идея LU-разложения: разложить сложную матрицу $A$ на произведение двух простых треугольных матриц $L$ и $U$, а затем решить две простые системы вместо одной сложной.

**Аналогия:**
*   Решать $Ax = b$ напрямую — как взбираться на отвесную скалу.
*   Решать через $LU$-разложение — как подняться по лестнице (ступенька за ступенькой).

### Метод Гаусса
Это классический алгоритм приведения системы к треугольному виду путем последовательного исключения переменных. LU-разложение — это матричная запись метода Гаусса.

---

# 2. Строгие определения

### Треугольные матрицы

**Нижняя треугольная матрица $L$:**
Все элементы выше главной диагонали равны нулю:
$$
L = \begin{pmatrix}
l_{11} & 0 & \dots & 0 \\
l_{21} & l_{22} & \dots & 0 \\
\vdots & \vdots & \ddots & \vdots \\
l_{n1} & l_{n2} & \dots & l_{nn}
\end{pmatrix}
$$

**Верхняя треугольная матрица $U$:**
Все элементы ниже главной диагонали равны нулю:
$$
U = \begin{pmatrix}
u_{11} & u_{12} & \dots & u_{nn} \\
0 & u_{22} & \dots & u_{2n} \\
\vdots & \vdots & \ddots & \vdots \\
0 & 0 & \dots & u_{nn}
\end{pmatrix}
$$

### LU-разложение
Пусть $A$ — квадратная матрица размера $n \times n$.
**LU-разложением** называется представление:
$$
A = L \cdot U
$$
где $L$ — нижняя треугольная, $U$ — верхняя треугольная.

**Важно:** Разложение существует, если все главные миноры матрицы $A$ отличны от нуля.

---

# 3. Алгоритмы

## Метод Гаусса (прямой ход)

### Цель: Привести расширенную матрицу к ступенчатому виду

**Алгоритм:**
1.  **Выбор ведущего элемента:** На шаге $k$ выбираем элемент $a_{kk}$ (ведущий).
2.  **Исключение:** Для всех строк $i > k$ вычитаем из строки $i$ строку $k$, умноженную на коэффициент:
    $$
    m_{ik} = \frac{a_{ik}}{a_{kk}}
    $$
3.  **Повтор:** Переходим к следующему столбцу ($k = k + 1$).

### Обратный ход (подстановка)

После приведения к треугольному виду:
$$
\begin{cases}
u_{11}x_1 + u_{12}x_2 + \dots + u_{1n}x_n = b'_1 \\
\quad \quad \quad u_{22}x_2 + \dots + u_{2n}x_n = b'_2 \\
\quad \quad \quad \quad \quad \quad \dots \\
\quad \quad \quad \quad \quad \quad \quad \quad u_{nn}x_n = b'_n
\end{cases}
$$

Решаем снизу вверх:
$$
x_n = \frac{b'_n}{u_{nn}}, \quad x_i = \frac{b'_i - \sum_{j=i+1}^{n} u_{ij}x_j}{u_{ii}}
$$

## Алгоритм LU-разложения (метод Краута)

1.  **Инициализация:** $l_{ii} = 1$ (для всех $i$).
2.  **Первая строка $U$:** $u_{1j} = a_{1j}$ для $j = 1, \dots, n$.
3.  **Первый столбец $L$:** $l_{i1} = \frac{a_{i1}}{u_{11}}$ для $i = 2, \dots, n$.
4.  **Рекуррентно** для $k = 2, \dots, n$:
    *   $u_{kj} = a_{kj} - \sum_{p=1}^{k-1} l_{kp}u_{pj}$ для $j = k, \dots, n$
    *   $l_{ik} = \frac{1}{u_{kk}}\left(a_{ik} - \sum_{p=1}^{k-1} l_{ip}u_{pk}\right)$ для $i = k+1, \dots, n$

---

# 4. Решение системы через LU-разложение

Система $Ax = b$ заменяется на:
$$
L \cdot U \cdot x = b
$$

**Два этапа:**

1.  **Прямая подстановка:** Решаем $L \cdot y = b$ относительно $y$.
2.  **Обратная подстановка:** Решаем $U \cdot x = y$ относительно $x$.

---

# 5. Типовые задачи и решения

### Задача 1. Метод Гаусса (3 уравнения)
**Задание:** Решить систему методом Гаусса:
$$
\begin{cases}
2x_1 + x_2 + x_3 = 4 \\
4x_1 + 3x_2 + 3x_3 = 10 \\
2x_1 + x_2 + 2x_3 = 5
\end{cases}
$$

**Решение:**
1.  **Расширенная матрица:**
    $$
    \left(\begin{array}{ccc|c}
    2 & 1 & 1 & 4 \\
    4 & 3 & 3 & 10 \\
    2 & 1 & 2 & 5
    \end{array}\right)
    $$
2.  **Исключаем $x_1$ из строк 2 и 3:**
    *   $R_2 \to R_2 - 2R_1$: $(4-4, 3-2, 3-2 | 10-8) = (0, 1, 1 | 2)$
    *   $R_3 \to R_3 - R_1$: $(2-2, 1-1, 2-1 | 5-4) = (0, 0, 1 | 1)$
    
    Получаем:
    $$
    \left(\begin{array}{ccc|c}
    2 & 1 & 1 & 4 \\
    0 & 1 & 1 & 2 \\
    0 & 0 & 1 & 1
    \end{array}\right)
    $$
3.  **Обратный ход:**
    *   Из строки 3: $x_3 = 1$
    *   Из строки 2: $x_2 + 1 = 2 \Rightarrow x_2 = 1$
    *   Из строки 1: $2x_1 + 1 + 1 = 4 \Rightarrow 2x_1 = 2 \Rightarrow x_1 = 1$

**Ответ:** $x_1 = 1, x_2 = 1, x_3 = 1$.

---

### Задача 2. LU-разложение матрицы
**Задание:** Найти LU-разложение матрицы:
$$
A = \begin{pmatrix}
2 & 1 & 1 \\
4 & 3 & 3 \\
2 & 1 & 2
\end{pmatrix}
$$

**Решение:**
1.  **Первая строка $U$ и первый столбец $L$:**
    *   $u_{11} = 2, u_{12} = 1, u_{13} = 1$
    *   $l_{11} = 1, l_{21} = 4/2 = 2, l_{31} = 2/2 = 1$
2.  **Вторая строка $U$ и второй столбец $L$:**
    *   $u_{22} = 3 - l_{21}u_{12} = 3 - 2\cdot1 = 1$
    *   $u_{23} = 3 - l_{21}u_{13} = 3 - 2\cdot1 = 1$
    *   $l_{22} = 1, l_{32} = \frac{1 - l_{31}u_{12}}{u_{22}} = \frac{1 - 1\cdot1}{1} = 0$
3.  **Третья строка $U$:**
    *   $u_{33} = 2 - (l_{31}u_{13} + l_{32}u_{23}) = 2 - (1\cdot1 + 0\cdot1) = 1$
4.  **Результат:**
    $$
    L = \begin{pmatrix}
    1 & 0 & 0 \\
    2 & 1 & 0 \\
    1 & 0 & 1
    \end{pmatrix}, \quad
    U = \begin{pmatrix}
    2 & 1 & 1 \\
    0 & 1 & 1 \\
    0 & 0 & 1
    \end{pmatrix}
    $$
5.  **Проверка:** $L \cdot U = A$ ✓

**Ответ:** $L$ и $U$ найдены выше.

---

### Задача 3. Решение системы через LU-разложение
**Задание:** Решить систему $Ax = b$ с матрицей из Задачи 2 и $b = (4, 10, 5)^T$.

**Решение:**
1.  **Прямая подстановка** ($Ly = b$):
    $$
    \begin{pmatrix}
    1 & 0 & 0 \\
    2 & 1 & 0 \\
    1 & 0 & 1
    \end{pmatrix}
    \begin{pmatrix} y_1 \\ y_2 \\ y_3 \end{pmatrix} =
    \begin{pmatrix} 4 \\ 10 \\ 5 \end{pmatrix}
    $$
    *   $y_1 = 4$
    *   $2y_1 + y_2 = 10 \Rightarrow y_2 = 10 - 8 = 2$
    *   $y_1 + y_3 = 5 \Rightarrow y_3 = 5 - 4 = 1$
2.  **Обратная подстановка** ($Ux = y$):
    $$
    \begin{pmatrix}
    2 & 1 & 1 \\
    0 & 1 & 1 \\
    0 & 0 & 1
    \end{pmatrix}
    \begin{pmatrix} x_1 \\ x_2 \\ x_3 \end{pmatrix} =
    \begin{pmatrix} 4 \\ 2 \\ 1 \end{pmatrix}
    $$
    *   $x_3 = 1$
    *   $x_2 + x_3 = 2 \Rightarrow x_2 = 1$
    *   $2x_1 + x_2 + x_3 = 4 \Rightarrow 2x_1 = 2 \Rightarrow x_1 = 1$

**Ответ:** $x = (1, 1, 1)^T$.

---

### Задача 4. Оценка сложности метода Гаусса
**Задание:** Оценить количество операций для метода Гаусса при размере $n$.

**Решение:**
1.  **Прямой ход:**
    *   На шаге $k$ выполняем $(n-k)$ умножений для каждой из $(n-k)$ строк.
    *   Всего: $\sum_{k=1}^{n} (n-k)^2 \approx \frac{n^3}{3}$ операций.
2.  **Обратный ход:**
    *   Примерно $\frac{n^2}{2}$ операций.
3.  **Итог:**
    *   Сложность: $O(n^3)$ для разложения.
    *   Для решения с новым $b$: $O(n^2)$ (если $LU$ уже найдено).

**Ответ:** $O(n^3)$ для разложения, $O(n^2)$ для подстановки.

---

### Задача 5. Проблема ведущего элемента
**Задание:** Почему может потребоваться выбор главного элемента (pivoting)?

**Решение:**
1.  **Проблема 1:** Ведущий элемент $a_{kk} = 0$. Деление невозможно.
2.  **Проблема 2:** Ведущий элемент очень мал. Ошибки округления усиливаются.
3.  **Решение:** Частичный выбор главного элемента — перестановка строк для выбора максимального по модулю элемента в столбце.
4.  **Матричная запись:** $PA = LU$, где $P$ — матрица перестановок.

**Ответ:** Для избежания деления на ноль и уменьшения ошибок округления.

---

# 6. Сравнение методов

| Характеристика | Метод Гаусса | LU-разложение |
|:---:|:---:|:---:|
| **Сложность** | $O(n^3)$ | $O(n^3)$ |
| **Повторное решение** | Требует нового прохода | Только подстановка $O(n^2)$ |
| **Память** | Модифицирует матрицу | Хранит $L$ и $U$ |
| **Применение** | Однократное решение | Множественные правые части |

---

# 7. Резюме модуля

| Понятие | Формула / Описание |
|:---:|:---:|
| **LU-разложение** | $A = L \cdot U$ |
| **Прямая подстановка** | Решение $Ly = b$ |
| **Обратная подстановка** | Решение $Ux = y$ |
| **Сложность Гаусса** | $O(n^3)$ операций |
| **Выбор главного** | $PA = LU$ для устойчивости |

**Ключевые выводы:**
1.  **Метод Гаусса** — базовый алгоритм решения СЛАУ.
2.  **LU-разложение** — матричная форма метода Гаусса, удобная для множественных правых частей.
3.  **Треугольные системы** решаются быстро ($O(n^2)$) прямой/обратной подстановкой.
4.  **Выбор главного элемента** критически важен для численной устойчивости.
5.  **Сложность** кубическая по $n$, поэтому для очень больших систем используют итерационные методы.

