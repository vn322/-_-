Мы переходим к многомерным (векторным) случайным величинам. Если одномерная величина описывает один числовой признак, то многомерная — набор признаков, которые могут быть связаны друг с другом. Например, рост и вес человека, координаты точки попадания снаряда и т.д.

---

### 1. Многомерная случайная величина

#### 1.1 Определение

**Определение.** **Двумерной случайной величиной** (или двумерным случайным вектором) называется упорядоченная пара \((X, Y)\), где \(X = X(\omega)\) и \(Y = Y(\omega)\) — случайные величины, определённые на одном и том же пространстве элементарных исходов \(\Omega\).

Значения двумерной величины — точки на плоскости \((x, y)\).

#### 1.2 Типы двумерных величин

Как и в одномерном случае, двумерные величины могут быть:
*   **Дискретными:** \(X\) и \(Y\) принимают конечные или счетные множества значений.
*   **Непрерывными:** существует совместная плотность распределения.
*   **Смешанного типа:** одна компонента дискретна, другая непрерывна.

Мы сосредоточимся на **непрерывном** случае, как наиболее общем и важном.

---

### 2. Совместная функция распределения

#### 2.1 Определение

**Определение.** **Совместной функцией распределения** двумерной случайной величины \((X, Y)\) называется функция:
\[
\boxed{F(x, y) = P(X < x, Y < y)}
\]
т.е. вероятность того, что \(X\) примет значение меньше \(x\), а \(Y\) — меньше \(y\) одновременно.

Геометрически это вероятность попадания случайной точки в бесконечный квадрант левее и ниже точки \((x, y)\) (левый нижний угол).

#### 2.2 Свойства совместной функции распределения

1.  **Неубывание по каждому аргументу:** Если \(x_1 < x_2\), то \(F(x_1, y) \le F(x_2, y)\); если \(y_1 < y_2\), то \(F(x, y_1) \le F(x, y_2)\).
2.  **Пределы на бесконечностях:**
    *   \(\lim_{x \to -\infty} F(x, y) = 0\) для любого \(y\)
    *   \(\lim_{y \to -\infty} F(x, y) = 0\) для любого \(x\)
    *   \(\lim_{x \to +\infty, y \to +\infty} F(x, y) = 1\)
3.  **Непрерывность слева по каждому аргументу.**
4.  **Связь с одномерными функциями распределения (маргинальные распределения):**
    \[
    F_X(x) = P(X < x) = \lim_{y \to +\infty} F(x, y)
    \]
    \[
    F_Y(y) = P(Y < y) = \lim_{x \to +\infty} F(x, y)
    \]
5.  **Вероятность попадания в прямоугольник:**
    \[
    P(a \le X < b, c \le Y < d) = F(b, d) - F(a, d) - F(b, c) + F(a, c)
    \]

---

### 3. Совместная плотность распределения

Для непрерывных двумерных случайных величин существует совместная плотность.

#### 3.1 Определение

**Определение.** **Совместной плотностью распределения** двумерной непрерывной случайной величины \((X, Y)\) называется неотрицательная функция \(f(x, y)\), такая что для любых \(x, y\):
\[
\boxed{F(x, y) = \int_{-\infty}^{x} \int_{-\infty}^{y} f(u, v) \, du \, dv}
\]

Если совместная плотность непрерывна в точке \((x, y)\), то:
\[
f(x, y) = \frac{\partial^2 F(x, y)}{\partial x \partial y}
\]

#### 3.2 Свойства совместной плотности

1.  **Неотрицательность:** \(f(x, y) \ge 0\) для всех \(x, y\).
2.  **Нормировка:**
    \[
    \int_{-\infty}^{+\infty} \int_{-\infty}^{+\infty} f(x, y) \, dx \, dy = 1
    \]
3.  **Вероятность попадания в область \(D\):**
    \[
    P((X, Y) \in D) = \iint_D f(x, y) \, dx \, dy
    \]
4.  **Маргинальные (частные) плотности** получаются интегрированием по "чужой" переменной:
    \[
    f_X(x) = \int_{-\infty}^{+\infty} f(x, y) \, dy
    \]
    \[
    f_Y(y) = \int_{-\infty}^{+\infty} f(x, y) \, dx
    \]
    Это плотности распределения каждой компоненты в отдельности.

---

### 4. Условные распределения

Для двумерной непрерывной величины можно определить **условную плотность** распределения \(X\) при условии, что \(Y = y\):

**Определение.** Если \(f_Y(y) > 0\), то условная плотность \(X\) при \(Y = y\) равна:
\[
f_{X|Y}(x|y) = \frac{f(x, y)}{f_Y(y)}
\]

Аналогично определяется \(f_{Y|X}(y|x)\).

**Условное математическое ожидание:**
\[
\mathbb{E}[X | Y = y] = \int_{-\infty}^{+\infty} x f_{X|Y}(x|y) \, dx
\]

---

### 5. Независимость случайных величин

**Определение.** Случайные величины \(X\) и \(Y\) называются **независимыми**, если для любых \(x, y\) выполняется:
\[
F(x, y) = F_X(x) \cdot F_Y(y)
\]
Для непрерывных величин эквивалентное условие:
\[
f(x, y) = f_X(x) \cdot f_Y(y)
\]
для всех \(x, y\) (почти всюду).

**Смысл:** Совместная плотность (функция распределения) распадается в произведение маргинальных — информация об одной величине ничего не говорит о другой.

---

### 6. Пример: Двумерное нормальное распределение

Важнейший пример — двумерное нормальное распределение. Его плотность:
\[
f(x, y) = \frac{1}{2\pi\sigma_x\sigma_y\sqrt{1-\rho^2}} \exp\left( -\frac{1}{2(1-\rho^2)} \left[ \frac{(x-\mu_x)^2}{\sigma_x^2} - \frac{2\rho(x-\mu_x)(y-\mu_y)}{\sigma_x\sigma_y} + \frac{(y-\mu_y)^2}{\sigma_y^2} \right] \right)
\]
где:
*   \(\mu_x, \mu_y\) — математические ожидания
*   \(\sigma_x, \sigma_y\) — среднеквадратические отклонения
*   \(\rho\) — коэффициент корреляции (\(|\rho| \le 1\))

Свойства:
*   Маргинальные распределения — одномерные нормальные.
*   Если \(\rho = 0\), то \(X\) и \(Y\) независимы (для нормального распределения некоррелированность означает независимость).
*   Условные распределения также нормальны.

#### Резюме

| Понятие | Формула |
|:---|:---|
| **Совместная функция распределения** | \(F(x, y) = P(X < x, Y < y)\) |
| **Совместная плотность** | \(f(x, y) = \frac{\partial^2 F}{\partial x \partial y}\), \(\iint f dx dy = 1\) |
| **Маргинальная плотность \(X\)** | \(f_X(x) = \int_{-\infty}^{+\infty} f(x, y) dy\) |
| **Условная плотность \(X\|Y=y\)** | \(f_{X\|Y}(x\|y) = \frac{f(x, y)}{f_Y(y)}\) |
| **Независимость** | \(f(x, y) = f_X(x) f_Y(y)\) |
