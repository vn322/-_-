**Математическое ожидание** (также называемое средним значением или ожидаемым значением) является одной из важнейших числовых характеристик случайной величины. Оно описывает "центр" распределения и в некотором смысле является средним взвешенным всех возможных значений с весами, равными их вероятностям.

Рассмотрим его определение для разных типов величин и основные свойства.

---

### 1. Определение математического ожидания

Математическое ожидание определяется отдельно для дискретных и непрерывных случайных величин, но физический смысл един: это среднее значение, которое мы получили бы при бесконечно большом числе повторений эксперимента.

#### 1.1 Для дискретной случайной величины

Пусть дискретная случайная величина \(X\) принимает значения \(x_1, x_2, \dots\) с вероятностями \(p_1, p_2, \dots\) соответственно (\(\sum p_i = 1\)).

**Определение.** **Математическим ожиданием** дискретной случайной величины \(X\) называется сумма произведений всех её возможных значений на соответствующие вероятности:
\[
\boxed{\mathbb{E}[X] = \sum_{i} x_i p_i}
\]

#### 1.2 Для непрерывной случайной величины

Пусть непрерывная случайная величина \(X\) имеет плотность распределения \(f(x)\).

**Определение.** **Математическим ожиданием** непрерывной случайной величины \(X\) называется интеграл:
\[
\boxed{\mathbb{E}[X] = \int_{-\infty}^{+\infty} x f(x) \, dx}
\]

#### 1.3 Для функции от случайной величины

Если нас интересует математическое ожидание не самой величины \(X\), а некоторой функции \(g(X)\) от неё, то можно воспользоваться следующими формулами:

*   Для дискретного случая: \(\mathbb{E}[g(X)] = \sum_i g(x_i) p_i\)
*   Для непрерывного случая: \(\mathbb{E}[g(X)] = \int_{-\infty}^{+\infty} g(x) f(x) \, dx\)

---

### 2. Свойства математического ожидания

Рассмотрим основные свойства математического ожидания. Предполагаем, что все рассматриваемые случайные величины имеют конечные математические ожидания.

#### 2.1 Линейность

Это самое важное и часто используемое свойство.

1.  **Константа:** Математическое ожидание константы равно самой константе.
    \[
    \mathbb{E}[c] = c, \quad \text{где } c \in \mathbb{R}
    \]

2.  **Вынесение константы:** Константу можно выносить за знак математического ожидания.
    \[
    \mathbb{E}[cX] = c \cdot \mathbb{E}[X]
    \]

3.  **Сумма:** Математическое ожидание суммы случайных величин равно сумме их математических ожиданий.
    \[
    \mathbb{E}[X + Y] = \mathbb{E}[X] + \mathbb{E}[Y]
    \]

Из этих трёх свойств следует общее свойство **линейности**:
\[
\boxed{\mathbb{E}[aX + bY + c] = a\mathbb{E}[X] + b\mathbb{E}[Y] + c}
\]

#### 2.2 Монотонность

Если \(X \ge Y\) (т.е. для любого исхода значение \(X\) не меньше значения \(Y\)), то
\[
\mathbb{E}[X] \ge \mathbb{E}[Y]
\]

В частности, если \(X \ge 0\), то \(\mathbb{E}[X] \ge 0\).

#### 2.3 Свойство для произведения независимых величин

Если случайные величины \(X\) и \(Y\) **независимы**, то математическое ожидание их произведения равно произведению математических ожиданий:
\[
\boxed{\mathbb{E}[XY] = \mathbb{E}[X] \cdot \mathbb{E}[Y]}
\]

**Важно:** Обратное неверно. Из равенства \(\mathbb{E}[XY] = \mathbb{E}[X]\mathbb{E}[Y]\) не следует независимость (это свойство называется некоррелированностью).

#### 2.4 Математическое ожидание индикатора события

Пусть \(I_A\) — индикатор события \(A\) (случайная величина, равная 1, если \(A\) произошло, и 0 иначе). Тогда:
\[
\mathbb{E}[I_A] = P(A)
\]

Это следует из определения: \(\mathbb{E}[I_A] = 1 \cdot P(A) + 0 \cdot P(\bar{A}) = P(A)\).

#### 2.5 Неравенство Маркова

Если \(X \ge 0\), то для любого \(\varepsilon > 0\):
\[
P(X \ge \varepsilon) \le \frac{\mathbb{E}[X]}{\varepsilon}
\]

Это неравенство связывает математическое ожидание с вероятностью больших отклонений.

---

### 3. Примеры вычисления

#### Пример 1 (дискретный)
Пусть \(X\) — число очков при бросании правильной игральной кости.
\[
\mathbb{E}[X] = 1 \cdot \frac{1}{6} + 2 \cdot \frac{1}{6} + 3 \cdot \frac{1}{6} + 4 \cdot \frac{1}{6} + 5 \cdot \frac{1}{6} + 6 \cdot \frac{1}{6} = \frac{21}{6} = 3.5
\]

#### Пример 2 (непрерывный)
Пусть \(X \sim U[a, b]\) (равномерное распределение на отрезке \([a, b]\)).
\[
\mathbb{E}[X] = \int_a^b x \cdot \frac{1}{b-a} \, dx = \frac{1}{b-a} \cdot \frac{x^2}{2} \Big|_a^b = \frac{b^2 - a^2}{2(b-a)} = \frac{a+b}{2}
\]

#### Пример 3 (использование линейности)
Пусть \(X\) — число успехов в \(n\) испытаниях Бернулли с вероятностью успеха \(p\). Тогда \(X = X_1 + X_2 + \dots + X_n\), где \(X_i\) — индикатор успеха в \(i\)-м испытании.
\[
\mathbb{E}[X_i] = p, \quad \mathbb{E}[X] = np
\]

#### Резюме

| Свойство | Формула |
|:---|:---|
| **Определение (дискретное)** | \(\mathbb{E}[X] = \sum x_i p_i\) |
| **Определение (непрерывное)** | \(\mathbb{E}[X] = \int x f(x) dx\) |
| **Линейность** | \(\mathbb{E}[aX + bY + c] = a\mathbb{E}[X] + b\mathbb{E}[Y] + c\) |
| **Монотонность** | \(X \ge Y \Rightarrow \mathbb{E}[X] \ge \mathbb{E}[Y]\) |
| **Произведение независимых** | \(X \perp Y \Rightarrow \mathbb{E}[XY] = \mathbb{E}[X] \mathbb{E}[Y]\) |
| **Индикатор события** | \(\mathbb{E}[I_A] = P(A)\) |
