**Методы решения нелинейных алгебраических уравнений** — это итерационные алгоритмы для нахождения корней уравнений вида \(f(x) = 0\) (в одномерном случае) или систем \(F(x) = 0\) (в многомерном случае). Они классифицируются по размерности задачи (одномерные/многомерные) и по порядку сходимости (нулевого, первого, второго порядка), который зависит от того, какую информацию о функции мы используем.

---

## 1. Одномерные методы

### 1.1 Методы нулевого порядка (используют только значения функции)

#### Метод бисекции (деления отрезка пополам)

**Идея:** Если \(f(a) \cdot f(b) < 0\), то на отрезке \([a, b]\) есть корень. Делим отрезок пополам и выбираем ту половину, где функция меняет знак.

**Алгоритм:**
1.  Вычислить \(c = (a + b)/2\).
2.  Если \(f(c) = 0\) или \(|b-a| < \varepsilon\), то корень найден.
3.  Если \(f(a) \cdot f(c) < 0\), то \(b = c\), иначе \(a = c\).
4.  Повторять.

**Сходимость:** Линейная, гарантированная. Скорость сходимости — один бит точности за итерацию.

#### Метод хорд (секущих)

**Идея:** Аппроксимируем функцию прямой через две последние точки и находим её пересечение с осью \(x\).

**Формула:**
\[
x_{n+1} = x_n - f(x_n) \frac{x_n - x_{n-1}}{f(x_n) - f(x_{n-1})}
\]

**Сходимость:** Порядок сходимости \(p \approx 1.618\) (золотое сечение).

### 1.2 Методы первого порядка (используют первую производную)

#### Метод Ньютона (метод касательных)

**Идея:** Линеаризуем функцию в текущей точке и находим корень линейной аппроксимации.

**Формула:**
\[
x_{n+1} = x_n - \frac{f(x_n)}{f'(x_n)}
\]

**Геометрический смысл:** Проводим касательную к графику в точке \((x_n, f(x_n))\) и находим её пересечение с осью \(x\).

**Сходимость:** Квадратичная (\(p = 2\)) вблизи простого корня (если начальное приближение достаточно близко).

**Недостатки:**
*   Требуется вычислять производную.
*   Может расходиться при плохом начальном приближении.
*   Затруднён при кратных корнях (сходимость становится линейной).

#### Модифицированный метод Ньютона

Если производная вычисляется только один раз в начальной точке:
\[
x_{n+1} = x_n - \frac{f(x_n)}{f'(x_0)}
\]
Сходимость становится линейной, но экономия на вычислении производной может быть оправдана.

### 1.3 Методы второго порядка (используют вторую производную)

Используются редко из-за сложности вычисления вторых производных. Пример — метод Галлея:
\[
x_{n+1} = x_n - \frac{2f(x_n)f'(x_n)}{2[f'(x_n)]^2 - f(x_n)f''(x_n)}
\]
Имеет кубическую сходимость (\(p = 3\)), но требует вычисления \(f''(x)\).

---

## 2. Многомерные методы

Для системы \(n\) уравнений с \(n\) неизвестными:
\[
\begin{cases}
f_1(x_1, x_2, \dots, x_n) = 0 \\
f_2(x_1, x_2, \dots, x_n) = 0 \\
\vdots \\
f_n(x_1, x_2, \dots, x_n) = 0
\end{cases}
\]
Обозначим \(F(x) = (f_1(x), f_2(x), \dots, f_n(x))^T\).

### 2.1 Метод простой итерации (нулевого порядка)

**Идея:** Приводим систему к виду \(x = \Phi(x)\) и итерируем:
\[
x^{(k+1)} = \Phi(x^{(k)})
\]

**Условие сходимости:** \(\|\Phi'(x)\| < 1\) в окрестности корня.

**Сходимость:** Линейная.

### 2.2 Метод Ньютона (первого порядка)

**Идея:** Линеаризация вектор-функции \(F(x)\) в текущей точке.

**Формула:**
\[
x^{(k+1)} = x^{(k)} - [J(x^{(k)})]^{-1} F(x^{(k)})
\]
где \(J(x)\) — матрица Якоби:
\[
J_{ij} = \frac{\partial f_i}{\partial x_j}
\]

На практике обратную матрицу не вычисляют, а решают систему линейных уравнений относительно поправки \(\Delta x^{(k)}\):
\[
J(x^{(k)}) \Delta x^{(k)} = -F(x^{(k)})
\]
\[
x^{(k+1)} = x^{(k)} + \Delta x^{(k)}
\]

**Сходимость:** Квадратичная вблизи простого корня.

**Недостатки:**
*   Требуется вычисление матрицы Якоби на каждой итерации.
*   Решение системы линейных уравнений (\(O(n^3)\) операций).
*   Чувствительность к начальному приближению.

### 2.3 Квазиньютоновские методы (Broyden, DFP, BFGS)

Эти методы аппроксимируют матрицу Якоби или обратную к ней, избегая её непосредственного вычисления.

#### Метод Бройдена

**Идея:** Начинаем с некоторого приближения \(B_0 \approx J(x_0)\) и обновляем его на каждой итерации по формуле:
\[
B_{k+1} = B_k + \frac{(y_k - B_k s_k) s_k^T}{s_k^T s_k}
\]
где \(s_k = x^{(k+1)} - x^{(k)}\), \(y_k = F(x^{(k+1)}) - F(x^{(k)})\).

**Сходимость:** Сверхлинейная (\(p \approx 1.618\)).

### 2.4 Методы второго порядка (метод Ньютона с гессианом)

Для задач оптимизации (минимизации функции \(f(x)\)) используют метод Ньютона, включающий вторые производные (матрицу Гессе \(H(x)\)):
\[
x^{(k+1)} = x^{(k)} - [H(x^{(k)})]^{-1} \nabla f(x^{(k)})
\]

Для решения систем уравнений \(F(x) = 0\) этот метод эквивалентен обычному методу Ньютона.

---

## 3. Сравнительная таблица

| Метод | Порядок | Требует | Сходимость | Применимость |
|:---|:---|:---|:---|:---|
| **Бисекция** | 0 | \(f\) | Линейная, гарантированная | Одномерные |
| **Хорд** | ~1.618 | \(f\) | Сверхлинейная | Одномерные |
| **Ньютон (1D)** | 2 | \(f, f'\) | Квадратичная (локально) | Одномерные |
| **Галлей** | 3 | \(f, f', f''\) | Кубическая | Одномерные |
| **Простая итерация** | 1 | \(F\) | Линейная | Многомерные |
| **Ньютон (nD)** | 2 | \(F, J\) | Квадратичная (локально) | Многомерные |
| **Бройден** | ~1.618 | \(F\) | Сверхлинейная | Многомерные |
| **BFGS** | ~1.618 | \(\nabla f\) | Сверхлинейная | Оптимизация |

---

## 4. Практические рекомендации

1. **Для одномерных задач:**
   *   Надёжнее всего — комбинация бисекции и метода Ньютона.
   *   Если производная недоступна — метод секущих или Брента (гибрид бисекции и секущих).

2. **Для многомерных задач:**
   *   Если есть возможность вычислять Якобиан — метод Ньютона (или его модификации).
   *   Если Якобиан слишком дорог — квазиньютоновские методы (Broyden).
   *   Для больших разреженных систем — методы типа Ньютона-Крылова.

3. **Критерии остановки:**
   *   По норме функции: \(\|F(x^{(k)})\| < \varepsilon_f\).
   *   По норме изменения: \(\|x^{(k+1)} - x^{(k)}\| < \varepsilon_x\).
   *   По максимальному числу итераций.

#### Резюме

| Тип | Особенности |
|:---|:---|
| **Нулевой порядок** | Только значения функции, надёжны, медленны |
| **Первый порядок** | Используют первые производные, быстрее |
| **Второй порядок** | Используют вторые производные, очень быстры (но локально) |
