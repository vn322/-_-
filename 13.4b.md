
Тема **Многомерных случайных величин** расширяет теорию вероятностей на случай, когда нас интересует не одна, а сразу несколько случайных величин, наблюдаемых совместно. Это фундамент для понимания корреляции, регрессии и многомерного статистического анализа.

---

# 1. Двумерная случайная величина

### Интуитивное понимание
**Одномерная величина:** Одна характеристика (например, рост человека).
**Двумерная величина:** Пара характеристик (например, рост и вес человека одновременно).

Важно: нас интересует не только поведение каждой величины по отдельности, но и **связь между ними** (зависят ли они друг от друга?).

**Примеры:**
*   $(X, Y)$ — координаты точки попадания снаряда.
*   $(X, Y)$ — температура и давление в двигателе.
*   $(X, Y)$ — оценка за экзамен и количество часов подготовки.

### Строгое определение
**Двумерной случайной величиной** (случайным вектором) называется упорядоченная пара $(X, Y)$, где $X$ и $Y$ — одномерные случайные величины, определенные на одном вероятностном пространстве $(\Omega, \mathcal{F}, P)$.

Каждому исходу $\omega \in \Omega$ ставится в соответствие точка $(X(\omega), Y(\omega))$ на плоскости.

---

# 2. Совместная функция распределения

### Интуитивное понимание
Функция $F(x, y)$ отвечает на вопрос: «Какова вероятность того, что $X$ примет значение меньше $x$ **И** одновременно $Y$ примет значение меньше $y$?»

Геометрически это вероятность попадания случайной точки $(X, Y)$ в бесконечный квадрант с вершиной в точке $(x, y)$, направленный влево-вниз.

### Строгое определение
**Совместной функцией распределения** системы $(X, Y)$ называется функция:
$$
F(x, y) = P(X < x, \ Y < y)
$$

### Свойства совместной функции распределения

1.  **Неубывание по каждому аргументу:**
    $$
    x_1 < x_2 \Rightarrow F(x_1, y) \le F(x_2, y)
    $$
    $$
    y_1 < y_2 \Rightarrow F(x, y_1) \le F(x, y_2)
    $$
2.  **Граничные значения:**
    $$
    \lim_{x \to -\infty} F(x, y) = 0, \quad \lim_{y \to -\infty} F(x, y) = 0
    $$
    $$
    \lim_{x \to +\infty, \ y \to +\infty} F(x, y) = 1
    $$
3.  **Непрерывность слева:**
    $$
    F(x, y) = F(x - 0, y - 0)
    $$
4.  **Вероятность попадания в прямоугольник:**
    Вероятность попадания точки $(X, Y)$ в прямоугольник $[x_1, x_2) \times [y_1, y_2)$:
    $$
    P(x_1 \le X < x_2, \ y_1 \le Y < y_2) = F(x_2, y_2) - F(x_1, y_2) - F(x_2, y_1) + F(x_1, y_1)
    $$
    *(Формула включений-исключений для вероятностей)*

### Связь с одномерными функциями (Маргинальные распределения)
Функции распределения отдельных величин получаются предельным переходом:
$$
F_X(x) = \lim_{y \to +\infty} F(x, y) = F(x, +\infty)
$$
$$
F_Y(y) = \lim_{x \to +\infty} F(x, y) = F(+\infty, y)
$$

---

# 3. Совместная плотность распределения

### Интуитивное понимание
Для непрерывной двумерной величины плотность $f(x, y)$ показывает «густоту» вероятности в точке $(x, y)$.
**Вероятность** попадания в область $D$ — это **объем** цилиндра над этой областью под поверхностью плотности.

### Строгое определение
Если совместная функция распределения $F(x, y)$ дифференцируема, то **совместной плотностью распределения** называется вторая смешанная производная:
$$
f(x, y) = \frac{\partial^2 F(x, y)}{\partial x \partial y}
$$

### Свойства совместной плотности

1.  **Неотрицательность:**
    $$
    f(x, y) \ge 0
    $$
2.  **Нормировка:**
    $$
    \int_{-\infty}^{+\infty} \int_{-\infty}^{+\infty} f(x, y) \, dx \, dy = 1
    $$
3.  **Вероятность попадания в область $D$:**
    $$
    P((X, Y) \in D) = \iint_{D} f(x, y) \, dx \, dy
    $$
4.  **Связь с функцией распределения:**
    $$
    F(x, y) = \int_{-\infty}^{x} \int_{-\infty}^{y} f(u, v) \, du \, dv
    $$

### Маргинальные плотности
Плотности отдельных величин получаются интегрированием по другой переменной:
$$
f_X(x) = \int_{-\infty}^{+\infty} f(x, y) \, dy
$$
$$
f_Y(y) = \int_{-\infty}^{+\infty} f(x, y) \, dx
$$

---

# 4. Независимость случайных величин

### Определение
Случайные величины $X$ и $Y$ называются **независимыми**, если их совместная функция распределения распадается на произведение маргинальных:
$$
F(x, y) = F_X(x) \cdot F_Y(y)
$$

### Для непрерывных величин
Эквивалентное условие для плотностей:
$$
f(x, y) = f_X(x) \cdot f_Y(y)
$$

**Важно:** Если это равенство нарушается хотя бы в одной точке (на множестве положительной меры), величины **зависимы**.

---

# 5. Типовые задачи и решения

### Задача 1. Проверка свойств совместной плотности
**Задание:** Является ли функция $f(x, y) = 4xy$ при $0 \le x \le 1, 0 \le y \le 1$ (и 0 иначе) совместной плотностью распределения?

**Решение:**
1.  **Проверка неотрицательности:** $4xy \ge 0$ в квадрате $[0, 1] \times [0, 1]$ ✓
2.  **Проверка нормировки:**
    $$
    \int_{0}^{1} \int_{0}^{1} 4xy \, dx \, dy = \int_{0}^{1} 4y \left( \int_{0}^{1} x \, dx \right) dy
    $$
    $$
    = \int_{0}^{1} 4y \cdot \frac{1}{2} \, dy = \int_{0}^{1} 2y \, dy = \left. y^2 \right|_{0}^{1} = 1 \quad ✓
    $$
3.  **Вывод:** Да, является плотностью распределения.

**Ответ:** Да, является.

---

### Задача 2. Нахождение совместной функции распределения
**Задание:** Для плотности из Задачи 1 найти функцию распределения $F(x, y)$ внутри квадрата.

**Решение:**
1.  **Формула:**
    $$
    F(x, y) = \int_{0}^{x} \int_{0}^{y} 4uv \, du \, dv, \quad 0 \le x, y \le 1
    $$
2.  **Вычисление:**
    $$
    F(x, y) = 4 \left( \int_{0}^{x} u \, du \right) \left( \int_{0}^{y} v \, dv \right)
    $$
    $$
    = 4 \cdot \frac{x^2}{2} \cdot \frac{y^2}{2} = x^2 y^2
    $$
3.  **Полная функция:**
    $$
    F(x, y) = \begin{cases}
    0, & x < 0 \text{ или } y < 0 \\
    x^2 y^2, & 0 \le x \le 1, \ 0 \le y \le 1 \\
    x^2, & 0 \le x \le 1, \ y > 1 \\
    y^2, & x > 1, \ 0 \le y \le 1 \\
    1, & x > 1, \ y > 1
    \end{cases}
    $$

**Ответ:** $F(x, y) = x^2 y^2$ в единичном квадрате.

---

### Задача 3. Проверка независимости
**Задание:** Зависимы ли $X$ и $Y$ в Задаче 1?

**Решение:**
1.  **Найдем маргинальные плотности:**
    $$
    f_X(x) = \int_{0}^{1} 4xy \, dy = 4x \left. \frac{y^2}{2} \right|_{0}^{1} = 2x, \quad 0 \le x \le 1
    $$
    $$
    f_Y(y) = \int_{0}^{1} 4xy \, dx = 4y \left. \frac{x^2}{2} \right|_{0}^{1} = 2y, \quad 0 \le y \le 1
    $$
2.  **Проверим условие независимости:**
    $$
    f_X(x) \cdot f_Y(y) = 2x \cdot 2y = 4xy
    $$
3.  **Сравнение:**
    $$
    f(x, y) = 4xy = f_X(x) \cdot f_Y(y)
    $$
4.  **Вывод:** Условие выполняется, величины **независимы**.

**Ответ:** Да, независимы.

---

### Задача 4. Вероятность попадания в область
**Задание:** Для плотности $f(x, y) = 4xy$ найти вероятность попадания точки $(X, Y)$ в область $D: 0 \le x \le 0.5, \ 0 \le y \le 0.5$.

**Решение:**
1.  **Формула:**
    $$
    P((X, Y) \in D) = \int_{0}^{0.5} \int_{0}^{0.5} 4xy \, dx \, dy
    $$
2.  **Вычисление:**
    $$
    = 4 \left( \int_{0}^{0.5} x \, dx \right) \left( \int_{0}^{0.5} y \, dy \right)
    $$
    $$
    = 4 \cdot \frac{(0.5)^2}{2} \cdot \frac{(0.5)^2}{2} = 4 \cdot \frac{0.25}{2} \cdot \frac{0.25}{2} = 4 \cdot 0.125 \cdot 0.125 = 0.0625
    $$
3.  **Через функцию распределения:**
    $$
    P = F(0.5, 0.5) = (0.5)^2 \cdot (0.5)^2 = 0.25 \cdot 0.25 = 0.0625
    $$

**Ответ:** 0.0625.

---

### Задача 5. Зависимые величины (Пример)
**Задание:** Плотность $f(x, y) = x + y$ при $0 \le x \le 1, 0 \le y \le 1$. Зависимы ли $X$ и $Y$?

**Решение:**
1.  **Маргинальные плотности:**
    $$
    f_X(x) = \int_{0}^{1} (x + y) \, dy = \left. xy + \frac{y^2}{2} \right|_{0}^{1} = x + \frac{1}{2}
    $$
    $$
    f_Y(y) = \int_{0}^{1} (x + y) \, dx = \left. \frac{x^2}{2} + xy \right|_{0}^{1} = y + \frac{1}{2}
    $$
2.  **Проверка независимости:**
    $$
    f_X(x) \cdot f_Y(y) = \left(x + \frac{1}{2}\right)\left(y + \frac{1}{2}\right) = xy + \frac{x}{2} + \frac{y}{2} + \frac{1}{4}
    $$
    $$
    f(x, y) = x + y
    $$
3.  **Сравнение:**
    $$
    x + y \neq xy + \frac{x}{2} + \frac{y}{2} + \frac{1}{4}
    $$
4.  **Вывод:** Условие не выполняется, величины **зависимы**.

**Ответ:** Да, зависимы.

---

### Задача 6. Геометрическая вероятность (Круг)
**Задание:** Точка $(X, Y)$ равномерно распределена в круге $x^2 + y^2 \le R^2$. Найти плотность распределения.

**Решение:**
1.  **Площадь круга:** $S = \pi R^2$.
2.  **Равномерное распределение:** Плотность постоянна внутри области и 0 снаружи.
3.  **Нормировка:**
    $$
    \iint_{x^2 + y^2 \le R^2} f(x, y) \, dx \, dy = 1
    $$
    $$
    f(x, y) \cdot \pi R^2 = 1 \quad \Rightarrow \quad f(x, y) = \frac{1}{\pi R^2}
    $$
4.  **Ответ:**
    $$
    f(x, y) = \begin{cases}
    \frac{1}{\pi R^2}, & x^2 + y^2 \le R^2 \\
    0, & \text{иначе}
    \end{cases}
    $$
5.  **Независимость:** Величины **зависимы**, так как область не прямоугольная (значение $X$ ограничивает возможные значения $Y$).

**Ответ:** $f(x, y) = \frac{1}{\pi R^2}$ в круге, величины зависимы.

---

# Резюме модуля

| Понятие | Формула / Свойство |
|:---:|:---:|
| **Совместная ФР** | $F(x, y) = P(X < x, Y < y)$ |
| **Совместная плотность** | $f(x, y) = \frac{\partial^2 F}{\partial x \partial y}$ |
| **Нормировка** | $\iint f(x, y) \, dx \, dy = 1$ |
| **Вероятность области** | $P((X,Y) \in D) = \iint_D f(x, y) \, dx \, dy$ |
| **Маргинальная плотность** | $f_X(x) = \int f(x, y) \, dy$ |
| **Независимость** | $f(x, y) = f_X(x) \cdot f_Y(y)$ |

**Ключевые выводы:**
1.  **Двумерная величина** описывает совместное поведение двух случайных величин.
2.  **Совместная функция распределения** определяет вероятность попадания в квадрант.
3.  **Совместная плотность** позволяет вычислять вероятности через двойные интегралы.
4.  **Маргинальные распределения** получаются предельным переходом (для ФР) или интегрированием (для плотности).
5.  **Независимость** проверяется разложением совместной плотности в произведение маргинальных.
6.  **Геометрия области** влияет на независимость: прямоугольная область часто (но не всегда) означает независимость, круглая — зависимость.

