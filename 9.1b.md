
Тема **Функций комплексного переменного (ФКП)** открывает новый огромный раздел математики — **Комплексный анализ**. Здесь числа становятся точками на плоскости, а функции — преобразованиями этой плоскости. Это язык современной физики, аэродинамики и электротехники.

---

# 1. Интуитивное понимание

### От действительных к комплексным числам
В вещественном анализе функция $y = f(x)$ отображает точки числовой прямой в точки числовой прямой.
В комплексном анализе функция $w = f(z)$ отображает точки **комплексной плоскости** в точки другой (или той же) комплексной плоскости.

*   **Аргумент:** $z = x + iy$ (точка на плоскости $Oxy$).
*   **Значение:** $w = u + iv$ (точка на плоскости $Ouv$).

Таким образом, одна комплексная функция эквивалентна двум вещественным функциям двух переменных:

$$
u = u(x, y), \quad v = v(x, y)
$$

### Дифференцируемость
В вещественном анализе предел отношения приращений должен существовать при $\Delta x \to 0$ (слева и справа).
В комплексном анализе $\Delta z$ может стремиться к нулю по **любому направлению** на плоскости (сверху, снизу, по диагонали, по спирали).
Если предел один и тот же при любом способе стремления — функция дифференцируема. Это очень жесткое требование!

---

# 2. Строгие определения

### Функция комплексного переменного
Пусть $D$ — некоторое множество точек на комплексной плоскости $\mathbb{C}$.
Функцией называется правило, ставящее в соответствие каждому $z \in D$ единственное число $w \in \mathbb{C}$:

$$
w = f(z)
$$

Разложим на действительную и мнимую части:

$$
f(z) = u(x, y) + i v(x, y), \quad \text{где } z = x + iy
$$

### Производная функции комплексного переменного
Функция $f(z)$ называется **дифференцируемой** в точке $z_0$, если существует предел:

$$
f'(z_0) = \lim_{\Delta z \to 0} \frac{f(z_0 + \Delta z) - f(z_0)}{\Delta z}
$$

**Важно:** Предел должен существовать и не зависеть от того, как $\Delta z$ стремится к нулю.

### Условия Коши-Римана
Это необходимое и достаточное условие дифференцируемости (при условии непрерывности частных производных).

Пусть $f(z) = u(x, y) + i v(x, y)$. Функция $f(z)$ дифференцируема в точке $z_0 = x_0 + i y_0$ тогда и только тогда, когда в этой точке выполняются условия:

$$
\frac{\partial u}{\partial x} = \frac{\partial v}{\partial y} \quad \text{и} \quad \frac{\partial u}{\partial y} = -\frac{\partial v}{\partial x}
$$

Если эти условия выполняются, производная вычисляется по формуле:

$$
f'(z) = \frac{\partial u}{\partial x} + i \frac{\partial v}{\partial x} = \frac{\partial v}{\partial y} - i \frac{\partial u}{\partial y}
$$

### Аналитическая функция
Функция называется **аналитической** (или голоморфной) в области $D$, если она дифференцируема не только в точке, но и в некоторой окрестности каждой точки этой области.

---

# 3. Геометрический смысл производной

Производная $f'(z_0)$ в комплексном анализе имеет наглядную геометрическую интерпретацию, связанную с преобразованием плоскости.

Пусть $f'(z_0) \neq 0$. Тогда в малой окрестности точки $z_0$ отображение $w = f(z)$ обладает следующими свойствами:

### 1. Модуль производной (Коэффициент растяжения)

$$
|f'(z_0)| = k
$$

Число $k$ показывает, во сколько раз **увеличиваются длины** малых отрезков, выходящих из точки $z_0$, при отображении на плоскость $w$.
*   Если $|f'(z_0)| > 1$ — растяжение.
*   Если $|f'(z_0)| < 1$ — сжатие.

### 2. Аргумент производной (Угол поворота)

$$
\arg f'(z_0) = \alpha
$$

Число $\alpha$ показывает, на какой угол **поворачиваются** все малые векторы, выходящие из точки $z_0$, при отображении.
*   Поворот происходит против часовой стрелки, если $\alpha > 0$.

### Конформное отображение
Так как угол поворота $\alpha$ одинаков для всех направлений, исходящих из $z_0$, отображение **сохраняет углы** между кривыми. Такие отображения называются **конформными**.

---

# 4. Типовые задачи и решения

### Задача 1. Проверка условий Коши-Римана
**Задание:** Исследовать на дифференцируемость функцию $f(z) = \bar{z}$ (комплексно сопряженная).

**Решение:**
1.  Запишем функцию через $x$ и $y$:

$$
    z = x + iy \quad \Rightarrow \quad f(z) = \bar{z} = x - iy
    $$

Здесь $u(x, y) = x$, $v(x, y) = -y$.

2.  Найдем частные производные:

$$
    \frac{\partial u}{\partial x} = 1, \quad \frac{\partial u}{\partial y} = 0
    $$

$$
    \frac{\partial v}{\partial x} = 0, \quad \frac{\partial v}{\partial y} = -1
    $$
    
3.  Проверим условия Коши-Римана:
    *   Первое условие: $\frac{\partial u}{\partial x} = \frac{\partial v}{\partial y} \quad \Rightarrow \quad 1 = -1$ (**Неверно**).
    *   Второе условие: $\frac{\partial u}{\partial y} = -\frac{\partial v}{\partial x} \quad \Rightarrow \quad 0 = 0$ (Верно).
4.  **Вывод:** Так как первое условие не выполняется ни в одной точке, функция нигде не дифференцируема.

**Ответ:** Функция $f(z) = \bar{z}$ не дифференцируема нигде.

---

### Задача 2. Дифференцируемость степенной функции
**Задание:** Проверить условия Коши-Римана для $f(z) = z^2$ и найти производную.

**Решение:**
1.  Разложим на действительную и мнимую части:

$$
    f(z) = (x + iy)^2 = x^2 - y^2 + i(2xy)
    $$

Здесь $u = x^2 - y^2$, $v = 2xy$.

2.  Найдем частные производные:

$$
    u_x = 2x, \quad u_y = -2y
    $$

$$
    v_x = 2y, \quad v_y = 2x
    $$
    
3.  Проверим условия Коши-Римана:
    *   $u_x = v_y \quad \Rightarrow \quad 2x = 2x$ (Выполняется).
    *   $u_y = -v_x \quad \Rightarrow \quad -2y = -2y$ (Выполняется).
4.  Условия выполняются для любых $x, y$. Значит, функция дифференцируема всюду.
5.  Найдем производную:

$$
    f'(z) = u_x + i v_x = 2x + i(2y) = 2(x + iy) = 2z
    $$

**Ответ:** Функция дифференцируема всюду, $f'(z) = 2z$.

---

### Задача 3. Восстановление функции по действительной части
**Задание:** Найти вид аналитической функции $f(z)$, если известно, что $u(x, y) = x^2 - y^2 + x$, и $f(0) = 0$.

**Решение:**
1.  Используем условия Коши-Римана для нахождения $v(x, y)$.

$$
    \frac{\partial v}{\partial y} = \frac{\partial u}{\partial x} = 2x + 1
    $$
    
2.  Интегрируем по $y$:

$$
    v(x, y) = \int (2x + 1) \, dy = (2x + 1)y + C(x) = 2xy + y + C(x)
    $$

где $C(x)$ — неизвестная функция от $x$.

3.  Используем второе условие Коши-Римана:

$$
    \frac{\partial u}{\partial y} = -\frac{\partial v}{\partial x}
    $$

$$
    -2y = -\frac{\partial}{\partial x} (2xy + y + C(x)) = -(2y + C'(x))
    $$

$$
    -2y = -2y - C'(x) \quad \Rightarrow \quad C'(x) = 0
    $$

Значит, $C(x) = C$ (константа).

4.  Получаем мнимую часть: $v = 2xy + y + C$.
5.  Восстанавливаем функцию:

$$
    f(z) = u + iv = (x^2 - y^2 + x) + i(2xy + y + C)
    $$

Группируем слагаемые, чтобы получить $z$:

$$
    f(z) = (x^2 - y^2 + i2xy) + (x + iy) + iC = z^2 + z + iC
    $$
    
6.  Используем условие $f(0) = 0$:

$$
    0^2 + 0 + iC = 0 \quad \Rightarrow \quad C = 0
    $$

**Ответ:** $f(z) = z^2 + z$.

---

### Задача 4. Геометрический смысл производной
**Задание:** Для функции $f(z) = z^2$ найти коэффициент растяжения и угол поворота в точке $z_0 = 1 + i$.

**Решение:**
1.  Найдем производную функции:

$$
    f'(z) = 2z
    $$
    
2.  Вычислим значение производной в точке $z_0$:

$$
    f'(1 + i) = 2(1 + i) = 2 + 2i
    $$
    
3.  **Коэффициент растяжения** (модуль):

$$
    k = |f'(z_0)| = |2 + 2i| = \sqrt{2^2 + 2^2} = \sqrt{8} = 2\sqrt{2}
    $$
    
4.  **Угол поворота** (аргумент):

$$
    \alpha = \arg(2 + 2i) = \arctan\left(\frac{2}{2}\right) = \arctan(1) = \frac{\pi}{4} \quad (45^\circ)
    $$
    
5.  **Интерпретация:**
    В окрестности точки $1+i$ отображение $w=z^2$ растягивает все длины в $2\sqrt{2}$ раз и поворачивает все векторы на $45^\circ$ против часовой стрелки.

**Ответ:** $k = 2\sqrt{2}$, $\alpha = \frac{\pi}{4}$.

---

### Задача 5. Точки, где нарушается аналитичность
**Задание:** Найти точки, в которых функция $f(z) = \frac{1}{z}$ не является аналитической.

**Решение:**
1.  Функция $f(z) = \frac{1}{z}$ является элементарной рациональной функцией.
2.  Известно, что рациональные функции аналитичны везде, где определен знаменатель.
3.  Знаменатель обращается в ноль при $z = 0$.
4.  В точке $z = 0$ функция не определена, следовательно, не дифференцируема.
5.  Проверим через Коши-Римана (для интереса):

$$
    \frac{1}{z} = \frac{\bar{z}}{|z|^2} = \frac{x - iy}{x^2 + y^2}
    $$

$$
    u = \frac{x}{x^2+y^2}, \quad v = \frac{-y}{x^2+y^2}
    $$

В точке $(0,0)$ частные производные не существуют (деление на ноль).

**Ответ:** Функция аналитична всюду, кроме точки $z = 0$.

---

# Резюме модуля

| Понятие | Формула / Условие |
|:---:|:---:|
| **Комплексная функция** | $w = u(x,y) + i v(x,y)$ |
| **Производная** | $\lim_{\Delta z \to 0} \frac{\Delta w}{\Delta z}$ (не зависит от направления) |
| **Условия Коши-Римана** | $u_x = v_y, \quad u_y = -v_x$ |
| **Геометрический смысл** | $|f'|$ — растяжение, $\arg f'$ — поворот |
| **Конформность** | Сохранение углов между кривыми |

**Ключевые выводы:**
1.  **Дифференцируемость в $\mathbb{C}$** — гораздо более сильное условие, чем в $\mathbb{R}$. Большинство функций (например, $\bar{z}$, $|z|$, $\text{Re}(z)$) не дифференцируемы.
2.  **Условия Коши-Римана** связывают действительную и мнимую части. Зная одну часть аналитической функции, можно восстановить вторую (с точностью до константы).
3.  **Геометрически** аналитические функции в малом являются поворотами с растяжением (конформные отображения).



