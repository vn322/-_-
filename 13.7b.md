
Тема **Ковариации и коэффициента корреляции** завершает изучение числовых характеристик систем случайных величин. Эти понятия позволяют количественно измерить **связь** между двумя случайными величинами: насколько изменение одной величины связано с изменением другой.

---

# 1. Интуитивное понимание

### Что такое ковариация?
**Математическое ожидание** описывает центр одной величины.
**Дисперсия** описывает разброс одной величины.
**Ковариация** описывает, как две величины меняются **вместе**.

*   **Положительная ковариация:** Когда $X$ выше среднего, $Y$ тоже обычно выше среднего (прямая связь).
    *   *Пример:* Рост и вес человека.
*   **Отрицательная ковариация:** Когда $X$ выше среднего, $Y$ обычно ниже среднего (обратная связь).
    *   *Пример:* Скорость автомобиля и время в пути.
*   **Нулевая ковариация:** Нет линейной связи между величинами.
    *   *Пример:* Номер билета в лотерее и температура на улице.

### Что такое корреляция?
Ковариация зависит от единиц измерения (метры, килограммы, секунды). **Коэффициент корреляции** — это нормированная ковариация, которая не зависит от масштаба и всегда лежит в диапазоне от **-1 до 1**.

*   $r = 1$: Полная прямая линейная связь ($Y = aX + b, a > 0$).
*   $r = -1$: Полная обратная линейная связь ($Y = aX + b, a < 0$).
*   $r = 0$: Нет линейной связи (величины некоррелированы).

---

# 2. Строгие определения

### Ковариация
**Ковариацией** двух случайных величин $X$ и $Y$ называется математическое ожидание произведения их отклонений от математических ожиданий:

$$
\text{Cov}(X, Y) = E\left[ (X - E[X]) \cdot (Y - E[Y]) \right]
$$

**Обозначения:** $\text{Cov}(X, Y)$, $\sigma_{XY}$, $K_{XY}$.

**Формула для вычисления (практическая):**
$$
\text{Cov}(X, Y) = E[XY] - E[X] \cdot E[Y]
$$

### Для дискретных величин:
$$
\text{Cov}(X, Y) = \sum_{i} \sum_{j} (x_i - E[X])(y_j - E[Y]) \cdot p_{ij}
$$
где $p_{ij} = P(X = x_i, Y = y_j)$ — совместная вероятность.

### Для непрерывных величин:
$$
\text{Cov}(X, Y) = \int_{-\infty}^{+\infty} \int_{-\infty}^{+\infty} (x - E[X])(y - E[Y]) \cdot f(x, y) \, dx \, dy
$$

### Коэффициент корреляции (Пирсона)
**Коэффициентом корреляции** называется нормированная ковариация:

$$
\rho_{XY} = \frac{\text{Cov}(X, Y)}{\sigma_X \cdot \sigma_Y} = \frac{\text{Cov}(X, Y)}{\sqrt{D[X]} \cdot \sqrt{D[Y]}}
$$

где $\sigma_X, \sigma_Y$ — среднеквадратические отклонения величин $X$ и $Y$.

---

# 3. Свойства ковариации и корреляции

### Свойства ковариации

1.  **Симметричность:**
    $$
    \text{Cov}(X, Y) = \text{Cov}(Y, X)
    $$

2.  **Связь с дисперсией:**
    $$
    \text{Cov}(X, X) = D[X]
    $$

3.  **Вынос константы:**
    $$
    \text{Cov}(aX, bY) = a \cdot b \cdot \text{Cov}(X, Y)
    $$

4.  **Линейность по каждому аргументу:**
    $$
    \text{Cov}(X + Y, Z) = \text{Cov}(X, Z) + \text{Cov}(Y, Z)
    $$

5.  **Независимость:**
    Если $X$ и $Y$ независимы, то:
    $$
    \text{Cov}(X, Y) = 0
    $$
    *(Обратное неверно! Из нулевой ковариации не следует независимость)*.

6.  **Дисперсия суммы:**
    $$
    D[X + Y] = D[X] + D[Y] + 2 \cdot \text{Cov}(X, Y)
    $$
    *(Для независимых ковариация равна 0, получаем обычную формулу)*.

### Свойства коэффициента корреляции

1.  **Безразмерность:**
    $\rho_{XY}$ не имеет единиц измерения (нормированная величина).

2.  **Границы:**
    $$
    -1 \le \rho_{XY} \le 1
    $$

3.  **Полная линейная связь:**
    $$
    |\rho_{XY}| = 1 \quad \Leftrightarrow \quad Y = aX + b \quad \text{(почти наверняка)}
    $$

4.  **Некоррелированность:**
    $$
    \rho_{XY} = 0 \quad \Rightarrow \quad \text{величины некоррелированы}
    $$
    *(Но не обязательно независимы!)*.

5.  **Инвариантность:**
    Корреляция не меняется при линейном преобразовании величин (с изменением знака при отрицательном коэффициенте):
    $$
    \rho_{aX+b, \ cY+d} = \text{sgn}(a \cdot c) \cdot \rho_{XY}
    $$

---

# 4. Типовые задачи и решения

### Задача 1. Вычисление ковариации по таблице
**Задание:** Дана совместная таблица распределения:

| X \ Y | 1 | 2 |
|:---:|:---:|:---:|
| **0** | 0.2 | 0.3 |
| **1** | 0.3 | 0.2 |

Найти $\text{Cov}(X, Y)$.

**Решение:**
1.  **Маргинальные распределения:**
    *   $P(X=0) = 0.5, \ P(X=1) = 0.5 \Rightarrow E[X] = 0.5$
    *   $P(Y=1) = 0.5, \ P(Y=2) = 0.5 \Rightarrow E[Y] = 1.5$
2.  **Математическое ожидание произведения $E[XY]$:**
    $$
    E[XY] = \sum x_i y_j p_{ij} = 0 \cdot 1 \cdot 0.2 + 0 \cdot 2 \cdot 0.3 + 1 \cdot 1 \cdot 0.3 + 1 \cdot 2 \cdot 0.2
    $$
    $$
    = 0 + 0 + 0.3 + 0.4 = 0.7
    $$
3.  **Ковариация:**
    $$
    \text{Cov}(X, Y) = E[XY] - E[X]E[Y] = 0.7 - 0.5 \cdot 1.5 = 0.7 - 0.75 = -0.05
    $$

**Ответ:** $-0.05$ (слабая обратная связь).

---

### Задача 2. Вычисление коэффициента корреляции
**Задание:** Известно: $D[X] = 4$, $D[Y] = 9$, $\text{Cov}(X, Y) = 3$. Найти $\rho_{XY}$.

**Решение:**
1.  **СКО:**
    $$
    \sigma_X = \sqrt{4} = 2, \quad \sigma_Y = \sqrt{9} = 3
    $$
2.  **Корреляция:**
    $$
    \rho_{XY} = \frac{\text{Cov}(X, Y)}{\sigma_X \sigma_Y} = \frac{3}{2 \cdot 3} = \frac{3}{6} = 0.5
    $$

**Ответ:** 0.5 (умеренная прямая связь).

---

### Задача 3. Дисперсия суммы зависимых величин
**Задание:** $D[X] = 5$, $D[Y] = 8$, $\rho_{XY} = 0.6$. Найти $D[X + Y]$.

**Решение:**
1.  **Найдем ковариацию:**
    $$
    \text{Cov}(X, Y) = \rho_{XY} \cdot \sigma_X \cdot \sigma_Y = 0.6 \cdot \sqrt{5} \cdot \sqrt{8}
    $$
    $$
    = 0.6 \cdot \sqrt{40} \approx 0.6 \cdot 6.325 = 3.795
    $$
2.  **Дисперсия суммы:**
    $$
    D[X + Y] = D[X] + D[Y] + 2 \cdot \text{Cov}(X, Y)
    $$
    $$
    = 5 + 8 + 2 \cdot 3.795 = 13 + 7.59 = 20.59
    $$

**Ответ:** $\approx 20.59$.

---

### Задача 4. Независимость и некоррелированность
**Задание:** Пусть $X \sim U[-1, 1]$ (равномерное), $Y = X^2$. Найти $\text{Cov}(X, Y)$ и проверить независимость.

**Решение:**
1.  **Математические ожидания:**
    *   $E[X] = 0$ (симметричное распределение).
    *   $E[Y] = E[X^2] = D[X] + (E[X])^2 = \frac{(1 - (-1))^2}{12} + 0 = \frac{4}{12} = \frac{1}{3}$.
    *   $E[XY] = E[X \cdot X^2] = E[X^3] = 0$ (нечетная функция на симметричном интервале).
2.  **Ковариация:**
    $$
    \text{Cov}(X, Y) = E[XY] - E[X]E[Y] = 0 - 0 \cdot \frac{1}{3} = 0
    $$
3.  **Вывод:**
    *   Ковариация равна 0 $\Rightarrow$ величины **некоррелированы**.
    *   Но $Y$ полностью определяется через $X$ ($Y = X^2$) $\Rightarrow$ величины **зависимы**.
4.  **Смысл:** Корреляция измеряет только **линейную** связь. Здесь связь квадратичная (нелинейная).

**Ответ:** $\text{Cov}(X, Y) = 0$, величины зависимы (пример некоррелированных зависимых величин).

---

### Задача 5. Интерпретация корреляции
**Задание:** В исследовании получено $\rho = 0.9$ между временем подготовки к экзамену и оценкой. Что это означает?

**Решение:**
1.  **Знак:** Положительный $\Rightarrow$ прямая связь (больше готовишься — выше оценка).
2.  **Модуль:** 0.9 близко к 1 $\Rightarrow$ связь сильная.
3.  **Важно:** Корреляция **не означает причинность**! Возможно, есть третий фактор (например, способности студента), влияющий на оба показателя.
4.  **Вывод:** Сильная положительная линейная зависимость, но причинно-следственная связь требует дополнительного анализа.

**Ответ:** Сильная прямая линейная связь, но причинность не доказана.

---

# 5. Таблица интерпретации коэффициента корреляции

| Значение $\rho$ | Характер связи |
|:---:|:---:|
| $0$ | Нет линейной связи (некоррелированы) |
| $0 < |\rho| < 0.3$ | Слабая связь |
| $0.3 \le |\rho| < 0.7$ | Умеренная связь |
| $0.7 \le |\rho| < 1$ | Сильная связь |
| $1$ или $-1$ | Полная линейная связь (функциональная) |

---

# Резюме модуля

| Понятие | Формула / Свойство |
|:---:|:---:|
| **Ковариация** | $\text{Cov}(X, Y) = E[XY] - E[X]E[Y]$ |
| **Корреляция** | $\rho_{XY} = \frac{\text{Cov}(X, Y)}{\sigma_X \sigma_Y}$ |
| **Границы корреляции** | $-1 \le \rho \le 1$ |
| **Дисперсия суммы** | $D[X+Y] = D[X] + D[Y] + 2\text{Cov}(X,Y)$ |
| **Независимость** | $\Rightarrow \text{Cov} = 0$ (обратное неверно!) |

**Ключевые выводы:**
1.  **Ковариация** показывает направление связи (знак) и её силу в единицах произведения величин.
2.  **Корреляция** — безразмерная мера линейной связи от -1 до 1.
3.  **Некоррелированность $\neq$ Независимость**. Нулевая корреляция означает только отсутствие линейной связи.
4.  **Дисперсия суммы** зависит от ковариации: положительная корреляция увеличивает разброс суммы, отрицательная — уменьшает.
5.  **Корреляция не означает причинность** — это важное предупреждение для интерпретации данных.

