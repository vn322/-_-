
Тема **Принципа сжимающего отображения и метода Ньютона** связывает фундаментальную теорию функционального анализа с одним из самых мощных практических алгоритмов численной математики. Принцип сжимающего отображения гарантирует сходимость итерационных процессов, а метод Ньютона является ярчайшим примером их применения.

---

# 1. Интуитивное понимание

### Принцип сжимающего отображения
Представьте, что у вас есть правило (отображение), которое берет любую точку пространства и перемещает её в другую точку.
Если это правило **«сжимает»** расстояния между любыми двумя точками (приближает их друг к другу), то при многократном применении этого правила все точки «схлопнутся» в одну единственную точку.
Эта точка называется **неподвижной точкой** (fixed point).

**Зачем это нужно?**
Это теоретическое обоснование метода простой итерации. Если мы можем записать уравнение $f(x) = 0$ в виде $x = \phi(x)$ так, чтобы $\phi$ было сжимающим, то решение найдется гарантированно.

### Метод Ньютона
Это способ быстро найти корень уравнения.
**Идея:** Заменяем сложную кривую $f(x)$ на касательную прямую в текущей точке. Корень касательной берется как следующее приближение к корню функции.
**Связь с сжимающим отображением:** Вблизи корня функция итераций метода Ньютона является сжимающим отображением (причем очень сильным), что обеспечивает сверхбыструю сходимость.

---

# 2. Принцип сжимающего отображения (Теорема Банаха)

### Строгие определения

**Метрическое пространство:** Множество $X$ с функцией расстояния $\rho(x, y)$ (например, $\mathbb{R}^n$ с евклидовой нормой).

**Сжимающее отображение:**
Оператор $A: X \to X$ называется сжимающим, если существует число $q$ ($0 \le q < 1$), такое что для любых $x, y \in X$:

$$
\rho(Ax, Ay) \le q \cdot \rho(x, y)
$$

Число $q$ называется **коэффициентом сжатия**.

**Неподвижная точка:**
Точка $x^* \in X$ называется неподвижной точкой отображения $A$, если:

$$
A x^* = x^*
$$

### Теорема Банаха о неподвижной точке

Пусть $X$ — **полное** метрическое пространство (например, $\mathbb{R}^n$), и $A: X \to X$ — сжимающее отображение.
Тогда:

1.  **Существование и единственность:** Существует единственная неподвижная точка $x^*$.
2.  **Сходимость итераций:** Последовательность $x_{k+1} = A x_k$ сходится к $x^*$ при любом начальном $x_0$.
3.  **Оценка погрешности:**
    *   **Априорная:**

$$
        \rho(x_k, x^*) \le \frac{q^k}{1 - q} \rho(x_1, x_0)
        $$
        
        
    *   **Апостериорная:**


$$
        \rho(x_k, x^*) \le \frac{q}{1 - q} \rho(x_k, x_{k-1})
        $$

### Критерий сжимаемости для функций
Для функции $\phi(x)$ на отрезке $[a, b]$ достаточным условием сжимаемости является:

$$
|\phi'(x)| \le q < 1 \quad \text{для всех } x \in [a, b]
$$

---

# 3. Метод Ньютона (Метод касательных)

### Вывод формулы
Ищем корень уравнения $f(x) = 0$.
Разложим $f(x)$ в ряд Тейлора в окрестности точки $x_k$:

$$
f(x) \approx f(x_k) + f'(x_k)(x - x_k)
$$

Приравняем к нулю и обозначим следующее приближение за $x_{k+1}$:

$$
0 = f(x_k) + f'(x_k)(x_{k+1} - x_k)
$$

Отсюда получаем **формулу Ньютона**:

$$
x_{k+1} = x_k - \frac{f(x_k)}{f'(x_k)}
$$

### Геометрический смысл
1.  Строим касательную к графику $y = f(x)$ в точке $(x_k, f(x_k))$.
2.  Находим точку пересечения касательной с осью $Ox$.
3.  Это пересечение и есть $x_{k+1}$.

### Сходимость метода Ньютона

**Локальная сходимость:**
Если $x^*$ — простой корень ( $f(x^*) = 0, f'(x^*) \neq 0$ ) и $f$ 

дважды непрерывно дифференцируема, то существует окрестность корня, в которой метод Ньютона сходится.

**Порядок сходимости:**
Метод Ньютона имеет **квадратичную сходимость** (второй порядок):

$$
\[ |x_{k+1} - x^*| \le C \cdot |x_k - x^*|^2 \]
$$

Это означает, что число верных знаков в ответе **удваивается** на каждой итерации.

### Связь с принципом сжимающего отображения
Метод Ньютона можно записать как метод простой итерации $x_{k+1} = \phi(x_k)$, где:

$$
\phi(x) = x - \frac{f(x)}{f'(x)}
$$

Производная функции итераций:

$$
\phi'(x) = 1 - \frac{(f'(x))^2 - f(x)f''(x)}{(f'(x))^2} = \frac{f(x)f''(x)}{(f'(x))^2}
$$

В точке корня $x^*$ (где $f(x^*) = 0$):

$$
\phi'(x^*) = 0
$$

Так как $|\phi'(x^*)| = 0 < 1$, то в окрестности корня отображение является сжимающим (и очень сильным), что объясняет быструю сходимость.

---

# 4. Метод Ньютона для систем уравнений

Для системы $F(x) = 0$, где $F: \mathbb{R}^n \to \mathbb{R}^n$, формула обобщается:

$$
x^{(k+1)} = x^{(k)} - [J_F(x^{(k)})]^{-1} F(x^{(k)})
$$

где $J_F$ — **матрица Якоби**:

$$
J_F = \begin{pmatrix}
\frac{\partial f_1}{\partial x_1} & \cdots & \frac{\partial f_1}{\partial x_n} \\
\vdots & \ddots & \vdots \\
\frac{\partial f_n}{\partial x_1} & \cdots & \frac{\partial f_n}{\partial x_n}
\end{pmatrix}
$$

**Практическая реализация:**
Вместо обращения матрицы решается система линейных уравнений на каждом шаге:

$$
J_F(x^{(k)}) \cdot \Delta x = -F(x^{(k)})
$$

$$
x^{(k+1)} = x^{(k)} + \Delta x
$$

---

# 5. Типовые задачи и решения

### Задача 1. Проверка условия сжимающего отображения
**Задание:** Является ли отображение $\phi(x) = \frac{x}{2} + \frac{1}{x}$ сжимающим на отрезке $[1, 2]$?

**Решение:**
1.  **Найдем производную:**

$$
    \phi'(x) = \frac{1}{2} - \frac{1}{x^2}
    $$
    
2.  **Оценим модуль на отрезке:**
    При $x \in [1, 2]$, $x^2 \in [1, 4]$, $\frac{1}{x^2} \in [0.25, 1]$.

$$
    \phi'(1) = 0.5 - 1 = -0.5
    $$

$$
    \phi'(2) = 0.5 - 0.25 = 0.25
    $$

Максимальный модуль: $q = \max_{x \in [1, 2]} |\phi'(x)| = 0.5$.

3.  **Вывод:** Так как $q = 0.5 < 1$, отображение является сжимающим.

**Ответ:** Да, является ($q = 0.5$).

---

### Задача 2. Метод Ньютона (одна итерация)
**Задание:** Найти приближение к корню уравнения $f(x) = x^3 - x - 1 = 0$, начиная с $x_0 = 1.5$.

**Решение:**
1.  **Производная:** $f'(x) = 3x^2 - 1$.
2.  **Значения в точке $x_0 = 1.5$:**

$$
    f(1.5) = 1.5^3 - 1.5 - 1 = 3.375 - 2.5 = 0.875
    $$

$$
    f'(1.5) = 3 \cdot 2.25 - 1 = 6.75 - 1 = 5.75
    $$
    
3.  **Формула Ньютона:**

$$
    x_1 = 1.5 - \frac{0.875}{5.75} \approx 1.5 - 0.1522 = 1.3478
    $$

**Ответ:** $x_1 \approx 1.3478$.

---

### Задача 3. Оценка погрешности (Теорема Банаха)
**Задание:** Для сжимающего отображения с $q = 0.5$ известно $\|x_1 - x_0\| = 0.1$. Оценить погрешность $\|x_3 - x^*\|$.

**Решение:**
1.  **Априорная оценка:**

$$
    \|x_k - x^*\| \le \frac{q^k}{1 - q} \|x_1 - x_0\|
    $$
    
2.  **Подстановка ($k = 3$):**

$$
    \|x_3 - x^*\| \le \frac{0.5^3}{1 - 0.5} \cdot 0.1 = \frac{0.125}{0.5} \cdot 0.1 = 0.25 \cdot 0.1 = 0.025
    $$

**Ответ:** Погрешность не превышает 0.025.

---

### Задача 4. Метод Ньютона для системы ( conceptual)
**Задание:** Записать одну итерацию метода Ньютона для системы:

$$
\begin{cases}
x^2 + y^2 - 1 = 0 \\
x - y = 0
\end{cases}
$$

в точке $(1, 0)$.

**Решение:**
1.  **Вектор функции:** $F(1, 0) = \begin{pmatrix} 1^2 + 0 - 1 \\ 1 - 0 \end{pmatrix} = \begin{pmatrix} 0 \\ 1 \end{pmatrix}$.
2.  **Матрица Якоби:**

$$
    J = \begin{pmatrix} 2x & 2y \\ 1 & -1 \end{pmatrix} \quad \Rightarrow \quad J(1, 0) = \begin{pmatrix} 2 & 0 \\ 1 & -1 \end{pmatrix}
    $$
    
3.  **Система для поправки $\Delta x$:**

$$
    \begin{pmatrix} 2 & 0 \\ 1 & -1 \end{pmatrix} \begin{pmatrix} \Delta x \\ \Delta y \end{pmatrix} = -\begin{pmatrix} 0 \\ 1 \end{pmatrix} = \begin{pmatrix} 0 \\ -1 \end{pmatrix}
    $$
    
4.  **Решение СЛАУ:**
    *   $2\Delta x = 0 \Rightarrow \Delta x = 0$
    *   $\Delta x - \Delta y = -1 \Rightarrow 0 - \Delta y = -1 \Rightarrow \Delta y = 1$
5.  **Новая точка:**

$$
    x_{new} = 1 + 0 = 1, \quad y_{new} = 0 + 1 = 1
    $$

**Ответ:** Следующее приближение $(1, 1)$.

---

### Задача 5. Сравнение скорости сходимости
**Задание:** Почему метод Ньютона быстрее метода простой итерации?

**Решение:**
1.  **Метод простой итерации:** $|\phi'(x^*)| = q$, где $0 < q < 1$. Сходимость линейная: $|e_{k+1}| \approx q |e_k|$.
2.  **Метод Ньютона:** $\phi'(x^*) = 0$. Сходимость квадратичная: $|e_{k+1}| \approx C |e_k|^2$.
3.  **Пример:** Если ошибка $10^{-2}$:
    *   Линейная ($q=0.5$): следующая ошибка $5 \cdot 10^{-3}$.
    *   Квадратичная: следующая ошибка $\approx 10^{-4}$.
4.  **Вывод:** Квадратичная сходимость уменьшает ошибку гораздо быстрее.

**Ответ:** Из-за квадратичной сходимости ($\phi'(x^*) = 0$).

---

# 6. Резюме модуля

| Понятие | Формула / Условие |
|:---:|:---:|
| **Сжимающее отображение** | $\rho(Ax, Ay) \le q \rho(x, y), \quad q < 1$ |
| **Теорема Банаха** | Гарантирует существование и сходимость итераций |
| **Метод Ньютона (1D)** | $x_{k+1} = x_k - \frac{f(x_k)}{f'(x_k)}$ |
| **Метод Ньютона (nD)** | $x^{(k+1)} = x^{(k)} - J^{-1} F(x^{(k)})$ |
| **Сходимость Ньютона** | Квадратичная (вблизи простого корня) |
| **Условие сходимости итераций** | $|\phi'(x)| \le q < 1$ |

**Ключевые выводы:**
1.  **Принцип сжимающего отображения** — фундаментальный критерий сходимости итерационных методов.
2.  **Метод Ньютона** локально является сжимающим отображением с коэффициентом $q \approx 0$.
3.  **Квадратичная сходимость** делает метод Ньютона эталоном скорости для гладких функций.
4.  **Для систем** метод требует вычисления матрицы Якоби и решения СЛАУ на каждом шаге.
5.  **Глобальная сходимость** не гарантирована (нужно хорошее начальное приближение).

