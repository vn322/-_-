https://chat.deepseek.com/a/chat/s/c6683299-5bad-415a-a5cc-98fd08f66101 
**1.1. Предел, непрерывность, производная**

$\lim_{x \to a} f(x) = L \;\Leftrightarrow\; \forall \varepsilon>0\;\exists \delta>0:\; 0<|x-a|<\delta \Rightarrow |f(x)-L|<\varepsilon$

$\lim_{x \to a} f(x) = f(a)$

Свойства на $[a,b]$: достижимость $\min/\max$, прохождение всех промежуточных значений, равномерная непрерывность

$f'(x_0) = \lim_{h \to 0} \frac{f(x_0+h)-f(x_0)}{h}$

**1.2. Функции многих переменных**

$df = \frac{\partial f}{\partial x}dx + \frac{\partial f}{\partial y}dy$

Геометрический смысл: касательная плоскость к $z=f(x,y)$

$$z - z_0 = f_x(x_0,y_0)(x-x_0) + f_y(x_0,y_0)(y-y_0)$$

Достаточное условие дифференцируемости: $f_x, f_y$ непрерывны в окрестности

$\nabla f = \left( \frac{\partial f}{\partial x}, \frac{\partial f}{\partial y} \right)$

**1.3. Интеграл Римана. Ньютон-Лейбниц**

Первообразная: $F'(x)=f(x)$

$\int f(x)dx = F(x)+C$

$$\int_a^b f(x)dx = \lim_{\max \Delta x_i \to 0} \sum_{i=1}^n f(\xi_i)\Delta x_i$$

$$\int_a^b f(x)dx = F(b)-F(a)$$

**1.4. Теоремы Ферма, Ролля, Лагранжа, Коши**

Ферма: если $x_0$ — локальный экстремум и $f$ дифференцируема, то $f'(x_0)=0$

Ролль: $f(a)=f(b)$, непрерывна на $[a,b]$, дифф. на $(a,b)$ $\Rightarrow$ $\exists c\in(a,b): f'(c)=0$

Лагранж: $\exists c\in(a,b): \frac{f(b)-f(a)}{b-a} = f'(c)$

Коши: $\frac{f(b)-f(a)}{g(b)-g(a)} = \frac{f'(c)}{g'(c)}$, если $g'(x)\neq0$

**1.5. Числовые ряды**

Сходимость: $\sum_{n=1}^\infty a_n = \lim_{N\to\infty} S_N$ (конечный)

Критерий Коши: $\forall \varepsilon>0\;\exists N: \forall n>N,\forall p\in\mathbb{N}\; |a_{n+1}+\dots+a_{n+p}|<\varepsilon$

Признаки: Даламбера $\left(\lim \frac{a_{n+1}}{a_n}<1\right)$, Коши $\left(\lim \sqrt[n]{|a_n|}<1\right)$, интегральный

**1.6. Абсолютная и условная сходимость**

Абсолютная: $\sum |a_n|$ сходится $\Rightarrow$ $\sum a_n$ сходится

Условная: $\sum a_n$ сходится, $\sum |a_n|$ расходится (Лейбниц: $a_n=(-1)^n b_n,\; b_n\downarrow 0$)

Свойство: абсолютно сходящийся ряд можно переставлять

Умножение рядов (Коши): $c_n = \sum_{k=0}^n a_k b_{n-k}$, при абс. сход. $\sum c_n = (\sum a_n)(\sum b_n)$

**1.7. Функциональные ряды**

Равномерная сходимость: $\sup_{x\in X} |\sum_{k=n+1}^\infty f_k(x)| \to 0$

Признак Вейерштрасса: $|f_n(x)|\le M_n$ и $\sum M_n$ сходится $\Rightarrow$ равномерная сходимость

Свойства: непрерывность суммы, почленное $\int$ и $\frac{d}{dx}$ (если $f_n'$ равномерно сходятся)

**1.8. Степенные ряды. Ряд Тейлора**

$\sum_{n=0}^\infty c_n (x-a)^n$

Радиус сходимости (Коши-Адамар): $\displaystyle R = \frac{1}{\limsup_{n\to\infty} \sqrt[n]{|c_n|}}$

Свойства: внутри $(a-R,a+R)$ — равномерная сходимость, почленное интегрирование и дифференцирование

Ряд Тейлора: $f(x)=\sum_{n=0}^\infty \frac{f^{(n)}(a)}{n!}(x-a)^n$

**1.9. Несобственные интегралы**

1-й род: $\int_a^\infty f(x)dx = \lim_{b\to\infty} \int_a^b f(x)dx$

2-й род (особенность в $b$): $\int_a^b f(x)dx = \lim_{\varepsilon\to0^+} \int_a^{b-\varepsilon} f(x)dx$

Сходимость: абсолютная ($\int |f|$ сходится) и условная

---

## 2. Алгебра и геометрия

**2.1. Прямые и плоскости в пространстве**

Плоскость: $Ax+By+Cz+D=0$, нормаль $\mathbf{n}=(A,B,C)$

Прямая: каноническое $\frac{x-x_0}{m}=\frac{y-y_0}{n}=\frac{z-z_0}{p}$, направляющий вектор $\mathbf{s}=(m,n,p)$

Параметрическое: $\mathbf{r}=\mathbf{r}_0 + t\mathbf{s}$

Расстояние от точки до плоскости: $d = \frac{|Ax_0+By_0+Cz_0+D|}{\sqrt{A^2+B^2+C^2}}$

**2.2. Кривые второго порядка**

Эллипс: $\frac{x^2}{a^2}+\frac{y^2}{b^2}=1$

Гипербола: $\frac{x^2}{a^2}-\frac{y^2}{b^2}=1$

Парабола: $y^2=2px$

Приведение к каноническому виду: поворот (уничтожение $xy$) и перенос (выделение полных квадратов)

**2.3. Системы линейных алгебраических уравнений (СЛАУ)**

Методы: Гаусса, Крамера ($x_i = \det A_i / \det A$), матричный ($X=A^{-1}B$)

Кронекер-Капелли: система совместна $\iff \mathrm{rank}(A)=\mathrm{rank}(A|B)$

Однородная $AX=0$: $X = C_1 X_1 + \dots + C_{n-r} X_{n-r}$, ФСР — $n-\mathrm{rank}(A)$ ЛНЗ решений

Неоднородная: $X = X_{\text{част}} + X_{\text{общ. однор.}}$

**2.4. Собственные векторы и собственные значения**

$A\mathbf{v} = \lambda \mathbf{v},\ \mathbf{v}\neq 0$

Характеристический многочлен: $\det(A - \lambda I)=0$

$\lambda$ — корни, $\mathbf{v}\in \ker(A-\lambda I)$

**2.5. Линейные операторы**

Определение: $A(\alpha x+\beta y)=\alpha Ax+\beta Ay$

Способы задания: матрицей $[A]$, образом базисных векторов

Действия: $(A+B)x=Ax+Bx$, $(cA)x=c(Ax)$, $(AB)x=A(Bx)$

---

## 3. Дифференциальные уравнения

**3.1. ДУ первого порядка. Теорема Коши**

$y' = f(x,y)$

Теорема Коши: если $f$ и $\frac{\partial f}{\partial y}$ непрерывны в области, то $\exists!$ решение через $(x_0,y_0)$

**3.2. ЛДУ второго порядка**

Однородное: $y'' + p(x)y' + q(x)y = 0$

Линейная зависимость функций: $\exists c_1,c_2$ не все $0$: $c_1y_1+c_2y_2=0$

Фундаментальная система решений (ФСР): два ЛНЗ решения

Определитель Вронского для ФСР: 

$$W(y_1,y_2)=\det\begin{pmatrix}y_1&y_2\\ 
y_1'&y_2'\end{pmatrix} \neq 0$$ 

Общее решение: $y = C_1 y_1 + C_2 y_2$

Линейное неоднородное: $y_{\text{общ}} = y_{\text{одн}} + y_{\text{част}}$

**3.3. Устойчивость по Ляпунову. Теоремы Ляпунова и Четаева**

Устойчивость: $\forall\varepsilon>0\;\exists\delta>0: \|x(0)\|<\delta \Rightarrow \|x(t)\|<\varepsilon\ \forall t\ge0$

Асимптотическая устойчивость: устойчивость + $\lim_{t\to\infty}\|x(t)\|=0$

Теорема Ляпунова: существует $V(x)>0$, $\dot V\le 0$ $\Rightarrow$ устойчивость; $\dot V<0$ $\Rightarrow$ асимптотическая устойчивость

Теорема Четаева: существует $V(x)>0$, $\dot V>0$ в любой близости к $0$ $\Rightarrow$ неустойчивость

**3.4. ДУЧП первого порядка**

Линейное: $a(x,y)u_x + b(x,y)u_y + c(x,y)u = d(x,y)$

Квазилинейное: $a(x,y,u)u_x + b(x,y,u)u_y = c(x,y,u)$

Метод характеристик: $\frac{dx}{a} = \frac{dy}{b} = \frac{du}{c}$

Структура общего решения: $\Phi(\phi_1,\phi_2)=0$, где $\phi_1,\phi_2$ — первые интегралы

---

## 4. Дискретная математика

**4.1. Комбинаторика**

Перестановки: $P_n = n!$

Размещения: $A_n^k = \frac{n!}{(n-k)!}$

Сочетания: $\binom{n}{k} = \frac{n!}{k!(n-k)!}$

Формула включений-исключений:

$$|\bigcup_{i=1}^n A_i| = \sum |A_i| - \sum |A_i\cap A_j| + \sum |A_i\cap A_j\cap A_k| - \cdots + (-1)^{n-1}|\bigcap A_i|$$

Бином Ньютона: $(a+b)^n = \sum_{k=0}^n \binom{n}{k} a^{n-k} b^k$

Свойства: $\binom{n}{k}=\binom{n}{n-k}$, $\binom{n}{k}=\binom{n-1}{k-1}+\binom{n-1}{k}$

**4.2. Булевы функции**

Принцип двойственности: замена $\wedge\leftrightarrow\vee$, $0\leftrightarrow1$

СДНФ (совершенная дизъюнктивная нормальная форма):

$$f(x_1,\dots,x_n) = \bigvee_{(a_1,\dots,a_n): f(a)=1} \left( x_1^{a_1} \wedge \cdots \wedge x_n^{a_n} \right),\quad x^a = \begin{cases}x & a=1\\ \neg x & a=0\end{cases}$$

СКНФ (совершенная конъюнктивная нормальная форма):

$$f(x_1,\dots,x_n) = \bigwedge_{(a_1,\dots,a_n): f(a)=0} \left( x_1^{a_1} \vee \cdots \vee x_n^{a_n} \right),\quad x^a = \begin{cases}\neg x & a=1\\ x & a=0\end{cases}$$

Алгоритм Куайна-Мак-Класки: поиск простых импликант $\to$ таблица покрытий

**4.3. Производящие функции. Рекуррентные соотношения**

Производящая функция: $G(x) = \sum_{n\ge0} a_n x^n$

Линейное рекуррентное соотношение: $a_n = c_1 a_{n-1} + \dots + c_k a_{n-k}$

Характеристическое уравнение: $\lambda^k - c_1\lambda^{k-1} - \dots - c_k = 0$

Решение: $a_n = \sum_{i=1}^k P_i(n) \lambda_i^n$, где $P_i(n)$ — многочлены степени (кратность $-1$)

---

## 5. Теория вероятностей и математическая статистика

**5.1. Аксиоматика вероятности**

$\sigma$-алгебра $\mathcal{F}$: замкнута относительно счётных $\cup,\cap,\setminus$

Аксиомы Колмогорова: $P(\Omega)=1$, $P(A)\ge0$, $P(\bigcup_{i=1}^\infty A_i)=\sum_{i=1}^\infty P(A_i)$ для несовместных

Классическая: $P(A)=\frac{|A|}{|\Omega|}$

Геометрическая: $P(A)=\frac{\text{mes}(A)}{\text{mes}(\Omega)}$

**5.2. Условная вероятность. Независимость. Формулы**

Условная: $P(A|B)=\frac{P(A\cap B)}{P(B)}$, $P(B)>0$

Умножения: $P(A\cap B)=P(A)P(B|A)$

Независимость (попарно и в совокупности): $\forall J\subset\{1,\dots,n\}$: $P(\bigcap_{i\in J} A_i)=\prod_{i\in J} P(A_i)$

Полная вероятность: $P(A)=\sum_{i=1}^n P(B_i)P(A|B_i)$, $\{B_i\}$ — разбиение $\Omega$

Формула Байеса: $P(B_k|A)=\frac{P(B_k)P(A|B_k)}{\sum_{i=1}^n P(B_i)P(A|B_i)}$

**5.3. Схема Бернулли. Предельные теоремы**

$P_n(k) = C_n^k p^k q^{n-k}$, $q=1-p$

Локальная Муавр-Лаплас: $P_n(k) \sim \frac{1}{\sqrt{npq}} \varphi\left(\frac{k-np}{\sqrt{npq}}\right)$, $\varphi(x)=\frac{e^{-x^2/2}}{\sqrt{2\pi}}$

Интегральная: $P\left(a \le \frac{k-np}{\sqrt{npq}} \le b\right) \to \Phi(b)-\Phi(a)$, $\Phi(x)=\int_{-\infty}^x \varphi(t)dt$

Пуассон: при $n\to\infty$, $p\to0$, $np\to\lambda$: $P_n(k) \to e^{-\lambda}\frac{\lambda^k}{k!}$

**5.4. Дискретные случайные величины**

Функция распределения: $F(x)=P(X\le x)=\sum_{x_i\le x} p_i$

Свойства: неубывающая, непрерывна справа, $\lim_{x\to-\infty}F(x)=0$, $\lim_{x\to+\infty}F(x)=1$

Биномиальное: $p_k=C_n^k p^k q^{n-k}$, $E=np$, $D=npq$

Пуассона: $p_k=e^{-\lambda}\lambda^k/k!$, $E=\lambda$, $D=\lambda$

Геометрическое: $p_k=(1-p)^{k-1}p$, $k=1,2,\dots$, $E=1/p$

**5.5. Непрерывные случайные величины**

Плотность: $f(x)=F'(x)$, $F(x)=\int_{-\infty}^x f(t)dt$

Свойства: $f(x)\ge0$, $\int_{-\infty}^\infty f(x)dx=1$

Равномерное $[a,b]$: $f(x)=1/(b-a)$, $E=(a+b)/2$, $D=(b-a)^2/12$

Экспоненциальное $\lambda$: $f(x)=\lambda e^{-\lambda x}\;(x\ge0)$, $E=1/\lambda$, $D=1/\lambda^2$

Нормальное $N(\mu,\sigma^2)$: $f(x)=\frac{1}{\sigma\sqrt{2\pi}}e^{-(x-\mu)^2/(2\sigma^2)}$, $E=\mu$, $D=\sigma^2$

Гамма: $f(x)=\frac{\beta^\alpha}{\Gamma(\alpha)}x^{\alpha-1}e^{-\beta x}\;(x>0)$, $E=\alpha/\beta$, $D=\alpha/\beta^2$

**5.6. Многомерные случайные величины**

Совместная ФР: $F(x,y)=P(X\le x, Y\le y)$

Свойства: неубывающая по каждому аргументу, непрерывна справа, $\lim_{x\to-\infty}F(x,y)=0$, $\lim_{x,y\to+\infty}F(x,y)=1$

Совместная плотность: $f(x,y)=\frac{\partial^2 F}{\partial x\partial y}$

Маргинальные: $f_X(x)=\int_{-\infty}^\infty f(x,y)dy$, $f_Y(y)=\int_{-\infty}^\infty f(x,y)dx$

**5.7. Математическое ожидание, дисперсия, ковариация**

Дискретная: $E[X]=\sum_i x_i p_i$

Непрерывная: $E[X]=\int_{-\infty}^\infty x f(x)dx$

Свойства: $E[aX+b]=aE[X]+b$, $E[X+Y]=E[X]+E[Y]$, для независимых $E[XY]=E[X]E[Y]$

Дисперсия: $D[X]=E[(X-E[X])^2]=E[X^2]-(E[X])^2$

Свойства: $D[aX+b]=a^2D[X]$, для независимых $D[X+Y]=D[X]+D[Y]$

Ковариация: $\mathrm{Cov}(X,Y)=E[(X-E[X])(Y-E[Y])]=E[XY]-E[X]E[Y]$

Коэффициент корреляции: $\rho_{XY}=\frac{\mathrm{Cov}(X,Y)}{\sqrt{D[X]D[Y]}}\in[-1,1]$

**5.8. Характеристические функции**

$\phi_X(t)=E[e^{itX}]$

Свойства: $\phi(0)=1$, $|\phi(t)|\le1$, $\phi_{aX+b}(t)=e^{ibt}\phi_X(at)$, функция равномерно непрерывна

Нормальное $N(\mu,\sigma^2)$: $\phi(t)=e^{i\mu t - \sigma^2 t^2/2}$

Пуассона $(\lambda)$: $\phi(t)=e^{\lambda(e^{it}-1)}$

Биномиальное $B(n,p)$: $\phi(t)=(pe^{it}+q)^n$

**5.9. Неравенство Чебышёва. ЗБЧ. ЦПТ**

Неравенство Чебышёва: $P(|X-E[X]|\ge\varepsilon) \le \frac{D[X]}{\varepsilon^2}$

ЗБЧ (Хинчин): $\frac{1}{n}\sum_{i=1}^n X_i \xrightarrow{p} \mu$ для i.i.d.

ЦПТ (Линдеберг-Леви): $\frac{\sum_{i=1}^n X_i - n\mu}{\sigma\sqrt{n}} \xrightarrow{d} N(0,1)$

**5.10. Математическая статистика**

Выборка: $X_1,\dots,X_n$

Вариационный ряд: $X_{(1)}\le X_{(2)}\le\dots\le X_{(n)}$

Эмпирическая функция распределения: $F_n(x) = \frac{1}{n}\sum_{i=1}^n \mathbf{1}_{X_i\le x} = \frac{\#\{X_i\le x\}}{n}$

Гистограмма: оценка плотности, полигон частот: ломаная по серединам интервалов

Выборочное среднее: $\bar{X} = \frac{1}{n}\sum_{i=1}^n X_i$

Выборочная дисперсия: $s^2 = \frac{1}{n}\sum_{i=1}^n (X_i-\bar{X})^2$

**5.11. Оценки параметров**

Несмещённость: $E[\hat{\theta}]=\theta$

Состоятельность: $\hat{\theta}\xrightarrow{p}\theta$

Эффективность: минимальная дисперсия среди несмещённых оценок

Функция правдоподобия: $L(\theta)=\prod_{i=1}^n f(x_i;\theta)$

Оценка максимального правдоподобия (ОМП): $\hat{\theta}_{\text{МП}} = \arg\max\limits_{\theta} L(\theta)$

**5.12. Проверка статистических гипотез**

$H_0$ — нулевая гипотеза, $H_1$ — альтернативная

Ошибка 1-го рода: отвергнуть $H_0$, когда она верна (уровень значимости $\alpha$)

Ошибка 2-го рода: принять $H_0$, когда она неверна ($\beta$)

Мощность критерия: $1-\beta$

Критерий $\chi^2$ Пирсона: $\sum_{i=1}^k \frac{(O_i-E_i)^2}{E_i} \sim \chi^2_{k-1-s}$, где $s$ — число оценённых параметров

---

## 6. Численные методы

**6.1. Ортогонализация Шмидта. Бессель. Парсеваль**

Процесс Грама-Шмидта:

$u_1 = v_1$

$u_k = v_k - \sum_{i=1}^{k-1} \frac{\langle v_k, u_i \rangle}{\langle u_i, u_i \rangle} u_i$

Неравенство Бесселя: $\sum_{k=1}^\infty |\langle f, e_k\rangle|^2 \le \|f\|^2$

Равенство Парсеваля: $\sum_{k=1}^\infty |\langle f, e_k\rangle|^2 = \|f\|^2$ (для полной ортонормированной системы)

**6.2. Интерполяция**

Интерполяционный многочлен Лагранжа:

$$L_n(x) = \sum_{i=0}^n y_i \prod_{j\neq i} \frac{x-x_j}{x_i-x_j}$$

Интерполяционная формула Ньютона (вперёд, равные узлы):

$$P_n(x)=y_0 + q\Delta y_0 + \frac{q(q-1)}{2!}\Delta^2 y_0 + \dots + \frac{q(q-1)\cdots(q-n+1)}{n!}\Delta^n y_0$$

$q=\frac{x-x_0}{h}$, $\Delta$ — конечная разность

**6.3. Численное решение задачи Коши для ОДУ**

Задача: $y'=f(x,y)$, $y(x_0)=y_0$

Метод Эйлера (1-й порядок): $y_{n+1}=y_n + h f(x_n,y_n)$

Метод 2-го порядка (усреднённый): $y_{n+1}=y_n + h f\!\left(x_n+\frac{h}{2}, y_n+\frac{h}{2}f(x_n,y_n)\right)$

Метод Рунге-Кутта 4-го порядка:

$$\begin{cases}
k_1 = f(x_n,y_n)\\
k_2 = f(x_n+\frac{h}{2}, y_n+\frac{h}{2}k_1)\\
k_3 = f(x_n+\frac{h}{2}, y_n+\frac{h}{2}k_2)\\
k_4 = f(x_n+h, y_n+hk_3)\\
y_{n+1}=y_n + \frac{h}{6}(k_1+2k_2+2k_3+k_4)
\end{cases}$$

**6.4. Треугольное разложение. Метод Гаусса**

$A = LU$

$L$ — нижняя треугольная с $l_{ii}=1$, $U$ — верхняя треугольная

Метод Гаусса: прямой ход $\to$ $L,U$; обратный ход: $Ly=b \Rightarrow y$, $Ux=y$

**6.5. Нормы векторов и матриц**

Векторные нормы:

$\|x\|_1=\sum_{i=1}^n |x_i|$

$\|x\|_2=\sqrt{\sum_{i=1}^n x_i^2}$

$\|x\|_\infty=\max_{1\le i\le n}|x_i|$

Подчинённые матричные нормы:

$\|A\|_1 = \max_{1\le j\le n} \sum_{i=1}^n |a_{ij}|$ (максимум по столбцам)

$\|A\|_\infty = \max_{1\le i\le n} \sum_{j=1}^n |a_{ij}|$ (максимум по строкам)

$\|A\|_2 = \sqrt{\rho(A^T A)}$ (спектральная норма)

**6.6. Итерационные методы решения СЛАУ**

$x^{(k+1)} = B x^{(k)} + c$

Сходимость $\iff \rho(B) < 1$ ($\rho$ — спектральный радиус)

Метод Якоби: $x_i^{(k+1)} = \frac{1}{a_{ii}}\left(b_i - \sum_{j\neq i} a_{ij}x_j^{(k)}\right)$

**6.7. Решение нелинейных уравнений**

Одномерный метод Ньютона (2-й порядок): $x_{n+1}=x_n - \frac{f(x_n)}{f'(x_n)}$

Многомерный метод Ньютона: $x_{n+1}=x_n - J_f^{-1}(x_n) f(x_n)$, $J_f$ — матрица Якоби

**6.8. Принцип сжимающего отображения**

$\|F(x)-F(y)\| \le q\|x-y\|$, $q<1$ $\Rightarrow$ существует единственная неподвижная точка $x^*$

Итерации $x_{n+1}=F(x_n)$ сходятся к $x^*$ со скоростью геометрической прогрессии

**6.9. Поиск минимума функций**

Стационарная точка: $\nabla f(x)=0$

Метод градиентного спуска: $x_{k+1}=x_k - \alpha_k \nabla f(x_k)$

Метод наискорейшего спуска: $\alpha_k = \arg\min_{\alpha>0} f(x_k - \alpha \nabla f(x_k))$

---

## 7. Комплексный анализ

**7.1. Функции комплексного переменного. Коши-Риман**

$f(z)=u(x,y)+iv(x,y)$, $z=x+iy$

Условия Коши-Римана: $u_x = v_y$, $u_y = -v_x$

Производная: $f'(z) = u_x + iv_x$

Геометрический смысл: $|f'(z)|$ — коэффициент растяжения, $\arg f'(z)$ — угол поворота

**7.2. Теорема Коши. Интеграл Коши**

Теорема Коши: $\oint_\gamma f(z)dz = 0$ для аналитической $f$ в односвязной области

Интегральная формула Коши: $f(a)=\frac{1}{2\pi i}\oint_\gamma \frac{f(z)}{z-a}dz$

**7.3. Степенные ряды. Ряд Лорана**

Степенной ряд: $\sum_{n=0}^\infty c_n (z-z_0)^n$

Ряд Лорана: $f(z)=\sum_{n=-\infty}^\infty c_n (z-z_0)^n$

$c_n = \frac{1}{2\pi i}\oint_\gamma \frac{f(z)}{(z-z_0)^{n+1}}dz$

**7.4. Особые точки. Вычеты**

Устранимая особая точка: $c_n=0$ для $n<0$

Полюс порядка $m$: конечное число ненулевых $c_n$ при $n<0$ ($c_{-m}\neq0$, $c_n=0$ для $n<-m$)

Существенно особая точка: бесконечное число ненулевых $c_n$ при $n<0$

Вычет в $z_0$: $\mathrm{Res}(f,z_0)=c_{-1} = \frac{1}{2\pi i}\oint_\gamma f(z)dz$

Теорема о вычетах: $\oint_\gamma f(z)dz = 2\pi i \sum_{k=1}^n \mathrm{Res}(f,z_k)$

---

## 8. Уравнения математической физики

**8.1. Пространства Соболева. Теорема вложения**

$W^{k,p}(\Omega) = \{ u\in L^p(\Omega) : D^\alpha u \in L^p(\Omega),\ |\alpha|\le k \}$

Норма: $\|u\|_{W^{k,p}} = \left( \sum_{|\alpha|\le k} \|D^\alpha u\|_{L^p}^p \right)^{1/p}$

Теорема вложения Соболева: $W^{k,p}(\Omega) \hookrightarrow C^{m,\alpha}(\Omega)$ при $kp > n + m\alpha$

**8.2. Эллиптические задачи**

Эллиптический оператор: $Lu = -\sum_{i,j=1}^n \frac{\partial}{\partial x_j}\left(a_{ij}(x)\frac{\partial u}{\partial x_i}\right) + c(x)u$

Фредгольмова разрешимость: $Lu = f$ разрешимо $\iff$ $f$ ортогональна всем решениям сопряжённой однородной задачи

Задача на собственные значения: $Lu = \lambda u$, $u|_{\partial\Omega}=0$

Гладкость обобщённых решений: если $f\in W^{k,2}$, то $u\in W^{k+2,2}$

**8.3. Метод Фурье (разделение переменных)**

$u(x,t)=X(x)T(t)$

Подстановка в уравнение $\to$ $X'' + \lambda X = 0$, $T' + \lambda T = 0$

Разложение по собственным функциям: $u(x,t)=\sum_{n=1}^\infty c_n e^{-\lambda_n t} X_n(x)$

**8.4. Метод Галёркина**

Аппроксимация: $u_n(x,t)=\sum_{k=1}^n c_k(t)\phi_k(x)$, $\{\phi_k\}$ — базис

Проекция невязки: $\int_\Omega (u_{nt} - \Delta u_n) \phi_j dx = 0$, $j=1,\dots,n$

Система ОДУ для $c_k(t)$

**8.5. Полугруппы**

Сильно непрерывная полугруппа: $T(t): X\to X$, $T(0)=I$, $T(t+s)=T(t)T(s)$, $\lim_{t\to0}\|T(t)x-x\|=0$

Решение задачи Коши $u_t = Au$, $u(0)=u_0$: $u(t)=T(t)u_0$

Генератор полугруппы: $Au = \lim_{t\to0}\frac{T(t)u-u}{t}$

---

## 9. Функциональный анализ

**9.1. Теорема Банаха-Штейнгауза (принцип равномерной ограниченности)**

Если $\sup_n \|A_n x\| < \infty$ для каждого $x\in X$, то $\sup_n \|A_n\| < \infty$

($A_n: X\to Y$ — линейные ограниченные операторы, $X$ — банахово)

**9.2. Теорема Банаха об обратном операторе**

Если $A: X\to Y$ — линейный биективный непрерывный оператор, $X,Y$ — банаховы, то $A^{-1}$ непрерывен

---

## 10. Методы оптимизации

**10.1. Необходимые условия экстремума. Эйлер-Лагранж**

Функционал: $J[y]=\int_{x_1}^{x_2} L(x,y,y')dx$

Уравнение Эйлера-Лагранжа (необходимое условие экстремума):

$$\frac{d}{dx}\left( \frac{\partial L}{\partial y'} \right) - \frac{\partial L}{\partial y} = 0$$

**10.2. Достаточные условия. Сопряжённые точки**

Уравнение Якоби: $\frac{d}{dx}\left( \frac{\partial^2 L}{\partial y'^2} h' \right) - \frac{\partial^2 L}{\partial y^2} h = 0$

Сопряжённая точка: $x=\tilde x > x_1$, в которой нетривиальное решение $h(x)$ уравнения Якоби с $h(x_1)=0$ обращается в ноль

Достаточное условие экстремума: если на $[x_1,x_2]$ нет сопряжённых точек, то экстремум достигается
