

Эта тема — сердце математического анализа. Мы свяжем две операции, которые кажутся разными: **дифференцирование** (поиск скорости) и **интегрирование** (поиск площади/накопления).

---

# 1. Первообразная и неопределенный интеграл

### Интуитивное понимание
Если производная отвечает на вопрос «Как быстро меняется функция?», то первообразная решает обратную задачу: «Зная скорость изменения, восстанови саму функцию».
*   **Пример из физики:** Если вы знаете скорость движения тела $v(t)$, то первообразная от скорости — это закон движения (путь) $S(t)$.
*   **Нюанс:** Зная скорость, мы не знаем, где тело находилось в начальный момент. Поэтому первообразных бесконечно много, они отличаются на константу $C$.

### Строгое определение
Функция $F(x)$ называется **первообразной** для функции $f(x)$ на некотором промежутке $X$, если для всех $x \in X$ выполняется равенство:
$$ F'(x) = f(x) $$

**Неопределенным интегралом** от функции $f(x)$ называется совокупность всех её первообразных.
**Обозначение:**

$$ \int f(x) \, dx = F(x) + C $$
где:
*   $\int$ — знак интеграла,
*   $f(x)$ — подынтегральная функция,
*   $dx$ — дифференциал переменной интегрирования,
*   $C$ — произвольная постоянная.

### Свойства
1.  $(\int f(x) \, dx)' = f(x)$ (Производная от интеграла равна подынтегральной функции).
2.  $\int (f(x) \pm g(x)) \, dx = \int f(x) \, dx \pm \int g(x) \, dx$ (Линейность).
3.  $\int k \cdot f(x) \, dx = k \cdot \int f(x) \, dx$ (Вынос константы).

### Типовая задача
**Задача:** Найти неопределенный интеграл $\int (3x^2 + 4\cos x) \, dx$.

**Решение:**
1.  Используем свойство линейности (разбиваем на сумму):

$$ \int 3x^2 \, dx + \int 4\cos x \, dx $$

2.  Выносим константы за знак интеграла:

$$ 3 \int x^2 \, dx + 4 \int \cos x \, dx $$

3.  Используем таблицу интегралов:
    *   Для степенной функции: $\int x^n \, dx = \frac{x^{n+1}}{n+1} + C$. Здесь $n=2$, значит $\int x^2 \, dx = \frac{x^3}{3}$.
    *   Для тригонометрии: $\int \cos x \, dx = \sin x + C$.
4.  Подставляем результаты:

$$ 3 \cdot \frac{x^3}{3} + 4 \cdot \sin x + C $$
5.  Сокращаем тройки и объединяем константы (сумма констант — это снова одна константа $C$):
    
$$ x^3 + 4\sin x + C $$

**Ответ:** $x^3 + 4\sin x + C$.

---

# 2. Определенный интеграл (Интеграл Римана)

### Интуитивное понимание
Если неопределенный интеграл — это *функция*, то определенный интеграл — это *число*.
Геометрически это **площадь криволинейной трапеции**: фигуры, ограниченной сверху графиком функции $y = f(x)$, снизу осью $Ox$, и по бокам прямыми $x = a$ и $x = b$.
*   Если график выше оси $X$, площадь положительна.
*   Если ниже — отрицательна.

### Строгое определение (Конструкция Римана)
Пусть функция $f(x)$ определена на отрезке $[a, b]$.
1.  **Разбиение:** Разделим отрезок $[a, b]$ точками $a = x_0 < x_1 < \dots < x_n = b$ на $n$ частичных отрезков $[x_{i-1}, x_i]$.
2.  **Длина отрезков:** Обозначим $\Delta x_i = x_i - x_{i-1}$.
3.  **Выбор точек:** На каждом отрезке $[x_{i-1}, x_i]$ выберем произвольную точку $\xi_i$.
4.  **Интегральная сумма:** Составим сумму:

$$ S_n = \sum_{i=1}^{n} f(\xi_i) \cdot \Delta x_i $$
    
    (Это сумма площадей прямоугольников, приближающих криволинейную трапецию).
5.  **Предел:** Если существует конечный предел этих сумм при стремлении максимального шага разбиения $\lambda = \max \Delta x_i$ к нулю, и этот предел не зависит от способа разбиения и выбора точек $\xi_i$, то он называется **определенным интегралом Римана**:
   
$$ \int_{a}^{b} f(x) \, dx = \lim_{\lambda \to 0} \sum_{i=1}^{n} f(\xi_i) \cdot \Delta x_i $$

### Геометрический смысл
Число $\int_{a}^{b} f(x) \, dx$ численно равно площади фигуры под графиком функции на отрезке $[a, b]$ (с учетом знака).

### Типовая задача (Концептуальная)
**Задача:** Оценить интеграл $\int_{0}^{1} x \, dx$ геометрически.

**Решение:**
1.  Подынтегральная функция $f(x) = x$.
2.  Отрезок интегрирования $[0, 1]$.
3.  График функции $y = x$ — это прямая линия, проходящая через начало координат под углом 45 градусов.
4.  Фигура под графиком на отрезке $[0, 1]$ — это прямоугольный треугольник с основанием $1$ и высотой $1$ (так как $f(1) = 1$).
5.  Площадь треугольника $S = \frac{1}{2} \cdot \text{основание} \cdot \text{высота} = \frac{1}{2} \cdot 1 \cdot 1 = 0.5$.
6.  Следовательно, значение интеграла должно быть равно 0.5.

**Ответ:** 0.5.

---

# 3. Формула Ньютона-Лейбница

### Интуитивное понимание
Это главная теорема анализа. Она связывает Часть 1 (Первообразная) и Часть 2 (Площадь/Предел сумм).
Она говорит: **Чтобы вычислить площадь (определенный интеграл), не нужно мучиться с пределами сумм. Достаточно найти первообразную и вычесть её значения на концах отрезка.**

### Формулировка
Если функция $f(x)$ непрерывна на отрезке $[a, b]$, а $F(x)$ — любая её первообразная на этом отрезке (то есть $F'(x) = f(x)$), то:

$$ \int_{a}^{b} f(x) \, dx = F(b) - F(a) $$

**Обозначение:** Часто записывают как $\left. F(x) \right|_{a}^{b}$.

### Типовая задача
**Задача:** Вычислить определенный интеграл $\int_{0}^{\pi} \sin x \, dx$.

**Решение:**
1.  **Найти первообразную:** Нам нужна функция $F(x)$, такая что $F'(x) = \sin x$.
    Из таблицы производных знаем: $(\cos x)' = -\sin x$.
    Значит, $(-\cos x)' = \sin x$.
    Берем $F(x) = -\cos x$.
2.  **Применить формулу Ньютона-Лейбница:**

$$ \int_{0}^{\pi} \sin x \, dx = \left. (-\cos x) \right|_{0}^{\pi} $$

3.  **Подставить пределы:**

$$ = (-\cos \pi) - (-\cos 0) $$

4.  **Вычислить значения:**
    *   $\cos \pi = -1$, значит $-\cos \pi = -(-1) = 1$.
    *   $\cos 0 = 1$, значит $-\cos 0 = -1$.
5.  **Итог:**

$$ 1 - (-1) = 1 + 1 = 2 $$

**Геометрическая проверка:** График $\sin x$ на $[0, \pi]$ — это одна "волна" над осью. Площадь этой волны равна 2.

**Ответ:** 2.

---

# Резюме модуля

1.  **Неопределенный интеграл** $\int f(x) dx$ — это множество функций (первообразных). Результат — **функция + C**.
2.  **Определенный интеграл** $\int_{a}^{b} f(x) dx$ — это предел сумм площадей прямоугольников. Результат — **число** (площадь).
3.  **Формула Ньютона-Лейбница** позволяет вычислять определенные интегралы через первообразные: $F(b) - F(a)$.

### Важное замечание о существовании
*   Не всякая функция имеет первообразную (в элементарных функциях), но если она непрерывна — имеет.
*   Не всякая функция интегрируема по Риману (например, функция Дирихле, равная 1 для рациональных и 0 для иррациональных, не интегрируема), но если функция непрерывна на отрезке — она интегрируема.

---

