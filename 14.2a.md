**Нормы векторов и матриц** — это фундаментальные понятия линейной алгебры и функционального анализа, позволяющие измерять "размер" или "величину" векторов и матриц. Они играют ключевую роль в численных методах, теории приближений, оптимизации и многих других областях.

---

### 1. Нормы векторов

#### 1.1 Определение

**Определение.** Нормой вектора \(x \in \mathbb{R}^n\) (или \(\mathbb{C}^n\)) называется функция \(\|\cdot\|: \mathbb{R}^n \to \mathbb{R}\), удовлетворяющая следующим аксиомам:

1.  **Неотрицательность:** \(\|x\| \ge 0\), причем \(\|x\| = 0 \iff x = \mathbf{0}\).
2.  **Однородность:** \(\|\alpha x\| = |\alpha| \cdot \|x\|\) для любого скаляра \(\alpha\).
3.  **Неравенство треугольника:** \(\|x + y\| \le \|x\| + \|y\|\).

#### 1.2 Основные векторные нормы

**1. \(l_p\)-нормы (нормы Гёльдера):** Для \(p \ge 1\):
\[
\|x\|_p = \left( \sum_{i=1}^n |x_i|^p \right)^{1/p}
\]

*   **\(l_1\)-норма (манхэттенская норма):** \(\|x\|_1 = \sum_{i=1}^n |x_i|\).
    *   Используется в задачах разреженности, LASSO-регрессии.
*   **\(l_2\)-норма (евклидова норма):** \(\|x\|_2 = \sqrt{\sum_{i=1}^n x_i^2}\).
    *   Это обычная длина вектора в евклидовом пространстве.
    *   Свойство: \(\|x\|_2^2 = x^T x\).
*   **\(l_\infty\)-норма (равномерная норма):** \(\|x\|_\infty = \max_{1 \le i \le n} |x_i|\).
    *   Получается как предел \(l_p\)-нормы при \(p \to \infty\).

**2. Обобщённое неравенство треугольника для норм:**
\[
|\|x\| - \|y\|| \le \|x - y\|
\]

#### 1.3 Эквивалентность норм в конечномерном пространстве

В конечномерных пространствах **все нормы эквивалентны**. Это означает, что для любых двух норм \(\|\cdot\|_a\) и \(\|\cdot\|_b\) существуют положительные константы \(C_1, C_2\) такие, что для всех \(x\):
\[
C_1 \|x\|_b \le \|x\|_a \le C_2 \|x\|_b
\]

Для \(\mathbb{R}^n\) классические соотношения:
\[
\|x\|_2 \le \|x\|_1 \le \sqrt{n} \|x\|_2
\]
\[
\|x\|_\infty \le \|x\|_2 \le \sqrt{n} \|x\|_\infty
\]
\[
\|x\|_\infty \le \|x\|_1 \le n \|x\|_\infty
\]

---

### 2. Нормы матриц

#### 2.1 Определение

**Определение.** Нормой матрицы \(A \in \mathbb{R}^{m \times n}\) называется функция \(\|\cdot\|\), удовлетворяющая тем же трём аксиомам, что и векторная норма, плюс дополнительное свойство (для квадратных матриц или совместимых размеров):

4.  **Мультипликативное неравенство:** \(\|AB\| \le \|A\| \cdot \|B\|\) (для матриц, произведение которых определено).

#### 2.2 Подчинённые (индуцированные) матричные нормы

Наиболее важный класс матричных норм — нормы, **подчинённые** векторным нормам:
\[
\boxed{\|A\| = \sup_{x \neq 0} \frac{\|Ax\|}{\|x\|} = \max_{\|x\| = 1} \|Ax\|}
\]

Геометрический смысл: максимальное "растяжение", которое матрица \(A\) применяет к единичному вектору.

**Основные подчинённые нормы:**

1.  **Норма, подчинённая \(l_1\)-норме (максимальная сумма по столбцам):**
    \[
    \|A\|_1 = \max_{1 \le j \le n} \sum_{i=1}^m |a_{ij}|
    \]

2.  **Норма, подчинённая \(l_2\)-норме (спектральная норма):**
    \[
    \|A\|_2 = \sqrt{\lambda_{\max}(A^T A)} = \sigma_{\max}(A)
    \]
    где \(\lambda_{\max}\) — максимальное собственное значение \(A^T A\), \(\sigma_{\max}\) — максимальное сингулярное число.

3.  **Норма, подчинённая \(l_\infty\)-норме (максимальная сумма по строкам):**
    \[
    \|A\|_\infty = \max_{1 \le i \le m} \sum_{j=1}^n |a_{ij}|
    \]

#### 2.3 Нормы Фробениуса (не подчинённые)

Норма Фробениуса (евклидова норма матрицы) — это покомпонентная норма:
\[
\|A\|_F = \sqrt{\sum_{i=1}^m \sum_{j=1}^n a_{ij}^2} = \sqrt{\operatorname{tr}(A^T A)} = \sqrt{\sum_{i=1}^{\min(m,n)} \sigma_i^2}
\]

Свойства:
*   \(\|A\|_F\) не является подчинённой никакой векторной норме (кроме случаев, когда \(n=1\) или \(m=1\)).
*   Она согласована с евклидовой векторной нормой: \(\|Ax\|_2 \le \|A\|_F \|x\|_2\).
*   Инвариантна относительно ортогональных преобразований: \(\|U A V\|_F = \|A\|_F\) для ортогональных \(U, V\).

---

### 3. Свойства матричных норм

#### 3.1 Основные неравенства

1.  **Согласованность с векторными нормами:** Если \(\|\cdot\|\) — подчинённая норма, то по определению \(\|Ax\| \le \|A\| \cdot \|x\|\). Для нормы Фробениуса также \(\|Ax\|_2 \le \|A\|_F \|x\|_2\).

2.  **Неравенство для произведения:**
    \[
    \|AB\| \le \|A\| \cdot \|B\|
    \]
    (для подчинённых норм и нормы Фробениуса).

3.  **Оценка спектрального радиуса:**
    \[
    \rho(A) = \max\{|\lambda| : \lambda \text{ — собственное число } A\} \le \|A\|
    \]
    для любой подчинённой нормы.

4.  **Теорема о сходимости итерационного процесса:** Если \(\|A\| < 1\) для некоторой подчинённой нормы, то матрица \(I - A\) обратима, и ряд Неймана \((I - A)^{-1} = \sum_{k=0}^\infty A^k\) сходится.

#### 3.2 Число обусловленности

Важнейшее применение матричных норм — **число обусловленности** матрицы:
\[
\kappa(A) = \|A\| \cdot \|A^{-1}\|
\]
(для вырожденных матриц \(\kappa(A) = \infty\)).

Свойства:
*   \(\kappa(A) \ge 1\).
*   \(\kappa(A)\) показывает чувствительность решения СЛАУ \(Ax = b\) к погрешностям входных данных.
*   Чем больше \(\kappa(A)\), тем хуже обусловлена задача.
*   Для ортогональных матриц \(\kappa_2(A) = 1\) (оптимальная обусловленность).

---

### 4. Сравнение норм

#### 4.1 Соотношения между матричными нормами

Для матрицы \(A \in \mathbb{R}^{n \times n}\):

\[
\|A\|_2 \le \|A\|_F \le \sqrt{n} \|A\|_2
\]
\[
\frac{1}{\sqrt{n}} \|A\|_\infty \le \|A\|_2 \le \sqrt{n} \|A\|_\infty
\]
\[
\frac{1}{\sqrt{n}} \|A\|_1 \le \|A\|_2 \le \sqrt{m} \|A\|_1
\]
\[
\|A\|_2^2 \le \|A\|_1 \cdot \|A\|_\infty
\]

#### 4.2 Сводная таблица

| Норма | Обозначение | Формула | Тип |
|:---|:---|:---|:---|
| **Векторная \(l_1\)** | \(\|x\|_1\) | \(\sum |x_i|\) | Гёльдера |
| **Векторная \(l_2\)** | \(\|x\|_2\) | \(\sqrt{\sum x_i^2}\) | Гёльдера |
| **Векторная \(l_\infty\)** | \(\|x\|_\infty\) | \(\max |x_i|\) | Гёльдера |
| **Матричная \(l_1\)** | \(\|A\|_1\) | \(\max_j \sum_i |a_{ij}|\) | Подчинённая |
| **Матричная \(l_2\) (спектральная)** | \(\|A\|_2\) | \(\sqrt{\lambda_{\max}(A^T A)}\) | Подчинённая |
| **Матричная \(l_\infty\)** | \(\|A\|_\infty\) | \(\max_i \sum_j |a_{ij}|\) | Подчинённая |
| **Фробениуса** | \(\|A\|_F\) | \(\sqrt{\sum_{i,j} a_{ij}^2}\) | Не подчинённая |

#### Резюме

| Свойство | Формулировка |
|:---|:---|
| **Аксиомы нормы** | 1. \(\|x\| \ge 0\), 2. \(\|\alpha x\| = |\alpha|\|x\|\), 3. \(\|x+y\| \le \|x\| + \|y\|\) |
| **Подчинённая норма** | \(\|A\| = \max_{\|x\|=1} \|Ax\|\) |
| **Спектральная норма** | \(\|A\|_2 = \sigma_{\max}(A)\) |
| **Число обусловленности** | \(\kappa(A) = \|A\| \cdot \|A^{-1}\|\) |
| **Эквивалентность норм** | Существуют константы \(c_1, c_2\): \(c_1 \|x\|_a \le \|x\|_b \le c_2 \|x\|_a\) |
