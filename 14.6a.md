**Поиск минимума функций** — одна из центральных задач оптимизации, имеющая огромное значение в науке, инженерии, машинном обучении и экономике. Мы рассмотрим основные понятия и два фундаментальных метода первого порядка: градиентный спуск и его модификацию — метод наискорейшего спуска.

---

## 1. Постановка задачи и основные понятия

### 1.1 Задача безусловной оптимизации

Требуется найти точку \(x^* \in \mathbb{R}^n\), в которой дифференцируемая функция \(f: \mathbb{R}^n \to \mathbb{R}\) достигает минимума:
\[
\min_{x \in \mathbb{R}^n} f(x)
\]

### 1.2 Стационарные точки

**Определение.** Точка \(x^*\) называется **стационарной точкой** функции \(f\), если градиент в этой точке равен нулю:
\[
\nabla f(x^*) = 0
\]

Стационарные точки могут быть:
*   **Точками локального минимума** (если матрица Гессе \(H(x^*)\) положительно определена)
*   **Точками локального максимума** (если \(H(x^*)\) отрицательно определена)
*   **Седловыми точками** (если \(H(x^*)\) неопределена)

**Необходимое условие экстремума первого порядка:** Если \(x^*\) — точка локального минимума и \(f\) дифференцируема, то \(x^*\) — стационарная точка.

**Достаточное условие локального минимума:** Если \(\nabla f(x^*) = 0\) и матрица Гессе \(H(x^*)\) положительно определена, то \(x^*\) — точка строгого локального минимума.

---

## 2. Метод градиентного спуска

### 2.1 Идея метода

Градиент функции \(\nabla f(x)\) указывает направление **наискорейшего возрастания** функции. Следовательно, антиградиент \(-\nabla f(x)\) указывает направление наискорейшего убывания. Двигаясь в этом направлении, мы можем уменьшать значение функции.

### 2.2 Итерационная формула

\[
x^{(k+1)} = x^{(k)} - \alpha_k \nabla f(x^{(k)})
\]
где:
*   \(x^{(k)}\) — текущее приближение
*   \(\nabla f(x^{(k)})\) — градиент в текущей точке
*   \(\alpha_k > 0\) — длина шага (learning rate)

### 2.3 Выбор длины шага

Способ выбора \(\alpha_k\) критически влияет на сходимость:

1.  **Постоянный шаг:** \(\alpha_k = \text{const}\). Просто, но требует аккуратного подбора. Слишком большой шаг — расходимость; слишком маленький — медленная сходимость.
2.  **Убывающий шаг:** \(\alpha_k = \frac{C}{k}\) или \(\alpha_k = \frac{C}{\sqrt{k}}\). Обеспечивает сходимость для выпуклых функций.
3.  **Адаптивный выбор** — например, метод наискорейшего спуска.

### 2.4 Сходимость градиентного спуска

Для выпуклых функций с липшицевым градиентом:
\[
f(x^{(k)}) - f(x^*) \le \frac{\|x^{(0)} - x^*\|^2}{2\alpha k}
\]
при оптимальном выборе шага. Это скорость \(O(1/k)\).

Для сильно выпуклых функций (с константой сильной выпуклости \(\mu\)) скорость становится геометрической:
\[
\|x^{(k)} - x^*\| \le C q^k, \quad q < 1
\]

---

## 3. Метод наискорейшего спуска

### 3.1 Идея метода

Метод наискорейшего спуска — это градиентный спуск, в котором на каждой итерации длина шага выбирается **оптимальным образом** из условия минимума функции вдоль направления антиградиента:
\[
\alpha_k = \arg\min_{\alpha > 0} \varphi(\alpha) = f(x^{(k)} - \alpha \nabla f(x^{(k)}))
\]

### 3.2 Одномерная минимизация

На каждом шаге решается вспомогательная одномерная задача минимизации:
\[
\min_{\alpha > 0} f(x^{(k)} - \alpha g_k), \quad g_k = \nabla f(x^{(k)})
\]

Для квадратичных функций эта задача решается аналитически. Для общего случая используют методы одномерного поиска:
*   Золотое сечение
*   Метод парабол
*   Метод с использованием условий Вольфа

### 3.3 Свойства метода наискорейшего спуска

1.  **Монотонность:** На каждой итерации функция строго убывает (если не достигнут минимум).
2.  **Ортогональность:** Направления поиска на соседних итерациях ортогональны:
    \[
    g_{k+1}^T g_k = 0
    \]
    Это следует из условия оптимальности шага.
3.  **Сходимость:** Для квадратичных функций сходимость линейная, но с коэффициентом, зависящим от обусловленности задачи.

### 3.4 Пример: квадратичная функция

Рассмотрим \(f(x) = \frac{1}{2}x^T A x\), где \(A\) — симметричная положительно определённая матрица.

Градиент: \(\nabla f(x) = A x\).

Метод наискорейшего спуска:
\[
x^{(k+1)} = x^{(k)} - \alpha_k A x^{(k)}
\]
\[
\alpha_k = \frac{(A x^{(k)})^T (A x^{(k)})}{(A x^{(k)})^T A (A x^{(k)})} = \frac{g_k^T g_k}{g_k^T A g_k}
\]

Скорость сходимости определяется числом обусловленности \(\kappa = \lambda_{\max}/\lambda_{\min}\):
\[
\|x^{(k)} - x^*\|_A \le \left( \frac{\kappa - 1}{\kappa + 1} \right)^{2k} \|x^{(0)} - x^*\|_A
\]

---

## 4. Сравнение методов

| Характеристика | Градиентный спуск | Наискорейший спуск |
|:---|:---|:---|
| **Выбор шага** | Постоянный или убывающий | Оптимальный (одномерный поиск) |
| **Трудоёмкость итерации** | Одно вычисление градиента | Одно вычисление градиента + одномерный поиск |
| **Скорость сходимости** | \(O(1/k)\) или геометрическая | Геометрическая |
| **Гарантия убывания** | Не всегда | Да (при точном поиске) |
| **Чувствительность к параметрам** | Высокая (нужно подбирать шаг) | Низкая |

---

## 5. Практические модификации

### 5.1 Метод сопряжённых градиентов

Использует не просто антиградиент, а специальные сопряжённые направления, что даёт сходимость за конечное число шагов для квадратичных функций и более быструю сходимость в общем случае.

### 5.2 Методы с инерцией (momentum)

\[
v^{(k+1)} = \beta v^{(k)} + \alpha \nabla f(x^{(k)})
\]
\[
x^{(k+1)} = x^{(k)} - v^{(k+1)}
\]

Позволяет ускорить сходимость в овражистых задачах.

### 5.3 Адаптивные методы (Adam, RMSprop)

Адаптируют шаг для каждой компоненты вектора на основе статистики градиентов.

---

## 6. Критерии остановки

1.  По норме градиента: \(\|\nabla f(x^{(k)})\| < \varepsilon_g\).
2.  По изменению функции: \(|f(x^{(k+1)}) - f(x^{(k)})| < \varepsilon_f\).
3.  По изменению аргумента: \(\|x^{(k+1)} - x^{(k)}\| < \varepsilon_x\).
4.  По достижении максимального числа итераций.

#### Резюме

| Понятие | Определение |
|:---|:---|
| **Стационарная точка** | \(\nabla f(x) = 0\) |
| **Градиентный спуск** | \(x^{(k+1)} = x^{(k)} - \alpha \nabla f(x^{(k)})\) |
| **Наискорейший спуск** | \(\alpha_k = \arg\min f(x^{(k)} - \alpha \nabla f(x^{(k)}))\) |
| **Сходимость** | Линейная (геометрическая) для сильно выпуклых функций |
| **Ортогональность** | \(g_{k+1}^T g_k = 0\) для наискорейшего спуска |
