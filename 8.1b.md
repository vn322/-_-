
Тема **Теорем Ляпунова и Четаева** — это вершина теории устойчивости дифференциальных уравнений. Эти теоремы позволяют судить о поведении решений системы в окрестности точки равновесия, не находя сами решения явно. Это критически важно для анализа динамических систем в физике, технике и биологии.

---

# 1. Интуитивное понимание

### Что такое устойчивость?
Представьте шарик в ландшафте:
*   **Устойчивое равновесие:** Шарик лежит на дне ямы. Если его слегка толкнуть, он покатается и вернётся обратно (или останется рядом).
*   **Неустойчивое равновесие:** Шарик лежит на вершине горки. Малейший толчок — и он укатится далеко.

**Прямой метод Ляпунова** позволяет определить тип равновесия, не решая дифференциальные уравнения, а с помощью специальной функции — **функции Ляпунова**.
*   Аналогия: Функция Ляпунова — это «энергия» системы. Если энергия убывает со временем, система стремится к покою (устойчива). Если энергия растет — система разваливается (неустойчива).

---

# 2. Строгие определения

### Автономная система уравнений
Система вида:

$$
\frac{dx_i}{dt} = f_i(x_1, x_2, \dots, x_n), \quad i = 1, \dots, n
$$

или в векторной форме:

$$
\dot{\mathbf{x}} = \mathbf{f}(\mathbf{x})
$$

где $\mathbf{x} \in \mathbb{R}^n$. Система называется **автономной**, так как правая часть не зависит явно от времени $t$.

### Положение равновесия
Точка $\mathbf{x}_0$ называется **положением равновесия** (или стационарной точкой), если:

$$
\mathbf{f}(\mathbf{x}_0) = \mathbf{0}
$$

В этой точке производные равны нулю, система «застывает». Без потери общности считаем $\mathbf{x}_0 = \mathbf{0}$ (начало координат).

### Устойчивость по Ляпунову
Положение равновесия $\mathbf{x} = \mathbf{0}$ называется **устойчивым**, если:

$$
\forall \varepsilon > 0 \quad \exists \delta > 0 \quad : \quad \|\mathbf{x}(0)\| < \delta \implies \|\mathbf{x}(t)\| < \varepsilon \quad \forall t \ge 0
$$

(Если стартуем близко, то остаемся близко).

**Асимптотическая устойчивость:**
Если система устойчива и дополнительно:

$$
\lim_{t \to \infty} \mathbf{x}(t) = \mathbf{0}
$$

(Если стартуем близко, то не только остаемся рядом, но и стремимся к нулю)

---

# 3. Теорема Ляпунова об устойчивости

### Формулировка
Пусть для системы $\dot{\mathbf{x}} = \mathbf{f}(\mathbf{x})$ существует функция $V(\mathbf{x})$ (функция Ляпунова), определенная в окрестности начала координат, такая что:

1.  **Знакоопределенность:** $V(\mathbf{x})$ положительно определена:

$$
    V(\mathbf{0}) = 0, \quad V(\mathbf{x}) > 0 \quad \text{при } \mathbf{x} \neq \mathbf{0}
    $$
    
2.  **Производная в силу системы:** Полная производная функции $V$ вдоль траекторий системы неположительна:

$$
    \dot{V}(\mathbf{x}) = \sum_{i=1}^{n} \frac{\partial V}{\partial x_i} f_i(\mathbf{x}) \le 0
    $$

Тогда положение равновесия $\mathbf{x} = \mathbf{0}$ **устойчиво**.

### Усиленная теорема (Асимптотическая устойчивость)
Если дополнительно $\dot{V}(\mathbf{x})$ **отрицательно определена** ($\dot{V}(\mathbf{0}) = 0$, $\dot{V}(\mathbf{x}) < 0$ при $\mathbf{x} \neq \mathbf{0}$), то положение равновесия **асимптотически устойчиво**.

### Геометрический смысл
*   $V(\mathbf{x}) = C$ — это замкнутые поверхности (как слои луковицы) вокруг начала координат.
*   Условие $\dot{V} \le 0$ означает, что траектории системы пересекают эти поверхности только внутрь (или касаются их).
*   Система не может «вырваться» наружу.

---

# 4. Теорема Четаева о неустойчивости

### Интуитивное понимание
Если мы хотим доказать **неустойчивость**, нам нужно показать, что существует хотя бы одна траектория, которая уходит от начала координат.
Четаев предложил конструкцию: найти область, где функция $V$ положительна и растет вдоль траекторий.

### Формулировка
Пусть существует функция $V(\mathbf{x})$ и область $G$ в окрестности начала координат, такие что:

1.  **Область $G$:** Граница области содержит начало координат ($\mathbf{0} \in \partial G$).
2.  **Знак функции:** $V(\mathbf{x}) > 0$ внутри $G$ и $V(\mathbf{x}) = 0$ на границе $\partial G$ (в окрестности нуля).
3.  **Производная:** $\dot{V}(\mathbf{x}) > 0$ для всех $\mathbf{x} \in G$.

Тогда положение равновесия $\mathbf{x} = \mathbf{0}$ **неустойчиво**.

### Геометрический смысл
В области $G$ функция $V$ положительна и строго растет. Траектория, попавшая в $G$, вынуждена двигаться в сторону увеличения $V$, то есть уходить от границы (где $V=0$) и от начала координат. Рано или поздно она покинет любую малую окрестность нуля.

---

# 5. Типовые задачи и решения

### Задача 1. Исследование устойчивости (Теорема Ляпунова)
**Задание:** Исследовать на устойчивость положение равновесия системы:

$$
\begin{cases}
\dot{x} = -x + y \\
\dot{y} = -x - y
\end{cases}
$$

**Решение:**
1.  **Положение равновесия:** Приравниваем правые части к нулю. Очевидно, $(0, 0)$.
2.  **Выбор функции Ляпунова:** Попробуем простую квадратичную форму (аналог энергии):

$$
    V(x, y) = x^2 + y^2
    $$
    
Она положительно определена ($V > 0$ при $(x,y) \neq (0,0)$).

3.  **Вычислим производную в силу системы:**

$$
    \dot{V} = \frac{\partial V}{\partial x} \dot{x} + \frac{\partial V}{\partial y} \dot{y} = 2x(-x + y) + 2y(-x - y)
    $$

$$
    \dot{V} = -2x^2 + 2xy - 2xy - 2y^2 = -2(x^2 + y^2)
    $$
    
4.  **Анализ знака:**
    $\dot{V} = -2(x^2 + y^2)$ отрицательно определена ($\dot{V} < 0$ при $(x,y) \neq (0,0)$).
5.  **Вывод:** По усиленной теореме Ляпунова, положение равновесия **асимптотически устойчиво**.

**Ответ:** Асимптотическая устойчивость.

---

### Задача 2. Исследование неустойчивости (Теорема Четаева)
**Задание:** Исследовать на устойчивость систему:

$$
\begin{cases}
\dot{x} = x + y^2 \\
\dot{y} = y
\end{cases}
$$

**Решение:**
1.  **Положение равновесия:** $(0, 0)$.
2.  **Попытка Ляпунова:** Если взять $V = x^2 + y^2$, то $\dot{V} = 2x(x+y^2) + 2y(y) = 2x^2 + 2xy^2 + 2y^2$. Знак не очевиден (есть слагаемое $2xy^2$).
3.  **Применим теорему Четаева.**
    Возьмем функцию $V(x, y) = x$.
    Определим область $G$: $x > 0$ (правая полуплоскость).
4.  **Проверка условий:**
    *   В области $G$: $V = x > 0$.
    *   На границе ($x=0$): $V = 0$. Начало координат на границе.
    *   Производная:
$$
        \dot{V} = \frac{\partial V}{\partial x} \dot{x} = 1 \cdot (x + y^2) = x + y^2
        $$
        
В области $G$ ($x > 0$): $\dot{V} = x + y^2 > 0$.

5.  **Вывод:** Все условия теоремы Четаева выполнены. Положение равновесия **неустойчиво**.

**Ответ:** Неустойчивость.

---

### Задача 3. Устойчивость, но не асимптотическая
**Задание:** Исследовать систему (математический маятник без трения в линеаризации):

$$
\begin{cases}
\dot{x} = y \\
\dot{y} = -x
\end{cases}
$$

**Решение:**
1.  **Функция Ляпунова:** Полная энергия (кинетическая + потенциальная):
$$
    V(x, y) = \frac{1}{2}x^2 + \frac{1}{2}y^2
    $$

Положительно определена.

2.  **Производная:**

$$
    \dot{V} = x \dot{x} + y \dot{y} = x(y) + y(-x) = xy - xy = 0
    $$
    
3.  **Анализ:**
    $\dot{V} \equiv 0$. Производная неположительна ($\le 0$), но не отрицательна.
    Это означает, что $V$ сохраняется (система консервативна). Траектории лежат на линиях уровня $V = const$ (окружностях).
4.  **Вывод:** По основной теореме Ляпунова система **устойчива**. Но так как $\dot{V}$ не отрицательно определена, асимптотической устойчивости нет (маятник не затухает, а колеблется вечно).

**Ответ:** Устойчивость (но не асимптотическая).

---

### Задача 4. Подбор функции Ляпунова
**Задание:** Для системы $\dot{x} = -x^3, \dot{y} = -y$ подобрать функцию Ляпунова и доказать асимптотическую устойчивость.

**Решение:**
1.  Попробуем сумму степенных функций, подбирая степени под уравнения:

$$
    V(x, y) = \frac{1}{4}x^4 + \frac{1}{2}y^2
    $$
    
(Степени выбраны так, чтобы при дифференцировании сократились степени в правых частях).

2.  **Проверка знака:** $V > 0$ при $(x,y) \neq (0,0)$, $V(0,0) = 0$.
3.  **Производная:**

$$
    \dot{V} = x^3 \cdot (-x^3) + y \cdot (-y) = -x^6 - y^2
    $$
    
4.  **Анализ:** $\dot{V}$ отрицательно определена.
5.  **Вывод:** Асимптотическая устойчивость.

**Ответ:** $V = \frac{1}{4}x^4 + \frac{1}{2}y^2$, асимптотическая устойчивость.

---

### Задача 5. Сравнение с методом линеаризации
**Задание:** Почему метод функций Ляпунова мощнее метода линеаризации (по собственным числам матрицы Якоби)?

**Решение (Качественный ответ):**
1.  **Метод линеаризации:** Работает только если все собственные числа матрицы Якоби имеют ненулевую вещественную часть. Если есть чисто мнимые или нулевые собственные числа (критический случай), линеаризация не дает ответа.
2.  **Метод Ляпунова:** Работает даже в критических случаях.
    *   *Пример:* Система из Задачи 3 ($\dot{x}=y, \dot{y}=-x$). Собственные числа $\lambda = \pm i$ (мнимые). Линеаризация не говорит об устойчивости нелинейной системы. Функция Ляпунова доказывает устойчивость точно.
3.  **Глобальность:** Функция Ляпунова может доказать устойчивость «в целом» (при любых начальных условиях), а линеаризация — только локально.

**Ответ:** Метод Ляпунова применим в критических случаях и дает глобальные результаты.

---

# Резюме модуля

| Теорема | Условия на $V$ | Условия на $\dot{V}$ | Вывод |
|:---:|:---:|:---:|:---:|
| **Ляпунова (устойч.)** | $V > 0$ | $\dot{V} \le 0$ | Устойчивость |
| **Ляпунова (асимпт.)** | $V > 0$ | $\dot{V} < 0$ | Асимптотическая устойчивость |
| **Четаева (неустойч.)** | $V > 0$ в области $G$ | $\dot{V} > 0$ в $G$ | Неустойчивость |

**Ключевые выводы:**
1.  **Функция Ляпунова** — это обобщение понятия энергии.
2.  **Главная сложность:** Не существует универсального алгоритма построения функции Ляпунова. Это искусство, требующее интуиции и опыта (часто пробуют квадратичные формы).
3.  **Применение:** Анализ устойчивости сложных нелинейных систем, где аналитическое решение невозможно.

