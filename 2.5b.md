
Тема **Линейных операторов** объединяет всё, что мы изучали в линейной алгебре: векторы, матрицы, системы уравнений. Оператор — это «функция», которая принимает вектор на входе и выдает вектор на выходе, сохраняя при этом линейную структуру пространства.

---

# 1. Интуитивное понимание

### Что такое оператор?
В обычном анализе функция $y = f(x)$ принимает число и выдает число.
В линейной алгебре **оператор** $\mathcal{A}$ принимает **вектор** $\mathbf{x}$ и выдает **вектор** $\mathbf{y}$.

$$
\mathbf{y} = \mathcal{A}(\mathbf{x})
$$

Представьте себе машину-трансформер. Вы кладете в неё стрелку (вектор), она её поворачивает, растягивает или проецирует на плоскость, и выдает новую стрелку.

### Что значит «линейный»?
Линейность означает, что оператор уважает операции сложения и умножения на число.
*   Если вы сложите два вектора *перед* подачей в оператор, результат будет таким же, как если бы вы подали их по отдельности, а потом сложили результаты.
*   Если вы растянете входной вектор в $k$ раз, выходной тоже растянется в $k$ раз.

---

# 2. Строгое определение

### Линейный оператор
Пусть $V$ и $W$ — два векторных пространства над одним полем (например, $\mathbb{R}$).
Отображение $\mathcal{A}: V \to W$ называется **линейным оператором**, если для любых векторов $\mathbf{x}, \mathbf{y} \in V$ и любого скаляра $\lambda \in \mathbb{R}$ выполняются два условия:

1.  **Аддитивность (сохранение суммы):**

$$
    \mathcal{A}(\mathbf{x} + \mathbf{y}) = \mathcal{A}(\mathbf{x}) + \mathcal{A}(\mathbf{y})
$$

2.  **Однородность (сохранение умножения на скаляр):**

$$
    \mathcal{A}(\lambda \mathbf{x}) = \lambda \mathcal{A}(\mathbf{x})
$$

Эти два условия можно объединить в одно (условие линейности):

$$
\mathcal{A}(\alpha \mathbf{x} + \beta \mathbf{y}) = \alpha \mathcal{A}(\mathbf{x}) + \beta \mathcal{A}(\mathbf{y})
$$

### Ядро и Образ
Важные характеристики оператора:
*   **Ядро ($\text{Ker } \mathcal{A}$):** Множество всех векторов, которые оператор переводит в ноль.

$$ \text{Ker } \mathcal{A} = \{ \mathbf{x} \in V \mid \mathcal{A}(\mathbf{x}) = \mathbf{0} \} $$

*   **Образ ($\text{Im } \mathcal{A}$):** Множество всех векторов в $W$, которые можно получить действием оператора.

$$ \text{Im } \mathcal{A} = \{ \mathbf{y} \in W \mid \exists \mathbf{x} \in V, \mathcal{A}(\mathbf{x}) = \mathbf{y} \} $$

---

# 3. Примеры линейных операторов

1.  **Нулевой оператор ($\mathcal{O}$):**
    Переводит любой вектор в нулевой: $\mathcal{O}(\mathbf{x}) = \mathbf{0}$.
2.  **Тождественный оператор ($\mathcal{E}$ или $I$):**
    Оставляет вектор без изменений: $\mathcal{E}(\mathbf{x}) = \mathbf{x}$.
3.  **Оператор поворота на угол $\alpha$ в плоскости $\mathbb{R}^2$:**
    Поворачивает каждый вектор против часовой стрелки. Линейность очевидна геометрически.
4.  **Оператор дифференцирования ($D$):**
    Действует в пространстве многочленов. $D(P(x)) = P'(x)$.
    Проверка: $(f+g)' = f' + g'$ и $(\lambda f)' = \lambda f'$.
5.  **Оператор умножения на матрицу:**
    Пусть $A$ — матрица $m \times n$. Оператор $\mathcal{A}(\mathbf{x}) = A\mathbf{x}$ действует из $\mathbb{R}^n$ в $\mathbb{R}^m$. Это самый важный пример для вычислений.

### Пример нелинейного оператора
Оператор сдвига: $\mathcal{B}(\mathbf{x}) = \mathbf{x} + \mathbf{b}$, где $\mathbf{b} \neq \mathbf{0}$.
Проверка: $\mathcal{B}(\mathbf{0}) = \mathbf{0} + \mathbf{b} = \mathbf{b} \neq \mathbf{0}$.
Линейный оператор обязательно должен переводить ноль в ноль. Значит, сдвиг нелинеен.

---

# 4. Способы задания линейных операторов

### 1. Действие на базисе
Линейный оператор однозначно определяется тем, как он действует на базисные векторы пространства.
Пусть $\{\mathbf{e}_1, \dots, \mathbf{e}_n\}$ — базис в $V$. Если мы знаем векторы $\mathcal{A}(\mathbf{e}_1), \dots, \mathcal{A}(\mathbf{e}_n)$, то для любого вектора $\mathbf{x} = x_1 \mathbf{e}_1 + \dots + x_n \mathbf{e}_n$ действие оператора вычисляется по линейности:

$$
\mathcal{A}(\mathbf{x}) = x_1 \mathcal{A}(\mathbf{e}_1) + \dots + x_n \mathcal{A}(\mathbf{e}_n)
$$

### 2. Матрица оператора
Если в пространствах $V$ и $W$ выбраны базисы, то оператору можно сопоставить **матрицу**.
Столбцы матрицы оператора — это координаты образов базисных векторов.
Если $\mathbf{y} = \mathcal{A}(\mathbf{x})$, то в координатной форме это записывается как умножение матрицы на столбец:

$$
Y = A \cdot X
$$

где $A$ — матрица оператора, $X$ — столбец координат вектора $\mathbf{x}$, $Y$ — столбец координат вектора $\mathbf{y}$.

---

# 5. Действия над линейными операторами

Множество линейных операторов само образует линейное пространство.

1.  **Сумма операторов:**

$$ (\mathcal{A} + \mathcal{B})(\mathbf{x}) = \mathcal{A}(\mathbf{x}) + \mathcal{B}(\mathbf{x}) $$

В матричной форме: $C = A + B$.

2.  **Умножение на скаляр:**

$$ (\lambda \mathcal{A})(\mathbf{x}) = \lambda \cdot \mathcal{A}(\mathbf{x}) $$

В матричной форме: $C = \lambda A$.

3.  **Композиция (Произведение) операторов:**
    Последовательное применение: сначала $\mathcal{B}$, потом $\mathcal{A}$.

$$ (\mathcal{A} \mathcal{B})(\mathbf{x}) = \mathcal{A}(\mathcal{B}(\mathbf{x})) $$

В матричной форме: $C = A \cdot B$.
    **Важно:** Умножение операторов (как и матриц) **некоммутативно**: $\mathcal{A}\mathcal{B} \neq \mathcal{B}\mathcal{A}$ в общем случае.

---

# 6. Типовые задачи и решения

### Задача 1. Проверка на линейность
**Задание:** Является ли оператор $\mathcal{A}: \mathbb{R}^2 \to \mathbb{R}^2$, заданный формулой $\mathcal{A}(x, y) = (x + 1, y)$, линейным?

**Решение:**
1.  Проверим условие $\mathcal{A}(\mathbf{0}) = \mathbf{0}$.
    Нулевой вектор в $\mathbb{R}^2$ это $(0, 0)$.

$$ \mathcal{A}(0, 0) = (0 + 1, 0) = (1, 0) \neq (0, 0) $$

2.  Так как нулевой вектор не переходит в нулевой, оператор нелинейный.

(Это оператор сдвига вдоль оси $X$).

**Ответ:** Нет, оператор нелинейный.

---

### Задача 2. Нахождение матрицы оператора
**Задание:** Оператор $\mathcal{A}$ в базисе $\{\mathbf{e}_1, \mathbf{e}_2\}$ действует следующим образом:

$$ \mathcal{A}(\mathbf{e}_1) = 2\mathbf{e}_1 + \mathbf{e}_2, \quad \mathcal{A}(\mathbf{e}_2) = -\mathbf{e}_1 + 3\mathbf{e}_2 $$

Найти матрицу оператора $A$ и найти образ вектора $\mathbf{x} = 3\mathbf{e}_1 - \mathbf{e}_2$.

**Решение:**
1.  **Матрица оператора:**
    Столбцы матрицы — это координаты образов базисных векторов.

1-й столбец (образ $\mathbf{e}_1$): $\begin{pmatrix} 2 \\ 1 \end{pmatrix}$

2-й столбец (образ $\mathbf{e}_2$): $\begin{pmatrix} -1 \\ 3 \end{pmatrix}$

$$ A = \begin{pmatrix} 2 & -1 \\ 1 & 3 \end{pmatrix} $$

2.  **Образ вектора:**
    Координаты вектора $\mathbf{x}$ в данном базисе: $X = \begin{pmatrix} 3 \\ -1 \end{pmatrix}$.
    Умножим матрицу на столбец:

$$ Y = A X = \begin{pmatrix} 2 & -1 \\ 1 & 3 \end{pmatrix} \begin{pmatrix} 3 \\ -1 \end{pmatrix} = \begin{pmatrix} 2 \cdot 3 + (-1) \cdot (-1) \\ 1 \cdot 3 + 3 \cdot (-1) \end{pmatrix} = \begin{pmatrix} 6 + 1 \\ 3 - 3 \end{pmatrix} = \begin{pmatrix} 7 \\ 0 \end{pmatrix} $$

Значит, $\mathcal{A}(\mathbf{x}) = 7\mathbf{e}_1 + 0\mathbf{e}_2 = 7\mathbf{e}_1$.

**Ответ:** Матрица 

$$A = \begin{pmatrix} 2 & -1 
\\ 
1 & 3 \end{pmatrix}$$

образ 

$$\mathcal{A}(\mathbf{x}) = 7\mathbf{e}_1$$

---

### Задача 3. Композиция операторов
**Задание:** Даны операторы в $\mathbb{R}^2$ с матрицами 

$A = \begin{pmatrix} 1 & 0 
\\ 
0 & 0 \end{pmatrix}$ 

(проекция на ось $X$) и 

$B = \begin{pmatrix} 0 & 0 
\\
0 & 1 \end{pmatrix}$

(проекция на ось $Y$). Найти матрицу оператора $\mathcal{C} = \mathcal{A} + \mathcal{B}$ и $\mathcal{D} = \mathcal{A}\mathcal{B}$.

**Решение:**
1.  **Сумма:**

$$ C = A + B = \begin{pmatrix} 1 & 0 \\ 0 & 0 \end{pmatrix} + \begin{pmatrix} 0 & 0 \\ 0 & 1 \end{pmatrix} = \begin{pmatrix} 1 & 0 \\ 0 & 1 \end{pmatrix} = I $$

Сумма проекций на оси дает тождественный оператор.

2.  **Произведение (Композиция):**

$$ D = A \cdot B = \begin{pmatrix} 1 & 0 \\ 0 & 0 \end{pmatrix} \begin{pmatrix} 0 & 0 \\ 0 & 1 \end{pmatrix} = \begin{pmatrix} 1\cdot0 + 0\cdot0 & 1\cdot0 + 0\cdot1 \\ 0\cdot0 + 0\cdot0 & 0\cdot0 + 0\cdot1 \end{pmatrix} = \begin{pmatrix} 0 & 0 \\ 0 & 0 \end{pmatrix} = O $$

Если сначала спроецировать на $Y$ (обнулив $x$), а потом на $X$ (обнулив $y$), останется нулевой вектор.

**Ответ:** $C = I$ (единичная матрица), $D = O$ (нулевая матрица).

---

### Задача 4. Нахождение оператора по действию на векторы
**Задание:** Линейный оператор $\mathcal{A}: \mathbb{R}^2 \to \mathbb{R}^2$ переводит векторы $\mathbf{a} = (1, 1)$ в $\mathbf{a}' = (2, 3)$ и $\mathbf{b} = (1, -1)$ в $\mathbf{b}' = (0, 1)$. Найти матрицу оператора в стандартном базисе.

**Решение:**
1.  Пусть матрица оператора $A = \begin{pmatrix} a & b \\ c & d \end{pmatrix}$.
2.  Запишем уравнения $A\mathbf{a} = \mathbf{a}'$ и $A\mathbf{b} = \mathbf{b}'$:

$$ \begin{pmatrix} a & b \\ c & d \end{pmatrix} \begin{pmatrix} 1 \\ 1 \end{pmatrix} = \begin{pmatrix} 2 \\ 3 \end{pmatrix} \implies \begin{cases} a + b = 2 \\ c + d = 3 \end{cases} $$

$$ \begin{pmatrix} a & b \\ c & d \end{pmatrix} \begin{pmatrix} 1 \\ -1 \end{pmatrix} = \begin{pmatrix} 0 \\ 1 \end{pmatrix} \implies \begin{cases} a - b = 0 \\ c - d = 1 \end{cases} $$

3.  Решаем системы:
    *   $a + b = 2$ и $a - b = 0 \implies 2a = 2 \implies a = 1, b = 1$.
    *   $c + d = 3$ и $c - d = 1 \implies 2c = 4 \implies c = 2, d = 1$.
4.  Матрица:

$$ A = \begin{pmatrix} 1 & 1 \\ 2 & 1 \end{pmatrix} $$

**Ответ:** $A = \begin{pmatrix} 1 & 1 \\ 2 & 1 \end{pmatrix}$.

---

# Резюме модуля

1.  **Линейный оператор:** Отображение векторов, сохраняющее сложение и умножение на число ($\mathcal{A}(\mathbf{0})=\mathbf{0}$).
2.  **Матрица оператора:** Числовое представление оператора в выбранном базисе. Столбцы — образы базисных векторов.
3.  **Действия:** Операторы можно складывать, умножать на число и перемножать (композиция).
4.  **Связь с координатами:** Действие оператора на вектор эквивалентно умножению матрицы оператора на столбец координат вектора ($Y = AX$).
5.  **Ядро и Образ:** Характеризуют «потери» информации и достижимость результатов оператором.



Жду твоего выбора!
