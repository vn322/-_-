Мы переходим от числовых рядов (сумма чисел) к функциональным (сумма функций). Это ключевой момент для понимания степенных рядов, рядов Фурье и многих приложений в физике.

Здесь появляется новое, тонкое понятие — **равномерная сходимость**. Именно оно позволяет нам смело работать с бесконечными суммами функций как с обычными многочленами.

---

# 1. Функциональный ряд и поточечная сходимость

### Интуитивное понимание
В числовом ряду члены были числами: $a_1 + a_2 + \dots$.
В функциональном ряду члены — это функции от переменной $x$: $u_1(x) + u_2(x) + \dots$.
Если мы зафиксируем конкретное значение $x = x_0$, то получится обычный числовой ряд. Если он сходится, то в этой точке функция определена.
**Область сходимости** — это множество всех таких $x$, где ряд сходится.

### Строгое определение
Пусть дана последовательность функций $\{u_n(x)\}$, определенных на множестве $E$.
Выражение

$$
\sum_{n=1}^{\infty} u_n(x) = u_1(x) + u_2(x) + \dots + u_n(x) + \dots
$$

называется **функциональным рядом**.

**Поточечная сходимость:**
Ряд сходится в точке $x_0 \in E$, если сходится числовой ряд $\sum u_n(x_0)$.
Если ряд сходится в каждой точке множества $E$, говорят, что он сходится на $E$ **поточечно**. Сумма ряда — это функция $S(x) = \lim_{n \to \infty} S_n(x)$, где $S_n(x)$ — $n$-я частичная сумма.

### Типовая задача 1
**Задание:** Найти область сходимости ряда $\sum_{n=1}^{\infty} \frac{x^n}{n}$.

**Решение:**
1.  Это степенной ряд. Применим признак Даламбера для числового ряда при фиксированном $x$.

$$
\lim_{n \to \infty} \left| \frac{u_{n+1}}{u_n} \right| = \lim_{n \to \infty} \left| \frac{x^{n+1}}{n+1} \cdot \frac{n}{x^n} \right| = |x| \cdot \lim_{n \to \infty} \frac{n}{n+1} = |x|
$$

2.  По признаку Даламбера ряд сходится, если $|x| < 1$, и расходится, если $|x| > 1$.
3.  Исследуем границы ($x = 1$ и $x = -1$):
    *   При $x = 1$: ряд $\sum \frac{1}{n}$ (гармонический) — расходится.
    *   При $x = -1$: ряд $\sum \frac{(-1)^n}{n}$ (знакочередующийся) — сходится условно.
4.  **Область сходимости:** $[-1, 1)$.

**Ответ:** $x \in [-1, 1)$.

---

# 2. Равномерная сходимость

### Интуитивное понимание
При поточечной сходимости скорость приближения $S_n(x)$ к $S(x)$ может быть разной в разных точках. Где-то быстро, где-то медленно.
**Равномерная сходимость** означает, что ряд сходится «одинаково быстро» во всех точках множества.
*   **Зачем это нужно?** Только при равномерной сходимости сохраняются важные свойства: непрерывность суммы, возможность почленного интегрирования и дифференцирования.

### Строгое определение (Коши)
Функциональный ряд $\sum u_n(x)$ сходится **равномерно** на множестве $E$, если:

$$
\forall \varepsilon > 0 \quad \exists N(\varepsilon) \quad : \quad \forall n > N, \quad \forall p \ge 1, \quad \forall x \in E \quad \implies \quad \left| \sum_{k=n+1}^{n+p} u_k(x) \right| < \varepsilon
$$

Или через остаток ряда $r_n(x) = S(x) - S_n(x)$:

$$
\lim_{n \to \infty} \left( \sup_{x \in E} |S(x) - S_n(x)| \right) = 0
$$

**Главное отличие:** Номер $N$ зависит только от $\varepsilon$, но **не зависит от $x$**.

### Наглядный пример
Ряд $\sum x^n$ на интервале $(-1, 1)$.
*   В точке $x = 0.5$ сходится быстро.
*   В точке $x = 0.99$ сходится очень медленно.
*   Чем ближе $x$ к 1, тем больше $N$ нужно для заданной точности.
*   **Вывод:** На всем интервале $(-1, 1)$ сходимость **не равномерная**.
*   Но на любом отрезке $[-a, a]$, где $0 < a < 1$, сходимость **равномерная**.

---

# 3. Признак Вейерштрасса (М-признак)

Это самый удобный инструмент для проверки равномерной сходимости.

### Формулировка
Если для членов ряда $\sum u_n(x)$ на множестве $E$ можно подобрать такие числа $M_n \ge 0$, что:
1.  $|u_n(x)| \le M_n$ для всех $x \in E$ (мажоранта),
2.  Числовой ряд $\sum M_n$ сходится,

то функциональный ряд $\sum u_n(x)$ сходится на $E **абсолютно и равномерно**.

### Интуитивное понимание
Мы «зажимаем» наш функциональный ряд между двумя числами. Если «верхний» числовой ряд сходится, то и наш функциональный «не убежит» и сойдется равномерно.

### Типовая задача 2
**Задание:** Исследовать на равномерную сходимость ряд $\sum_{n=1}^{\infty} \frac{\cos(nx)}{n^2}$ на всей числовой прямой $x \in \mathbb{R}$.

**Решение:**
1.  Оценим модуль общего члена:

$$
|u_n(x)| = \left| \frac{\cos(nx)}{n^2} \right| = \frac{|\cos(nx)|}{n^2}
$$

2.  Так как $|\cos(nx)| \le 1$ для любых $x$, то:

$$
|u_n(x)| \le \frac{1}{n^2}
$$

3.  Возьмем $M_n = \frac{1}{n^2}$.
4.  Ряд $\sum_{n=1}^{\infty} M_n = \sum_{n=1}^{\infty} \frac{1}{n^2}$ сходится (обобщенный гармонический ряд, $p=2 > 1$).
5.  По признаку Вейерштрасса исходный ряд сходится **равномерно** на $\mathbb{R}$.

**Ответ:** Ряд сходится равномерно на $\mathbb{R}$.

---

# 4. Свойства равномерно сходящихся рядов

Это самая важная часть теории. Она legitимизирует наши действия с бесконечными суммами.

### 1. Непрерывность суммы
**Теорема:** Если все члены $u_n(x)$ непрерывны на $E$ и ряд сходится **равномерно** на $E$, то сумма ряда $S(x)$ также непрерывна на $E$.
*   *Пример нарушения:* Если сходимость неравномерная, сумма может иметь разрыв, даже если все члены непрерывны.

### 2. Почленное интегрирование
**Теорема:** Если $u_n(x)$ непрерывны на $[a, b]$ и ряд сходится **равномерно** на $[a, b]$, то можно интегрировать почленно:

$$
\int_{a}^{b} \left( \sum_{n=1}^{\infty} u_n(x) \right) dx = \sum_{n=1}^{\infty} \int_{a}^{b} u_n(x) \, dx
$$

### 3. Почленное дифференцирование
**Теорема:** Чтобы можно было дифференцировать почленно:

$$
\left( \sum_{n=1}^{\infty} u_n(x) \right)' = \sum_{n=1}^{\infty} u_n'(x)
$$

нужно выполнение **трех** условий:
1.  Исходный ряд $\sum u_n(x)$ сходится хотя бы в одной точке.
2.  Все $u_n(x)$ имеют непрерывные производные.
3.  Ряд из производных $\sum u_n'(x)$ сходится **равномерно**.

---

# Типовые задачи на свойства

### Задача 3 (Почленное интегрирование)
**Задание:** Вычислить сумму ряда $\sum_{n=0}^{\infty} x^n$ при $|x| < 1$ и проинтегрировать его от 0 до $t$ ($|t| < 1$).

**Решение:**
1.  Мы знаем сумму геометрической прогрессии:

$$
S(x) = \sum_{n=0}^{\infty} x^n = 1 + x + x^2 + \dots = \frac{1}{1-x}, \quad |x| < 1
$$

На любом отрезке $[-a, a] \subset (-1, 1)$ сходимость равномерная (по признаку Вейерштрасса), значит, можно интегрировать почленно.
2.  Интегрируем левую часть почленно:

$$
\int_{0}^{t} \left( \sum_{n=0}^{\infty} x^n \right) dx = \sum_{n=0}^{\infty} \int_{0}^{t} x^n \, dx = \sum_{n=0}^{\infty} \frac{t^{n+1}}{n+1}
$$

3.  Интегрируем правую часть (функцию суммы):

$$
\int_{0}^{t} \frac{1}{1-x} \, dx = \left. -\ln|1-x| \right|_{0}^{t} = -\ln(1-t)
$$

4.  Приравниваем результаты:

$$
\sum_{n=1}^{\infty} \frac{t^n}{n} = -\ln(1-t)
$$

    (Мы сдвинули индекс $n+1 \to n$ ).

**Вывод:** Мы получили разложение логарифма в степенной ряд, законность чего гарантирована равномерной сходимостью.

---

### Задача 4 (Проверка условия дифференцирования)
**Задание:** Можно ли дифференцировать ряд $\sum_{n=1}^{\infty} \frac{\sin(nx)}{n^3}$ почленно?

**Решение:**
1.  Проверим сходимость исходного ряда. По признаку Вейерштрасса ($|\frac{\sin(nx)}{n^3}| \le \frac{1}{n^3}$), ряд сходится равномерно на $\mathbb{R}$.
2.  Составим ряд из производных:

$$
u_n'(x) = \left( \frac{\sin(nx)}{n^3} \right)' = \frac{n \cos(nx)}{n^3} = \frac{\cos(nx)}{n^2}
$$

3.  Исследуем ряд $\sum \frac{\cos(nx)}{n^2}$ на равномерную сходимость.
    Снова используем признак Вейерштрасса: $|\frac{\cos(nx)}{n^2}| \le \frac{1}{n^2}$.
    Ряд $\sum \frac{1}{n^2}$ сходится.
    Значит, ряд производных сходится равномерно.
4.  **Вывод:** Все условия теоремы выполнены. Почленное дифференцирование законно.

**Ответ:** Да, можно.

---

# Резюме модуля

1.  **Функциональный ряд:** Сумма функций $S(x)$. Область сходимости находится как для числового ряда при фиксированном $x$.
2.  **Равномерная сходимость:** Скорость сходимости не зависит от $x$. Критерий: $\sup |S(x) - S_n(x)| \to 0$.
3.  **Признак Вейерштрасса:** Если ряд мажорируется сходящимся числовым рядом, то он сходится равномерно.
4.  **Свойства:**
    *   Равномерная сходимость + непрерывные члены $\to$ непрерывная сумма.
    *   Равномерная сходимость $\to$ можно интегрировать почленно.
    *   Равномерная сходимость ряда производных $\to$ можно дифференцировать почленно.

