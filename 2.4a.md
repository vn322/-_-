Мы переходим к одной из самых важных тем линейной алгебры, которая имеет огромное значение в механике, квантовой физике, анализе данных (метод главных компонент) и многих других областях. Собственные векторы и значения позволяют понять внутреннюю структуру линейного преобразования, найти направления, в которых оно действует самым простым образом — простым растяжением или сжатием.

---

### 1. Основные понятия

#### 1.1 Определение собственного вектора и собственного значения

Пусть $\(A\)$ — квадратная матрица размера $\(n \times n\)$ (линейное преобразование в $\(n\)$-мерном пространстве).

**Определение.** Ненулевой вектор $\(\mathbf{x} \neq \mathbf{0}\)$ называется **собственным вектором** матрицы $\(A\)$, если найдется такое число $\(\lambda\)$ , что:

$$\[
\boxed{A\mathbf{x} = \lambda \mathbf{x}}
\]$$

Число $\(\lambda\)$ называется **собственным значением** матрицы $\(A\)$, соответствующим данному собственному вектору $\(\mathbf{x}\)$.

**Геометрический смысл:** При действии матрицы $\(A\)$ на собственный вектор $\(\mathbf{x}\)$ результат коллинеарен исходному вектору. Вектор не поворачивается, а лишь растягивается (если $\(|\lambda| > 1\))$, сжимается (если $\(0 < |\lambda| < 1\)$ ) или меняет направление на противоположное (если $\(\lambda < 0\)$ ). Направление собственного вектора — это "главная ось" преобразования.

#### 1.2 Собственное подпространство

Множество всех собственных векторов, соответствующих данному собственному значению $\(\lambda\)$ , вместе с нулевым вектором образует линейное подпространство, называемое **собственным подпространством**. Его размерность равна геометрической кратности $\(\lambda\)$ (максимальному числу линейно независимых собственных векторов).

---

### 2. Характеристический многочлен

Как найти собственные значения? Для этого нужно решить уравнение $\(A\mathbf{x} = \lambda \mathbf{x}\)$.

#### 2.1 Вывод характеристического уравнения

Перепишем равенство $\(A\mathbf{x} = \lambda \mathbf{x}\)$ в виде:

$$\[
A\mathbf{x} - \lambda \mathbf{x} = \mathbf{0} \quad \Rightarrow \quad (A - \lambda E)\mathbf{x} = \mathbf{0}
\]$$

где $\(E\)$ — единичная матрица.

Мы получили однородную систему линейных уравнений относительно \(\mathbf{x}\). Чтобы у неё существовало ненулевое решение ($\(\mathbf{x} \neq \mathbf{0}\)$ ), необходимо и достаточно, чтобы её определитель был равен нулю (иначе была бы только нулевое решение, $а \(\mathbf{x}\)$ должен быть ненулевым):

$$\[
\boxed{\det(A - \lambda E) = 0}
\]$$

#### 2.2 Определение характеристического многочлена

**Определение.** Многочлен степени $\(n\)$ от переменной $\(\lambda\)$:

$$\[
P_A(\lambda) = \det(A - \lambda E)
\]$$

называется **характеристическим многочленом** матрицы $\(A\)$.

Уравнение $\(\det(A - \lambda E) = 0\)$ называется **характеристическим уравнением**. Его корни $\(\lambda_1, \lambda_2, \dots, \lambda_n\)$ (с учетом кратностей) и есть собственные значения матрицы $\(A\)$.

**Свойства характеристического многочлена:**
1.  Степень многочлена равна порядку матрицы $\(n\)$.
2.  Коэффициенты многочлена являются инвариантами матрицы (не меняются при переходе к подобным матрицам).
3.  След матрицы $\(\trace A = \sum_{i=1}^n a_{ii}\)$ равен сумме всех корней характеристического многочлена (с учетом кратностей): $\(\lambda_1 + \lambda_2 + \dots + \lambda_n = \trace A\)$.
4.  Определитель матрицы $\(\det A\)$ равен произведению всех корней: $\(\lambda_1 \lambda_2 \dots \lambda_n = \det A\)$.

---

### 3. Алгоритм нахождения собственных векторов и значений

1.  **Находим собственные значения:**
    Составляем и решаем характеристическое уравнение $\(\det(A - \lambda E) = 0\)$. Находим все корни $\(\lambda_1, \lambda_2, \dots, \lambda_k\)$ (с их кратностями).

2.  **Для каждого $\(\lambda_i\)$ находим собственные векторы:**
    Подставляем $\(\lambda_i\)$ в матрицу $\((A - \lambda_i E)\)$ и решаем однородную систему:

    $$\[
    (A - \lambda_i E) \mathbf{x} = \mathbf{0}
    \]$$
    
    Находим фундаментальную систему решений (ФСР) этой системы. Ненулевые решения этой системы и будут собственными векторами, соответствующими $\(\lambda_i\)$. Любая линейная комбинация этих векторов (кроме нулевой) также будет собственным вектором.

---

### 4. Пример

Найдем собственные значения и векторы для матрицы $\(A = \begin{pmatrix} 2 & 1 \\ 1 & 2 \end{pmatrix}\)$.

**Шаг 1. Характеристическое уравнение:**

$$\[
\det(A - \lambda E) = \begin{vmatrix} 2-\lambda & 1 \\ 1 & 2-\lambda \end{vmatrix} = (2-\lambda)^2 - 1 = \lambda^2 - 4\lambda + 3 = 0
\]$$

Корни: $\(\lambda_1 = 1\), \(\lambda_2 = 3\)$.

**Шаг 2. Собственные векторы для $\(\lambda_1 = 1\)$ :**

$$\[
A - 1 \cdot E = \begin{pmatrix} 1 & 1 \\ 1 & 1 \end{pmatrix}
\]$$

Решаем систему $\(\begin{pmatrix} 1 & 1 \\ 1 & 1 \end{pmatrix} \begin{pmatrix} x_1 \\ x_2 \end{pmatrix} = \begin{pmatrix} 0 \\ 0 \end{pmatrix}\)$.
Получаем одно уравнение $\(x_1 + x_2 = 0\), т.е. \(x_2 = -x_1\)$.
ФСР: $\(\mathbf{x}_1 = \begin{pmatrix} 1 \\ -1 \end{pmatrix}\)$. Все собственные векторы для $\(\lambda = 1\)$ имеют вид $\(C \cdot \begin{pmatrix} 1 \\ -1 \end{pmatrix}\), \(C \neq 0\)$.

**Шаг 3. Собственные векторы для $\(\lambda_2 = 3\)$ :**

$$\[
A - 3E = \begin{pmatrix} -1 & 1 \\ 1 & -1 \end{pmatrix}
\]$$

Решаем $\(\begin{pmatrix} -1 & 1 \\ 1 & -1 \end{pmatrix} \begin{pmatrix} x_1 \\ x_2 \end{pmatrix} = \begin{pmatrix} 0 \\ 0 \end{pmatrix}\)$.
Получаем $\(-x_1 + x_2 = 0\), т.е. \(x_2 = x_1\)$.
ФСР: $\(\mathbf{x}_2 = \begin{pmatrix} 1 \\ 1 \end{pmatrix}\)$. Все собственные векторы для $\(\lambda = 3\)$ имеют вид $\(C \cdot \begin{pmatrix} 1 \\ 1 \end{pmatrix}\), \(C \neq 0\)$.

---

### 5. Свойства собственных векторов и значений

1.  **Линейная независимость:** Собственные векторы, соответствующие **различным** собственным значениям, линейно независимы.
2.  **Симметричные матрицы:** Если матрица $\(A\)$ симметрична ($\(A^T = A\)$ ), то все её собственные значения действительны, а собственные векторы, соответствующие разным собственным значениям, ортогональны.
3.  **След и определитель:**
    *   $\(\tr A = \sum \lambda_i\)$ (сумма собственных значений с учетом кратности).
    *   $\(\det A = \prod \lambda_i\)$ (произведение собственных значений с учетом кратности).
4.  **Обратимость:** Матрица обратима тогда и только тогда, когда все её собственные значения отличны от нуля.

#### Резюме

| Понятие | Формула / Свойство |
| :--- | :--- |
| **Собственный вектор** | $\(A\mathbf{x} = \lambda \mathbf{x}\), \(\mathbf{x} \neq \mathbf{0}\)$ |
| **Собственное значение** | Число $\(\lambda\)$ из уравнения $\(A\mathbf{x} = \lambda \mathbf{x}\)$ |
| **Характеристический многочлен** | $\(P_A(\lambda) = \det(A - \lambda E)\)$ |
| **Характеристическое уравнение** | $\(\det(A - \lambda E) = 0\)$ |
| **След** | $\(\sum \lambda_i = \tr A\)$ |
| **Определитель** | $\(\prod \lambda_i = \det A\)$ |
| **Симметричная матрица** | Все $\(\lambda_i\)$ действительны, собственные векторы ортогональны |
