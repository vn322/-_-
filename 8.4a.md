Мы соединяем две важные темы: определитель Вронского (инструмент для проверки линейной независимости решений) и линейное неоднородное уравнение (общая теория).

---

### 1. Определитель Вронского (вронскиан)

#### 1.1 Определение

Пусть дана система из $\(n\) функций \(y_1(x), y_2(x), \dots, y_n(x)\)$, имеющих производные до $\((n-1)\)$-го порядка включительно.

**Определение.** **Определителем Вронского** (или **вронскианом**) называется определитель матрицы, составленной из этих функций и их производных:

$(W(x)) = (\W[y_1, y_2, \dots, y_n](x))$  

$$\begin{vmatrix}
y_1(x) & y_2(x) & \dots & y_n(x) \\
y_1'(x) & y_2'(x) & \dots & y_n'(x) \\
\vdots & \vdots & \ddots & \vdots \\
y_1^{(n-1)}(x) & y_2^{(n-1)}(x) & \dots & y_n^{(n-1)}(x)
\end{vmatrix}
\]$$

Для двух функций ($\(n=2\)$ ):

$$\[
W(x) = \begin{vmatrix} y_1 & y_2 \\ y_1' & y_2' \end{vmatrix} = y_1 y_2' - y_2 y_1'
\]$$

#### 1.2 Свойства вронскиана

1.  **Линейная зависимость $\(\Rightarrow\) \(W(x) \equiv 0\)$:** Если функции $\(y_1, \dots, y_n\)$ линейно зависимы на интервале, то их вронскиан тождественно равен нулю на этом интервале.
2.  **Для решений ЛОДУ верно и обратное:** Если $\(y_1, \dots, y_n\)$ — решения линейного однородного дифференциального уравнения (ЛОДУ) $\(n\)$-го порядка, и их вронскиан не равен нулю хотя бы в одной точке интервала, то эти решения линейно независимы на всем интервале.
3.  **Формула Лиувилля-Остроградского:** Для решений ЛОДУ $\(y^{(n)} + a_1(x) y^{(n-1)} + \dots + a_n(x) y = 0\)$ вронскиан вычисляется по формуле:
$$\[
    W(x) = W(x_0) \exp\left( -\int_{x_0}^x a_1(t) \, dt \right)
    \]$$

Для уравнения второго порядка \(y'' + p(x) y' + q(x) y = 0\):

$$\[
    W(x) = W(x_0) \exp\left( -\int_{x_0}^x p(t) \, dt \right)
    \]$$

---

### 2. Линейное неоднородное уравнение (ЛНДУ)

#### 2.1 Общий вид

Рассмотрим линейное неоднородное дифференциальное уравнение $\(n\)$-го порядка:

$$\[
y^{(n)} + a_1(x) y^{(n-1)} + \dots + a_n(x) y = f(x)
\]$$

где $\(f(x) \not\equiv 0\)$ — правая часть (неоднородность).

Соответствующее однородное уравнение (ЛОДУ):

$$\[
y^{(n)} + a_1(x) y^{(n-1)} + \dots + a_n(x) y = 0
\]$$

#### 2.2 Структура общего решения ЛНДУ

**Теорема (о структуре общего решения).** Общее решение $\(y_{\text{общ}}\)$ линейного неоднородного уравнения равно сумме общего решения $\(y_{\text{о.о.}}\)$ соответствующего однородного уравнения и любого частного решения $\(y_{\text{ч}}\)$ неоднородного уравнения:

$$\[
\boxed{y_{\text{общ}} = y_{\text{о.о.}} + y_{\text{ч}}}
\]$$

где
*   $\(y_{\text{о.о.}} = C_1 y_1 + C_2 y_2 + \dots + C_n y_n\)$ — общее решение ЛОДУ (линейная комбинация фундаментальной системы решений),
*   $\(y_{\text{ч}}\)$ — любое частное решение ЛНДУ.

#### 2.3 Методы нахождения частного решения

1.  **Метод подбора (неопределенных коэффициентов):** Применяется, когда коэффициенты постоянны, а правая часть имеет специальный вид (многочлены, экспоненты, синусы, косинусы и их комбинации).
2.  **Метод вариации произвольных постоянных (метод Лагранжа):** Универсальный метод, применимый для любых правых частей (и даже для уравнений с переменными коэффициентами).

---

### 3. Связь вронскиана с методом вариации постоянных

Для уравнения второго порядка $\(y'' + p(x) y' + q(x) y = f(x)\)$ метод вариации дает частное решение в виде:

$$\[
y_{\text{ч}}(x) = y_1(x) \int \frac{-y_2(x) f(x)}{W(x)} \, dx + y_2(x) \int \frac{y_1(x) f(x)}{W(x)} \, dx
\]$$

где $\(y_1, y_2\)$ — фундаментальная система решений ЛОДУ, а $\(W(x)\)$ — их вронскиан.

**Важно:** Знаменатель $\(W(x)\)$ не равен нулю (так как решения линейно независимы).

#### Резюме

| Понятие | Формула / Свойство |
|:---|:---|
| **Вронскиан (для двух функций)** | $\(W(x) = y_1 y_2' - y_2 y_1'\)$ |
| **Критерий линейной независимости решений ЛОДУ** | $\(W(x) \neq 0\)$ хотя бы в одной точке |
| **Формула Лиувилля-Остроградского** | $\(W(x) = W(x_0) \exp\left( -\int_{x_0}^x p(t) dt \right)\)$ |
| **Общее решение ЛНДУ** | $\(y_{\text{общ}} = y_{\text{о.о.}} + y_{\text{ч}}\)$ |
| **Метод вариации постоянных** | $\(y_{\text{ч}} = y_1 \int \frac{-y_2 f}{W} dx + y_2 \int \frac{y_1 f}{W} dx\)$ |
