**Характеристическая функция** — это один из самых мощных инструментов теории вероятностей. Она представляет собой преобразование Фурье распределения и позволяет значительно упростить доказательства многих теорем (включая ЦПТ), работу с суммами независимых величин и анализ свойств распределений.

---

### 1. Определение характеристической функции

#### 1.1 Определение

**Определение.** **Характеристической функцией** случайной величины \(X\) называется функция \(\varphi_X(t): \mathbb{R} \to \mathbb{C}\), определяемая формулой:
\[
\boxed{\varphi_X(t) = \mathbb{E}\left[e^{itX}\right]}
\]
где \(i\) — мнимая единица, \(t \in \mathbb{R}\).

Для дискретной величины с рядом распределения \(P(X = x_k) = p_k\):
\[
\varphi_X(t) = \sum_k p_k e^{it x_k}
\]

Для непрерывной величины с плотностью \(f(x)\):
\[
\varphi_X(t) = \int_{-\infty}^{+\infty} e^{itx} f(x) \, dx
\]

#### 1.2 Почему это работает?

Характеристическая функция всегда существует для любого распределения (в отличие от производящей функции моментов, которая может расходиться), поскольку \(|e^{itX}| = 1\) и математическое ожидание ограниченной величины всегда конечно.

---

### 2. Основные свойства характеристических функций

#### 2.1 Элементарные свойства

1.  **Ограниченность:** \(|\varphi_X(t)| \le \varphi_X(0) = 1\) для всех \(t\).
2.  **Сопряженная симметрия:** \(\varphi_X(-t) = \overline{\varphi_X(t)}\) (черта означает комплексное сопряжение).
3.  **Непрерывность:** \(\varphi_X(t)\) равномерно непрерывна на \(\mathbb{R}\).
4.  **Значение в нуле:** \(\varphi_X(0) = 1\).

#### 2.2 Свойства, связанные с преобразованиями случайных величин

5.  **Линейное преобразование:** Для \(Y = aX + b\):
    \[
    \varphi_Y(t) = e^{itb} \varphi_X(at)
    \]

6.  **Сумма независимых величин:** Если \(X\) и \(Y\) независимы, то характеристическая функция суммы равна произведению характеристических функций:
    \[
    \varphi_{X+Y}(t) = \varphi_X(t) \cdot \varphi_Y(t)
    \]
    Это свойство делает характеристические функции незаменимыми при изучении сумм независимых величин.

#### 2.3 Связь с моментами

Если у случайной величины \(X\) существует момент \(k\)-го порядка (\(\mathbb{E}[|X|^k] < \infty\)), то характеристическая функция \(k\) раз дифференцируема, и:
\[
\boxed{\mathbb{E}[X^k] = \frac{\varphi_X^{(k)}(0)}{i^k}}
\]

В частности:
*   \(\mathbb{E}[X] = \frac{\varphi'(0)}{i}\)
*   \(\mathbb{E}[X^2] = -\varphi''(0)\)

#### 2.4 Теорема единственности

**Теорема.** Характеристическая функция однозначно определяет распределение. То есть если \(\varphi_X(t) = \varphi_Y(t)\) для всех \(t\), то \(X\) и \(Y\) имеют одинаковое распределение.

Это позволяет идентифицировать распределения по их характеристическим функциям.

#### 2.5 Формула обращения

По характеристической функции можно восстановить функцию распределения (или плотность, если она существует).

Для непрерывной случайной величины с плотностью \(f(x)\):
\[
f(x) = \frac{1}{2\pi} \int_{-\infty}^{+\infty} e^{-itx} \varphi_X(t) \, dt
\]

#### 2.6 Критерий сходимости (теорема Леви)

**Теорема Леви о непрерывности.** Если последовательность характеристических функций \(\varphi_n(t)\) сходится к некоторой функции \(\varphi(t)\) для каждого \(t\), и \(\varphi(t)\) непрерывна в нуле, то \(\varphi(t)\) является характеристической функцией некоторого распределения, и соответствующие распределения слабо сходятся к этому распределению.

Это ключевой результат для доказательства ЦПТ.

---

### 3. Характеристические функции основных распределений

#### 3.1 Дискретные распределения

| Распределение | Вероятность \(P(X = k)\) | Характеристическая функция \(\varphi(t)\) |
|:---|:---|:---|
| **Вырожденное** \(X = c\) | \(P(X = c) = 1\) | \(e^{itc}\) |
| **Бернулли** \(Ber(p)\) | \(p\) при \(k=1\), \(1-p\) при \(k=0\) | \(1 - p + p e^{it}\) |
| **Биномиальное** \(Bin(n, p)\) | \(\binom{n}{k} p^k (1-p)^{n-k}\) | \((1 - p + p e^{it})^n\) |
| **Пуассона** \(Pois(\lambda)\) | \(\frac{\lambda^k}{k!} e^{-\lambda}\) | \(\exp\left(\lambda (e^{it} - 1)\right)\) |
| **Геометрическое** \(Geom(p)\) (число неудач) | \((1-p)^k p\) | \(\frac{p}{1 - (1-p)e^{it}}\) |

#### 3.2 Непрерывные распределения

| Распределение | Плотность \(f(x)\) | Характеристическая функция \(\varphi(t)\) |
|:---|:---|:---|
| **Равномерное** \(U[a, b]\) | \(\frac{1}{b-a}\) на \([a, b]\) | \(\frac{e^{itb} - e^{ita}}{it(b-a)}\) |
| **Нормальное** \(N(\mu, \sigma^2)\) | \(\frac{1}{\sigma\sqrt{2\pi}} e^{-\frac{(x-\mu)^2}{2\sigma^2}}\) | \(\exp\left(i\mu t - \frac{\sigma^2 t^2}{2}\right)\) |
| **Экспоненциальное** \(Exp(\lambda)\) | \(\lambda e^{-\lambda x}, x \ge 0\) | \(\frac{\lambda}{\lambda - it}\) |
| **Гамма** \(\Gamma(\alpha, \beta)\) | \(\frac{\beta^\alpha}{\Gamma(\alpha)} x^{\alpha-1} e^{-\beta x}\) | \(\left(1 - \frac{it}{\beta}\right)^{-\alpha}\) |
| **Коши** \(Cauchy(\mu, \gamma)\) | \(\frac{1}{\pi\gamma\left[1 + \left(\frac{x-\mu}{\gamma}\right)^2\right]}\) | \(\exp\left(i\mu t - \gamma|t|\right)\) |

**Важное замечание:** У распределения Коши нет математического ожидания, но характеристическая функция существует и имеет простой вид.

---

### 4. Примеры использования

#### Пример 1: Сумма независимых пуассоновских величин

Пусть \(X \sim Pois(\lambda_1)\), \(Y \sim Pois(\lambda_2)\), независимы. Тогда:
\[
\varphi_X(t) = e^{\lambda_1(e^{it}-1)}, \quad \varphi_Y(t) = e^{\lambda_2(e^{it}-1)}
\]
Для суммы \(Z = X + Y\):
\[
\varphi_Z(t) = \varphi_X(t) \varphi_Y(t) = e^{(\lambda_1 + \lambda_2)(e^{it}-1)}
\]
Это характеристическая функция распределения \(Pois(\lambda_1 + \lambda_2)\). Таким образом, сумма независимых пуассоновских величин — снова пуассоновская.

#### Пример 2: Сумма независимых нормальных величин

Пусть \(X \sim N(\mu_1, \sigma_1^2)\), \(Y \sim N(\mu_2, \sigma_2^2)\), независимы. Тогда:
\[
\varphi_X(t) = \exp\left(i\mu_1 t - \frac{\sigma_1^2 t^2}{2}\right), \quad \varphi_Y(t) = \exp\left(i\mu_2 t - \frac{\sigma_2^2 t^2}{2}\right)
\]
Для суммы \(Z = X + Y\):
\[
\varphi_Z(t) = \exp\left(i(\mu_1 + \mu_2) t - \frac{(\sigma_1^2 + \sigma_2^2) t^2}{2}\right)
\]
Это характеристическая функция \(N(\mu_1 + \mu_2, \sigma_1^2 + \sigma_2^2)\).

#### Пример 3: Доказательство ЦПТ (идея)

Для независимых одинаково распределенных величин \(X_i\) с \(\mu = 0, \sigma^2 = 1\):
\[
\varphi_{S_n/\sqrt{n}}(t) = \left[ \varphi\left(\frac{t}{\sqrt{n}}\right) \right]^n
\]
Разлагая \(\varphi(s) = 1 - \frac{s^2}{2} + o(s^2)\) при \(s \to 0\), получаем:
\[
\varphi_{S_n/\sqrt{n}}(t) \to e^{-t^2/2}
\]
что соответствует стандартному нормальному распределению.

#### Резюме

| Свойство | Формула |
|:---|:---|
| **Определение** | \(\varphi_X(t) = \mathbb{E}[e^{itX}]\) |
| **Линейное преобразование** | \(\varphi_{aX+b}(t) = e^{itb}\varphi_X(at)\) |
| **Сумма независимых** | \(\varphi_{X+Y}(t) = \varphi_X(t)\varphi_Y(t)\) |
| **Связь с моментами** | \(\mathbb{E}[X^k] = \varphi^{(k)}(0)/i^k\) |
| **Теорема единственности** | \(\varphi_X = \varphi_Y \Rightarrow X \stackrel{d}{=} Y\) |
| **Теорема Леви** | Сходимость \(\varphi_n \to \varphi\) непрерывной в 0 \(\Rightarrow\) сходимость распределений |
