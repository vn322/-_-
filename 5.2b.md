
Тема **Схемы Бернулли и предельных теорем** связывает классическую вероятность с математическим анализом. Она отвечает на вопрос: как вести себя с вероятностями, когда число испытаний становится очень большим?

---

# 1. Схема Бернулли

### Интуитивное понимание
Представьте, что вы много раз повторяете один и тот же эксперимент с двумя исходами: «успех» или «неудача».
*   **Примеры:** Подбрасывание монеты (орел/решка), контроль качества (годен/брак), стрельба по мишени (попадание/промах).
*   Важно: вероятность успеха $p$ не меняется от испытания к испытанию, и испытания независимы.

### Строгое определение
**Схема Бернулли** — это последовательность из $n$ независимых испытаний, в каждом из которых событие $A$ (успех) происходит с вероятностью $p$, а не происходит с вероятностью $q = 1 - p$.

**Формула Бернулли:**
Вероятность того, что в $n$ испытаниях событие произойдет ровно $k$ раз:
$$
P_n(k) = \binom{n}{k} p^k q^{n-k}
$$
где $\binom{n}{k} = \frac{n!}{k!(n-k)!}$ — число сочетаний.

### Числовые характеристики
Для схемы Бернулли важны две величины:
1.  **Математическое ожидание** числа успехов: $E = np$
2.  **Дисперсия** числа успехов: $D = npq$

---

# 2. Предельная теорема Пуассона

### Интуитивное понимание
Используется для **редких событий**.
Если вероятность успеха $p$ очень мала, а число испытаний $n$ очень велико, то формула Бернулли становится неудобной (факториалы огромных чисел сложно считать).
Теорема Пуассона позволяет заменить биномиальное распределение на более простое.

### Формулировка
Если $n \to \infty$, $p \to 0$, причем произведение $\lambda = np$ остается постоянным ($\lambda \le 10$), то:
$$
P_n(k) \approx \frac{\lambda^k e^{-\lambda}}{k!}
$$
Это **распределение Пуассона**.

### Когда применять?
*   $n \ge 50$
*   $p \le 0.1$
*   $\lambda = np$ — среднее число успехов.

---

# 3. Локальная теорема Муавра-Лапласа

### Интуитивное понимание
Если $n$ велико, а $p$ не слишком близко к 0 или 1, то график вероятностей $P_n(k)$ похож на колокол (кривую Гаусса).
**Локальная** теорема позволяет найти вероятность того, что успех произойдет **ровно $k$ раз**.

### Формулировка
Если $n$ велико, то:
$$
P_n(k) \approx \frac{1}{\sqrt{npq}} \cdot \varphi(x)
$$
где:
*   $x = \frac{k - np}{\sqrt{npq}}$ — нормированное отклонение.
*   $\varphi(x) = \frac{1}{\sqrt{2\pi}} e^{-x^2/2}$ — функция Гаусса (табличная функция).

### Свойства функции $\varphi(x)$
1.  $\varphi(x)$ — четная: $\varphi(-x) = \varphi(x)$.
2.  Максимум при $x = 0$: $\varphi(0) \approx 0.3989$.
3.  Быстро убывает при $|x| > 3$.

---

# 4. Интегральная теорема Муавра-Лапласа

### Интуитивное понимание
Часто нас интересует не точное число успехов, а диапазон: «от $k_1$ до $k_2$ раз».
Суммировать формулу Бернулли для каждого $k$ долго. Интегральная теорема заменяет сумму на интеграл (площадь под кривой Гаусса).

### Формулировка
Вероятность того, что число успехов $k$ заключено в пределах от $k_1$ до $k_2$:
$$
P_n(k_1 \le k \le k_2) \approx \Phi(x_2) - \Phi(x_1)
$$
где:
*   $x_1 = \frac{k_1 - np}{\sqrt{npq}}, \quad x_2 = \frac{k_2 - np}{\sqrt{npq}}$
*   $\Phi(x) = \frac{1}{\sqrt{2\pi}} \int_{0}^{x} e^{-t^2/2} dt$ — **функция Лапласа** (табличная).

### Свойства функции $\Phi(x)$
1.  $\Phi(x)$ — нечетная: $\Phi(-x) = -\Phi(x)$.
2.  $\Phi(0) = 0$.
3.  $\Phi(\infty) = 0.5$.
4.  Для больших $x$ ($x > 5$) можно считать $\Phi(x) \approx 0.5$.

---

# 5. Типовые задачи и решения

### Задача 1. Формула Бернулли
**Задание:** Монету подбрасывают 5 раз. Найти вероятность того, что герб выпадет ровно 3 раза.

**Решение:**
1.  Параметры: $n = 5$, $k = 3$, $p = 0.5$, $q = 0.5$.
2.  По формуле Бернулли:
    $$
    P_5(3) = \binom{5}{3} (0.5)^3 (0.5)^{5-3}
    $$
3.  Вычисляем:
    $$
    \binom{5}{3} = \frac{5!}{3!2!} = 10
    $$
    $$
    P_5(3) = 10 \cdot (0.5)^5 = 10 \cdot 0.03125 = 0.3125
    $$

**Ответ:** 0.3125.

---

### Задача 2. Теорема Пуассона (Редкие события)
**Задание:** Завод отправил 1000 изделий. Вероятность повреждения изделия в пути равна 0.002. Найти вероятность того, что будут повреждены ровно 3 изделия.

**Решение:**
1.  Проверка условий: $n = 1000$ (велико), $p = 0.002$ (мало).
2.  Находим параметр $\lambda$:
    $$
    \lambda = np = 1000 \cdot 0.002 = 2
    $$
3.  Применяем формулу Пуассона для $k = 3$:
    $$
    P_{1000}(3) \approx \frac{\lambda^3 e^{-\lambda}}{3!} = \frac{2^3 \cdot e^{-2}}{6}
    $$
4.  Вычисляем ($e^{-2} \approx 0.1353$):
    $$
    P \approx \frac{8 \cdot 0.1353}{6} \approx 0.1804
    $$

**Ответ:** 0.1804.

---

### Задача 3. Локальная теорема Муавра-Лапласа
**Задание:** Вероятность рождения мальчика равна 0.51. Найти вероятность того, что среди 200 новорожденных ровно 100 будут мальчиками.

**Решение:**
1.  Параметры: $n = 200$, $k = 100$, $p = 0.51$, $q = 0.49$.
2.  Проверка условий: $n$ велико, $p$ близко к 0.5. Условия выполнены.
3.  Находим $x$:
    $$
    np = 200 \cdot 0.51 = 102
    $$
    $$
    \sqrt{npq} = \sqrt{200 \cdot 0.51 \cdot 0.49} = \sqrt{49.98} \approx 7.07
    $$
    $$
    x = \frac{k - np}{\sqrt{npq}} = \frac{100 - 102}{7.07} = \frac{-2}{7.07} \approx -0.28
    $$
4.  Находим $\varphi(x)$ по таблице (используем четность $\varphi(-0.28) = \varphi(0.28)$):
    $$
    \varphi(0.28) \approx 0.3836
    $$
5.  Вычисляем вероятность:
    $$
    P_{200}(100) \approx \frac{1}{7.07} \cdot 0.3836 \approx 0.0543
    $$

**Ответ:** 0.0543.

---

### Задача 4. Интегральная теорема Муавра-Лапласа
**Задание:** Вероятность попадания в мишень 0.6. Стрелок делает 100 выстрелов. Найти вероятность того, что он попадет от 55 до 65 раз.

**Решение:**
1.  Параметры: $n = 100$, $p = 0.6$, $q = 0.4$.
    $$
    np = 60, \quad \sqrt{npq} = \sqrt{100 \cdot 0.6 \cdot 0.4} = \sqrt{24} \approx 4.90
    $$
2.  Границы $k_1 = 55$, $k_2 = 65$. Находим $x_1, x_2$:
    $$
    x_1 = \frac{55 - 60}{4.90} = \frac{-5}{4.90} \approx -1.02
    $$
    $$
    x_2 = \frac{65 - 60}{4.90} = \frac{5}{4.90} \approx 1.02
    $$
3.  Применяем формулу:
    $$
    P \approx \Phi(1.02) - \Phi(-1.02)
    $$
    Так как $\Phi$ нечетная: $\Phi(-1.02) = -\Phi(1.02)$.
    $$
    P \approx 2 \Phi(1.02)
    $$
4.  По таблице Лапласа: $\Phi(1.02) \approx 0.3461$.
    $$
    P \approx 2 \cdot 0.3461 = 0.6922
    $$

**Ответ:** 0.6922.

---

### Задача 5. Сравнение методов
**Задание:** Для $n=10, p=0.5$ найти вероятность $k=5$ тремя способами и сравнить.

**Решение:**
1.  **Точная формула Бернулли:**
    $$
    P_{10}(5) = \binom{10}{5} (0.5)^{10} = 252 \cdot 0.0009766 \approx 0.2461
    $$
2.  **Локальная теорема Муавра-Лапласа:**
    $$
    np = 5, \quad \sqrt{npq} = \sqrt{2.5} \approx 1.58
    $$
    $$
    x = \frac{5 - 5}{1.58} = 0, \quad \varphi(0) \approx 0.3989
    $$
    $$
    P \approx \frac{1}{1.58} \cdot 0.3989 \approx 0.2525
    $$
3.  **Погрешность:**
    $$
    \frac{|0.2525 - 0.2461|}{0.2461} \approx 2.6\%
    $$
    Для малого $n=10$ погрешность заметна. Теорема работает лучше при $n \ge 50$.

**Ответ:** Точное значение 0.2461, приближенное 0.2525.

---

# Резюме модуля

| Теорема | Формула | Условия применения | Для чего |
|:---:|:---:|:---:|:---:|
| **Бернулли** | $\binom{n}{k} p^k q^{n-k}$ | Любые $n, p$ | Точный расчет |
| **Пуассон** | $\frac{\lambda^k e^{-\lambda}}{k!}$ | $n$ велико, $p$ мало | Редкие события |
| **Муавра-Лаплас (лок.)** | $\frac{1}{\sqrt{npq}} \varphi(x)$ | $n$ велико, $p$ не мало | Точное $k$ |
| **Муавра-Лаплас (инт.)** | $\Phi(x_2) - \Phi(x_1)$ | $n$ велико, $p$ не мало | Диапазон $[k_1, k_2]$ |

**Правила выбора:**
1.  Если $n$ маленькое (< 50) — используйте точную формулу Бернулли.
2.  Если $n$ большое и $p$ маленькое ($np < 10$) — используйте Пуассона.
3.  Если $n$ большое и $p$ среднее — используйте Муавра-Лапласа.

