

Тема **кривых второго порядка** — это классика аналитической геометрии. Эти кривые возникают при пересечении конуса плоскостью (поэтому их часто называют коническими сечениями) и описываются уравнениями второй степени.

---

# 1. Общее уравнение кривой второго порядка

### Строгое определение
Кривой второго порядка называется множество точек на плоскости, координаты которых удовлетворяют уравнению второй степени относительно $x$ и $y$:

$$
Ax^2 + Bxy + Cy^2 + Dx + Ey + F = 0
$$

где $A, B, C, D, E, F$ — действительные числа, причем хотя бы один из коэффициентов $A, B, C$ не равен нулю.

### Интуитивное понимание
В зависимости от значений коэффициентов, это уравнение может описывать:
1.  **Эллипс** (включая окружность).
2.  **Гиперболу**.
3.  **Пароболу**.
4.  **Вырожденные случаи** (точку, пару прямых, пустое множество).

Наша задача — научиться распознавать тип кривой и приводить уравнение к простому (**каноническому**) виду, чтобы легко видеть её свойства (центр, оси, фокусы).

---

# 2. Канонические уравнения и свойства

Канонический вид — это «идеальная» форма уравнения, когда оси координат совпадают с осями симметрии кривой, а начало координат находится в центре (или вершине) симметрии.

### 1. Эллипс
**Каноническое уравнение:**
$$
\frac{x^2}{a^2} + \frac{y^2}{b^2} = 1, \quad a \ge b > 0
$$
*   **Геометрия:** Замкнутая овальная кривая.
*   **Параметры:** $a$ — большая полуось, $b$ — малая полуось.
*   **Фокусное расстояние:** $c = \sqrt{a^2 - b^2}$. Фокусы находятся в точках $(\pm c, 0)$.
*   **Эксцентриситет:** $\varepsilon = \frac{c}{a}$ (характеризует вытянутость, $0 \le \varepsilon < 1$).
*   **Окружность:** Частный случай эллипса, когда $a = b = R$. Уравнение: $x^2 + y^2 = R^2$.

### 2. Гипербола
**Каноническое уравнение:**
$$
\frac{x^2}{a^2} - \frac{y^2}{b^2} = 1
$$
*   **Геометрия:** Две симметричные ветви, расходящиеся в бесконечность.
*   **Параметры:** $a$ — действительная полуось, $b$ — мнимая полуось.
*   **Асимптоты:** Прямые, к которым приближаются ветви: $y = \pm \frac{b}{a} x$.
*   **Фокусное расстояние:** $c = \sqrt{a^2 + b^2}$. Фокусы в $(\pm c, 0)$.
*   **Эксцентриситет:** $\varepsilon = \frac{c}{a}$ (всегда $\varepsilon > 1$).

### 3. Парабола
**Каноническое уравнение:**
$$
y^2 = 2px \quad (p > 0)
$$
*   **Геометрия:** Разомкнутая кривая, симметричная относительно оси $Ox$.
*   **Параметр:** $p$ — расстояние от фокуса до директрисы.
*   **Фокус:** Находится в точке $(\frac{p}{2}, 0)$.
*   **Директриса:** Прямая $x = -\frac{p}{2}$.
*   **Эксцентриситет:** $\varepsilon = 1$.

---

# 3. Приведение к каноническому виду

Если в уравнении нет члена $xy$ (то есть $B = 0$), приведение осуществляется методом **выделения полных квадратов**.
Если $B \neq 0$, требуется поворот системы координат (это более сложная процедура, здесь мы рассмотрим случай $B=0$).

### Алгоритм (для случая $B=0$):
1.  Сгруппировать слагаемые с $x$ и с $y$.
2.  Вынести коэффициенты при $x^2$ и $y^2$ за скобки.
3.  Дополнить выражения в скобках до полных квадратов $(x - x_0)^2$ и $(y - y_0)^2$.
4.  Перенести лишние числа в правую часть.
5.  Разделить всё уравнение на число в правой части, чтобы получить 1.

---

# Типовые задачи и решения

### Задача 1. Определение типа кривой
**Задание:** Определить тип кривой, заданной уравнением $9x^2 + 4y^2 - 36 = 0$.

**Решение:**
1.  Перенесем свободный член вправо:
    $$ 9x^2 + 4y^2 = 36 $$
2.  Разделим обе части на 36, чтобы получить единицу справа:
    $$ \frac{9x^2}{36} + \frac{4y^2}{36} = 1 $$
3.  Сократим дроби:
    $$ \frac{x^2}{4} + \frac{y^2}{9} = 1 $$
4.  Сравниваем с каноническими формами. Знаки при $x^2$ и $y^2$ одинаковые (плюс), значит это **эллипс**.
    Так как знаменатель при $y^2$ больше ($9 > 4$), большая ось направлена вдоль оси $Oy$.
    $a^2 = 9 \implies a = 3$, $b^2 = 4 \implies b = 2$.

**Ответ:** Эллипс с полуосями $a=3$ (вертикальная) и $b=2$ (горизонтальная).

---

### Задача 2. Приведение к каноническому виду (Эллипс)
**Задание:** Привести уравнение $4x^2 + 9y^2 - 8x + 36y - 4 = 0$ к каноническому виду и найти центр симметрии.

**Решение:**
1.  **Группировка:** Сгруппируем слагаемые с $x$ и $y$, свободный член перенесем вправо:
    $$ (4x^2 - 8x) + (9y^2 + 36y) = 4 $$
2.  **Вынос коэффициентов:** Вынесем 4 за скобку в первой группе и 9 во второй:
    $$ 4(x^2 - 2x) + 9(y^2 + 4y) = 4 $$
3.  **Выделение полных квадратов:**
    *   Для $x$: $(x^2 - 2x)$ дополняем до $(x - 1)^2 = x^2 - 2x + 1$. Внутри скобки добавили 1.
    *   Для $y$: $(y^2 + 4y)$ дополняем до $(y + 2)^2 = y^2 + 4y + 4$. Внутри скобки добавили 4.
    *   Чтобы равенство не нарушилось, нужно прибавить то же самое к правой части, учитывая вынесенные коэффициенты:
    $$ 4(x^2 - 2x + 1) + 9(y^2 + 4y + 4) = 4 + 4 \cdot 1 + 9 \cdot 4 $$
4.  **Упрощение:**
    $$ 4(x - 1)^2 + 9(y + 2)^2 = 4 + 4 + 36 = 44 $$
5.  **Нормировка:** Делим всё на 44:
    $$ \frac{4(x - 1)^2}{44} + \frac{9(y + 2)^2}{44} = 1 $$
    $$ \frac{(x - 1)^2}{11} + \frac{(y + 2)^2}{44/9} = 1 $$
6.  **Анализ:**
    Это уравнение эллипса с центром в точке $O'(1, -2)$.
    Полуоси: $a = \sqrt{11}$, $b = \sqrt{44/9} = \frac{2\sqrt{11}}{3}$.

**Ответ:** Канонический вид $\frac{(x - 1)^2}{11} + \frac{(y + 2)^2}{44/9} = 1$, центр $(1, -2)$.

---

### Задача 3. Приведение к каноническому виду (Гипербола)
**Задание:** Привести к каноническому виду уравнение $x^2 - 4y^2 + 6x + 8y - 11 = 0$.

**Решение:**
1.  **Группировка:**
    $$ (x^2 + 6x) - (4y^2 - 8y) = 11 $$
    *(Внимание на знак минус перед второй группой!)*
2.  **Вынос коэффициентов:**
    $$ (x^2 + 6x) - 4(y^2 - 2y) = 11 $$
3.  **Выделение полных квадратов:**
    *   Для $x$: добавляем 3 внутри скобки ($6/2 = 3$).
    *   Для $y$: добавляем 1 внутри скобки ($2/2 = 1$).
    *   Балансируем правую часть:
    $$ (x^2 + 6x + 9) - 4(y^2 - 2y + 1) = 11 + 9 - 4 \cdot 1 $$
    $$ (x + 3)^2 - 4(y - 1)^2 = 16 $$
4.  **Нормировка:** Делим на 16:
    $$ \frac{(x + 3)^2}{16} - \frac{4(y - 1)^2}{16} = 1 $$
    $$ \frac{(x + 3)^2}{16} - \frac{(y - 1)^2}{4} = 1 $$
5.  **Анализ:**
    Это гипербола с центром в $(-3, 1)$.
    Действительная полуось $a = 4$ (направлена вдоль $Ox$), мнимая $b = 2$.

**Ответ:** $\frac{(x + 3)^2}{16} - \frac{(y - 1)^2}{4} = 1$.

---

### Задача 4. Приведение к каноническому виду (Парабола)
**Задание:** Привести к каноническому виду $y^2 - 4y - 8x + 20 = 0$.

**Решение:**
1.  Здесь есть $y^2$, но нет $x^2$. Это признак параболы.
2.  Выделим полный квадрат по $y$:
    $$ (y^2 - 4y) = 8x - 20 $$
    $$ (y^2 - 4y + 4) = 8x - 20 + 4 $$
    $$ (y - 2)^2 = 8x - 16 $$
3.  Вынесем 8 в правой части:
    $$ (y - 2)^2 = 8(x - 2) $$
4.  **Анализ:**
    Это парабола с вершиной в точке $(2, 2)$.
    Ветви направлены вправо (так как коэффициент при $x$ положительный).
    Параметр $2p = 8 \implies p = 4$.
    Фокус находится на расстоянии $p/2 = 2$ от вершины вправо: $(2+2, 2) = (4, 2)$.

**Ответ:** $(y - 2)^2 = 8(x - 2)$, вершина $(2, 2)$, фокус $(4, 2)$.

---

# Резюме модуля

1.  **Общее уравнение:** $Ax^2 + Bxy + Cy^2 + Dx + Ey + F = 0$.
2.  **Типы кривых:**
    *   **Эллипс:** Знаки при $x^2$ и $y^2$ одинаковые.
    *   **Гипербола:** Знаки при $x^2$ и $y^2$ разные.
    *   **Парабола:** Квадрат есть только у одной переменной.
3.  **Приведение к каноническому виду:**
    *   Метод выделения полных квадратов позволяет найти центр симметрии (для эллипса и гиперболы) или вершину (для параболы).
    *   Сдвиг начала координат в центр упрощает уравнение до канонического.
4.  **Геометрический смысл:** Каноническое уравнение сразу показывает размеры осей, положение фокусов и форму кривой.


