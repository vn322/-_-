
Тема **Поиска минимума функций и градиентных методов** — это фундамент оптимизации. Эти методы лежат в основе машинного обучения, глубоких нейронных сетей, управления системами и многих инженерных задач.

---

# 1. Интуитивное понимание

### Задача оптимизации
Мы хотим найти точку, в которой функция принимает **наименьшее значение**.

**Аналогия:**
Представьте, что вы находитесь в горах в тумане и хотите спуститься в самую низкую точку долины. Вы не видите всю карту, но можете ощутить уклон под ногами.
*   **Градиент** — показывает направление наибыстрейшего подъема.
*   **Антиградиент** — показывает направление наибыстрейшего спуска.
*   **Градиентный спуск** — делаем шаг вниз по склону, повторяем, пока не достигнем дна.

### Стационарные точки
Это точки, где функция «не растет и не убывает» в первом приближении — градиент равен нулю.
*   **Минимум:** Дно ямы
*   **Максимум:** Вершина горы
*   **Седловая точка:** Перевал между горами

---

# 2. Строгие определения

## Стационарные точки

Пусть $f(x)$ — дифференцируемая функция $f: \mathbb{R}^n \to \mathbb{R}$.

**Стационарной точкой** называется точка $x^*$, в которой градиент функции равен нулю:

$$
\nabla f(x^*) = 0
$$

### Классификация стационарных точек

| Тип точки | Условие (1D) | Условие (nD) |
|:---:|:---:|:---:|
| **Минимум** | $f'(x^*) = 0, f''(x^*) > 0$ | $\nabla f = 0$, матрица Гессе положительно определена |
| **Максимум** | $f'(x^*) = 0, f''(x^*) < 0$ | $\nabla f = 0$, матрица Гессе отрицательно определена |
| **Седловая** | $f'(x^*) = 0, f''(x^*) = 0$ | $\nabla f = 0$, матрица Гессе неопределенна |

$$f^{\prime}(x^*) = 0, f^{\prime\prime}(x^*) > 0$$

$\nabla f = 0$, матрица Гессе положительно определена 
$$f'(x^*) = 0, f''(x^*) < 0$$

$\nabla f = 0$, матрица Гессе отрицательно определена

$$f'(x^*) = 0, f''(x^*) = 0$$  

$\nabla f = 0$, матрица Гессе неопределенна

**Матрица Гессе:**

$$
H(f) = \begin{pmatrix}
\frac{\partial^2 f}{\partial x_1^2} & \cdots & \frac{\partial^2 f}{\partial x_1 \partial x_n} \\
\vdots & \ddots & \vdots \\
\frac{\partial^2 f}{\partial x_n \partial x_1} & \cdots & \frac{\partial^2 f}{\partial x_n^2}
\end{pmatrix}
$$

---

## Градиент функции

**Градиент** — вектор частных производных, указывающий направление наибыстрейшего роста функции:

$$
\nabla f(x) = \left( \frac{\partial f}{\partial x_1}, \frac{\partial f}{\partial x_2}, \dots, \frac{\partial f}{\partial x_n} \right)^T
$$

---

# 3. Метод градиентного спуска

### Идея
Двигаемся в направлении, противоположном градиенту (антиградиент), с фиксированным шагом.

### Алгоритм

1.  Выбрать начальное приближение $x^{(0)}$.
2.  Выбрать шаг $\alpha > 0$ (learning rate).
3.  Итерационный процесс:

$$
    x^{(k+1)} = x^{(k)} - \alpha \cdot \nabla f(x^{(k)})
    $$
    
4.  Повторять пока $\|\nabla f(x^{(k)})\| < \varepsilon$ или $\|x^{(k+1)} - x^{(k)}\| < \varepsilon$.

### Выбор шага $\alpha$

| Стратегия | Описание | Плюсы | Минусы |
|:---:|:---:|:---:|:---:|
| **Фиксированный** | $\alpha = \text{const}$ | Простота | Может расходиться или сходиться медленно |
| **Адаптивный** | $\alpha$ уменьшается со временем | Устойчивость | Требует настройки |
| **Линейный поиск** | Подбор $\alpha$ на каждой итерации | Точность | Дороже вычислительно |

### Условия сходимости

Метод сходится, если:
1.  Функция выпукла и ограничена снизу.
2.  Градиент удовлетворяет условию Липшица.
3.  Шаг $\alpha$ выбран достаточно малым: $0 < \alpha < \frac{2}{L}$, где $L$ — константа Липшица градиента.

### Скорость сходимости
**Линейная сходимость:**

$$
f(x^{(k)}) - f(x^*) \le C \cdot q^k, \quad 0 < q < 1
$$

---

# 4. Метод наискорейшего спуска

### Идея
Это вариант градиентного спуска, где на каждом шаге **оптимально подбирается длина шага** $\alpha_k$.

### Алгоритм

1.  Вычислить градиент: $d^{(k)} = -\nabla f(x^{(k)})$.
2.  Найти оптимальный шаг $\alpha_k$ из условия:

$$
    \alpha_k = \arg\min_{\alpha \ge 0} f(x^{(k)} + \alpha d^{(k)})
    $$
    
3.  Обновить точку:

$$
    x^{(k+1)} = x^{(k)} + \alpha_k d^{(k)}
    $$

### Отличия от обычного градиентного спуска

| Характеристика | Градиентный спуск | Наискорейший спуск |
|:---:|:---:|:---:|
| **Шаг** | Фиксированный $\alpha$ | Оптимальный $\alpha_k$ на каждом шаге |
| **Вычисления** | Дешевле на итерацию | Дороже (требуется минимизация) |
| **Сходимость** | Может зигзагообразить | Меньше итераций |
| **Направления** | Любые | Ортогональны на соседних шагах |

### Свойство ортогональности
В методе наискорейшего спуска направления на соседних итерациях ортогональны:

$$
\nabla f(x^{(k+1)}) \perp \nabla f(x^{(k)})
$$

---

# 5. Типовые задачи и решения

### Задача 1. Нахождение стационарных точек
**Задание:** Найти стационарные точки функции $f(x, y) = x^2 + 2y^2 - 2x - 4y$.

**Решение:**
1.  **Градиент:**

$$
    \nabla f = \begin{pmatrix} 2x - 2 \\ 4y - 4 \end{pmatrix}
    $$
    
2.  **Приравниваем к нулю:**

$$
    2x - 2 = 0 \Rightarrow x = 1
    $$

$$
    4y - 4 = 0 \Rightarrow y = 1
    $$
    
3.  **Стационарная точка:** $(1, 1)$.
4.  **Проверка типа (матрица Гессе):**

$$
    H = \begin{pmatrix} 2 & 0 \\ 0 & 4 \end{pmatrix}
    $$

Собственные числа: $2, 4 > 0$ → положительно определена → **минимум**.

**Ответ:** Стационарная точка $(1, 1)$ — локальный минимум.

---

### Задача 2. Метод градиентного спуска (одна итерация)
**Задание:** Выполнить одну итерацию градиентного спуска для $f(x) = x^2$ с $x_0 = 3$ и $\alpha = 0.1$.

**Решение:**
1.  **Градиент:** $f'(x) = 2x$.
2.  **Значение в точке:** $f'(3) = 6$.
3.  **Формула:**

$$
    x_1 = x_0 - \alpha \cdot f'(x_0) = 3 - 0.1 \cdot 6 = 3 - 0.6 = 2.4
    $$

**Ответ:** $x_1 = 2.4$.

---

### Задача 3. Метод наискорейшего спуска (квадратичная функция)
**Задание:** Найти оптимальный шаг $\alpha$ для $f(x) = x^2$ в точке $x_0 = 3$.

**Решение:**
1.  **Направление спуска:** $d = -f'(x_0) = -6$.
2.  **Функция одного переменного:**

$$
    \phi(\alpha) = f(x_0 + \alpha d) = (3 - 6\alpha)^2
    $$
    
3.  **Минимизируем по $\alpha$:**

$$
    \phi'(\alpha) = 2(3 - 6\alpha) \cdot (-6) = 0
    $$

$$
    3 - 6\alpha = 0 \Rightarrow \alpha = 0.5
    $$
    
4.  **Новая точка:**

$$
    x_1 = 3 - 0.5 \cdot 6 = 0
    $$

Для квадратичной функции одной итерации достаточно!

**Ответ:** $\alpha = 0.5$, $x_1 = 0$ (точный минимум).

---

### Задача 4. Градиентный спуск для функции двух переменных
**Задание:** Выполнить одну итерацию для $f(x, y) = x^2 + y^2$ с $(x_0, y_0) = (3, 4)$ и $\alpha = 0.1$.

**Решение:**
1.  **Градиент:**

$$
    \nabla f = \begin{pmatrix} 2x \\ 2y \end{pmatrix}
    $$
    
2.  **В точке (3, 4):**

$$
    \nabla f(3, 4) = \begin{pmatrix} 6 \\ 8 \end{pmatrix}
    $$
    
3.  **Обновление:**

$$
    \begin{pmatrix} x_1 \\ y_1 \end{pmatrix} = \begin{pmatrix} 3 \\ 4 \end{pmatrix} - 0.1 \cdot \begin{pmatrix} 6 \\ 8 \end{pmatrix} = \begin{pmatrix} 3 - 0.6 \\ 4 - 0.8 \end{pmatrix} = \begin{pmatrix} 2.4 \\ 3.2 \end{pmatrix}
    $$

**Ответ:** $(x_1, y_1) = (2.4, 3.2)$.

---

### Задача 5. Проблема овражных функций
**Задание:** Объясните, почему градиентный спуск медленно сходится для функции $f(x, y) = x^2 + 100y^2$.

**Решение:**
1.  **Градиент:**

$$
    \nabla f = \begin{pmatrix} 2x \\ 200y \end{pmatrix}
    $$
    
2.  **Проблема:** Градиент по $y$ в 100 раз больше, чем по $x$.
3.  **Последствия:**
    *   При большом шаге — расходимость по $y$.
    *   При малом шаге — очень медленное движение по $x$.
    *   Траектория зигзагообразно движется вдоль «оврага».
4.  **Решение:**
    *   Метод наискорейшего спуска (подбор шага).
    *   Метод сопряженных градиентов.
    *   Предобуславливание (масштабирование переменных).

**Ответ:** Разный масштаб по осям вызывает зигзагообразную траекторию и медленную сходимость.

---

### Задача 6. Критерий остановки
**Задание:** Предложите критерии остановки для градиентного спуска.

**Решение:**
Основные критерии:
1.  **По норме градиента:**

$$
    \|\nabla f(x^{(k)})\| < \varepsilon
    $$

(Наиболее теоретически обоснованный)

2.  **По изменению аргумента:**

$$
    \|x^{(k+1)} - x^{(k)}\| < \varepsilon
    $$
    
3.  **По изменению функции:**

$$
    |f(x^{(k+1)}) - f(x^{(k)})| < \varepsilon
    $$
    
4.  **По числу итераций:** $k < k_{\max}$

**Ответ:** Комбинация критериев 1 и 2 наиболее надежна.

---

# 6. Сравнение методов оптимизации первого порядка

| Метод | Шаг | Сходимость | Вычисления на итерацию |
|:---:|:---:|:---:|:---:|
| **Градиентный спуск** | Фиксированный $\alpha$ | Линейная | Градиент |
| **Наискорейший спуск** | Оптимальный $\alpha_k$ | Линейная (быстрее) | Градиент + минимизация 1D |
| **Сопряженных градиентов** | Оптимальный + направление | Суперлинейная | Градиент + векторные операции |
| **Метод Ньютона** | Оптимальный | Квадратичная | Градиент + Гессе + СЛАУ |

---

# 7. Резюме модуля

| Понятие | Формула / Условие |
|:---:|:---:|
| **Стационарная точка** | $\nabla f(x^*) = 0$ |
| **Градиент** | $\nabla f = (\frac{\partial f}{\partial x_1}, \dots, \frac{\partial f}{\partial x_n})^T$ |
| **Градиентный спуск** | $x^{(k+1)} = x^{(k)} - \alpha \nabla f(x^{(k)})$ |
| **Наискорейший спуск** | $\alpha_k = \arg\min_\alpha f(x^{(k)} - \alpha \nabla f(x^{(k)}))$ |
| **Матрица Гессе** | $H_{ij} = \frac{\partial^2 f}{\partial x_i \partial x_j}$ |
| **Условие минимума** | $\nabla f = 0$, $H$ положительно определена |

**Ключевые выводы:**
1.  **Стационарные точки** — кандидаты на экстремум (градиент = 0).
2.  **Градиент** указывает направление наибыстрейшего роста, антиградиент — спуска.
3.  **Метод градиентного спуска** прост, но требует careful выбора шага.
4.  **Метод наискорейшего спуска** эффективнее за счет оптимального шага на каждой итерации.
5.  **Овражные функции** — проблема для градиентных методов (требуется предобуславливание).
6.  **Критерии остановки** должны балансировать точность и вычислительные затраты.

