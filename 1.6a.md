Мы переходим к следующему уровню сложности. Если в числовых рядах членами были числа, то в функциональных рядах членами являются **функции**. Суммой такого ряда становится не число, а **функция**. И здесь возникает ключевой вопрос: если каждая функция-член ряда обладает каким-то свойством (например, непрерывностью), сохраняется ли это свойство у суммы ряда? Ответ зависит от типа сходимости — поточечной или равномерной.

---

### 1. Функциональный ряд. Основные понятия

#### 1.1 Определение

**Определение.** **Функциональным рядом** называется выражение вида:

$$\[
\sum_{n=1}^{\infty} u_n(x) = u_1(x) + u_2(x) + \dots + u_n(x) + \dots
\]$$

где $\(u_n(x)\)$ — функции, определенные на некотором множестве \(X\) (обычно промежутке на числовой оси).

Если мы зафиксируем конкретное значение $\(x = x_0 \in X\)$, то получим **числовой ряд** $\(\sum u_n(x_0)\)$. Если этот числовой ряд сходится, то $\(x_0\)$ называется **точкой сходимости**. Множество всех таких точек образует **область сходимости** функционального ряда.

#### 1.2 Сумма ряда

На области сходимости $\(D\)$ определена функция $\(S(x)\)$, называемая **суммой ряда**:

$$\[
S(x) = \sum_{n=1}^{\infty} u_n(x), \quad x \in D
\]$$

Также определяются **частичные суммы** ряда:

$$\[
S_n(x) = u_1(x) + u_2(x) + \dots + u_n(x)
\]$$
По определению, $\(S(x) = \lim_{n \to \infty} S_n(x)\)$ для каждого $\(x \in D\)$. Это так называемая **поточечная сходимость**: для каждого $\(x\)$ мы смотрим на свой числовой предел.

---

### 2. Равномерная сходимость

Поточечной сходимости часто недостаточно для того, чтобы сумма ряда наследовала хорошие свойства членов ряда. Нужна более сильная — равномерная — сходимость.

#### 2.1 Проблема поточечной сходимости

Представим, что $\(S_n(x)\)$ приближается к $\(S(x)\)$. При поточечной сходимости скорость этого приближения может быть разной в разных точках $\(x\)$. Чем ближе $\(x\)$ к какой-то особой точке, тем больше $\(n\)$ нужно взять, чтобы добиться заданной точности $\(\varepsilon\)$. Равномерность требует, чтобы скорость сходимости была примерно одинаковой для всех $\(x\)$ сразу.

#### 2.2 Определение равномерной сходимости (на языке $\(\varepsilon-n\)$ )

**Определение.** Функциональная последовательность $\(\{S_n(x)\}\)$ (или ряд $\(\sum u_n(x)\)$ с частичными суммами $\(S_n(x)\))$ сходится к функции $\(S(x)\)$ **равномерно** на множестве $\(X\)$, если:

$$\[
\forall \varepsilon > 0 \quad \exists N = N(\varepsilon) \quad \text{такое, что} \quad \forall n > N \quad \text{и} \quad \forall x \in X \quad \text{выполняется} \quad |S_n(x) - S(x)| < \varepsilon.
\]$$

**Ключевое отличие от поточечной сходимости:** Номер $\(N\)$ зависит **только от $\(\varepsilon\)$** и не зависит от выбора точки $\(x\)$. Он должен "работать" сразу для всех $\(x\)$ из множества $\(X\)$.

#### 2.3 Геометрический смысл

Неравенство $\(|S_n(x) - S(x)| < \varepsilon\)$ означает, что график $\(S_n(x)\)$ для всех $\(n > N\)$ целиком попадает в " $\(\varepsilon\)$ -полоску" вокруг графика $\(S(x)\)$. Графики частичных сумм равномерно "прижимаются" к предельному графику на всём промежутке сразу.

---

### 3. Признак равномерной сходимости Вейерштрасса

Как проверить, что функциональный ряд сходится равномерно? Самый простой и часто используемый признак — **мажорантный признак Вейерштрасса** (для абсолютно сходящихся рядов).

#### 3.1 Формулировка признака

**Теорема (Вейерштрасс).** Пусть дан функциональный ряд $\(\sum_{n=1}^{\infty} u_n(x)\)$, определенный на множестве $\(X\)$. Если существует такой сходящийся **числовой** знакоположительный ряд $\(\sum_{n=1}^{\infty} M_n\)$ (называемый **мажорантой**), что для всех $\(x \in X\)$ и для всех $\(n\)$ выполняется неравенство:

$$\[
|u_n(x)| \le M_n,
\]$$

то функциональный ряд $\(\sum u_n(x)\)$ сходится на множестве $\(X\)$ **абсолютно и равномерно**.

#### 3.2 Идея доказательства (наглядная)

1.  **Абсолютная сходимость:** Для каждого фиксированного $\(x\) ряд \(\sum |u_n(x)|\)$ сходится по признаку сравнения (так как его "зажимает" сверху сходящийся ряд $\(\sum M_n\)$ ).
2.  **Равномерная сходимость:** Остаток ряда $\(r_n(x) = \sum_{k=n+1}^{\infty} u_k(x)\)$ по модулю не превосходит $\(\sum_{k=n+1}^{\infty} |u_k(x)| \le \sum_{k=n+1}^{\infty} M_k\)$. Остаток числового ряда \(\sum M_n\) не зависит от $\(x\)$ и стремится к нулю. Значит, мы можем подобрать $\(N\)$ так, чтобы $\(\sum_{k=N+1}^{\infty} M_k < \varepsilon\)$ , и тогда для всех $\(x\)$ сразу $\(|r_n(x)| < \varepsilon\)$ при $\(n > N\)$.

---

### 4. Свойства равномерно сходящихся рядов

Это центральный пункт. Равномерная сходимость позволяет "менять местами" два предельных перехода: предел последовательности функций (сумму ряда) и другие операции (непрерывность, интегрирование, дифференцирование).

#### 4.1 Непрерывность суммы

**Теорема.** Если функциональный ряд $\(\sum u_n(x)\)$ состоит из функций $\(u_n(x)\)$, **непрерывных** на некотором промежутке $\([a, b]\)$, и сходится на этом промежутке **равномерно** к функции $\(S(x)\)$, то сумма ряда $\(S(x)\)$ также **непрерывна** на $\([a, b]\)$.

**Смысл:** Равномерная сходимость "передает" свойство непрерывности от членов ряда к его сумме.

#### 4.2 Почленное интегрирование

**Теорема.** Если функциональный ряд \(\sum u_n(x)\) состоит из функций \(u_n(x)\), **интегрируемых** (по Риману) на отрезке $\([a, b]\)$, и сходится на этом отрезке **равномерно** к функции \(S(x)\), то его можно почленно интегрировать на любом отрезке $\([x_1, x_2] \subset [a, b]\)$. То есть:

$$\[
\int_{x_1}^{x_2} S(x) \, dx = \int_{x_1}^{x_2} \left( \sum_{n=1}^{\infty} u_n(x) \right) dx = \sum_{n=1}^{\infty} \int_{x_1}^{x_2} u_n(x) \, dx
\]$$

(знак суммы и интеграла можно менять местами).

#### 4.3 Почленное дифференцирование

Здесь условие немного сложнее. Просто равномерной сходимости исходного ряда недостаточно, нужно потребовать равномерной сходимости ряда из производных.

**Теорема.** Пусть функции $\(u_n(x)\)$ **непрерывно дифференцируемы** на отрезке $\([a, b]\)$ (т.е. имеют непрерывные производные). Если:
1.  Ряд $\(\sum u_n(x)\)$ сходится (хотя бы поточечно) к функции $\(S(x)\) на \([a, b]\)$.
2.  Ряд из производных $\(\sum u_n'(x)\)$ сходится **равномерно** на $\([a, b]\)$,
то функция $\(S(x)\)$ дифференцируема на $\([a, b]\)$, и её производная равна сумме ряда из производных:

$$\[
S'(x) = \left( \sum_{n=1}^{\infty} u_n(x) \right)' = \sum_{n=1}^{\infty} u_n'(x)
\]$$

**Краткая мнемоника:**
*   **Непрерывность и интегрирование:** достаточно равномерной сходимости **самого** ряда.
*   **Дифференцирование:** нужна равномерная сходимость **продифференцированного** ряда.

#### Резюме

1.  **Функциональный ряд** — это бесконечная сумма функций.
2.  **Равномерная сходимость** означает, что скорость приближения частичных сумм к предельной функции одинакова для всех $\(x\)$.
3.  **Признак Вейерштрасса** — простой способ доказать равномерную сходимость, если члены ряда можно "зажать" членами сходящегося числового ряда.
4.  **Главная ценность равномерной сходимости:** она позволяет законно менять местами суммирование с операциями взятия предела, интеграла и (при доп. условиях) производной. Без равномерной сходимости эти операции могут привести к неверному результату.
