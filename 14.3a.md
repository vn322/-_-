**Итерационные методы** решения систем линейных алгебраических уравнений (СЛАУ) — это методы, которые строят последовательность приближений \(x^{(k)}\), сходящуюся к точному решению \(x^* = A^{-1}b\). В отличие от прямых методов (таких как метод Гаусса), которые дают точное решение за конечное число шагов (в арифметике с бесконечной точностью), итерационные методы являются приближенными и особенно эффективны для больших разреженных систем, где прямые методы требуют слишком много памяти и времени.

---

### 1. Общая идея итерационных методов

#### 1.1 Постановка задачи

Требуется решить СЛАУ:
\[
Ax = b
\]
где \(A \in \mathbb{R}^{n \times n}\) — невырожденная матрица, \(b \in \mathbb{R}^n\).

Итерационный метод строится следующим образом: выбирается начальное приближение \(x^{(0)}\) и строится последовательность векторов \(x^{(k)}\) по формуле:
\[
x^{(k+1)} = \Phi(x^{(k)})
\]
такая, что \(x^{(k)} \to x^*\) при \(k \to \infty\).

#### 1.2 Каноническая форма одношаговых итерационных методов

Большинство итерационных методов можно записать в единой канонической форме:
\[
x^{(k+1)} = x^{(k)} - B^{-1}(A x^{(k)} - b)
\]
где \(B\) — некоторая невырожденная матрица (предобуславливатель), которая выбирается так, чтобы:
1.  Система \(B z = r\) легко решалась.
2.  Итерационный процесс сходился быстро.

Если \(B = A\), то метод сходится за одну итерацию, но решение исходной системы не становится проще. Искусство заключается в выборе \(B\), близкого к \(A\), но легко обращаемого.

---

### 2. Классические итерационные методы

#### 2.1 Метод Якоби

**Идея:** Каждое уравнение решается относительно своей диагональной переменной, используя значения с предыдущей итерации.

Разложим матрицу \(A\) на диагональную \(D\), строго нижнюю треугольную \(L\) и строго верхнюю треугольную \(U\):
\[
A = D + L + U
\]
где:
*   \(D = \text{diag}(a_{11}, a_{22}, \dots, a_{nn})\)
*   \(L\) — нижняя треугольная часть с нулями на диагонали
*   \(U\) — верхняя треугольная часть с нулями на диагонали

**Формула метода Якоби:**
\[
x^{(k+1)} = D^{-1}(b - (L+U)x^{(k)})
\]
В покомпонентной записи:
\[
x_i^{(k+1)} = \frac{1}{a_{ii}} \left( b_i - \sum_{j \neq i} a_{ij} x_j^{(k)} \right), \quad i = 1, \dots, n
\]

#### 2.2 Метод Гаусса-Зейделя

**Идея:** При вычислении \(i\)-й компоненты используются уже найденные на текущей итерации значения компонент с меньшими индексами.

**Формула метода Гаусса-Зейделя:**
\[
x^{(k+1)} = (D + L)^{-1}(b - U x^{(k)})
\]
В покомпонентной записи:
\[
x_i^{(k+1)} = \frac{1}{a_{ii}} \left( b_i - \sum_{j=1}^{i-1} a_{ij} x_j^{(k+1)} - \sum_{j=i+1}^{n} a_{ij} x_j^{(k)} \right)
\]

Метод Гаусса-Зейделя обычно сходится быстрее метода Якоби.

#### 2.3 Метод релаксации (SOR — Successive Over-Relaxation)

**Идея:** Метод Гаусса-Зейделя даёт некоторое промежуточное значение \(\tilde{x}_i^{(k+1)}\). Окончательное значение берётся как линейная комбинация предыдущего и этого промежуточного с параметром \(\omega\):
\[
x_i^{(k+1)} = (1 - \omega) x_i^{(k)} + \omega \tilde{x}_i^{(k+1)}
\]
где \(\tilde{x}_i^{(k+1)}\) — значение, полученное по формуле Гаусса-Зейделя.

В матричной форме:
\[
x^{(k+1)} = (D + \omega L)^{-1} \left( \omega b - (\omega U + (\omega - 1)D) x^{(k)} \right)
\]

Параметр релаксации \(\omega\) выбирают в интервале \((0, 2)\):
*   \(0 < \omega < 1\) — метод нижней релаксации
*   \(\omega = 1\) — метод Гаусса-Зейделя
*   \(1 < \omega < 2\) — метод верхней релаксации (обычно ускоряет сходимость)

Оптимальный параметр для симметричных положительно определённых матриц с ленточной структурой:
\[
\omega_{\text{opt}} = \frac{2}{1 + \sqrt{1 - \rho^2}}
\]
где \(\rho\) — спектральный радиус матрицы итераций метода Якоби.

---

### 3. Сходимость итерационных методов

#### 3.1 Матрица перехода

Любой линейный одношаговый итерационный метод можно записать в виде:
\[
x^{(k+1)} = T x^{(k)} + c
\]
где \(T\) — матрица перехода (итерационная матрица), \(c\) — некоторый вектор.

Точное решение \(x^*\) удовлетворяет: \(x^* = T x^* + c\).

**Теорема (необходимое и достаточное условие сходимости).** Итерационный метод сходится при любом начальном приближении тогда и только тогда, когда спектральный радиус матрицы перехода \(\rho(T) < 1\).

**Скорость сходимости.** Асимптотическая скорость сходимости определяется величиной \(-\ln \rho(T)\). Чем меньше \(\rho(T)\), тем быстрее сходимость.

#### 3.2 Достаточные условия сходимости

1.  **Диагональное преобладание:** Если матрица \(A\) имеет строгое диагональное преобладание (\(|a_{ii}| > \sum_{j \neq i} |a_{ij}|\) для всех \(i\)), то методы Якоби и Гаусса-Зейделя сходятся.
2.  **Положительная определённость:** Если \(A\) — симметричная положительно определённая матрица, то метод Гаусса-Зейделя сходится.
3.  **Для метода Якоби:** Если \(A\) — симметричная с положительными диагональными элементами, то метод Якоби сходится тогда и только тогда, когда \(A\) и \(2D - A\) положительно определены.

---

### 4. Современные итерационные методы

#### 4.1 Метод сопряжённых градиентов (CG)

Для симметричных положительно определённых матриц метод сопряжённых градиентов является одним из наиболее эффективных. Он относится к классу проекционных методов и минимизирует энергию ошибки на подпространствах Крылова.

**Алгоритм:**
```
x(0) = начальное приближение
r(0) = b - A x(0)
p(0) = r(0)
for k = 0, 1, 2, ...
    α_k = (r(k), r(k)) / (p(k), A p(k))
    x(k+1) = x(k) + α_k p(k)
    r(k+1) = r(k) - α_k A p(k)
    β_k = (r(k+1), r(k+1)) / (r(k), r(k))
    p(k+1) = r(k+1) + β_k p(k)
end
```

#### 4.2 Методы для несимметричных матриц

*   **GMRES (Generalized Minimal Residual)** — минимизирует норму невязки на подпространствах Крылова, требует хранения всех базисных векторов.
*   **BiCGSTAB (Stabilized Biconjugate Gradient)** — стабилизированная версия метода бисопряжённых градиентов.
*   **CGS (Conjugate Gradient Squared)** — метод квадрата сопряжённых градиентов.

#### 4.3 Предобуславливание

Ключевой приём для ускорения сходимости — **предобуславливание**. Вместо исходной системы решается система:
\[
M^{-1} A x = M^{-1} b
\]
где \(M\) — предобуславливающая матрица, близкая к \(A\), но легко обратимая.

Распространённые предобуславливатели:
*   Диагональный (Якоби): \(M = D\)
*   Неполное LU-разложение (ILU)
*   Неполное разложение Холецкого (IC)
*   Многосеточные методы

---

### 5. Сравнение методов

| Метод | Тип | Применимость | Сходимость | Сложность на итерацию |
|:---|:---|:---|:---|:---|
| **Якоби** | Простой | Общий случай | Медленная | \(O(n^2)\) (разреженные — \(O(nnz)\)) |
| **Гаусса-Зейделя** | Простой | Общий случай | Лучше Якоби | \(O(n^2)\) |
| **SOR** | Простой | Симметричные/положительно определённые | Сильно зависит от \(\omega\) | \(O(n^2)\) |
| **Сопряжённых градиентов** | Крыловский | Симметричные положительно определённые | Быстрая | \(O(nnz)\) |
| **GMRES** | Крыловский | Несимметричные | Быстрая | \(O(nnz) + O(n^2)\) (с ростом итераций) |
| **BiCGSTAB** | Крыловский | Несимметричные | Быстрая | \(O(nnz)\) |

---

### 6. Критерии остановки

Итерации продолжаются до выполнения одного из условий:
1.  По норме невязки: \(\|r^{(k)}\| < \varepsilon_r \|b\|\) или \(\|r^{(k)}\| < \varepsilon_a\).
2.  По норме изменения решения: \(\|x^{(k+1)} - x^{(k)}\| < \varepsilon_x\).
3.  По достижении максимального числа итераций.

#### Резюме

| Понятие | Определение |
|:---|:---|
| **Итерационный метод** | \(x^{(k+1)} = T x^{(k)} + c\) |
| **Спектральный радиус** | \(\rho(T) = \max |\lambda_i(T)|\) |
| **Условие сходимости** | \(\rho(T) < 1\) |
| **Предобуславливание** | Замена системы на \(M^{-1} A x = M^{-1} b\) |
