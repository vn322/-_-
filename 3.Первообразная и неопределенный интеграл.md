Мы переходим от дифференцирования (поиска скорости изменения) к интегрированию — процессу, который можно назвать «сборкой целого из частей» или «восстановлением функции по её скорости».

Это три кита математического анализа: **Первообразная**, **Интеграл Римана** и связывающая их **Формула Ньютона-Лейбница**.

---

### 1. Первообразная и неопределенный интеграл: «Обратная задача»

**Интуиция:**
Если производная отвечает на вопрос «Как быстро меняется функция?», то первообразная решает обратную задачу: **«Зная скорость изменения, восстанови саму функцию»**.

*   *Пример из жизни:* Вы знаете скорость автомобиля в каждый момент времени $v(t)$. Можете ли вы узнать, какой путь $S(t)$ он проехал? Да, нужно найти функцию, производная которой равна скорости. Эта функция и есть первообразная.

**Определение:**
Функция $F(x)$ называется **первообразной** для функции $f(x)$ на некотором промежутке, если для всех $x$ из этого промежутка выполняется:
$$ F'(x) = f(x) $$

**Важнейший нюанс: Константа $C$**
Если $F(x)$ — первообразная, то и $F(x) + 5$, и $F(x) - 100$, и $F(x) + C$ тоже будут первообразными, потому что производная от константы всегда равна нулю.
$$ (x^2)' = 2x $$
$$ (x^2 + 10)' = 2x $$
$$ (x^2 - 3.5)' = 2x $$
Значит, у одной функции $f(x)$ существует **бесконечно много** первообразных. Они отличаются друг от друга только сдвигом вверх или вниз по вертикали.

**Неопределенный интеграл:**
Множество всех первообразных функции $f(x)$ называется **неопределенным интегралом** и обозначается так:
$$ \int f(x) \, dx = F(x) + C $$
Где:
*   $\int$ — знак интеграла (стилизованная буква S от слова *Summa*).
*   $f(x)$ — подынтегральная функция.
*   $dx$ — указывает, по какой переменной мы интегрируем.
*   $C$ — произвольная постоянная.

**Геометрический смысл:**
График неопределенного интеграла — это не одна кривая, а **семейство кривых** (целый «веер» линий), которые получаются друг из друга параллельным переносом вдоль оси $Y$. В каждой точке с одинаковой координатой $x$ касательные ко всем этим кривым параллельны (имеют одинаковый угловой коэффициент $f(x)$).

**Примеры табличных интегралов:**
Так как интегрирование — действие, обратное дифференцированию, мы просто читаем таблицу производностей справа налево:
1.  Так как $(x^n)' = n x^{n-1}$, то $\int x^n \, dx = \frac{x^{n+1}}{n+1} + C$.
    *   $\int x^2 \, dx = \frac{x^3}{3} + C$.
2.  Так как $(\sin x)' = \cos x$, то $\int \cos x \, dx = \sin x + C$.
3.  Так как $(e^x)' = e^x$, то $\int e^x \, dx = e^x + C$.

---

### 2. Интеграл Римана: «Площадь под кривой»

Здесь мы меняем подход. Если неопределенный интеграл — это набор функций, то **определенный интеграл (интеграл Римана)** — это конкретное **число**.

**Задача:**
Найти площадь фигуры, ограниченной сверху графиком функции $y = f(x)$, снизу осью $OX$, и по бокам прямыми $x = a$ и $x = b$.
Обозначается:
$$ \int_{a}^{b} f(x) \, dx $$
Числа $a$ и $b$ называются пределами интегрирования.

**Как Бернхард Риман предложил это считать? (Метод прямоугольников)**
Представьте, что вам нужно измерить площадь участка земли под извилистой рекой.
1.  Разбейте отрезок $[a, b]$ на множество маленьких кусочков шириной $\Delta x$.
2.  На каждом кусочке постройте прямоугольник. Высота прямоугольника равна значению функции $f(x)$ в какой-то точке этого кусочка.
3.  Площадь одного прямоугольника: $S_i \approx f(x_i) \cdot \Delta x$.
4.  Сложите площади всех прямоугольников: $S \approx \sum f(x_i) \cdot \Delta x$. Это **интегральная сумма**.
5.  Теперь представьте, что вы делаете кусочки всё тоньше и тоньше ($\Delta x \to 0$), а количество прямоугольников стремится к бесконечности.

**Определение:**
Предел этих интегральных сумм при стремлении ширины наибольшего кусочка к нулю и называется **определенным интегралом Римана**:
$$ \int_{a}^{b} f(x) \, dx = \lim_{\max \Delta x_i \to 0} \sum_{i=1}^{n} f(\xi_i) \Delta x_i $$

**Геометрический смысл:**
Если $f(x) \ge 0$, то интеграл численно равен **площади криволинейной трапеции** под графиком.
*   Если график уходит ниже оси $X$, площадь считается со знаком «минус».
*   Итоговое значение интеграла — это алгебраическая сумма площадей (над осью минус под осью).

**Пример:**
Найдем площадь под параболой $y = x^2$ от $x=0$ до $x=1$.
Интуитивно понятно, что эта площадь меньше площади квадрата $1\times1$ (которая равна 1), но больше площади треугольника (0.5).
Точный ответ мы найдем чуть ниже, он равен $1/3$. Метод Римана позволил бы нам приблизиться к этому числу, складывая тысячи узких прямоугольников.

---

### 3. Формула Ньютона-Лейбница: «Великая связь»

До XVII века математики считали две разные задачи:
1.  Поиск площади (геометрия, метод исчерпывания, суммы Римана) — очень сложно и громоздко.
2.  Поиск первообразной (обратное дифференцирование) — тоже непросто, но есть правила.

Никто не догадывался, что эти вещи связаны. Пока Исаак Ньютон и Готфрид Лейбниц независимо друг от друга не открыли **Основную теорему математического анализа**.

**Суть теоремы:**
Вычисление площади (определенного интеграла) сводится к поиску первообразной и вычитанию её значений на концах отрезка. Нам больше не нужно суммировать миллионы прямоугольников!

**Формула:**
Если $f(x)$ непрерывна на $[a, b]$, а $F(x)$ — любая её первообразная ($F'(x) = f(x)$), то:
$$ \int_{a}^{b} f(x) \, dx = F(b) - F(a) $$
Часто записывают кратко через вертикальную черту:
$$ \int_{a}^{b} f(x) \, dx = F(x) \Big|_{a}^{b} $$

**Почему константа $C$ исчезает?**
Потому что мы вычитаем: $(F(b) + C) - (F(a) + C) = F(b) - F(a)$. Константа сокращается. Поэтому для определенного интеграла нам достаточно найти *любую* одну первообразную (обычно берут ту, где $C=0$).

#### Наглядный пример применения

Вернемся к задаче: найти площадь под $y = x^2$ от 0 до 1.
$$ I = \int_{0}^{1} x^2 \, dx $$

1.  **Шаг 1: Найти первообразную.**
    Нам нужна функция, производная которой равна $x^2$.
    Вспоминаем правило степени: $(x^3)' = 3x^2$. Нам нужно убрать тройку.
    Значит, $F(x) = \frac{x^3}{3}$. (Проверка: $(\frac{x^3}{3})' = \frac{3x^2}{3} = x^2$. Верно).

2.  **Шаг 2: Применить формулу Ньютона-Лейбница.**
    Подставляем верхний предел ($b=1$) и нижний ($a=0$):
    $$ F(1) = \frac{1^3}{3} = \frac{1}{3} $$
    $$ F(0) = \frac{0^3}{3} = 0 $$

3.  **Шаг 3: Вычесть.**
    $$ I = F(1) - F(0) = \frac{1}{3} - 0 = \frac{1}{3} $$

**Ответ:** Площадь равна $1/3$.
Без этой формулы нам пришлось бы строить сложные пределы сумм. С ней это заняло три строчки.

---

### 4. Физический смысл связи

Представьте снова движение автомобиля.
*   $v(t)$ — скорость (функция).
*   $\int_{t_1}^{t_2} v(t) \, dt$ — определенный интеграл. Геометрически это площадь под графиком скорости. Физически это **пройденный путь** за время от $t_1$ до $t_2$.
*   $S(t)$ — координата (путь как функция времени). Она является первообразной для скорости: $S'(t) = v(t)$.

**Формула Ньютона-Лейбница в физике звучит так:**
> Путь, пройденный за промежуток времени, равен разности координат в конечный и начальный моменты:
> $$ \text{Путь} = S(t_2) - S(t_1) $$

Это кажется очевидным здравым смыслом («где был в конце» минус «где был в начале»), но именно эта простая идея объединила геометрию площадей и алгебру функций.

---

### Резюме темы

| Понятие | Что это? | Результат | Ключевая идея |
| :--- | :--- | :--- | :--- |
| **Первообразная** | Функция, производная которой равна данной. | Семейство функций $F(x) + C$. | Обратное действие к дифференцированию. |
| **Неопределенный интеграл** | Совокупность всех первообразных. | Формула с константой $C$. | $\int f(x)dx = F(x)+C$. |
| **Интеграл Римана** | Предел суммы площадей узких прямоугольников. | Число (площадь). | Измерение площади под кривой через предельный переход. |
| **Формула Ньютона-Лейбница** | Мост между первыми двумя понятиями. | Число, вычисленное через разность значений. | Площадь = Разность первообразных на границах. |

**Почему это великое открытие?**
Оно превратило интегрирование из мучительного процесса суммирования бесконечных рядов в удобную алгоритмическую процедуру:
1.  Найди первообразную (используя таблицу).
2.  Подставь границы.
3.  Вычти.

Это основа для расчета работ сил, центров масс, вероятностей в статистике и любых накопленных величин в науке.
