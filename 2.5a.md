Мы переходим к центральному понятию линейной алгебры — линейным операторам. Если матрицы — это конкретные таблицы чисел, то линейные операторы — это абстрактные отображения (функции), которые действуют на векторы, сохраняя их линейную структуру. Матрица — это просто способ записи линейного оператора в конкретном базисе.

---

### 1. Определение линейного оператора

#### 1.1 Основное определение

Пусть $\(V\) и \(W\)$ — линейные пространства над одним и тем же полем $\(\mathbb{R}\) (или \(\mathbb{C}\))$.

**Определение.** Отображение $\(\mathcal{A}: V \to W\)$ называется **линейным оператором** (или линейным отображением), если оно обладает двумя свойствами:
1.  **Аддитивность:** $\(\mathcal{A}(\mathbf{x} + \mathbf{y}) = \mathcal{A}(\mathbf{x}) + \mathcal{A}(\mathbf{y})\) для любых \(\mathbf{x}, \mathbf{y} \in V\)$.
2.  **Однородность:** $\(\mathcal{A}(\alpha \mathbf{x}) = \alpha \mathcal{A}(\mathbf{x})\) для любого \(\alpha \in \mathbb{R}\) и \(\mathbf{x} \in V\)$.

**Кратко:** Линейный оператор переводит линейные комбинации векторов в линейные комбинации их образов с теми же коэффициентами:

$$\[
\mathcal{A}(\alpha \mathbf{x} + \beta \mathbf{y}) = \alpha \mathcal{A}(\mathbf{x}) + \beta \mathcal{A}(\mathbf{y})
\]$$

Если $\(V = W\), то \(\mathcal{A}\)$ называют **линейным преобразованием** пространства $\(V\)$ (или эндоморфизмом).

---

### 2. Примеры линейных операторов

1.  **Нулевой оператор:** $\(\mathcal{O}(\mathbf{x}) = \mathbf{0}\)$ для любого $\(\mathbf{x}\)$. Всё переводит в ноль.
2.  **Тождественный оператор:** $\(\mathcal{I}(\mathbf{x}) = \mathbf{x}\)$. Оставляет вектор на месте.
3.  **Оператор подобия (гомотетии):** $\(\mathcal{A}(\mathbf{x}) = k \mathbf{x}\), где \(k\)$ — фиксированное число. Растягивает (сжимает) векторы.
4.  **Оператор поворота** на плоскости $\(\mathbb{R}^2\)$ на угол $\(\varphi\)$ против часовой стрелки. Переводит вектор $\((x, y)\) в \((x\cos\varphi - y\sin\varphi, x\sin\varphi + y\cos\varphi)\)$.
5.  **Оператор проектирования** на ось $\(Ox\) в \(\mathbb{R}^3\): \(\mathcal{A}(x, y, z) = (x, 0, 0)\)$.
6.  **Оператор дифференцирования** в пространстве многочленов степени не выше $\(n\): \(\mathcal{A}(P(t)) = P'(t)\)$. Производная суммы равна сумме производных, константу можно выносить.

---

### 3. Свойства линейных операторов

Из определения следуют важные свойства (для любого линейного оператора $\(\mathcal{A}\))$ :

1.  **Образ нуля есть нуль:** $\(\mathcal{A}(\mathbf{0}) = \mathbf{0}\)$.
2.  **Образ противоположного вектора:** $\(\mathcal{A}(-\mathbf{x}) = -\mathcal{A}(\mathbf{x})\)$.
3.  **Линейная зависимость сохраняется:** Если векторы $\(\mathbf{x}_1, \dots, \mathbf{x}_k\)$ линейно зависимы, то их образы $\(\mathcal{A}(\mathbf{x}_1), \dots, \mathcal{A}(\mathbf{x}_k)\)$ также линейно зависимы.
    *   *Важно:* Обратное неверно! Линейно независимые векторы могут перейти в линейно зависимые (например, при проекции на плоскость два независимых вектора могут стать коллинеарными).

---

### 4. Ядро и образ оператора

С каждым линейным оператором $\(\mathcal{A}: V \to W\)$ связаны два важных подпространства:

1.  **Ядро оператора ($\(\ker \mathcal{A}\)$ )** — множество всех векторов из $\(V\)$, которые оператор переводит в ноль:

    $$\[
    \ker \mathcal{A} = \{\mathbf{x} \in V \mid \mathcal{A}(\mathbf{x}) = \mathbf{0}\}
    \]$$
    
    Ядро — это подпространство в $\(V\)$. Размерность ядра называется **дефектом** оператора.

3.  **Образ оператора ($\(\text{Im} \mathcal{A}\)$ )** — множество всех векторов из $\(W\)$, в которые оператор переводит хотя бы один вектор из $\(V\)$ :

    $$\[
    \text{Im} \mathcal{A} = \{\mathbf{y} \in W \mid \exists \mathbf{x} \in V: \mathcal{A}(\mathbf{x}) = \mathbf{y}\}
    \]$$
    
    Образ — это подпространство в $\(W\)$. Размерность образа называется **рангом** оператора.

**Теорема о размерностях (основная теорема линейной алгебры):**

$$\[
\boxed{\dim V = \dim \ker \mathcal{A} + \dim \text{Im} \mathcal{A}}
\]$$

---

### 5. Способы задания линейных операторов

Линейный оператор полностью определяется тем, как он действует на базисные векторы.

#### 5.1 Матрица линейного оператора в базисе

Пусть $\(V\) — \(n\)$ -мерное пространство с базисом $\(\mathbf{e}_1, \mathbf{e}_2, \dots, \mathbf{e}_n\), \(W\) — \(m\)$-мерное пространство с базисом $\(\mathbf{f}_1, \dots, \mathbf{f}_m\)$.

Рассмотрим образы базисных векторов $\(\mathcal{A}(\mathbf{e}_j)\)$. Каждый такой образ лежит в $\(W\)$ и может быть разложен по базису $\(\mathbf{f}_i\)$:

$$\[
\mathcal{A}(\mathbf{e}_j) = \sum_{i=1}^{m} a_{ij} \mathbf{f}_i
\]$$

**Матрицей линейного оператора $\(\mathcal{A}\)$ .** в паре базисов $\(\{\mathbf{e}_j\}\) и \(\{\mathbf{f}_i\}\)$ называется матрица $\(A\)$ размера $\(m \times n\)$, у которой $\(j\)$ -й столбец состоит из координат вектора $\(\mathcal{A}(\mathbf{e}_j)\)$ в базисе $\(\{\mathbf{f}_i\}\)$ :

$$\[
A = \begin{pmatrix}
a_{11} & a_{12} & \dots & a_{1n} \\
a_{21} & a_{22} & \dots & a_{2n} \\
\vdots & \vdots & \ddots & \vdots \\
a_{m1} & a_{m2} & \dots & a_{mn}
\end{pmatrix}
\]$$

**Действие оператора в координатах:** Если вектор $\(\mathbf{x}\)$ имеет координаты $\((x_1, \dots, x_n)^T\)$ в базисе $\(\{\mathbf{e}_j\}\)$, то координаты вектора $\(\mathbf{y} = \mathcal{A}(\mathbf{x})\)$ в базисе $\(\{\mathbf{f}_i\}\)$ находятся по формуле:

$$\[
\mathbf{y} = A \mathbf{x}
\]$$

(матрица умножается на столбец координат).

#### 5.2 Аналитическая формула

Иногда оператор задают прямой формулой (как в примерах выше): $\(\mathcal{A}(x, y, z) = (x + y, y - z, 2x)\)$ . Это тоже задание оператора, из которого можно получить матрицу, подставив базисные векторы.

---

### 6. Действия над линейными операторами

Если операторы действуют из одного и того же пространства $\(V\)$ в одно и то же пространство $\(W\)$, над ними можно выполнять алгебраические операции.

#### 6.1 Сложение операторов

**Определение.** Суммой двух линейных операторов $\(\mathcal{A}\) и \(\mathcal{B}\)$ называется оператор $\(\mathcal{A} + \mathcal{B}\)$, действующий по правилу:

$$\[
(\mathcal{A} + \mathcal{B})(\mathbf{x}) = \mathcal{A}(\mathbf{x}) + \mathcal{B}(\mathbf{x})
\]$$

**Матрица:** Сумме операторов соответствует сумма их матриц (в одном и том же базисе).

#### 6.2 Умножение оператора на число

**Определение.** Произведением оператора $\(\mathcal{A}\)$ на число $\(\alpha\)$ называется оператор $\(\alpha \mathcal{A}\)$, действующий по правилу:

$$\[
(\alpha \mathcal{A})(\mathbf{x}) = \alpha \cdot \mathcal{A}(\mathbf{x})
\]$$

**Матрица:** Матрица умножается на это число.

#### 6.3 Композиция (произведение) операторов

Пусть $\(\mathcal{A}: V \to W\) и \(\mathcal{B}: U \to V\)$ — линейные операторы. Тогда их **композицией** (произведением) $\(\mathcal{A} \circ \mathcal{B}\)$ называется оператор, действующий из $\(U\) в \(W\)$ по правилу:

$$\[
(\mathcal{A} \circ \mathcal{B})(\mathbf{x}) = \mathcal{A}(\mathcal{B}(\mathbf{x}))
\]$$

(Сначала применяем $\(\mathcal{B}\), потом к результату — \(\mathcal{A}\))$.

**Матрица:** Матрица композиции равна произведению матриц операторов в соответствующих базисах:

$$\[
[\mathcal{A} \circ \mathcal{B}] = [\mathcal{A}] \cdot [\mathcal{B}]
\]$$

(размеры должны быть согласованы).

#### 6.4 Обратный оператор

Если оператор $\(\mathcal{A}: V \to V\)$ является **биективным** (взаимно однозначным), то существует **обратный оператор** $\(\mathcal{A}^{-1}\)$, такой что:

$$\[
\mathcal{A}^{-1} \circ \mathcal{A} = \mathcal{A} \circ \mathcal{A}^{-1} = \mathcal{I}
\]$$

**Матрица:** Матрица обратного оператора равна обратной матрице: $\([\mathcal{A}^{-1}] = [\mathcal{A}]^{-1}\)$.

#### Резюме

| Понятие | Формула / Свойство |
| :--- | :--- |
| **Линейный оператор** | $\(\mathcal{A}(\alpha\mathbf{x}+\beta\mathbf{y}) = \alpha\mathcal{A}(\mathbf{x}) + \beta\mathcal{A}(\mathbf{y})\)$ |
| **Ядро** | $\(\ker\mathcal{A} = \{\mathbf{x} \mid \mathcal{A}(\mathbf{x}) = \mathbf{0}\}\)$ |
| **Образ** | $\(\text{Im}\mathcal{A} = \{\mathcal{A}(\mathbf{x}) \mid \mathbf{x} \in V\}\)$ |
| **Теорема о размерностях** | $\(\dim V = \dim \ker \mathcal{A} + \dim \text{Im} \mathcal{A}\)$ |
| **Матрица оператора** | $\(j\)-й столбец — координаты \(\mathcal{A}(\mathbf{e}_j)\)$ |
| **Сложение операторов** | \((\mathcal{A}+\mathcal{B})(\mathbf{x}) = \mathcal{A}(\mathbf{x}) + \mathcal{B}(\mathbf{x})\) |
| **Умножение на число** | \((\alpha\mathcal{A})(\mathbf{x}) = \alpha\mathcal{A}(\mathbf{x})\) |
| **Композиция** | \((\mathcal{A}\circ\mathcal{B})(\mathbf{x}) = \mathcal{A}(\mathcal{B}(\mathbf{x}))\) |
| **Обратный оператор** | \(\mathcal{A}^{-1} \circ \mathcal{A} = \mathcal{I}\) |
