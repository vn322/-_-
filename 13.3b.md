
Тема **Непрерывных случайных величин и плотности распределения** — это следующий уровень теории вероятностей. В отличие от дискретных величин, непрерывные могут принимать любые значения из некоторого интервала, что требует нового математического аппарата.

---

# 1. Непрерывная случайная величина

### Интуитивное понимание
**Дискретная величина:** Можно пересчитать возможные значения (1, 2, 3...).
**Непрерывная величина:** Может принимать **любое** значение из интервала (например, время ожидания, рост человека, температура).

**Важное отличие:** Для непрерывной величины вероятность принять **конкретное** значение равна нулю:
$$
P(X = a) = 0
$$
Поэтому мы работаем с вероятностью попадания в **интервал**: $P(a < X < b)$.

### Строгое определение
Случайная величина $X$ называется **непрерывной**, если её функция распределения $F(x)$ непрерывна и кусочно-дифференцируема.

---

# 2. Плотность распределения вероятностей

### Интуитивное понимание
Плотность $f(x)$ показывает, насколько «густо» вероятность сконцентрирована вокруг точки $x$.
*   **Аналогия с физикой:** Если функция распределения — это «масса», то плотность — это «плотность вещества» в точке.
*   **Вероятность** попадания в интервал — это **площадь** под кривой плотности на этом интервале.

### Строгое определение
**Плотностью распределения** (probability density function, PDF) непрерывной случайной величины называется производная от функции распределения:
$$
f(x) = F'(x) = \frac{d}{dx} P(X < x)
$$

### Связь с функцией распределения
$$
F(x) = \int_{-\infty}^{x} f(t) \, dt
$$
$$
P(a < X < b) = F(b) - F(a) = \int_{a}^{b} f(x) \, dx
$$

### Свойства плотности распределения

1.  **Неотрицательность:**
    $$
    f(x) \ge 0 \quad \text{для всех } x
    $$
2.  **Нормировка:**
    $$
    \int_{-\infty}^{+\infty} f(x) \, dx = 1
    $$
    *(Площадь под всей кривой равна 1)*
3.  **Вероятность точки:**
    $$
    P(X = a) = \int_{a}^{a} f(x) \, dx = 0
    $$

---

# 3. Основные непрерывные распределения

## 1. Равномерное распределение

### Интуитивное понимание
Все значения из интервала $[a, b]$ **равновероятны**.
*   **Пример:** Время ожидания автобуса (если автобусы ходят строго по расписанию), случайное число из диапазона.

### Плотность распределения
$$
f(x) = \begin{cases}
\frac{1}{b - a}, & a \le x \le b \\
0, & \text{иначе}
\end{cases}
$$

### Функция распределения
$$
F(x) = \begin{cases}
0, & x < a \\
\frac{x - a}{b - a}, & a \le x \le b \\
1, & x > b
\end{cases}
$$

### Числовые характеристики
$$
E[X] = \frac{a + b}{2}, \quad D[X] = \frac{(b - a)^2}{12}
$$

**Обозначение:** $X \sim U(a, b)$

---

## 2. Экспоненциальное распределение

### Интуитивное понимание
Описывает **время ожидания** до следующего события в пуассоновском потоке.
*   **Пример:** Время безотказной работы прибора, время между звонками в колл-центр, время жизни радиоактивного атома.

### Плотность распределения
$$
f(x) = \begin{cases}
\lambda e^{-\lambda x}, & x \ge 0 \\
0, & x < 0
\end{cases}
$$
где $\lambda > 0$ — параметр интенсивности.

### Функция распределения
$$
F(x) = \begin{cases}
1 - e^{-\lambda x}, & x \ge 0 \\
0, & x < 0
\end{cases}
$$

### Числовые характеристики
$$
E[X] = \frac{1}{\lambda}, \quad D[X] = \frac{1}{\lambda^2}
$$

### Свойство отсутствия памяти
$$
P(X > t + s \mid X > t) = P(X > s)
$$
*(Вероятность проработать ещё $s$ часов не зависит от того, сколько прибор уже проработал)*.

**Обозначение:** $X \sim \text{Exp}(\lambda)$

---

## 3. Нормальное распределение (Гаусса)

### Интуитивное понимание
Самое важное распределение в теории вероятностей! Описывает сумму многих независимых случайных величин.
*   **Пример:** Рост людей, ошибки измерений, IQ, доходы населения.

### Плотность распределения
$$
f(x) = \frac{1}{\sigma \sqrt{2\pi}} e^{-\frac{(x - \mu)^2}{2\sigma^2}}
$$
где:
*   $\mu$ — математическое ожидание (центр распределения)
*   $\sigma$ — среднеквадратическое отклонение (ширина «колокола»)

### Функция распределения
Не выражается через элементарные функции. Обозначается $\Phi(x)$ или через функцию ошибок $\text{erf}(x)$.

### Числовые характеристики
$$
E[X] = \mu, \quad D[X] = \sigma^2
$$

### Стандартное нормальное распределение
При $\mu = 0$, $\sigma = 1$:
$$
\varphi(x) = \frac{1}{\sqrt{2\pi}} e^{-x^2/2}
$$
**Обозначение:** $X \sim N(\mu, \sigma^2)$

### Правило трёх сигм
*   $P(\mu - \sigma < X < \mu + \sigma) \approx 68.3\%$
*   $P(\mu - 2\sigma < X < \mu + 2\sigma) \approx 95.4\%$
*   $P(\mu - 3\sigma < X < \mu + 3\sigma) \approx 99.7\%$

---

## 4. Гамма-распределение

### Интуитивное понимание
Обобщение экспоненциального распределения. Описывает время ожидания **k-го события** в пуассоновском потоке.
*   **Пример:** Время до отказа системы с резервными элементами, время обслуживания k клиентов.

### Плотность распределения
$$
f(x) = \begin{cases}
\frac{\lambda^k}{\Gamma(k)} x^{k-1} e^{-\lambda x}, & x \ge 0 \\
0, & x < 0
\end{cases}
$$
где:
*   $k > 0$ — параметр формы
*   $\lambda > 0$ — параметр масштаба
*   $\Gamma(k)$ — гамма-функция ($\Gamma(n) = (n-1)!$ для целых $n$)

### Числовые характеристики
$$
E[X] = \frac{k}{\lambda}, \quad D[X] = \frac{k}{\lambda^2}
$$

**Обозначение:** $X \sim \Gamma(k, \lambda)$

### Связь с другими распределениями
*   При $k = 1$: превращается в экспоненциальное.
*   При целых $k$: сумма $k$ независимых экспоненциальных величин.

---

# 4. Типовые задачи и решения

### Задача 1. Проверка плотности распределения
**Задание:** Является ли функция $f(x) = 2x$ при $0 \le x \le 1$ (и 0 иначе) плотностью распределения?

**Решение:**
1.  **Проверка неотрицательности:** $2x \ge 0$ на $[0, 1]$ ✓
2.  **Проверка нормировки:**
    $$
    \int_{-\infty}^{+\infty} f(x) \, dx = \int_{0}^{1} 2x \, dx = \left. x^2 \right|_{0}^{1} = 1 \quad ✓
    $$
3.  **Вывод:** Да, является плотностью распределения.

**Ответ:** Да, является.

---

### Задача 2. Равномерное распределение
**Задание:** Автобусы ходят каждые 10 минут. Пассажир приходит на остановку в случайный момент. Найти вероятность ожидания не более 3 минут.

**Решение:**
1.  **Распределение:** $X \sim U(0, 10)$, где $X$ — время ожидания.
2.  **Плотность:** $f(x) = \frac{1}{10}$ при $0 \le x \le 10$.
3.  **Вероятность:**
    $$
    P(X \le 3) = \int_{0}^{3} \frac{1}{10} \, dx = \frac{3}{10} = 0.3
    $$
    *(Или через функцию распределения: $F(3) = \frac{3-0}{10-0} = 0.3$)*

**Ответ:** 0.3.

---

### Задача 3. Экспоненциальное распределение
**Задание:** Среднее время безотказной работы прибора 100 часов. Найти вероятность, что он проработает более 150 часов.

**Решение:**
1.  **Параметр:** $E[X] = \frac{1}{\lambda} = 100 \Rightarrow \lambda = 0.01$.
2.  **Функция распределения:** $F(x) = 1 - e^{-0.01x}$.
3.  **Вероятность:**
    $$
    P(X > 150) = 1 - F(150) = e^{-0.01 \cdot 150} = e^{-1.5} \approx 0.223
    $$

**Ответ:** $\approx 0.223$.

---

### Задача 4. Нормальное распределение
**Задание:** Рост мужчин распределён нормально с $\mu = 175$ см, $\sigma = 7$ см. Найти вероятность, что случайно выбранный мужчина имеет рост от 168 до 182 см.

**Решение:**
1.  **Стандартизация:** Переходим к $Z = \frac{X - \mu}{\sigma} \sim N(0, 1)$.
2.  **Границы:**
    $$
    z_1 = \frac{168 - 175}{7} = -1, \quad z_2 = \frac{182 - 175}{7} = 1
    $$
3.  **Вероятность:**
    $$
    P(168 < X < 182) = P(-1 < Z < 1) = \Phi(1) - \Phi(-1)
    $$
    По таблице стандартного нормального распределения:
    $$
    \Phi(1) \approx 0.8413, \quad \Phi(-1) = 1 - \Phi(1) \approx 0.1587
    $$
    $$
    P = 0.8413 - 0.1587 = 0.6826
    $$
    *(Это правило одной сигмы!)*

**Ответ:** $\approx 0.683$.

---

### Задача 5. Гамма-распределение
**Задание:** Время жизни системы из 3 резервных элементов (каждый Exp(λ)) имеет гамма-распределение. Найти математическое ожидание времени жизни системы при $\lambda = 0.01$.

**Решение:**
1.  **Параметры:** $k = 3$ (число элементов), $\lambda = 0.01$.
2.  **Математическое ожидание:**
    $$
    E[X] = \frac{k}{\lambda} = \frac{3}{0.01} = 300 \text{ часов}
    $$
3.  **Смысл:** Система работает пока не откажут все 3 элемента последовательно.

**Ответ:** 300 часов.

---

### Задача 6. Нахождение константы нормировки
**Задание:** Функция $f(x) = C e^{-|x|}$ определена на $\mathbb{R}$. Найти $C$ чтобы это была плотность распределения.

**Решение:**
1.  **Условие нормировки:**
    $$
    \int_{-\infty}^{+\infty} C e^{-|x|} \, dx = 1
    $$
2.  **Интеграл** (функция чётная):
    $$
    2C \int_{0}^{+\infty} e^{-x} \, dx = 2C \left. [-e^{-x}] \right|_{0}^{+\infty} = 2C(0 - (-1)) = 2C
    $$
3.  **Находим C:**
    $$
    2C = 1 \quad \Rightarrow \quad C = \frac{1}{2}
    $$
    *(Это распределение Лапласа)*

**Ответ:** $C = \frac{1}{2}$.

---

# Резюме модуля

| Распределение | Плотность $f(x)$ | $E[X]$ | $D[X]$ | Применение |
|:---:|:---:|:---:|:---:|:---:|
| **Равномерное** $U(a,b)$ | $\frac{1}{b-a}$ на $[a,b]$ | $\frac{a+b}{2}$ | $\frac{(b-a)^2}{12}$ | Случайный выбор из интервала |
| **Экспоненциальное** $\text{Exp}(\lambda)$ | $\lambda e^{-\lambda x}$, $x \ge 0$ | $\frac{1}{\lambda}$ | $\frac{1}{\lambda^2}$ | Время до события |
| **Нормальное** $N(\mu, \sigma^2)$ | $\frac{1}{\sigma\sqrt{2\pi}} e^{-\frac{(x-\mu)^2}{2\sigma^2}}$ | $\mu$ | $\sigma^2$ | Сумма многих величин |
| **Гамма** $\Gamma(k, \lambda)$ | $\frac{\lambda^k}{\Gamma(k)} x^{k-1} e^{-\lambda x}$ | $\frac{k}{\lambda}$ | $\frac{k}{\lambda^2}$ | Время до k-го события |

**Свойства плотности:**
1.  $f(x) \ge 0$
2.  $\int_{-\infty}^{+\infty} f(x) \, dx = 1$
3.  $P(a < X < b) = \int_{a}^{b} f(x) \, dx$

**Ключевые выводы:**
1.  **Плотность распределения** — аналог вероятности для непрерывных величин (вероятность = площадь).
2.  **Нормальное распределение** — центральное в теории вероятностей (Центральная предельная теорема).
3.  **Экспоненциальное** обладает свойством отсутствия памяти (важно для теории надёжности).
4.  **Гамма-распределение** обобщает экспоненциальное на время до k-го события.
5.  **Стандартизация** ($Z = \frac{X-\mu}{\sigma}$) позволяет использовать таблицы нормального распределения для любых $\mu, \sigma$.

